# real__role

- 활동: 롤플레잉
- 상태: 실제 제품 라우트 첫 화면
- 경로: /learn/roleplay?level=1&lesson=4
- 화면 폭: 360 (높이 693)
- 언어: en — **앱 기본값이다.** `localStorage` 에 값이 없으면 영어로 뜬다
- 재현: 비로그인 둘러보기 상태로 위 주소를 연다. `capture-real.sh` 참고
- 정본 존재: `activity__role` (표시 컴포넌트 쪽)
- 구현 경로: 실제 라우트 — `src/routes/learn/` 아래
- Storybook: 없음 — **제품 화면을 직접 떴다**
- 관찰: 확정 셸을 쓴다. 구조는 정본과 거의 같다 — 연습 순서 토글·대사 목록·녹음 도크·오른쪽 다음 화살표. **다른 것은 진행 표시가 있다는 것**(정본에는 없다)과 문구뿐이다. **확정 구조로 옮기기 가장 쉬운 화면이다.**
- 디자인 검토 필요: 예
- 분류: DRIFT
