# real__jamo_combine

- 활동: 자모 2단 조합
- 상태: 실제 제품 라우트 첫 화면
- 경로: /learn/jamo?level=1&lesson=1&group=1&sub=2
- 화면 폭: 360 (높이 693)
- 언어: en — **앱 기본값이다.** `localStorage` 에 값이 없으면 영어로 뜬다
- 재현: 비로그인 둘러보기 상태로 위 주소를 연다. `capture-real.sh` 참고
- 정본 존재: `activity__write_2` (표시 컴포넌트 쪽)
- 구현 경로: 실제 라우트 — `src/routes/learn/` 아래
- Storybook: 없음 — **제품 화면을 직접 떴다**
- 관찰: **진행 표시가 없다.** 그리고 정본이 목표 글자를 그대로 보여 주는 자리에, 실제는 **`?` 로 감추고 「힌트」 버튼**을 둔다 — 소리를 듣고 만들라는 뜻이다. 이쪽은 **정본 목업이 낡았다**(BLOCKERS 의 2026-08-24 변경).
- 디자인 검토 필요: 예
- 분류: DRIFT
