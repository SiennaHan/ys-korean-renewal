# 막힌 것과 정해지지 않은 것

2026-08-21 기준. 인계받은 사람이 **먼저 부딪힐 순서**로 적었다.
[README.md](README.md) 가 저장소 안내이고, 이 문서는 지금 상태다.

아래에서 `*.html` 을 이름만으로 부르는데 **전부 `phase1/` 안에 있다.**
옛 판은 `phase1/_superseded/` 다.

---

## 1. 프로덕션 빌드 · 고쳤다 (2026-08-20)

원인은 React 버전이 아니라 **락파일이 둘이었던 것**이다.

```
package.json     "@tanstack/react-router": "^1.114.13"
pnpm-lock.yaml   1.136.8      ← 이 프로젝트의 도구는 pnpm 이다
package-lock.json 1.170.29    ← 섞여 들어온 것. node_modules 는 이쪽으로 깔려 있었다
```

캐럿이 1.170 까지 떠올랐고 그 사이에서 라이브러리가 `React["use"]` 를 쓰기 시작했다.
1.136.8 은 쓰지 않는다 — 그래서 **의존성을 올리거나 내릴 필요가 없었다.**

한 일 세 가지.

| | |
|---|---|
| `package.json` | 라우터 셋을 <b>정확히 `1.136.8`</b> 로 고정(캐럿 제거) |
| `package-lock.json` | 지웠다. 루트 `.claude/launch.json` 이 `npm --prefix app run dev` 였던 것도 pnpm 으로 바꿨다 — 설치 명령이 아니라 빌드가 깨지진 않았지만, 남겨 두면 누군가 `npm install` 을 치게 만든다 |
| `pnpm-lock.yaml` | specifier 를 새 값으로 갱신 |

```bash
pnpm build            # 통과 · 총 21.2MB (gzip 4.7MB)
pnpm typecheck        # 통과
pnpm parity:activity  # "모두 같다"
```

⚠️ **npm 으로 설치하지 마라.** `package-lock.json` 이 다시 생기면 같은 일이 반복된다.
`pnpm install` 을 쓴다.

`app/README.md` 은 갈아 썼다. 벤더에서 온 영어 글이 사실과 어긋난 곳이 넷이었다 —
React 19(실제 18.3.1) · koreanapi 포트 8000(실제 8799) · 없는 `.env.example` ·
`/jamolist` 를 자모 화면이라 적은 것(빈 스텁이다).

## 2. 자모 라우트 — 명세가 옳았고 전제가 늦게 왔다 · 진행 중

`dev_spec_v1.html` §4 는 이렇게 못박는다.

> `/learn/jamo?level=1&lesson=&sub=1~6` ← 5개 라우트 · **6→1 통합**
> **콘텐츠 ID를 URL에서 제거한다** — `$code`(YK0041)·`$id`(C4) 노출 폐지.

구현은 아직 **6개 라우트에 `?code=` 를 남기고 있다.**

```
/learn/jamo/pronounce?code=YK0001      ← listen-repeat   (sub 1)
/learn/jamo/combine?code=YK0002        ← write           (sub 2)
/learn/jamo/word-repeat?code=YK0003    ← listen-repeat2  (sub 3)
/learn/jamo/word-write?code=YK0004     ← read-write      (sub 4)
/learn/jamo/choose?code=YK0005         ← listen          (sub 5)
/learn/jamo/combine3?code=YK0032       ← write3          (sub 6)
```

**어긴 것이 아니다.** 명세의 주소 방식은 자모 콘텐츠가 `(과, sub, 묶음)` 으로
주소를 잡을 때 성립하는데, 그 콘텐츠가 원장에 없었다. 구 콘텐츠는 **묶음이 모듈
경계**여서(모음1·자음1·자음2가 각각 별개 모듈) `과+sub` 하나로는 3개를 가리켰다.
그래서 코드로 주소를 잡고 있었다.

**2026-08-20 에 그 전제를 채웠다.** 구 앱 자모 529문항을 원장으로 포팅했다 —
`글로벌_교재기반_콘텐츠_v28.xlsx` 의 `n8_jamo`, 컬럼 12→27.
**그 사이에 한 번 잃고 다시 만들었다 — 아래 §2-b.**
묶음이 `jamo_group` · `letter` 라는 **데이터 칸**이 되었으므로 이제
`?level&lesson&sub` 로 주소가 잡힌다.

**남은 것은 순서뿐이다.**

| | | |
|---|---|---|
| 3 | **포팅 검수** | 529행 전부 `review_status=draft`. 기계적으로 옮긴 것이라 원본 대조가 필요하다 — `legacy_id`(`Y3W35`)·`legacy_module`(`YK0001`) 열로 한 행씩 맞춰 본다 |
| 4 | 생성기 + 화면 | **했다 (2026-08-24)** — `build-content.py` 가 `n8_jamo.json` 529행을 낸다. 화면은 `problem.ts` 대신 그것을 읽는다 |
| 5 | 라우트 통합 | **했다 (2026-08-24)** — 6개 → `/learn/jamo?level&lesson&group&sub` 하나. `?code=` 는 URL 에서 빠졌다 |

**검수는 4·5 를 막지 않는다 (2026-08-24 판단 변경).** 전에는 "검수가 끝나기 전에는
4·5 를 하지 않는다" 고 적었다. 걱정은 "검수 안 된 콘텐츠가 화면에 올라간다" 였는데
**그 전제가 성립하지 않는다.**

자모 6화면이 지금 읽는 것이 바로 `problem.ts` 다(9곳에서 import). `n8_jamo` 는
그 `problem.ts` 를 옮긴 것이므로 —

```
지금 :  화면 ← problem.ts (529항목)
4 뒤 :  화면 ← n8_jamo JSON ← 원장 ← problem.ts (같은 529항목)
```

**생성기를 붙여도 학습자가 보는 내용이 안 바뀐다. 배관만 바뀐다.**
"검수 안 된 콘텐츠가 올라간다" 는 걱정은 **이미 올라가 있고 같은 것**이라 성립하지 않는다.

그리고 위험을 재 봤다. 새 책 기준(`unit.ts`)의 낱자를 529행이 덮는지 여덟 묶음
전수로 셌더니 **빠짐 0**, 원천 `problem.ts` 와 값 대조 **15필드 0건 차이**,
`legacy_id`·`item_id` 중복 **0**. 아래 표가 이미 "여덟 묶음의 낱자를 원장과 전수
대조한 결과 어긋난 것은 모음1 하나" 라고 적어 두었고 그 하나는 고쳤다.
**구 앱 자모와 새 책이 실질적으로 같다.**

그러니 **4·5 는 지금 해도 된다.** `review_status` 는 `draft` 로 그대로 둔다 —
콘텐츠가 검수를 기다리는 것과 배관이 검수를 기다리는 것은 다르다.

**검수로 실제로 남은 것은 하나뿐이다.** `unit.ts` 의 받침·겹받침 묶음은 제목이
그냥 "받침"·"겹받침" 이라 **낱자 목록이 없다.** 그래서 그 두 묶음(67행)만
커버리지를 기계로 확인할 수가 없다 — 교재 한글(3)과 목차를 펴면 끝난다.
나머지 둘은 급하지 않다: 같은 낱자를 두 묶음이 가르치는 26행(구 앱이 그렇게
돌고 있었다) · `word_refs` 80행(읽는 코드가 아직 없다 — §2-b).

포팅하면서 명세 오류 넷을 찾았다. `jamo_authoring_spec_v1.html` 맨 위에 적어 두었다 —
영상 컬럼 누락(288문항) · 자모 격자가 `selection1~3` 에 안 들어감 · 답이 셋 필요 ·
`answer_index` 가 0 부터(§4 는 1 부터라고 적었으나 원장의 다른 시트가 전부 0 부터다).

**목업 대조가 자모 화면에서 찾은 것 (2026-08-21).**
자모 목록을 목업 대조에 넣어 보니 넷이 갈렸다. **여덟 묶음의 낱자를 원장과 전수
대조한 결과 어긋난 것은 모음1 하나였고**(나머지 여섯은 낱자·순서까지 같다) 그것은
고쳤다. 남은 셋은 **목업 쪽이 구 교재 기준**이다.

| 무엇 | 원장 (새 책) | 목업 `screens_SOT`(홈·목록 절) | 앱 | 처리 |
|---|---|---|---|---|
| 모음1 낱자 | **10자** 아·애·어·에·오·외·우·위·으·이 | **10자** ㅏㅓㅗㅜㅡㅣ**ㅐㅔ**ㅚㅟ | 8자였다 | **고쳤다** — `unit.ts` 에 ㅐ·ㅔ 추가(순서도 원장·목업과 같게 ㅣ 뒤) |
| 묶음 이름 | `모음1` | `모음 1` | `모음1` | 원장·앱이 같다. **목업이 갱신 대상** |
| 활동 이름 | — | `자모 듣고 따라하기` · `자모 쓰기` | `발음 듣고 따라하기` · `자음-모음 조합하고 쓰기` | 앱이 새 책 기준. **목업이 갱신 대상** |
| 진행 상태 | — | 완료 · 진행 중 | 전부 빈 자리 | 자모는 진행 기록을 아직 안 읽는다(§6 서버 대기) |

**목업을 새 책 기준으로 갱신했다 (2026-08-21).** `phase1/screens_SOT.html`(홈·목록 절) 의
자모 화면이 구 교재 기준이었다. 고친 것 —

| | 전 | 후 |
|---|---|---|
| 활동 이름 | `자모 듣고 따라하기` · `자모 쓰기` · `단어 읽고 쓰기` | `발음 듣고 따라하기` · `자음-모음 조합하고 쓰기` · `단어 쓰기` |
| 묶음 이름 | `모음 1` · `자음 1` | `모음1` · `자음1` (원장과 같게) |
| 활동 수 | 묶음마다 3개·2개 | **묶음마다 5개** — 실제로 다섯이 고르게 있다 |

**활동 이름을 한 번 더 줄였다 (2026-08-24).** `자음-모음 조합하고 쓰기` →
**`자음-모음 조합하기`**. 위 두 표는 08-21 시점의 기록이라 그대로 둔다.
바꾼 곳 — `app/src/shared/data/module.ts`(앱이 쓰는 값, 8곳) ·
`app/src/screens_ref/nav__jamo__resume.html` · `phase1/screens_SOT.html` ·
`phase1/G1_content_gate_v1.html` · 코드 주석 셋.
`phase1/_snapshots/` 는 그때 뜬 날것이라 두고 `TWIN_ALLOW` 에 이유를 적었다.

**받침 묶음도 `자음-모음 조합하기` 로 둔다 — 이름을 바꾸지 마라 (기획자 결정).**
`write3`(sub=6)은 받침·겹받침 묶음(9·10)에서만 쓰이는데 화면 이름이 sub=2 와 같은
`자음-모음 조합하기` 다. 받침 활동인데 "자음-모음" 이라 어긋나 보이지만 —
**받침도 자음이다.** 따로 이름을 두면 길고 촌스러워지기만 한다. 두 묶음에는
`write`(sub=2)가 없어서 한 화면에서 이름이 겹치지도 않는다.

> **내가 여기서 한 번 틀렸다.** "`write3` 이 아직 `받침까지 조합하고 쓰기` 다" 라고
> 적어서 마치 화면에 그렇게 나오는 것처럼 읽히게 했다. 실제로 그 문구는
> `jamo.ts` 의 **코드 주석**과 이 문서에만 있었고 **화면에 나온 적이 없다**.
> 확인하지 않고 적었다.

**아직 안 바꾼 것 하나.** `module.ts` 의 `eng` 가 `Consonant-Vowel writing` 이다.
이 값은 리뉴얼 화면이 아니라 구 `routes/book/*` 이 쓴다.
| 묶음 수 | 한글 1과에 2개 | **3개** — 모음1 · 자음1 · 자음2 |
| 급 탭 | 한글 + 1~3급 | **한글 + 1~8급** (두 화면이 공유하는 정의였다) |
| 1급 과 칩 | 4~9과 | **4~15과** — 1~3과는 한글 탭으로 간다 |

캡처를 다시 떠서(`phase1/_snapshots/` → `app/src/screens_ref/`) **자모 목록이 목업 대조에
들어갔다** — 그때 스물일곱이었고, 그 뒤 VocaShot 셋과 게임 열일곱이 더 들어왔다
(지금 수는 `README.md`). 렌더 뒤에 붙는 스크롤 표시 속성(`data-cue-bound` 등)은
디자인이 아니라 걷어냈다.

이 저장소는 **구현이 목업과 다르면 목업이 기준**이다. 이번엔 드물게 목업이 낡은
쪽이었다 — 그 규칙은 시각(레이아웃·치수·색)에 대한 것이고 어긋남은 **콘텐츠**였다.
콘텐츠 정본은 원장이므로 앱을 되돌리지 않고 목업을 고쳤다.

## 3. 라우트 통합 — 다 했다. 죽은 옛 라우트만 남았다 (2026-08-24 갱신)

| 명세가 요구하는 것 | 지금 |
|---|---|
| `/learn/word` · `grammar` · `listen` · `read` · `roleplay` | ✔ 옮겼다 |
| `/learn/flashcard?level=&lesson=` | ✔ **옮겼다** |
| `/learn/mission-chat?level=&lesson=` | ✔ **옮겼다** |
| `/learn/jamo` | ✔ **옮겼다 (2026-08-24)** — 6개 → 하나. `?level&lesson&group&sub` — §2-c |

**플래시카드·미션대화가 끝났다.** 교재학습 목록
(`app/src/components/main/textbook/index.tsx`)이 `/learn/flashcard` ·
`/learn/mission-chat` 으로 `{level, lesson}` 을 넘긴다. 둘 다
`parseLearnSearch` 로 검증하므로 **URL 에 콘텐츠 ID 가 없다.**
결과 화면도 명세대로 셸 공통으로 접었다 — `components/learn/flashcard-result.tsx`
가 라우트가 아니라 컴포넌트 상태다.

**남은 것은 죽은 옛 라우트를 걷어내는 것이다.** `app/src/routes/book/` 에 19개가
남아 `routeTree.gen.ts` 에 등록돼 있다 — 주소를 직접 치면 닿는다. 플래시카드와
미션대화 쪽으로 `navigate` 하는 코드가 **한 곳 있다**(2026-08-24 다시 확인) —
`components/main/course-list/mission-chat.tsx:36` 이 `/book/chapter/unit/mission_chat/{code}`
로 보낸다. 그 경로는 지금 `/main/textbook` 으로 리다이렉트되므로 **누르면 교재학습에 떨어진다.**
다만 그 컴포넌트를 쓰는 곳은 `/main/game/missionchat` 라우트뿐이고 **아무도 그 라우트를
링크하지 않는다** — 주소를 직접 쳐야 닿는 고아다. 걷어낼 목록에 같이 넣는다.

```bash
# 옛 경로로 가는 코드가 정말 없는지
grep -rn 'to: "/book/chapter/unit/' app/src --include='*.tsx' | grep -v routeTree
```

지우기 전에 확인할 것 — `/book/list` 처럼 아직 쓰이는 것이 섞여 있다.
**자모(§2-c)가 2026-08-24 에 끝났으므로 기다릴 것은 이제 없다** — 한꺼번에 걷어내면 된다.

---

## 2-c. 자모 배관과 라우트를 했다 (2026-08-24)

§2 의 4·5 다. 게이트를 낮춘 근거는 §2 에 있다 — 내용이 안 바뀌므로 검수를 안 기다렸다.

### 4 생성기

`build-content.py` 의 `EXTRA_SHEETS` 에 `n8_jamo` 를 넣었다 →
`app/src/shared/data/n8_jamo.json` **529행 · 27열**. 화면은 이제 이것을 읽는다.
`problem.ts` 는 자모 화면에서 떨어졌다(구 `/book/...` 라우트 셋이 아직 쓴다 — §3-b).

**처음엔 반만 했다 (2026-08-25 에 마저 했다).** 8/24 에 라우트만 원장으로 옮기고
"화면이 JSON 을 읽는다" 고 적었는데 **거짓이었다** — 화면 여섯은 그대로
`problems.filter(module_code)` 로 구 앱 덤프에서 문항을 꺼내고 있었고,
`n8_jamo` 는 **주소 풀이에만** 쓰였다. 라우트가 `resolveJamo` 를 쓰는 것까지만
보고 화면 안을 안 열어 본 것이다.

마저 한 방법 — `shared/data/jamo.ts` 에 `jamoProblems` 를 두었다. 원장 행을
화면이 쓰는 `ProblemType` 모양으로 이름만 바꿔 내주므로, 컴포넌트는
`problems` → `jamoProblems` 로 **import 한 줄만** 바뀐다.

```
id ← legacy_id · instructions ← instruction · type ← problem_type
content ← target_jamo 또는 target_word (글자 수로 갈린다) · 나머지는 이름이 같다
```

**529행 × 15필드를 전수 대조해 다른 칸 0개**를 확인한 뒤에 바꿨다. 화면이 보는
값이 안 바뀐다는 뜻이다. 브라우저로도 봤다 — 모음1 낱자 10자, 겹받침 3단 조합 정상.

### 5 라우트 — 여섯이 하나가 됐다

```
전  /learn/jamo/pronounce?code=YK0001   … 여섯 라우트
후  /learn/jamo?level=1&lesson=1&group=1&sub=1
```

**주소에 group 이 하나 더 있다.** 명세(`dev_spec_v1` §4)는 `?level&lesson&sub` 라고
적었는데 그것만으로는 묶음을 못 짚는다 — 한 과에 묶음이 셋이고 각자 자기 활동을
갖는다. §2 도 "(과, sub, 묶음)" 셋이 필요하다고 적어 두었다.
**(과, 묶음, 활동) 이 모듈을 유일하게 가리키는 것은 전수로 확인했다 — 38 주소 ↔ 38 모듈, 양방향 1:1.**

| 무엇 | 어디 |
|---|---|
| 주소 ↔ 모듈 변환 | `app/src/shared/data/jamo.ts` — `resolveJamo` · `addressOfModule` · `subsInGroup` |
| 검색 파라미터 | `app/src/routes/learn/-jamo-search.ts`. **옛 `?code=` 도 받아 주소로 푼다**(북마크) |
| 라우트 하나 | `app/src/routes/learn/jamo.tsx` — `sub` 로 갈라 컴포넌트를 부른다 |
| 화면 여섯 | `app/src/components/learn/jamo/` — 라우트에서 옮긴 것이고 내용은 그대로다 |
| 구 링크 리다이렉트 | `/book/chapter/unit/{scene}/$code` 여섯이 주소로 풀어 보낸다 |

**`module_code` 는 데이터에 남는다.** `wordgroup` 처럼 그 키로 조인하는 옛 표가
아직 있다. URL 에서만 빠졌다 — 명세가 요구한 것이 그것이다.

### 3과는 활동이 다섯이 아니다

받침·겹받침 묶음은 활동이 **[1,4,5,6]** 이다(2·3 이 없고 6 이 있다).
"묶음마다 5개" 를 코드에 가정으로 두지 않았다 — `subsInGroup` 이 데이터에서 센다.
없는 활동을 물으면 빈 화면이 아니라 `catalog.noModules` 를 낸다.

**이 과정에서 v30 정정이 나왔다.** `write3`(sub6)를 `write`(sub2)로 넣어 뒀던
15행이다 — 그대로 두면 `sub=2` 가 겹받침을 2단 조합 화면으로 냈다. §2-b 를 봐라.

### 돌려서 확인했다

| 주소 | 나온 것 |
|---|---|
| `lesson=1&group=1&sub=1` | Lesson 1 · 모음1 · "Listen and repeat." · 낱자 **10자**(애·에 포함) |
| `lesson=3&group=2&sub=6` | Lesson 3 · 겹받침 · 삯 · **초성·중성·종성 셋** |
| `lesson=3&group=1&sub=2` | 없는 활동 → "학습할 활동이 없어요" |
| `/book/chapter/unit/write3/YK0032` | → `/learn/jamo?level=1&lesson=3&group=1&sub=6` |

typecheck · biome(내 파일 0건) · 목업 대조 · 문서 검사 통과.

---

## 2-b. 자모 529행을 한 번 잃고 다시 만들었다 (2026-08-24)

**원장 정본은 `글로벌_교재기반_콘텐츠_v*.xlsx` 중 가장 높은 번호다.**
번호를 여기 적지 않는다 — 적었다가 v29·v30 이 나오며 이 문서만 낡았다.
지금 무엇인지는 `cd app && python3 scripts/build-content.py --check` 첫 줄이 찍는다.

`illust/build_v24.py` 가 첫 줄에서 `shutil.copy(v23, v24)` 를 하는데 **대상이 이미
있는지 보지 않는다.** v24 는 8/20 에 자모 529행을 담아 만든 파일이었고, 8/24 12:01 에
그 스크립트가 v23 사본으로 덮었다. v23 계열은 자모가 1행(예시)뿐이라 529행이 사라졌다.
`shutil.copy` 는 휴지통을 거치지 않고, 원장은 `.gitignore` 라 이력도 없다.
휴지통 · 로컬 스냅샷 · Time Machine · iCloud 전부 확인했고 **자동 복구는 없었다.**

**되살릴 수 있었던 이유** — 포팅 원본 `app/src/shared/data/problem.ts` 가 529항목
그대로 살아 있고(git HEAD 와 바이트 동일) `legacy_id` 로 맞춰진다. 그리고 사라진
529행은 **전부 `review_status=draft`** 였다. 사람이 검수한 판단은 하나도 잃지 않았다 —
잃은 것은 기계 변환의 출력이고, 그 변환의 근거는 이 문서 §2 와
`phase1/jamo_authoring_spec_v1.html` 맨 위에 남아 있었다.

### 원장 버전의 지위

| | 자모 | 지위 |
|---|---|---|
| v23 | 1행 | 8/19. 자모 포팅 전 |
| v24 | 1행 | **8/20 판은 없다.** 지금 파일은 v23 사본 + n4 정정 2건 |
| v25 | 129행 | v24 에서 만든 것. 자모는 한글 1과만 |
| v26 | 529행 | v25 에 사라진 400행을 이었다 |
| v27 | 529행 | `n8_자모_미보유` → `99_폐기_` 이름 정리 · n7 열 정리 |
| v28 | 529행 | n4 해설 714행을 앱 JSON 과 맞췄다 — 위쪽 |
| v29~ | 529행 | 그 뒤 판. **정본은 늘 가장 높은 번호**이고 생성기가 그것을 고른다 |

### 같은 덮어쓰기로 **자모만 잃은 것이 아니다** (2026-08-24 대조에서 나왔다)

`n4_blank_question` 의 `grammar_focus_수정` 컬럼도 v23 판으로 되돌아갔다.
**앱 JSON 이 원장보다 새롭다** — 836행을 `id` 로 맞춰 비교하면 **63행이 다르고,
전부 JSON 쪽이 옳다.**

**고쳤다 (2026-08-24, v28).** 전수로 재 보니 63행이 아니라 **714행**이었다.

| | 몇 행 | 무엇 | 처리 |
|---|---|---|---|
| 앞 따옴표가 먹힌 것 | **57** | JSON `'유리'는…` / v26 `유리'는…` | JSON 을 받았다 |
| 앞뒤 공백 | **651** | 대체로 끝의 한 칸 | JSON 을 받았다 |
| 내용까지 다른 것 | **6** | 해설이 다른 낱말을 가리켰다 | JSON 을 받았다 (아래) |
| 같음 | 122 | — | — |

> **651 은 앱에 닿을 수 없었던 것이다.** `build-content.py` 의 `cell()` 이 문자열을
> `strip()` 한다(그 파일 82행). 그래서 **실제로 학습자에게 갈 수 있었던 차이는 63행**
> — 따옴표 57 + 내용 6 — 이고, 공백 651 은 원장 쪽 잡티였다. 714행을 다 맞춘 것은
> 그래도 이득이다. 이제 원장과 JSON 이 **836행 전부 바이트까지 같아서** 다음 대조가
> "다르면 진짜 다른 것" 이 된다.

**공백 651행이 그쪽 집계에서 빠져 있었다.** 값이 다른 것은 맞으므로 생성기가
`쓸 것` 으로 판정하는 원인이었다.

**내용 6행 중 다섯째가 문서에 없었다** — `GF-1-13-004`.

```
문항  방학에 운동( ) 배우고 싶어요.   정답 을
원장  "한국어'는 받침이 없어요. '를'을 사용해요."   ← 다른 낱말 · 반대 이형태
JSON  "'운동'은 받침이 있어요. 운동+을"
```

받침 유무로 조사를 고르는 문항인데 해설이 **정답과 반대**를 설명하던 사례가
넷이 아니라 **다섯**이었다.

**방향이 한쪽이 아니어서 칸마다 갈라 처리했다.** 통째로 밀면 v24 정정이 지워진다.

| 칸 | 처리 | 왜 |
|---|---|---|
| `grammar_focus_수정` 714행 | JSON | 학습자용 해설. JSON 이 고쳐진 쪽 |
| `grammar_focus` **넷** | JSON | 원장이 다른 낱말을 설명했다(연필·영화·한국어·마시) |
| `grammar_focus` 둘 | **원장 유지** | `GF-4-9-007` 은 ㅅ불규칙 탈락과 틀린 형태까지 적고, `GF-4-11-007` 은 v24 의 높임 정정(마시지→**드시지**)을 담는다 |
| `change_note` **다섯** | JSON | JSON 이 `원장 값 + "v25 해설 정정: …"` 덧붙임이다. 정정 이유는 원장이 쥐어야 한다 |
| `change_note` 둘 | 원장 유지 | v24 정정 이유를 담는다 |

`illust/build_v28.py` 가 이 규칙을 담고, **덧붙임 관계가 아니면 멈춘다** —
이유를 몰래 잃지 않게. 지금 `build-content --check` 는 n4 를 **`같음`** 으로 찍고
따옴표 경고도 **0** 이다.

자모가 눈에 띈 이유는 행 수가 529 → 1 로 줄어서다. 이쪽은 **행 수가 그대로**(836)이고
칸 안의 글자만 바뀌어서 아무 검사도 울리지 않았다. 생성기의 홀수 따옴표 경고 61개는
울렸지만 **원래 있던 상태처럼 보였다.**

### 앱이 해설의 **정정판을 안 그린다**

`components/learn/fill-blank.tsx:286` 이 그리는 것은 `question.grammar_focus` —
**원본 기호식**이다. `grammar_focus_revised`(학습자에게 보여 줄 문장)가 아니다.
836행 **전부** 두 컬럼의 값이 다르다.

| | 학습자가 지금 보는 것 | 정정판 |
|---|---|---|
| id 1 | `받침 O → -을까요 (먹 + 을까요)` | `받침이 있어요. 먹+을까요` |
| id 2 | `먹다 → 먹 + 어요` | `먹+-어요/아요/여요=먹어요` |

**고쳤다 (2026-08-24).** `fill-blank.tsx` 가 이제
`grammar_focus_revised?.trim() || grammar_focus` 를 그린다 —
836행 중 한 행(`GF-7-7-003`)만 정정판이 비어 있어 그때는 기호식으로 돌아간다.
`BlankQuestion` 타입에 그 컬럼이 아예 없어서 화면이 쓸 수 없던 것이고, 타입에 넣었다.

**순서대로 다 했다** — 원장 714행 복구(v28) → 생성기 실행 → 화면을 정정판으로.
생성기를 돌려 원장이 앞섰던 세 문항이 앱으로 내려갔다(`GF-4-11-007` 높임 정정 ·
`GF-8-11-004` 보조키→보조 열쇠 · `GF-4-9-007` 해설).

### `n8_자모_미보유` 시트가 v26 과 어긋난다

그 시트는 활동 27개를 **"문제 없음"** 이라 적었다(`YK0012`~`YK0040`). v25 의 구글
시트 가져오기에는 맞는 말이었지만 **v26 의 `n8_jamo` 에는 그 27개가 다 있다** —
`problem.ts` 에서 되살린 400행이 바로 그것이다.
**처리했다** — v27 에서 `99_폐기_n8_자모_미보유` 로 옮겼다. 지우지 않고 남겨
그때 무엇을 오판했는지가 보인다.

### 어떻게 다시 만들었나 — `illust/build_v26.py`

`problem.ts` 529항목을 v25 의 27컬럼 스키마로 옮겼다. 파생 규칙 둘:

- **활동은 모듈 위치가 정한다.** 묶음마다 모듈이 정확히 다섯이다(§2 의 "묶음마다 5개").
  **지시문으로 가르면 틀린다** — `YK0023` 은 62행 전부 낱말(꼬리·토끼·꾸미다)이고
  위치가 3번(단어 듣고 따라하기)인데 **50행의 지시문이 낱자용으로 잘못 적혀 있다.**
  스크립트가 이 어긋남을 실행마다 찍는다
- **묶음 이름은 `app/src/shared/data/unit.ts` 가 정본이다.** v25 는 모음1 을 8자로
  적었고 unit.ts 는 10자다(§2 에서 ㅐ·ㅔ 를 더한 기록)

**검증** — v25 에 이미 있던 129행을 필드 대조했더니 **어긋난 칸 0개**다. 즉 새로
만든 400행은 **알려진 정답 129개로 검증된 규칙**의 산출물이다. 영상이 붙은 행은
**288개**로 `jamo_authoring_spec_v1` 이 기록한 수와 같다.

### 남은 판단 둘

| 무엇 | 상태 |
|---|---|
| `word_refs` 80행 | **규명됐다 — 채울 것이 아니라 뺄 칸이다.** 80행이 전부 이미 채워진 쪽을 그대로 복사한 값이다 — `target_jamo` 와 같은 것 **70**(자모 활동), `target_word` 와 같은 것 **10**(`listen-repeat2` 단어 활동). 두 칸은 서로 배타적으로 채워지므로 `word_refs` 에 **새 정보가 0** 이다. 나머지 449행을 채우는 것이 아니라 칸을 없애거나 비워 두는 쪽이 맞다 — 원장 판단이다 |
| 영상 288의 세부 분포 | `jamo_authoring_spec_v1` 은 `sub1 211 · sub3 76 · sub5 1` 이라 적었다. 총합은 같지만 위치 규칙은 `237/50/1` 이다. **옛 포팅이 쓴 규칙은 문서에 없다** — 총합만 기록됐다 |

**재발 방지** — `illust/build_v20~v28.py` 전부에 대상 존재 확인을 넣었다. 실제로 막힌다 — `build_v28.py` 를 짜다 v27 을 덮으려던 것을 가드가 잡았다.
**원장을 만드는 코드도 이제 git 안이다** — 2026-08-25 에 `.gitignore` 를 `illust/*` +
`!illust/*.py` 로 바꿨다. 스크립트 98개 · 792K 만 들어오고 삽화 5040장 · 원장 xlsx ·
하위 폴더는 계속 막힌다. 전에는 이게 밖이라 v24 사고의 원인을 커밋 로그로 못 찾았다.

---

## 3-b. 빈 스텁 라우트 넷이 프로덕션에 나간다 (2026-08-21 발견)

`dev_spec_v1.html` §4 는 **"테스트 라우트 9종과 빈 스텁 4종 제외"** 를 요구했다.
**둘 다 지워서 끝냈다 (2026-08-24)** — 아래.
**절반만 됐다.**

```
app/rsbuild.config.ts
  routeFileIgnorePattern: NODE_ENV === "production" ? "routes/test/" : undefined
                                                      ↑ 테스트만 빠진다
```

스텁 넷은 그대로 등록되어 주소를 치면 닿는다. 내용은 `Hello "/…"` 한 줄이다.

| 라우트 | 파일 | 줄 수 |
|---|---|---|
| `/flashcard` | `app/src/routes/flashcard.tsx` | 9 |
| `/missionchat` | `app/src/routes/missionchat.tsx` | 9 |
| `/jamolist` | `app/src/routes/jamolist.tsx` | 9 |
| `/about` | `app/src/routes/about.tsx` | 9 |

앞의 셋은 **이름이 실제 기능과 겹쳐서 더 나쁘다** — 진짜는 `/learn/flashcard` ·
`/learn/mission-chat` 이고 자모 목록은 `/main/textbook/jamo` 다. 사람이든 AI든
`flashcard` 를 찾다가 이 스텁을 먼저 집는다. (`README` 가 `/jamolist` 를 자모 화면이라
적어 두었던 것도 이것 때문이다 — §1 에서 고쳤다.)

### 프로덕션에 실려 가는 죽은 것 — 전수 (2026-08-24 앱↔기획서 대조)

스텁 넷 말고도 있다. **아무도 링크하지 않는데 주소를 치면 닿는다.**

| 무엇 | 어디 | 줄 수 | 문서가 말한 것 |
|---|---|---|---|
| ~~빈 스텁 넷~~ | ~~`routes/{flashcard,missionchat,jamolist,about}.tsx`~~ | ~~36~~ | **지웠다 2026-08-24** — 아래 |
| ~~테스트 아홉~~ | ~~`routes/test/**`~~ | ~~1,196~~ | **지웠다 2026-08-24** — 제외가 안 되고 있었고, 지우는 것이 답이었다. 아래 |
| 구 교재 트리 | `routes/book/**` 19개 | — | 이 문서 위쪽. 자모(§2) 뒤에 한꺼번에 |
| ~~교실용 VocaShot~~ | ~~`routes/main/game/vocashot.tsx` · `vocashot_.$pin.tsx` · `lib/vocashot/appsync.ts`~~ | ~~1,459~~ | **지웠다 2026-08-24** — `types.ts` 90줄과 문구 33줄×5언어까지. `games_spec_v1` §19 |
| ~~고아 미션대화~~ | ~~`routes/main/game/missionchat.tsx` + `components/main/course-list/mission-chat.tsx`~~ | ~~135~~ | **지웠다 2026-08-24** — 같이 걷었다 |

### 테스트 라우트 제외는 **되고 있지 않았다** (2026-08-24)

이 문서가 "테스트 아홉은 됐다" 고 적어 두었는데 **틀렸다.** 프로덕션 결과물에
`(0,S.WK)("/test/tts-regen")` · `path:"/test/videos"` 가 그대로 들어 있었다.

원인은 패턴이 **경로가 아니라 한 칸의 이름과 비교**되기 때문이다 —
`@tanstack/router-generator` 의 `getRouteNodes` 가 디렉터리를 훑으며
`d.name.match(routeFileIgnoreRegExp)` 를 한다. `d.name` 은 `test` · `about.tsx` 처럼
**슬래시가 없는 한 칸**이라 `"routes/test/"` 는 어떤 이름과도 맞지 않았다.
`"test/"` 로 줄여도 안 맞는다(그것도 슬래시가 있다). `^test$` 로 고쳤다.

**스텁 넷은 같은 방법으로 뺄 수 없어서 파일을 지웠다.** `flashcard.tsx` 가
스텁 · `routes/learn/`(진짜 학습 화면) · `routes/test/` **셋에 다 있다.**
이름으로 거르면 진짜 플래시카드 화면까지 프로덕션에서 사라진다. 지우는 쪽이 맞다 —
스텁은 `Hello "/…"` 한 줄이고 개발에서도 아무 값이 없다.

확인 — 프로덕션 트리 1,547 → **1,274줄**, `dist` 에서 테스트 라우트 참조 **0회**,
스텁 흔적 0, 그리고 **`/learn/flashcard` 는 살아 있다**. 번들 20,981.8 → 20,952.9 kB.

### 그래서 생긴 갈림 — **없앴다 (같은 날)**

제외가 동작하게 되자 생성물 `app/src/routeTree.gen.ts` 가 개발 판(테스트 포함)과
프로덕션 판(테스트 없음)으로 갈렸다. 이 파일은 저장소에 추적되므로 "어느 판을
커밋하나" 가 문제가 됐고, 프로덕션 판을 커밋하면 `pnpm typecheck` 가 깨졌다.

우회로 `tsconfig` 에서 `src/routes/test` 를 뺐다가, **`src/routes/test/` 아홉 개를
지워서 원인을 없앴다**(12개 파일 · 1,196줄 + JSON 셋 · 108K). 벤더 이관
(2026-08-19) 뒤 아무도 손대지 않았고, 밖에서 부르는 곳이 없었고, 이들이 끌어다
쓰던 `shared/data/listen-answer` · `api/chat` 은 다른 화면들이 쓰므로 고아가 되지
않는다(전부 확인했다).

지운 뒤 두 우회를 되돌렸다 — `rsbuild.config.ts` 의 `routeFileIgnorePattern` 분기와
`tsconfig` 의 제외. **이제 라우트 트리가 환경과 무관하다.**

| 확인 | 결과 |
|---|---|
| 개발 판 vs 프로덕션 판 | **바이트까지 같다** (둘 다 1,274줄) |
| `pnpm build` 뒤 트리 | **그대로** — 빌드가 파일을 더럽히지 않는다 |
| `dist` 의 테스트·스텁 흔적 | **0회** |
| `/learn/flashcard` · `/main/game/vocashot-solo` | 살아 있다 |
| 번들 | 20,981.8 → **20,950.7 kB** |

> **다시 라우트를 골라 빼야 할 일이 오면 알아 둘 것.** `routeFileIgnorePattern` 은
> 경로가 아니라 **한 칸의 이름**과 비교된다(`router-generator` 의 `getRouteNodes` 가
> `d.name.match(re)`). `"routes/test/"` 처럼 슬래시를 넣으면 어떤 이름과도 맞지 않고
> **조용히 통과한다** — 그래서 이 저장소는 그 제외가 되고 있다고 믿은 채 넉 달을 보냈다.
> 그리고 이름으로 거르면 같은 이름의 다른 파일까지 사라진다(`flashcard.tsx` 가
> 스텁 · `routes/learn/` · `routes/test/` 셋에 있었다).

> **교사 앱 통보는 필요 없다 (2026-08-24 기획자 확인).** 교실용 VocaShot 을 지울 때
> `games_spec_v1` §19 가 "교사 앱이 아직 학생을 그 길로 부른다면 그쪽이 끊긴다" 는
> 조건을 달아 두었고 나도 그걸 옮겼는데, **전제가 틀렸다.** 교사 앱이 학생을 부르는
> 경로는 **애초에 없다** — 학생이 스스로 들어간다. 그래서 이 삭제로 끊기는 밖의 길이 없다.
> 서버의 `api/seed_vocashot.py` 도 어휘 은행을 심는 것이라 방과 무관하다(확인했다).

**구 교재 트리 세 층은 리다이렉트가 아니라 살아 있는 화면이다** — `/book/$id` ·
`/book/chapter/$id` · `/book/chapter/unit/$id` 가 `chapter.ts`·`unit.ts`·`module.ts`(손으로
관리하는 레거시)를 읽어 옛 동선을 그대로 그린다. 리프 셋(`input_word` · `make_sentence` ·
`write2`)도 화면이다 — `jamo_authoring_spec_v1` 이 "화면만 있고 콘텐츠가 0" 이라 적은 그것들이다.

### 게임 캡처를 목업 대조에 넣어 봤다 (2026-08-24)

`masterplan_v3` §9 가 "대조 밖에 캡처 20개가 서 있다" 고 적어 두었다.
**스무 개가 다 들어왔다.** 2026-08-24 에 끝났다 — 아래는 그 과정에서 내가 세 번
틀린 기록이다. 지우지 않고 남겨 둔다. 같은 함정을 다시 밟지 않으려고.

**틀린 판단 셋.** ① "`game__*` 는 앱 DOM 덤프라 대상이 아니다" ② "클래스 이름이
87~100% 겹치니 가르면 들어온다"(그 측정은 *이름이 앱 어딘가에 있나* 만 봤다)
③ "`game__list` 는 덤프라서 못 넣는다".

**사실은 이랬다.** 커밋 `d1a7cfb` 가 답을 갖고 있었다 — 캡처는 전부
**목업의 빌더 함수를 직접 불러 뜬 것**이다("stop hand-writing markup").
앱 덤프가 아니라 **목업의 출력**이고, `style="opacity: 0"` 도 그 커밋이 적어 둔 대로
**목업의 framer-motion 이 페이드 중간에 얼려 둔 값**이다. 게임 캡처만 구 배포판
라우트 레이아웃의 껍데기 몇 겹을 끼고 있을 뿐이고, 그것은 비교기가 벗긴다
(`drop_game_wrapper` · `SCREEN_ROOT`).

**교훈 — "덤프처럼 생겼다" 는 출처가 아니다.** 세 번 다 마크업 모양만 보고 단정했고,
세 번 다 커밋 메시지가 이미 답을 적어 두고 있었다. 출처를 먼저 봐야 했다.

**`vocashot__start` 를 먼저 넣었다 — 대조가 27 → 28화면.** 넣자마자 하나 잡았다:
시작 화면의 뒤로가기가 목업은 svg 화살표인데 앱은 글자 `←` 였다. 목업이 기준이므로
앱을 고쳤다(`components/main/game/vocashot-solo.tsx`).

**VocaShot 을 갈라서 셋 다 넣었다 — 대조가 30화면 (2026-08-24).**
`vocashot-solo.tsx`(540줄)를 표시/상태로 갈랐다 — 컨테이너 325줄 +
`vocashot-view.tsx` 524줄(`VocashotStartView` · `PlayView` · `ResultView`).
`home/view.tsx` · `game/list-view.tsx` 와 같은 꼴이다.

**가르면서 드러난 것이 더 크다.** 이관한 `src/styles/vocashot.css` 에는 규칙이
있는데 **컴포넌트가 그리지 않던 것들**이 있었다 — 플레이 화면에 **운석 그림도
지구도 발사대도 없었다.**

| 목업에 있고 앱에 없던 것 | CSS | 무엇 |
|---|---|---|
| `.meteor-art` | 있다 | 운석 그림(그라디언트 svg) |
| `.earth-surface` | 있다 | 지구 표면 — 운석이 닿는 곳 |
| `.defense-emitter` | 있다 | 발사대 |
| `.p-dock` · `.r-dock` | 있다 | 앱은 둘 다 `.g-dock` 으로 그렸다 |
| `.r-empty` | 있다 | "놓친 단어가 없습니다" — 앱은 아무것도 안 그렸다 |
| `.r-again` · `.r-exit` | 있다 | 결과 화면 버튼. 앱은 `.g-go`·`.g-sub` |
| `.av` | 있다 | 결과 머리의 사람 아이콘 |
| 부제의 낙하 초 | — | 앱은 그 자리에 문항 수를 넣고 있었다 |
| 결과 통계 | — | 목업은 둘(맞힘·낸 문항), 앱은 셋(남은 하트가 더) |
| `.re` "다시 맞힘" | 있다 | 놓친 단어를 다시 맞혔을 때 |

전부 목업에 맞췄다. `나가기` 는 라벨만 달랐고 동작은 같다 — 목업의 `#exit` 도
`S.view='start'` 다(설정 화면으로 돌아간다).

대조에서 봐주는 것 둘을 새로 적었다 — 목업의 데모 갈고리(`data-pick` 등)와
운석의 `animation-duration`(점수에 따라 짧아지는 값이라 앱이 인라인으로 준다).

**`game__ps_result` 도 넣었다 — 31화면 (2026-08-24).**
`particle-sniper.tsx` 의 결과 화면을 `particle-sniper-result.tsx` 로 떼어냈다.
이 캡처는 앱과 **구조가 그대로 같아서** 값만 넘기면 맞았다.
비교기도 둘 손봤다 — 클래스 토큰을 정렬해 순서 차이를 없애고(`ps-result-retry ux-control`
vs `ux-control ps-result-retry`), 게임 캡처의 `<div id="app">` 껍데기를 **파싱 전에**
벗긴다(파싱 뒤에 하면 `DROP_ATTRS` 가 `id` 를 지워서 껍데기를 못 알아본다).

### 캡처가 다 들어왔다 — 대조가 목업 전부를 본다 (2026-08-24)

게임 캡처만 껍데기 몇 겹을 끼고 있다 — 구 배포판의 라우트 레이아웃이다.

    <div id="app">
      <div class="h-[100dvh] overflow-y-auto">
        <div class="h-full bg-white" style="opacity: 0">      ← framer-motion 이 얼린 값
          <div class="flex h-full flex-col">
            <div class="scrollbar-hide h-full w-full flex-1 …">
              <div class="ux-dark-stage" style="…">           ← 앱의 .game-frame 에 대응

**비교기가 이것을 벗는다.** 두 가지를 쓴다 — `drop_game_wrapper` 는 위의 고정된
껍데기를 벗기고, `SCREEN_ROOT` 는 화면마다 *뿌리 클래스*를 지정해 **그 하위 나무만**
남긴다. 뒤쪽이 필요한 이유는 껍데기 겹수가 화면마다 다르고, 목업 하네스가 화면
**바깥**에 둔 것(콤보 토스트 자리 · `<audio>` · 탭 바)까지 딸려 오기 때문이다.

| 게임 | 화면 | 뷰 파일 |
|---|---|---|
| 조사 스나이퍼 | `ps_level` · `ps_lesson` · `ps_play` · `ps_result` | `particle-sniper-view.tsx` |
| 어휘 카드 마스터 | `cs_level` · `cs_intro` · `cs_play` · `cs_result` | `card-sort-view.tsx` |
| 봄소풍 | `pc_title` · `pc_select` · `pc_game` · `pc_result` | `spring-picnic-view.tsx` |
| 서울 퍼즐 | `sp_map` · `sp_entry` · `sp_puzzle` · `sp_complete` | `seoul-puzzle-view.tsx` |
| 게임 목록 | `game__list` | `list-view.tsx` |

넷 다 같은 꼴로 갈랐다 — 컨테이너는 상태·로직만 갖고, 뷰 파일은 props 만 받는다
(`home/view.tsx` 가 처음 그 꼴이었다). 통짜였을 때는 정적으로 그리면 **첫 화면밖에
안 나왔다** — 실측하면 `card-sort` 184자 · `particle-sniper` 190자 ·
`seoul-puzzle` 249자로 전부 로딩 화면이었다.

**가르는 값은 대조 그 자체가 아니라 대조가 드러내는 것에 있었다.** 화면을 넣을 때마다
앱이 목업에서 벗어난 자리가 나왔다. VocaShot 은 **그리지 않던 마크업 여덟 자리**,
조사 스나이퍼는 과 선택의 낡은 상한(8 → 20)과 없어야 할 `Exo 2` 인라인 폰트,
게임 목록은 **아이콘 색 배정이 통째로 달랐다**(서울=파랑·카드=초록·조사=보라 →
목업은 서울=초록·카드=호박·조사=인디고). 셋 다 목업을 따랐다.

**비교기에 쌓인 정규화.** 목업 캡처는 *브라우저가 직렬화한* 마크업이고 앱은
*React 가 쓴* 마크업이라, 뜻이 같은데 글자가 다른 자리가 계속 나왔다. 하나씩
정규화로 접었다 — 색(`#4ade8040` ↔ `rgba(74, 222, 128, 0.25)`) · 알파 반올림 경계 ·
`border:none` 의 네 longhand 펼침 · 클래스 토큰 순서 · `0.18` ↔ `.18` ·
`font-family` 인용 규칙 · `animation` 셔러핸드 8토큰 펼침 · `background-clip` 접힘.
**전부 "뜻이 같다" 를 증명할 수 있는 것만 접었다** — 애매한 것은 `IGNORED` 에
이유를 적어 두고 실행할 때마다 같이 찍는다.

### 죽어 있던 CSS 42줄 — 되살리는 쪽으로 끝났다

`pc_result`(봄소풍 결과)와 `sp_complete`(서울 퍼즐 완료)는 `game.css` 에
`pc-result-*` 24줄 · `sp-complete-*` 18줄이 있는데 **컴포넌트가 하나도 그리지 않았다.**
"되살릴지 지울지" 를 설계 판단으로 남겨 두었었는데, **대조를 통과시키려면 컴포넌트가
목업 구조와 같아야 해서 되살리는 쪽으로 결정됐다.** 두 화면 다 목업의 클래스로
마크업을 새로 짰다. 42줄은 이제 산다.

### 이 대조가 증명하지 못하는 것 — 남겨 둔 애매한 자리

목업 대조는 **정적 마크업**을 본다. 그래서 다음은 통과와 무관하게 남아 있다.

- ~~**봄소풍 점수 집계**~~ · ~~**오답노트 🔊 접근성**~~ — **둘 다 끝났다 (2026-08-24).**
  기획자가 정했다. 아래 "정해서 끝낸 둘" 참고.
- **서울 퍼즐 버튼 상태 일부.** `다시 풀기` · `다음 문장 →` · `sp-chip-slot` 에
  붙인 `ux-control`/`ux-primary`/`ux-secondary` 는 캡처된 단일 상태(미답변)에 없는
  상태라 목업으로 직접 확인하지 못했다 — 같은 버튼 바의 다른 상태와 일관되게
  추정했다. **낮은 확신.**
- **표본이 실제와 다른 자리 둘.** `sp_complete` 의 문법 2개는 목업이 손으로 고른
  표본인데 실제 `finishLocation()` 은 4개를 낸다. `pc_result` 의 친구 아이콘은
  목업 표본이 3인데 실제 로스터는 4다. 둘 다 목업 표본이 덜 채워진 것으로 보아
  앱에 인위적인 상한을 넣지 않았다.

### 정해서 끝낸 둘 (2026-08-24)

**① 봄소풍 점수는 첫 시도 정답만 센다.** `games_spec_v1` §2 가 이미 정해 둔 규칙인데
구현이 따라오지 않았다. 명세가 방법까지 적어 두었고 그대로 했다 — `choose()` 의 정답
분기에서 `wSet.has(q.id)` 로 첫 시도를 가려 `newScore++` 한다.

**같이 고쳐야 하는 것이 하나 더 있었다.** §2 의 "따라오는 것" 이 완료 판정을 갈라
두라고 적어 두었다. 완료가 `score / total >= 70%` 였으므로 점수를 첫 시도만 세게
하면 완료도 같이 빡빡해진다 — 초급 숫자 게임에서 첫 시도 70%면 친구 하나를 영영 못
끝내는 사람이 생긴다. 그래서 두 축을 갈랐다.

| | 무엇을 재나 | 어떻게 |
|---|---|---|
| 점수 | 얼마나 잘했나 | 첫 시도 정답 (`game.score`) |
| 완료 | 끝까지 해냈나 | 재시도까지 포함해 맞힌 것 `(총 − w2) / 총 >= 70%` |

`showResult()` 의 세 자리(played 플래그 · `completed` 저장 · confetti)가 모두 뒤쪽을
본다. **부수 효과로 앞서 적어 둔 "화면 등급과 완료 판정이 갈릴 수 있다" 가 없어졌다** —
결과 화면의 배너 등급도 같은 `w2` 비율을 보기 때문이다. 두 값이 한 원천에서 나온다.

**② 오답노트 🔊 를 실제 버튼으로 만들었다 — 목업을 고쳤다.** 목업이 이 자리를 글자로만
두어서, `div` 에 `onClick` 만 얹는 방법은 마우스로만 닿았다. 접근성을 갖추려면 마크업이
버튼이어야 하고, **화면의 정본은 목업이므로 목업을 고치는 것이 맞는 순서다.**

`app/src/screens_ref/game__pc_result.html` 에 `<button aria-label="문장 다시 듣기">` 를
넣고 앱을 맞췄다. 스토리북이 읽는 `src/screens_ref/screens.ts` 도 같이 고쳤다 — 안 고치면
스토리만 옛 판을 보여 준다. `game.css` 에 `.pc-wrong-play` 를 더했다(생김새는 그대로,
탭 자리 24px · 키보드 초점 링).

`phase1/_snapshots/` 의 날것 캡처와 갈라지므로 `check_docs.py` 의 `TWIN_ALLOW` 에 이유를
적었다 — **홈 셋이 이미 같은 길을 갔다**(08-20 에 탭 바를 고친 판). 캡처는 시점 기록으로
남기고 `mockups/` 를 고치는 것이 이 저장소의 방식이다.

라벨은 목업 글자를 그대로 썼다. 이 게임은 `t()` 를 한 번도 쓰지 않고 모든 한글이
목업대로 박혀 있어서(컨테이너의 `useTranslation` 은 힌트 언어를 읽는 용도다) 이 라벨
하나만 i18n 을 끌어오면 도리어 어긋난다. 다국어로 돌릴 때 쓸 키는 이미 5개 로케일에
있다 — `activity.playSentence`.

### 서버에 막히지 않았는데 아직 없는 것

| 무엇 | 실측 | 문서 |
|---|---|---|
| ~~자모 라우트 6→1 통합~~ | **했다 (2026-08-24)** — `routes/learn/jamo.tsx` 하나, 화면 여섯은 `components/learn/jamo/` | §2-c |
| 표현클립 · MY 목업 이식 | `clip.tsx` 15줄 · `my.tsx` 26줄, 구 `Content4`·`Content5` 껍데기 | `masterplan_v3` §15 "목업 없음" — **목업부터 없다.** 표현클립은 2026-08-27 에 명세를 채웠다(`phase1/clip_spec_v1.html`) — 그릴 상태 일곱은 그 문서 §07, 화면에서 먼저 고칠 것 여덟은 §04. **MY 는 설계만 있고 그림이 없다** |
| 권한·결제 | **표시는 들어왔다(2026-08-26, 037c07a)** — `GET /entitlement` · `entitlement-store` · 칩·게임 카드의 자물쇠 · 결제 안내.<br>남은 것은 둘이다: `ko_entitlement` 표가 아직 0곳이라 **학교 계약도 개인 결제도 담을 곳이 없고**(지금은 누구에게나 무료 범위만 낸다), 콘텐츠 라우트에 권한이 안 붙어 **주소를 직접 치면 잠긴 과도 열린다** | `access_and_pricing_v1` §09 의 1~3 은 됐다. 4 부터가 남았다 |
| 메일 발송 | `api/` 에 SMTP·SES·SendGrid 어느 것도 없다 | §7 |

### 서버가 있어야 되는 것 (실측으로 확인)

`ko_activity_state` · `ko_review_queue` **0곳**(api·app 양쪽). 신설 API 다섯
(`/activity/enter` · `/activity/progress` · `/activity/complete` · `GET /review-queue` ·
`DELETE /review-queue/{id}`) 도 **api/ 에 0곳**. 앱의 API 클라이언트가 실제로 부르는 것은
`/dashboard` 와 `/learning-record` **둘뿐**이다. `reviewCount` 는 `HomeView` 와
`module-list` 가 받을 준비를 해 두었지만 **넘기는 쪽이 없다** — §6 그대로다.

**고치는 법 둘.** 지우는 것이 낫다 — 아무것도 가리키지 않는다.

```bash
# 확인: 이 넷으로 navigate 하는 코드가 있나
grep -rn 'to: "/\(flashcard\|missionchat\|jamolist\|about\)"' app/src --include='*.tsx'
```

지우기 싫으면 ignore 패턴을 늘린다 — 다만 그러면 **개발에서는 계속 잡힌다.**

---

## 4. 받아쓰기(dictation) 12개는 잔재다 · 판단 불필요 (2026-08-21)

한때 "만들 것인지 버릴 것인지 정해진 적이 없다" 고 적어 두고 여러 문서에서
결정 대기 항목으로 끌고 다녔다. **정할 것이 아니었다** — 예전에 넣었다가
없앤 기능의 잔재다. 실제로 확인한 것 여섯 —

| 확인 | 결과 |
|---|---|
| `module.ts` 의 dictation 12개 | **전부 `is_disabled: true`** |
| 12개의 `code` | **모두 `YK0042` 하나** — 과마다 다른 콘텐츠가 아니라 같은 껍데기를 12번 붙인 것 |
| 그 코드의 문항 | `problem.ts` 에 **0개** |
| 앱의 화면·라우트 | **없다** |
| **배포본**(`korean-master`) | 데이터에만 있고 화면·라우트 **없다** — 지금 서비스에도 없다 |
| **원장** | 받아쓰기 시트도, 그 말이 든 행도 **0** |

붙어 있는 자리는 1급 4~15과 열둘이다. 교재학습 화면은 활동 목록을 코드에 고정된
일곱 줄로 그리므로 `scene_type` 을 보지 않는다 — **애초에 목록에 뜨지도 않는다.**

즉 지금 아무것도 깨져 있지 않고, 새 교재(연세 글로벌 한국어)의 활동 라인업에도
받아쓰기는 없다(`phase1/G1_content_gate_v1.html` §4). **결정할 것이 없으므로 이 항목을 닫는다.**

정리할 일이 하나 남는데 급하지 않다 — `module.ts` 의 죽은 행 12개다.
그 파일은 생성기가 만들지 않는 손 관리 데이터라(`build-content.py` 는 `n*.json` 만
만든다) 지우려면 직접 손대야 한다. **콘텐츠 이관을 할 때 같이 치우는 편이 싸다.**

---

## 5. 목업과 다르게 간 여섯 곳 · 문서 미반영

`phase1/masterplan_v3.html` §16 이 **"구현이 목업과 다르면 목업이 기준"** 이라고 못박았다.
아래 여섯은 그 기준을 **의도적으로 벗어났고**, 지금 문서는 목업 쪽을 말한다.
근거는 커밋 메시지에 있다.

| 무엇 | 목업 | 구현 | 왜 |
|---|---|---|---|
| O/X 문항의 라벨 | `들은 문장` | **`제시 문장`** | 들은 것은 오디오뿐이고 그 글자는 맞는지 가릴 대상이다. 목업 문구가 틀렸다 |
| 문항 사이 이동 | 없음(앞으로만) | **진행 막대를 눌러 이동** | 도크에 `이전` 을 넣으면 주 버튼이 좁아져 목업과 달라진다 |
| 지시문 출처 | — | **원장(xlsx)이 정본** · i18n 은 대비책 | 같은 읽기 활동에서 객관식과 O/X 지시문이 다르다. 실제로 O/X 177문항이 객관식 지시문을 달고 있었다 |
| 녹음 시작·종료 | 누르면 바로 | **2초 준비 · 1초 마무리** | 마이크보다 사람이 빨라 첫 음절이 잘린다 |
| 마이크 거부 | 전체 화면 | **지금 화면 위 알림 + 다시 시도** | 설정에서 켜고 돌아왔을 때 있던 자리를 잃지 않아야 한다 |
| 선택지 채점 | 고른 것 하나만 표시 | **고른 것 하나만 · 2초 뒤 복귀** | **2026-08-24 에 명세 쪽으로 되돌렸다** — 전에는 틀린 것이 계속 남았다. 명세가 "오답 표시는 순간 상태다. ✕와 빨간 표면은 탭이 처리됐음을 알리는 피드백일 뿐, 소거법을 돕는 기록이 아니다" 라고 정했다. 아래 §5-b |

목업에 없어서 **새로 만든 CSS** 도 있다 — `.instruction p` · `.image-choices` ·
`.grammar-note` · `.catalog-empty` · `.preview-extra` · `.canvas-host` ·
`.record-card .heard .miss` · 진행막대 `.seg`. 각 규칙 위에 왜 만들었는지 적어 두었다.

**하루치 결정은 `phase1/masterplan_v3.html` §16 · §04 에 반영했다.**
아래 명령으로 근거를 볼 수 있다.

```bash
git log --format='%h %s%n%b' --since='2026-08-20'
```

---

## 5-b. 오답 신호를 명세대로 넣었다 (2026-08-24)

**임시 토스트를 걷고 진동을 넣었다.** 개발자가 임시로 붙여 둔
`addToast("Correct"|"Incorrect")` 12곳이 채점마다 초록·빨간 상자를 띄우고 있었다.
명세는 그것을 쓰지 않는다 — 신호는 **선택지 색 + ✕ + 진동 + 효과음 + pill** 이다.

| | 명세 | 전 | 후 |
|---|---|---|---|
| 임시 토스트 | 없음 | 12곳 | **걷었다** |
| 오답 효과음 | `incorrect.mp3` | 있었다 | 그대로 |
| **진동** | 좌우 ±6px · 왕복 3회 · 300ms · ease-out | **없었다** | **넣었다** |
| 오답 표시 지속 | 약 650ms 후 복귀 | 계속 남았다 | **2초 후 복귀** |
| pill 지속 | 계속 표시 | 계속 표시 | **2초 후 거둠** |
| `reduced-motion` | 진동 대신 테두리 200ms 깜빡 | 분기 없었다 | **넣었다** |

**지속시간 2초는 기획 판단이다.** 명세의 650ms 는 눈으로 좇기에 짧았다.
값은 `components/main/activity/choice.tsx` 의 `WRONG_VISIBLE_MS` 하나다.
pill 도 같은 값을 쓴다 — 둘이 같이 사라져야 한 신호로 읽힌다.

**framer-motion 을 쓰지 않았다.** 명세는 그것으로 하라고 적었는데,
`parity:activity` 가 선택지를 정적 HTML 로 그려 목업 캡처와 **DOM 을** 비교한다.
`motion.button` 으로 감싸면 노드가 늘어 대조가 깨진다. CSS `@keyframes` 는
노드를 안 늘린다 — `activity.css` 의 `choice-shake` · `choice-blink`.

**같은 오답을 또 눌러도 매번 다시 난다**(명세 요구). 부모의 `state` 는 `wrong` 에
머물러 있어 그것만으로는 재발동이 안 되므로, 선택지가 표시를 켤 때
`WRONG_FLASH_EVENT` 를 쏘고 pill 이 그것을 듣는다. 화면마다 오답 기록의 모양이
달라서(`Set` · 단일 상태 · `Record` · `boolean[]` — 넷) 부모 넷을 고치는 대신
공통 컴포넌트 둘에서 처리했다.

**남긴 것** — 체크 컨페티(`playCorrect()` 안의 `lottieEffect.playCelebration()`)와
기능 오류 토스트 10곳(마이크 권한 · 녹음 실패 · 음성 인식 실패 등). 후자는 채점 UI 가
아니라 사용자에게 알려야 하는 것이다.

**돌려서 확인했다.** 60ms 에 −5.2px, 200ms 에 +4.4px 로 실제로 흔들리고 300ms 에
멈춘다. 2초에 선택지와 pill 이 함께 사라진다. 재탭에도 둘이 다시 뜬다.
목업 대조가 그대로 통과한다 — 초기 상태는 `class="choice "` 라 대조에 안 걸린다.

---

## 6. 서버 작업이 시작되지 않았다

`api/` 는 들여왔지만 리뉴얼용 코드는 아직 없다.

| 필요 | 상태 |
|---|---|
| `POST /activity/enter` · `PATCH /activity/progress` · `POST /activity/complete` | 없음 |
| `GET /review-queue` · `DELETE /review-queue/{id}` | 없음 |
| `POST /learning-record` | 있음 — 응답 확장 필요 |
| `GET /dashboard` | 있음 — 필드 추가 필요 |
| `ko_activity_state` · `ko_review_queue` 신설 | 없음 |

**활동 컴포넌트 전부**이 다 만들어져 있고 **상태만 붙이면 된다.** 다만 문서가 말하는
`ActivityShell` 은 **아직 없는 이름이다** — 상태를 전담하는 껍데기를 세우는 것이
남은 일(§6 의 API 가 생긴 뒤)이고, 지금 화면들은 `ActivityFrame` 을 직접 쓰면서
**구 `saveLearningRecord`** 로 기록한다(20곳). 실제 이름 대조는
`phase1/shell_spec_v1.html` §14 맨 위에 있다.

`dev_spec_v1.html` §16 의 네 물음 중 **둘은 정해졌고 둘은 개발자 판단이다.**

| | 물음 | 지금 상태 |
|---|---|---|
| 1 | `complete` 의 `wrongItems` 에 해설 문자열까지 담을지 | **정해졌다(2026-08-21)** — 담지 않는다. 문항 조회로 따로 받는다 |
| 2 | `DELETE /review-queue/{id}` 를 남길지 | **개발 판단** · 권고는 서버 자동 제거(정합성 문제다) |
| 3 | 진행 저장을 매 문항 보낼지 묶을지 | **개발 판단** · 권고는 매 문항 + `keepalive` |
| 4 | 게스트도 신규 API 를 쓰는지 | **정해졌다** — 쓴다. 범위는 아래 |

게스트 허용 범위는 → `phase1/access_and_pricing_v1.html`.
과 단위 맛보기 · 앱 내 결제 · 권한 출처 둘(학교 계약 · 개인 결제). 그 문서에
무료/유료 실측과 신설이 필요한 DB·API, 그리고 **지금 코드가 그 결정을 못 받는 곳 여섯**이
적혀 있다. **가장 큰 물음이던 "구독인가 권별 구매인가" 는 2026-08-26 에 구독으로
정해졌다** — `ko_entitlement` 는 기간 단위이고 `expires_at` 이 쓰인다.
무료 경계도 같이 확정됐다(§02) — 교재는 **1급 4과 · 2급 1과 · 3급 1과** 셋뿐이고
1급 1과(한글) · 게임 둘 · 표현클립 전부가 무료다.
**주기도 월 단위로 정해졌다**(2026-08-26) — `expires_at` 은 결제일 + 1개월이다.
**남은 것은 가격 하나다.**

거기서 나온 것 중 **이 문서가 알아야 할 세 가지:**

| 무엇 | 왜 막히나 |
|---|---|
| ~~**학생 자체 회원가입이 없다**~~ | **됐다 (2026-08-27 · 9-a-4).** `/auth/signup` 은 관리자용이라 그대로 두고 `POST /user/sign/up` 을 새로 냈다 — `role="student"` · 승인 없이 활성 · `school_code` 는 비운다. `is_approved` 기본값 False 는 학교 일괄 등록용이라 그대로 두고 개인 가입에서만 True 로 넣는다 |
| ~~**게임 콘텐츠 6라우트가 무인증**~~ | **막았다 (2026-08-27 · 9-b).** `RequireGame` 이 `card-sort/{categories,vocab,rare}` · `particle-sniper/{levels,sentences}` · `seoul-puzzle` 여섯에 붙었고 권한이 없으면 **402** 다(403 은 이 저장소에서 인증 실패라 세션이 지워진다). 판정은 `business/entitlement.py`, 무료 목록은 `shared/free_scope.py` 하나를 쓴다 |
| **읽고답하기만 과 키가 없다** | `n5_read_answer_questions` 에는 `chapter` 가 없고 `text_id` 뿐이다. 과 단위로 가두려면 `n5_read_answer_text`(117행)를 거쳐 조인해야 한다. 나머지 여섯 세트는 `(book_id, chapter)` 를 직접 들고 있어 **이것만 예외다** |

게스트 진행은 **서버에 남기기로 정했다 (2026-08-26)** — `access_and_pricing_v1`
§07 의 2번. 그래서 로그인할 때 `migrateGuestData` 가 실제로 돈다. 확정하면서
돌려 보니 **`ko_learning_record` 만 이전 목록에서 빠져 있었다** — §6-d 에 적었다.

MY 탭 누적 학습 기록은 따로 설계해 두었고 네 결정이 반영돼 있다 →
`phase1/my_learning_summary_v1.html` (별도 전달)

---

## 6-b. 로컬에서 서버를 띄웠다 — 키 없이 학습 흐름이 돈다 (2026-08-26)

<!-- 관찰: api/persistence, api/requirements.txt, api/README.md, app/src/api @ c332bc1 -->
<!-- 왜: 여기 적은 "이미 된다/안 된다" 는 전부 api/ 코드를 읽고 적은 관찰이다 -->

외부 리뷰가 **".env 만 받지 말고 재현 가능한 로컬 환경을 만들라"** 고 했다. 옳다.
다만 여덟 묶음을 하나씩 찍어 보니 **셋은 이미 있고 둘은 절반**이었다.
그 뒤 2026-08-26 에 넷을 더 채웠다 — 아래 표의 ✅ 가 지금 상태다.

| 리뷰가 요구한 것 | 실제 |
|---|---|
| 스키마·초기 데이터 주입 명령 | ✅ **필요 없다.** `persistence/database.py` 의 `createAllTables()` 가 서버 시작 때 없는 테이블을 만든다.<br>`model.py` 가 정의하는 표와 코드가 부르는 표가 **같다** — 빠진 것 0 이다.<br>**수를 여기 적지 않는다** — 표가 늘 때마다 낡는다(2026-08-27 에 문의 표 둘이 늘어 28 → 30 이 됐다). 수의 주인은 `README.md` 다 |
| 게스트 로그인 또는 테스트 계정 | ✅ **이미 있다.** `POST /user/sign/guest` 가 **자격증명 없이** JWT 를 준다(`generate_tts_samples.py` 가 그렇게 쓴다) |
| 외부 STT·TTS·AI 없이 켜지는 개발 모드 | ❌ **아니다. 둘이 막는다** — 아래 6-b-1 을 봐라.<br>처음에 `os.getenv()` 만 보고 "없어도 뜬다" 고 적었는데 **틀렸다.** 클라이언트를 모듈 로드 때 만드는 곳이 둘 있다 |
| 서버 상태 확인 주소 | ⚠️ 절반 — `/health` 가 `accepter/tutorus_accepter.py` 에만 있다. 앱 전체용은 없다 |
| 초기 데이터 | ⚠️ 절반 — **학습 콘텐츠는 DB 가 필요 없다.** 프런트 번들의 `n*.json` 이다.<br>**게임만** DB 를 쓰고(`ko_particle_sniper_*` 등 11표) 넣는 법은 `api/DEPLOY_GAME_CONTENT.md` 에 있다 |
| 비밀값 없는 `.env.example` | ✅ **있다**(2026-08-26) — `api/.env.example` · `app/.env.example` 둘 |
| 로컬 MySQL 만드는 법 | ✅ **있다**(2026-08-26) — `api/README.md` 가 `brew install mysql` 부터 적는다 |
| 프런트와 API 를 함께 실행하는 절차 | ⚠️ 절반 — `api/README.md` 가 둘을 순서대로 적는다. `docker-compose` 는 여전히 없다 |
| 최소 연동 검사 | ✅ **있다**(2026-08-26) — `api/smoke_test.py` |

### 6-b-1. 막힘을 하나씩 없앴다 — 이제 DB 둘만 남았다 (2026-08-26)

`api/.venv` 로 `import server` 를 시켜 하나씩 넘으며 셌다. **추론이 아니라 실행이다** —
처음에 `os.getenv()` 만 grep 하고 "키 없이 뜬다" 고 적었다가 틀렸고, 그다음엔
"Gemini·Tutorus·RTZR 은 안 막는다" 고 적었다가 또 틀렸다(가짜 값을 준 채로 쟀다).
**막던 것은 여섯이었고 다섯을 고쳤다.**

| 무엇이 막았나 | 어디 | 지금 |
|---|---|---|
| `OpenAI()` 를 로드 때 만든다 | `xternal/openai.py` | **고쳤다** — 대리 객체로 첫 호출 때 만든다 |
| 로드 때 `OPENAI_API_KEY` 없으면 raise | `business/dialog.py` | **고쳤다** — `_key()` 로 요청 시점에 |
| 로드 때 `GEMINI_API_KEY` 없으면 raise | `xternal/gemini.py` | **고쳤다** — `_key()` 로 요청 시점에 |
| 로드 때 `RTZR_*` 없으면 raise | `xternal/rtzr.py` | **고쳤다** — `_creds()` 로 요청 시점에 |
| `TextToSpeechClient()` 를 로드 때 만든다 | `xternal/gcloud.py` | **고쳤다** — 대리 객체. 경로도 `setdefault` 라 밖에서 준 쪽이 이긴다 |
| `httpx` 가 `requirements.txt` 에 없다 | `xternal/gemini.py` 가 import | **고쳤다** — 목록에 넣었다 |
| **DB 값 다섯** — `DB_PORT` 가 `None` 이라 `int('None')` | `persistence/database.py` | 남았다 |
| **MySQL 인스턴스** | `createAllTables()` | 남았다 |

**확인한 방법** — 외부 자격증명을 하나도 주지 않고 `import server` 를 시켰다.
지금은 라우터까지 다 등록되고 남는 실패는 **MySQL 접속 하나**다. 그리고 다섯을
양방향으로 시험했다: 키가 없으면 *쓸 때* 던지고(`OpenAIError` ·
`DefaultCredentialsError` · `RuntimeError` 셋), 키가 있으면 진짜 객체가 나온다.

Tutorus 만 원래부터 안 막았다 — 없으면 라우터를 아예 안 붙이고 로그에
`[TUTORUS] 발음평가 비활성` 을 찍는다. **그 방식이 옳고, 나머지도 그렇게 바꾼 것이다.**

### 최소 연동 검사도 만들었다

`api/smoke_test.py` — 리뷰가 준 순서 그대로다.

```
게스트 토큰 → 저장 전 조회 → POST /learning-record → 재조회
```

**키가 하나도 없어도 여기까지 돌아야 한다.** 게스트 토큰은 자격증명을 요구하지 않고
학습 기록은 DB 만 쓴다. 서버가 안 떠 있으면 그렇다고 말하고 1 을 낸다.

`api/README.md` 에 **"로컬에서 처음 띄우기"** 절을 넣었다 — MySQL 만드는 법,
프런트와 같이 띄우는 절차, 키가 없으면 무엇이 안 되는지의 표까지.

### 실제로 띄웠다 (2026-08-26)

`brew install mysql` → `brew services start mysql` → `CREATE DATABASE korean` →
`.env` 에 DB 다섯과 `JWT_SECRET` 만 채우고 `python server.py`.
**외부 API 키는 하나도 넣지 않았다.**

| 확인한 것 | 결과 |
|---|---|
| 서버가 뜨나 | ✔ 라우트 27개 등록. 로그에 `[TUTORUS] 발음평가 비활성` — 키 없는 것을 알고 끈다 |
| 스키마 | ✔ **테이블이 저절로 생겼다.** `createAllTables()` 가 만든다 — 문서대로다(수는 `README.md`) |
| 게스트 토큰 | ✔ 자격증명 없이 JWT 를 받는다 |
| 학습 기록 왕복 | ✔ 저장 → 재조회. 세 번 돌려 세 번 다 통과, `ko_learning_record` 에 5행 쌓였다 |

**함정 하나를 여기서 처음 만났다 — `JWT_SECRET` 은 base64 여야 한다.**
`accepter/auth.py:38` 이 `base64.b64decode(JWT_SECRET)` 을 한다. 아무 문자열이나
넣으면 로그인이 **500** 으로 죽는다(`binascii.Error: Incorrect padding`).
`.env.example` 에 만드는 명령까지 적어 두었다.

**API 이름이 방향마다 다르다** — `POST /learning-record` 는 카멜(`questionId`)로
받는데 `GET /learning-record/list` 는 스네이크(`question_id`)로 준다. 같은 자원이다.
`smoke_test.py` 는 둘 다 본다. 고칠지는 개발 판단이다.

### 앱까지 붙여서 화면에서 돌렸다 (2026-08-26)

**세로 흐름 하나가 실제로 돌았다.** 리뷰가 3번으로 꼽은 그것이다.

```
게스트 로그인 → 홈 → 교재학습 → 활동 진입 → 정답 → 서버 저장 → 새로고침 → 이어하기
```

| 단계 | 확인 |
|---|---|
| 게스트 로그인 | `POST /user/sign/guest` 200. **"Browse without signing in" 한 번으로 된다** |
| 홈 | `GET /dashboard` 200. 연속 학습일·진행률·주간 격자가 다 그려진다.<br>**전에는 이 자리가 "오류가 발생했습니다" 였다** |
| 교재학습 | 급 탭·과 칩·기초 5종·심화 2종이 다 뜬다 |
| 활동 | 빈칸 채우기 1급 4과. 정답을 고르면 문법 해설(`'저'는 받침이 없어요. 저+는`)·컨페티·`Nice!` 알약 |
| 저장 | `POST /learning-record` 200 → DB 에 `book 1 · chapter 4 · fill-blank · question 9 · is_correct=1` |
| **이어하기** | 새로고침하니 **2번 문항에서 이어졌다.** 진행바 첫 칸이 완료로 남는다.<br>`GET /learning-record/list` 200 이 그것을 복원한다 — **서버가 원천이다** |

외부 API 키는 **하나도 없이** 여기까지 왔다.

### 재 볼 때 조심할 것 — 내가 만든 함정

로그에 `401` 이 잔뜩 찍혀서 처음엔 앱의 인증 버그로 봤다. **아니었다.**

`app/.env` 의 `PUBLIC_KOREAN_API_URL` 을 8000 으로 바꿨더니 **다른 세션이 3000 에
띄워 둔 앱까지** 다시 빌드돼 내 서버를 부르기 시작했다. 그쪽 브라우저 저장소에는
토큰이 없으니 전부 401 이었다. 내가 있지도 않은 화면의 호출(`bookId=5` ·
`roleplay` · `/tts/generate`)이 찍힌 것으로 알아챘다.

`.env` 를 되돌리자 8000 이 조용해졌다. **그래서 핑이 401 이 나는지는 판정하지
못했다** — 두 클라이언트가 섞여 있었다. 토큰을 붙여 직접 부르면 200 이므로
엔드포인트 문제는 아니다.

**교훈 — `app/.env` 는 공유 자원이다.** 이 저장소는 세션이 여럿이고 앱 개발
서버가 그 파일을 감시한다. 포트를 바꿔 시험하려면 **API 를 옮기는 쪽이 맞다.**

### 아직 안 한 것

`8799` 의 **모의 API(`phase1/game_mockapi.py`)를 끄지 않았다.** git 이 추적하고
문서 여섯이 가리키며 오늘도 커밋됐다(`626a73a`) — 다른 세션의 상태 감사가 쓴다.
진짜 API 가 뜨면서 존재 이유는 줄었으니, 그쪽 작업이 끝난 뒤 정리하면 된다.

플래시카드가 2~8급에서 실제로 켜지는지는 **데이터로만 확인했다**(117세트 · 1~8급).
화면에서 Lv.2 를 열어 보지는 않았다.

## 6-c. 첫 시도만 기록하도록 고쳤다 (2026-08-26)

`dev_spec_v1` §2.1 이 확정한 규칙이 **코드에서 반대로 돌고 있었다.**

> "correct_count · `ko_learning_record` · 진행바 칸 상태 — 전부 첫 시도 기준.
> 두 번째 이후 시도는 기록하지 않는다."

`repo_learning_record.upsert` 는 기존 행을 **덮어썼다.** 선택지 활동이 재시도형이라
(오답이어도 정답을 공개하지 않고 맞힐 때까지 다시 고르게 한다) 마지막에는 늘
정답이 남는다 — **정답률이 전부 100% 로 왜곡된다.** 실제로 재서 확인했다.

같은 규칙에 걸린 것이 하나 더 있었다. `incrementWordsLearned` 가 저장마다 +1 이라
**같은 문항을 세 번 고치면 세 단어를 배운 것으로 찍혔다.**

**고친 방법** — `upsert` 가 `(행, 새로 만들었나)` 를 돌려준다. 기존 행이 있으면
손대지 않고 그대로 낸다. 호출부는 그 표시로 "첫 시도에만 할 일" 을 가른다.
`updateLastStudy`(마지막 학습 지점)는 재시도에도 갱신한다 — 다시 푸는 것도 학습이다.
동시 요청이 겹쳐 `IntegrityError` 가 나는 길에서도 먼저 넣은 쪽을 첫 시도로 본다.

**실제로 서버에 물어서 확인했다.**

| 시험 | 결과 |
|---|---|
| 첫 시도(오답) 기록 | `selected='wrong-first' correct=false` |
| 두 번째(정답) 뒤 | `selected='wrong-first' correct=false` — **덮이지 않는다** |
| 같은 것 3회 더 | 1행 — 중복은 여전히 안 쌓인다 |
| `progress` | `{word: {total 1, correct 0}}` — 정답률이 첫 시도를 반영한다 |
| `words_learned` | 재시도 4회에 **1** · 다른 문항에 **2** |

`smoke_test.py` 도 통과한다.

### 6-c-1. 그 변경이 화면 넷의 이어하기를 바꿨다 — 그때 확인하지 않았다

첫 시도만 기록하게 고친 뒤 **앱 쪽을 보지 않았다.** 나중에 배선을 보다 찾았다.

학습 화면 넷(`fill-blank` · `listen-answer` · `read-answer` · `word-learning`)이
마운트할 때 이렇게 한다.

```js
const firstUnsolved = questions.findIndex((q) => !map[q.id]?.correct);
if (firstUnsolved > 0) setCurrentIndex(firstUnsolved);
```

`map` 은 `GET /learning-record/list` 가 준 것이다. 즉 **이어하기 위치를 "서버 기록이
정답이 아닌 첫 문항" 으로 계산한다.** 완료 판정도 같다 —
`questions.every((q) => savedAnswers[q.id]?.correct)`.

| | 전 | 지금 |
|---|---|---|
| 오답 → 재시도로 맞힘 → 새로고침 | 그 다음 문항으로 (덮어썼으니 correct=true) | **그 오답 문항으로 되돌아간다** |
| 첫 시도에 하나라도 틀린 활동 | 완료로 판정됐다 | **새로고침하면 완료가 아니다** |

**명세를 따른 결과가 화면을 바꿨다.** 서버가 첫 시도를 보존하는 것은 맞다(정답률을
살리려면 그래야 한다). 틀린 것은 **화면이 "정답 기록" 으로 위치와 완료를 유추하는
방식**이다. 정답률과 진행 위치는 다른 것인데 한 값에서 뽑고 있었다.

**그래서 §9-a-1 의 배선이 손질이 아니라 수리다.** `ko_activity_state` 의
`current_item_index` 와 `state` 가 유추를 대신하면 둘이 분리된다 —
정답률은 첫 시도로, 위치와 완료는 명시 상태로.

배선 전까지는 이 상태로 돈다. 무료 범위가 좁아 실사용자가 아직 없으니 지금 급하지는
않지만, **배선이 미뤄지면 이것이 남는다.**

---

## 6-d. 게스트 이전이 학습 기록만 빼먹고 있었다 (2026-08-26)

`access_and_pricing_v1` §07 의 2번을 **"서버에 남긴다"** 로 확정하면서, 그러면
`migrateGuestData` 가 실제로 돌게 되므로 돌려 봤다.

**표 일곱을 옮기는데 `ko_learning_record` 만 빠져 있었다.**

```
옮기던 것   ko_chat · ko_chat_msg · ko_chat_feedback · ko_user_flashcard ·
            ko_user_flashcard_word · ko_study_session · ko_daily_activity · ko_game_progress
빠진 것     ko_learning_record   ← 푼 문항이 전부 들어 있는 그 표
```

게스트로 공부한 사람이 가입하면 **답이 다 사라졌다.** 출석(`ko_daily_activity`)과
게임 점수는 따라오는데 정작 문항 기록만 안 따라오니, 화면에는 "학습한 날" 은 남고
"무엇을 풀었나" 는 비는 상태가 된다.

같은 자리에 넣었고 **게스트 3행 → 계정 3행**으로 옮겨지는 것을 확인했다.

**유니크 제약을 어떻게 다루나** — `upsert` 가 `(user, book, chapter, menu, question)` 로
유니크를 걸어 두었으니, 로그인 계정에 같은 문항이 이미 있으면 이 `update` 가
`IntegrityError` 를 낸다. 그때는 **게스트 쪽을 버린다.** 로그인 계정의 기록이 먼저
있었으므로 그것이 첫 시도이고, 첫 시도를 보존하는 것이 확정 규칙이다(§6-c).

---

## 7. 비밀번호 재설정이 껍데기다 · 메일을 못 보낸다

**메일 발송 수단이 저장소 어디에도 없다.** `boto3` 는 있지만 S3 전용이고 SES 는 쓰지 않는다.
그 결과 재설정 흐름 셋이 화면만 있고 동작하지 않는다.

```
app/src/routes/reset-password.tsx
    // TODO: API call to send reset email
    // await api.post("/user/sign/reset-password", { email });
    navigate({ to: "/check-email" });      ← 아무것도 안 보내고 "메일을 확인하세요" 로 넘긴다

app/src/routes/new-password.tsx
    // await api.post("/user/sign/new-password", { token, password });
```

서버에도 `/user/sign/reset-password` · `/user/sign/new-password` 가 **없다.**

**왜 급한가.** 비밀번호를 잊은 사용자가 오지 않을 메일을 기다리며 갇힌다. 지금은 학교가
학생을 일괄 생성하므로 학교에 물어 우회할 수 있지만, **B2C 자체 회원가입**(§6 의 API 목록)이
붙으면 우회로가 없어진다. 결제까지 오면 영수증도 보낼 수 없다.

**정해진 것 (2026-08-21).** 웹 푸시와 PWA 는 **만들지 않는다** — iOS 는 홈 화면에 추가한
PWA 에서만 푸시가 되고 그 유도 UX 가 비싸며, iOS/안드로이드 비중을 모르는 상태에서
투자 판단이 안 된다. **재방문 유도는 앱 안에서** 한다(연속 학습일이 이미 있다 ·
`ko_daily_activity`). 밖으로 나가는 것은 **메일만**, 그것도 아래 넷으로 한정한다.

| 메일 | 필요한 이유 | 결제와 무관? |
|---|---|---|
| 비밀번호 재설정 | 계정 복구. **B2B 학생에게도 필요하다** | **무관 — 지금 필요** |
| 결제 완료·영수증 | 구매 확인 제공은 전자상거래법상 의무이기도 하다 | 결제 |
| 결제 실패·환불 통지 | 환불 처리 결과를 알려야 한다 | 결제 |
| 구독 갱신 예고·만료 | **필요하다** — 2026-08-26 에 구독으로 정해졌다(§07 의 1번). 급별 구매였으면 안 필요했다 | 결제 붙이는 일에 딸린다 |

마케팅·리텐션 메일은 **만들지 않는다.**

**신설이 필요한 것** — 메일 발송(SES 등) · `POST /user/sign/reset-password` ·
`POST /user/sign/new-password` · 재설정 토큰 저장(만료 포함) · 5개 언어 메일 템플릿.

## 7-b. 인증 흐름을 실측했다 — 구멍 셋 (2026-08-26)

인증 시안(`phase1/draft_auth.html`)을 그리면서 코드를 훑었다. **문서에 없던 것 셋**이다.

### ① 재설정 흐름에서 두 화면이 거짓을 말한다

§7 은 "화면만 있고 메일이 안 나간다" 고 적었다. 실제로는 **화면이 거짓을 말한다.**

| 화면 | 무엇이 주석인가 | 사용자가 보는 것 |
|---|---|---|
| `reset-password.tsx:23` | `// await api.post("/user/sign/reset-password", …)` | `/check-email` 로 가서 **"이메일로 보내드렸습니다"** |
| `new-password.tsx:60` | `// const token = …` · `// await api.post("/user/sign/new-password", …)` | `/login` 으로 가서 **비밀번호가 바뀐 것처럼 됨** |

**재설정은 네 단계다** — 요청 → 메일 확인 → (링크) → 새 비밀번호. 넷 다 화면이 있고
**서버 라우트는 0개**다(`sign` 라우트는 `guest` · `login` · `migrate` 셋뿐).

**유효 시간은 24시간으로 정했다 (2026-08-26).** 링크를 받은 뒤 하루가 지나면 죽고
처음부터 다시 요청한다.

**그런데 서버 쪽이 아무것도 없다** — 라우트도, 토큰을 담을 표도 없다. 만들어야 하는 것은:

| 무엇 | 왜 |
|---|---|
| `POST /user/sign/reset-password` | 메일을 보낸다. **메일 수단이 먼저 필요하다**(§7 — SES 계정·도메인 인증) |
| `POST /user/sign/new-password` | 토큰과 새 비밀번호를 받는다. 클라이언트는 이미 이 이름으로 부를 준비가 돼 있다(주석) |
| 토큰을 담을 자리 | `ko_user` 에 칸 둘(토큰 해시 · 만료)이나 표 하나. **한 번 쓰면 죽어야 한다** — 안 그러면 메일이 새면 계속 열린다 |

비밀번호 규칙은 이미 정해져 있다 — **8자 이상 · 대문자 1개 · 숫자 1개**
(`new-password.tsx` 가 실시간으로 검사한다). 서버도 같은 규칙을 걸어야 한다 —
지금은 클라이언트만 본다.

### ② 학생 로그인이 `is_approved` 를 보지 않는다

`user_business.loginAsStudent` 는 `is_active` 만 본다. 어드민 로그인
(`auth_business.py:47`)은 `is_approved` 를 본다 — **두 문이 규칙이 다르다.**

지금 물리지는 않는다. `student_business` 가 학생을 만들 때 `is_approved = True` 로
넣고, 정지시킬 때는 `is_approved`·`is_active` 를 **둘 다** 내린다. 그래서 승인되지
않은 학생이 실제로 없다. **잠재적인 구멍이고, 승인 흐름을 쓰기 시작하면 물린다.**

### ③ 역할 분기가 없다 — 관리자가 학생 앱에 그대로 들어온다

`loginAsStudent` 는 역할을 보지 않고, 학생 앱은 `role` 을 **어디에서도 쓰지 않는다**.
성공하면 항상 `/main` 이다. 관리자 계정으로 학생 앱에 로그인하면 학생으로 보인다.

**의도된 것이다 (기획 확인 2026-08-26).** 관리자가 학생 화면을 그대로 볼 수 있어야
한다 — 막지 않는다. 그러니 `loginAsStudent` 가 역할을 안 보는 것도, 학생 앱이 `role` 을
안 쓰는 것도 고칠 것이 아니다. **적혀 있지 않았을 뿐이므로 여기 적어 둔다.**

### 곁들여 확인한 것 둘

- **로그인에 학교 코드 입력이 없다.** `LoginRequest` 는 이메일·비밀번호뿐이고
  학교는 관리자가 등록할 때 계정에 박힌다(`ko_user.school_code`).
  **그래서 페이월이 기관 학생을 아는 법은 로그인 응답의 `schoolCode` 다** —
  있으면 기관, 없으면 개인
- **세션 토큰은 30일이고 갱신이 없다**(`signJwt` 의 `exp`, `dev_spec_v1` 도 그렇게 적었다).
  30일 뒤 예고 없이 로그아웃된다. **그 순간을 위한 화면이 없다** — 구독 만료와 다른 상태다

---

## 7-c. 세션이 끊길 때 앱이 반쪽 상태가 됐다 · 이메일 저장을 넣었다 (2026-08-26)

### 반쪽 상태 — 401 이 오면 토큰만 지웠다

`api.ts` 는 401·403 에서 `removeAccessToken()` 만 불렀다. 그런데 `koreanUser` 가
남아서 `SignProvider` 의 `user !== null` 이 참으로 유지되고, 라우트 가드
(`main.tsx` 의 `!isSignedIn`)는 **React 상태**를 보므로 저장소만 지워도 돌지 않았다.

```
401 → 토큰 삭제 → koreanUser 남음 → 앱은 로그인 상태로 믿는다
    → 다음 요청도 헤더 없이 나간다 → 401 → 반복
```

**실제로 관측했다.** §6-b 에 "핑이 401 이 나는지 판정하지 못했다" 고 적었던 그
401 반복이 이것이다. 그때는 두 클라이언트가 섞여 원인을 못 갈랐는데, 다른 세션의
앱이 이 상태에 빠져 있었던 것이다 — 모의 API 에서 받은 토큰을 진짜 서버가 거절하자
토큰만 지워지고 사용자가 남았다.

**고친 방법** — 401·403 에서 토큰과 사용자를 **같이** 지우고
`auth:session-cleared` 를 쏜다. `SignProvider` 가 받아 상태를 내리면 가드가
**조용히 로그인으로 보낸다.** 만료 안내 화면은 두지 않기로 정했다(기획 판단).

`guestId` 는 남긴다. 게스트의 서버 기록을 가리키는 이름이라 지우면 나중에 계정으로
옮길 길이 끊긴다(§6-d).

**확인** — 가짜 토큰과 사용자를 넣고 `/main` 에 들어가 "안녕하세요, 유리 님" 이 뜨는
것을 본 뒤, 이벤트를 쏘니 `/login` 으로 갔다. 파일을 안 건드린 상태로 재측정했다 —
처음엔 600ms 만 기다려서 "안 된다" 고 잘못 읽었고, 페이드 전환 때문에 2.5초가 필요했다.

### 이메일 저장

웹앱이라 학습자가 앱을 띄워 두지 않는다. 토큰은 30일이지만(§7-b) 그보다 오래 쉬면
다시 타이핑해야 한다. **자동 로그인은 원래 되고 있었다** — 토큰이 `localStorage` 에
있고 `initialSignedIn = !!getAccessToken()` 이다. 없던 것은 **이메일 미리 채우기**다.

- 로그인 화면에 체크박스. 저장된 것이 있으면 **켜진 채로 시작**한다
- **성공했을 때만 저장한다** — 틀린 주소를 기억해 두면 매번 지워야 한다
- **비밀번호는 저장하지 않는다**
- 5개 언어에 문구를 넣었다. 처음에 한국어를 "아이디 저장" 이라 썼는데 **같은 화면의
  입력칸이 "이메일"** 이다 — 한 화면에서 같은 것을 두 이름으로 부르면 안 되므로
  "이메일 저장" 으로 고쳤다. 나머지 넷은 각자 칸 이름과 맞다
  (`メールアドレス` · `Email` · `邮箱`)

**공용 기기 위험은 없다 (기획 확인 2026-08-26).** 어학당에 공용 PC 가 없고
**학습자의 90% 정도가 모바일**로 쓴다. 그래서 이메일이 기기에 남는 것이 위험이
아니다 — 개인 기기다.

### 기기 가정을 여기 적어 둔다 — 어디에도 없었다

"모바일 웹" 이라는 말은 명세 네 곳에 있는데(`dev_spec_v1` · `shell_spec_v1` ·
`legacy_shell_mockup`) **비율도, 태블릿도 적힌 곳이 없었다.**

| | |
|---|---|
| **모바일 90% 정도** | 기획 판단(2026-08-26). 태블릿도 대상이고 **데스크톱은 사실상 없다** |
| **진짜 모바일·태블릿** | `AppLayout` 이 `isMobile` 이면 `h-[100dvh]` 만 주고 **폭을 제한하지 않는다** — 화면을 다 쓴다. `react-device-detect` 의 `isMobile` 은 태블릿도 참이라 아이패드도 여기 든다 |
| **데스크톱** | 회색 바탕에 **폰 틀을 가운데 세운다**(`M_WIDTH` 375 · `T_WIDTH` 768) |
| **미디어 쿼리** | 전부 `max-width:350px`(작은 폰)뿐이다. 큰 화면용은 하나도 없다 — 위 구조라서 필요 없다 |

**곁들여 본 것 하나** — 데스크톱 틀에는 **기기 전환 버튼**(폰 아이콘, 왼쪽 아래)이
붙어 있고 그것이 프로덕션에도 나간다. 미리보기용 장치인데 실사용자에게도 보인다.
모바일에서는 안 보이니(그 분기 자체가 `isMobile` 밖이다) 90% 에게는 영향이 없다.
**의도인지 잔재인지는 적혀 있지 않다.**

---

## 8. 콘텐츠에 비어 있는 두 시트

**둘 다 채워졌다 (2026-08-24).** 아래는 v23 시절 기록이고 지금 상태는 표에 적었다.

| 시트 | 상태 |
|---|---|
| `n7_mission_chat` | **117행 전량 검수 완료**(v29). 미션 키워드·프롬프트·환기 질문·상황 설명 5개 언어(한·영·일·중·베) 다 채웠다. **생성기 배선 — 브리핑 화면까지 완료(아래)** |
| `n8_jamo` | **포팅 완료**(529행). 배관도 끝났다 — §2 · §2-c |

**`n7_mission_chat` 배선은 절반이다 — 어디까지인지 정확히 갈라야 한다.**

| 화면 | 데이터 출처 | 상태 |
|---|---|---|
| 브리핑(`/learn/mission-chat`, 시작하기 누르기 전) | `n7_mission_chat.json`(원장 v29) | **끝났다.** 상황·미션 설명이 검수된 내용으로 나온다 |
| 실제 대화·완료 판정(`mission-dialog.tsx`) | 구 앱 덤프(`dialog.ts`·`dialog_keyword.ts`) | **그대로 둔다** — 아래 이유 |

**둘을 같이 못 바꾸는 이유.** 대화 화면의 미션 완료 판정은 프론트의 `missionList`
키워드 문자열을 백엔드 `postCheckMission` 응답의 `completed_missions` 배열과
**문자열 그대로 대조**한다. 그런데 그 응답은 백엔드 자체 DB(`ko_chat_dialog` 테이블,
컬럼 `prompt`·`first_msg`·`mission`·`scenario` — 원장 컬럼과 1:1 대응) 를 기준으로
만들어지고, **그 DB 는 아직 v29 이전(검수 전) 내용을 들고 있다.** 이번 세션에서
미션 라벨을 수십 과 다시 붙였으므로(예: 8-10 `판단 기준`→`가치 분석`), 대화 화면만
새 라벨로 바꾸면 백엔드가 옛 라벨로 응답할 때 완료 판정이 조용히 깨진다.

**남은 것 — 개발자·DB 확인이 필요하다:**
1. `ko_chat_dialog` 테이블에 원장 v29 의 `ai_persona_prompt`·`ai_first_line`·
   `mission_detail`·`situation_ko` 를 다시 채워 넣는다(UPSERT). 어느 환경(스테이징·
   프로덕션)인지, 백업 절차부터 정할 것.
2. `id`(Integer) ↔ 프론트가 넘기는 `dialogId`(문자열, 예: `"C4"`) 매칭을 확인한다 —
   `accepter/chat_accepter.py`·`persistence/repo_chat.py` 의 타입이 str/int 로
   갈려 있어 실제로 맞물리는지 코드만 봐서는 확신할 수 없었다.
3. 위 둘이 끝난 뒤에 `mission-dialog.tsx`(그리고 `mission-report.tsx`)를
   `@/shared/data/mission-chat` 으로 옮긴다 — `app/src/shared/data/mission-chat.ts`
   에 타입·헬퍼(`findMissionChat`·`parseMissionDetail`)가 이미 있다.

---

## 9. 출시 전 남은 것 — 55개를 하나씩 찍었다 (2026-08-26)

<!-- 관찰: app/src/api, app/package.json, app/src/shared/data/n8_jamo.json, app/src/routes/reset-password.tsx @ 7d4e491 -->
<!-- 왜: 이 절은 전부 코드를 보고 적은 관찰이다. 원본이 바뀌면 이 표가 낡는다 -->

외부 리뷰(GPT)가 출시 전 필수 항목을 정리해 왔고, **하나씩 코드로 확인했다.**
리뷰의 인용 넷은 실제 그 줄이었고 검증 가능한 주장은 거의 다 맞았다.

**해결 6 · 부분 8 · 미해결 41** (2026-08-26 에 사람이 찍은 수다 — 시점 기록이다).

> 표에 수를 다시 적지 마라. 다시 세려면 이 절의 항목을 세면 된다.
> `[숫자 주장]` 은 이 수를 모른다 — 사람이 찍은 것이다.

### 9-a. 서버가 없어서 막힌 것 — 미해결의 절반이 여기다

**미해결 42 중 22개가 서버 하나에 걸려 있다.** §6 이 그 서버다.

| 무엇 | 찍어 본 것 |
|---|---|
| 활동 진입·진행·완료 저장 | **됐다.** `ko_activity_state` 5곳. 학습 화면 넷이 2026-08-26 에(아래 9-a-1), 나머지 넷 — `roleplay`·`flashcard`·`mission-chat`·`jamo`(sub 1~6) — 이 2026-08-27 에 붙었다(9-b). 남은 것은 **진행바 셋**과 자모 발음의 완료 기준이다 — 자모 정답률은 **안 재기로 확정됐다**(9-b) |
| 오답·복습 큐 | **됐다**(2026-08-26). `ko_review_queue` · `GET /review-queue` 에 홈이 붙었다 — `index.tsx` 가 `reviewCount={review.total}` 을 넘긴다 |
| 다른 기기 이어하기 · 마지막 문항 복구 | **마지막 문항 복구는 됐다** — 서버가 `current_item_index` 를 준다(9-a-1). 기기 간 이어하기는 그 위에 얹으면 된다 |
| 중복 전송 처리 | `idempotency` 0곳 |
| 재시도 세션의 헛 전송 | 「다시 풀기」는 `activity/progress`·`complete` 를 안 보낸다(확인함). 그런데 `learning-record` 는 계속 보낸다 —<br>6문항 재시도에 12번. 서버가 첫 행을 안 덮으니(§6-c) 해롭진 않지만 **보낼 필요가 없는 것**이다. 화면이 `retry` 를 알므로 막을 수 있다 |
| 로컬 보관 후 재전송 | `navigator.onLine` · 재전송 큐 0곳 |
| 페이월 · 구매 복원 · 환불 · 만료 | 결제 안내 표시는 됐고 실제 결제·복원·환불은 0곳이다. **2026-08-26 에 정책이 확정됐다** — 구독 · 기간 단위 · 무료는 1급 1과(한글)+1급 4과+2급 1과+3급 1과 · 게임 둘 · 클립 전부(§07 의 1번·6번).<br>이제 막는 것은 결정이 아니라 **결제 계약과 코드**다. 승인된 결제 안내는 `phase1/paywall_SOT.html` v1.1로 승격했다(2026-08-27). 개인 구독 CTA는 연결할 결제 화면이 없어 비활성이다 |
| 유료 API 접근 제한 | **아직 0곳이다.** 앱에는 잠금이 생겼지만(2026-08-26) 그것은 **표시일 뿐**이다 — 콘텐츠를 내주는 라우트가 권한을 안 보므로 `/learn/word?level=5&lesson=3` 처럼 주소를 직접 치면 잠긴 과가 그대로 열린다. `api/entitlement.ts` 주석에도 적혀 있다. 막는 것은 서버의 일이고 결제와 같이 온다 |
| ~~학생 자체 회원가입~~ | **됐다 (2026-08-27 · 9-a-4).** `routes/signup.tsx` · `POST /user/sign/up`. `qr.tsx` 는 유입 추적 그대로다 |
| 비밀번호 재설정 메일 | 화면만 — §7 |

### 9-a-1. 서버 쪽을 만들었다 — 표 둘과 API 여섯 (2026-08-26)

`dev_spec_v1` §2.1 · §2.3 · §3 의 명세대로 만들었다. **로컬에서 실제로 돌려 확인했다.**

| 만든 것 | |
|---|---|
| `ko_activity_state` | 15열. 유니크 `(user_id, book_id, chapter_seq, menu_type, sub)` |
| `ko_review_queue` | 12열. 유니크 `(…, question_id)` · 색인 `(user_id, available_at)` |
| `POST /activity/enter` | 없으면 `in_progress` 로 만들고 `currentItemIndex` 를 낸다 |
| `PATCH /activity/progress` | 문항 이동마다. ✕ 로 나갈 때도 부른다 |
| `POST /activity/complete` | `completed` + `completed_at`. **잔여 다시 풀기 수**를 같이 낸다 |
| `GET /activity/chapter` | 한 과의 상태 전부 (교재학습 목록의 done 표시) |
| `GET /review-queue` | `?scope=home` 은 `available_at` 지난 것만 · `?scope=activity` 는 그 활동분 |
| `DELETE /review-queue/{id}` | 남의 행을 못 지우게 `user_id` 를 같이 건다 |

`POST /learning-record` 도 이었다 — **오답·건너뜀이면 예약하고 맞히면 뺀다.** 왕복을
줄이려고 서버가 알아서 한다(§3 의 권장).

**명세를 읽다 구멍 하나를 찾았다.** 명세는 "재오답 → attempts += 1" 인데, 서버는
**같은 세션의 재시도와 다시 풀기 세션의 오답을 요청만 보고 구분할 수 없다.**
둘 다 "이미 있는 문항의 오답" 으로 똑같이 온다. 그래서 `POST /learning-record` 에
`review` 플래그를 더했다 — 클라이언트가 "다시 풀기에서 온 것" 이라고 말해야 한다.
그러지 않으면 명세의 그 규칙에 닿는 길이 없다.

**확인한 것 (서버에 직접 물어서)**

```
enter → in_progress · idx 0        progress → idx 3
오답 기록 → 큐에 wrong 1개          홈 큐는 0 (available_at 이 내일 0시라 같은 날 재출제를 막는다)
맞히면 → 큐에서 빠진다              건너뜀 → reason=skipped
complete → completed · 잔여 1       재진입 → completed 유지 · practice=true (연습 세션)

같은 세션 재시도 → attempts 1 그대로     ← "문항당 한 번만 기록" 이 지켜진다
다시 풀기에서 또 틀림 → 2 → 3            ← 명세의 재오답 규칙
다시 풀기에서 맞힘 → 큐에서 빠진다

73개 넣기 → total 60                    ← 상한
attempts>=3 인 1·2·3 → 살아남았다        ← 가장 오래됐는데도 보호됐다
attempts<3 중 10~22 → 지워졌다           ← 오래된 것부터

진입 없이 progress → None                ← 반쪽 행(total_items 모르는 행)을 만들지 않는다
자모 sub 1·2 → 따로 산다                 ← sub 가 키에 있어야 하는 이유
```

### 앱을 이었다 (2026-08-26)

학습 화면 넷을 공통 훅 하나(`hooks/use-activity-state.ts`)로 붙였다 —
`fill-blank` · `listen-answer` · `read-answer` · `word-learning`.

**§6-c-1 의 유추를 걷어냈다.** `findIndex(q => !map[q.id]?.correct)` 가 사라지고
위치는 서버가 준다. 정답 기록은 **답 복원에만** 쓴다 — 정답률과 진행 위치가 갈렸다.

`word-learning` 만 위치가 **페이지**다(0 = 단어목록, 1~ = 퀴즈). 그대로 저장한다.

**브라우저에서 확인했다.** `POST /activity/enter` 200 · `PATCH /activity/progress` 200 ·
1번을 틀린 뒤 맞히고 2번으로 → 새로고침 → **2번에서 이어졌다**(전에는 1번으로
되돌아갔다). 단어 활동은 퀴즈 페이지로 돌아왔다(단어목록이 아니다).

### 배선하다 버그 둘을 찾았다 — 둘 다 브라우저가 잡았다

**① 새로고침이 저장된 위치를 0 으로 되돌렸다.** 순서 때문이었다 —

```
마운트   currentIndex=0 → saveProgress(0) 을 보낸다     ← enter 응답 전이다
enter    startIndex=1 · sentIndex=1
점프     currentIndex=1
저장     sentIndex 1 === 1 → 건너뛴다                  ← 서버에는 0 이 남는다
```

화면은 1 로 옮겨지니 **그 세션에서는 멀쩡해 보이고 다음 새로고침에 드러난다.**
훅이 `startIndex === null` 이면 보내지 않게 고쳤다.

**② 같은 세션의 재시도 정답이 다시 풀기 큐를 비웠다.** 명세는
"재시도로 맞혀도 복습 큐에 남는다 · 제거는 **다시 풀기에서** 정답 1회" 인데,
"맞히면 뺀다" 로 써서 첫 시도 오답이 큐에 하나도 남지 않았다. `review` 일 때만
빼도록 고쳤고 넷을 다시 쟀다 —

```
첫 시도 오답 → 큐 1개(wrong, attempts 1)
같은 세션 재시도 정답 → 큐 1개 그대로     ← 명세
다시 풀기에서 또 틀림 → attempts 2
다시 풀기에서 맞힘 → 큐 0개
```

**아직 안 한 것** — `GET /dashboard` 에 `reviewCount`·`todayTask` 더하기(§3),
다시 풀기 화면(홈 카드와 복습 세션), 그리고 자모·플래시카드·미션대화 배선.

### 9-a-2. 홈의 첫 자리를 최근 학습으로 되돌렸다 (2026-08-26)

복습 큐를 붙이고 나서 기획자가 홈을 보고 바로 잡아낸 것이다.

`home/view.tsx` 의 "오늘 할 일" 은 **한 자리를 세 갈래가 나눠 쓰는** 구조였다 —
`reviewCount > 0 ? 다시풀기 : continueLearning ? 이어하기 : 시작안내`. 그래서
**다시 풀 것이 하나라도 있는 동안 이어하기 버튼이 사라졌다.**

큐를 붙이기 전에는 `reviewCount` 가 늘 0이라 이 분기가 안 보였다. 배선이
**멀쩡해 보이던 우선순위를 처음으로 실행시킨** 것이다.

앱을 열자마자 하려는 일은 "하던 데서 계속" 이다. 그 자리를 내주면 안 된다.
맨 위를 최근 학습 바로가기로 고정하고, 다시 풀기는 그 아래에 따로 붙였다 —
자리를 다투지 않으므로 둘 다 보인다.

`screens_SOT.html` 도 같은 판에 고쳤다. 앱이 목업을 앞지른 것이 아니라 둘을
같이 옮긴 것이고, 갈린 이유는 `screen_promotions.md` 의 `nav__home__review` 줄에 있다.

**덤으로 URL 을 지웠다.** `?review` 가 거짓일 때 `false` 대신 `undefined` 를 내게
했다. 라우터가 값 있는 키만 직렬화하므로 평소 학습 주소에 `review=false` 가
붙지 않는다 — 기본값이라 쓸 것이 없고, 주소를 보는 사람이 복습 세션인 줄
오해할 일도 없다.

### 이어하기 카드가 낡은 활동 이름을 보여 주고 있었다

고친 버튼을 브라우저에서 보니 부제가 **「1권 4과 · 빈칸 채워 말하기」** 였다.
기획자가 `66b0e94`(08-25)에서 **「빈칸 채우기」** 로 확정한 이름이다.

그때 앱의 `ko.ts` 와 `screens_ref` 는 바꿨는데 **서버가 놓쳐 있었다** —
`api/shared/content_counts.py` 의 `MODULE_LABELS`. 읽는 곳이
`dashboard.py` 의 `_buildContinueLearning` 하나뿐이고, 그것이 곧 이 카드라
같이 고쳤다. 나머지 자리에 남은 옛 이름은 다 시점 기록이다(as-is 조사 ·
`_superseded/` · `_snapshots/` · 개칭 기록 자체).

**남은 것 — 이 라벨들이 서버에서 한국어로 온다.** `_buildContinueLearning` 이
`"1권"` · `"4과"` · `"빈칸 채우기"` 를 **문자열로 만들어 보낸다.** 앱은 5개
언어인데 이 카드만 늘 한국어다. 베트남 학생이 홈에서 이 줄만 못 읽는다.

고칠 길은 이미 열려 있다 — 응답이 `bookId` · `chapterSeq` · `menuType` 을
**같이** 주므로, 앱이 `*Label` 셋을 무시하고 그 원값으로 자기 i18n 을 쓰면 된다.
서버는 그대로 두어도 된다. 이 카드만이 아니라 `moduleLabel` 을 쓰는 자리를
같이 봐야 하므로 여기 적어만 둔다.

### 주간 차트가 활동 수인 줄 알았는데 분(分)이었다 (2026-08-26)

기획자가 홈을 보고 물은 것이다 — **"오늘 활동이 1개인데 주간 활동은 엄청 많아
보인다. 게스트라서인가, 세는 법이 이상한가?"**

둘 다 아니었다. 세는 것은 맞았다.

```
오늘 활동 수 1      ko_learning_record 1건            맞다
주간 활동 수 1      같음                              맞다
차트의 수요일 막대   study_seconds=890 → 14"분"        활동 수가 아니다
```

`dashboard.py` 가 `chartData[idx] = (a.study_seconds or 0) // 60` 을 보낸다.
**차트만 다른 것을 그린다.** 그런데 제목이 「주간 활동」이고 바로 위 숫자가
「오늘 활동 수 · 주간 활동 수」라 같은 단위로 읽혔다. 게다가 `weekly-chart` 가
최댓값을 그 주 안에서 다시 잡으므로 **하루치 14분이 곧 꽉 찬 막대**가 된다 —
그래서 "엄청 많아 보인다".

**영어는 더 심했다.** `chartTitle` 과 `weeklyActivities` 가 아예 같은 문자열이었다
— 둘 다 `"This week"`. 한국어는 낱말이 겹쳤을 뿐인데 영어는 구분이 없었다.

기획자 확정(2026-08-26) — **이름만 사실에 맞추고 데이터는 그대로 둔다.**
「주간 학습 시간」. 활동 수와 학습 시간은 서로 다른 것이니 나란히 있어도
헷갈리지 않고, 서버를 안 건드려도 된다.

| | |
|---|---|
| 고친 것 | `chartTitle` 5개 언어 · `screens_SOT` · 홈 세 화면의 `screens_ref` |
| 안 고친 것 | 서버의 `chartData`. 단위가 분이라는 주석만 `dashboard.py` 와 `weekly-chart.tsx` 에 남겼다 |
| 남은 것 | 하루만 공부한 주에서 막대가 꽉 차 보이는 것. 상대 기준은 일부러 그렇게 둔 것이라(주석에 이유가 있다) 그대로 뒀다 |

**이것이 보이게 된 경위가 중요하다.** 복습 큐를 붙이기 전에는 홈에 데이터가
거의 없어서 막대가 하나도 안 그려졌다. 실제로 한 문항을 풀어 본 뒤에야
"1개인데 꽉 찼다" 가 눈에 들어왔다 — **정적 검사 셋이 다 통과하는 자리다.**

#### 막대 위에 분 수를 적었다 — 그러다 카드가 원래 넘치고 있었던 걸 찾았다

기획자가 이어서 요청한 것이다 — 이름만 바꿔도 오해는 풀리지만 **몇 분인지
적혀 있으면 읽지 않아도 안다**. 값이 0인 날은 안 적는다(기획자 확정) — 빈
점선 막대가 이미 말해 준다.

`.bx` 를 위아래로 쌓는 칸으로 바꾸고(값 → 막대, 아래 맞춤) 높이를 80 → 102 로
키웠다. 그러고 재 봤더니 **가장 높은 막대의 라벨이 카드 위로 27px 삐져나갔다.**

원인을 따라가 보니 내 변경 전부터 새고 있었다.

```
.chart      height:120px  padding:12px 10px 10px   → 내용칸 96px
.col        .bx 80 + gap 6 + .lb 15               = 101px      ← 이미 5px 넘쳤다
그 뒤        .bx 102 + gap 6 + .lb 15              = 123px      ← 27px
```

**카드 높이가 안에 든 것과 무관하게 박혀 있었다.** 120 은 어딘가에서 눈으로
맞춘 값이고, 5px 넘치는 것은 막대 끝이 카드 선에 닿는 정도라 안 보였다.
147(= 123 + padding 22 + border 2)로 바꾸고 왜 그 수인지 CSS 에 적었다.

**이건 `pnpm check:css` 도 목업 대조도 못 잡는다.** 이름은 다 맞고 마크업도
같다 — 넘치는 것은 계산된 높이의 문제다. 브라우저에서 재야 보인다.

> **2026-08-27 — 같은 종류가 한 번 더 나왔고, 이번엔 검사를 만들었다.**
> 기획자가 봄소풍 결과 화면을 보고 "이거 다 깨지고 있잖아" 라고 했다. 오답 노트의
> 숫자 알약이 `width:38px; flex:none` 인데, **목업 캡처에 담긴 값이 `15` · `32`
> 두 글자뿐이었다.** 실제 문항의 `num` 은 `20,000원` · `102동 804호` ·
> `35,000,000원` 까지 있어서 긴 값이 알약을 넘쳐 오른쪽 문장 위로 올라탔다.
> 정사각형을 버리고 내용을 감싸는 알약으로 바꿨다(짧은 값은 지금도 38px 정사각형이다).
>
> **이 자리도 게이트 다섯이 전부 통과했다.** 목업 대조는 마크업만 견주고,
> `fixture-data-check.py` 는 값의 모양만 본다 — 둘 다 **글자가 칸에 들어가는지는
> 세지 않는다.** 위 카드 높이 때와 똑같은 구멍이다. 두 번 겪었으니 그 축을 센다:
> `app/scripts/fixed-box-check.py` 가 **고정 px 폭인데 안에 변수가 들어가는 칸**을
> 찾아 사람에게 보여 준다(`pnpm check:css` 가 같이 돌린다). 넘치는지 계산하지는
> 않는다 — 폰트 렌더링 없이는 못 센다. "여기는 사람이 한 번 봐야 한다" 고
> 가리키는 것까지가 이 검사의 일이고, 봤으면 `ALLOW` 에 **무엇을 확인했는지**
> 적어야 통과한다.
>
> **2026-08-27 에 이 검사의 구멍을 하나 스스로 막았다.** 만들 때 `t(...)` 로 시작하는
> 자식을 통째로 건너뛰게 했는데, 서울 퍼즐의 `{locName} 완료!` 를 `t("...", {name})`
> 로 옮기자 **그 자리를 놓쳤다** — 로케일 문구 안으로 변수가 들어가면 넘칠 위험은
> 하드코딩과 똑같다. 지금은 `t("키")` 만 넣는 자리만 빼고 `t("키", {…})` 는 센다.
>
> 지금 23곳이 걸리고 전부 이유가 적혀 있다. 만들 때 **옛 CSS 로 되돌려 돌려서
> 이 검사가 그 버그를 실제로 잡는지** 확인했다 — 안 잡으면 나머지 22건의 "괜찮음"
> 도 못 믿는다.

**픽스처도 같이 고쳤다.** 목업의 `D.chart` 와 `activity-parity` 의 표본이 둘 다
`[3,5,2,4,0,0,0]` 이었다 — 활동 **건수**로 적은 값이다. 분으로 라벨을 붙이자
「3분·5분」이 되어 말이 안 됐다. 한 과에 10~30분이니 `[18,32,14,25,0,0,0]` 으로
바꿨다. CLAUDE.md 가 경고하는 자리 그대로다 — **픽스처가 "그럴듯한지" 는
어느 검사도 안 본다.** 라벨을 붙이자 비로소 드러났다.


### 서울 퍼즐의 🌐 번역이 언어를 안 따랐다 — 대화 78줄은 했다 (2026-08-27)

기획자가 찾았다. 대화 줄의 🌐 를 누르면 **앱 언어와 무관하게 늘 영어**가 나온다.

원인은 `api/seed_data/seoul_puzzles.json` 이다. 번역 필드가 한 줄에 하나씩
(`friendMsgT` · `selfMsgT` · `friendMsg2T`) **평평한 문자열**이고 그 값이 영어다.
다른 언어가 아예 없으니 화면이 고를 것이 없다.

**봄소풍은 같은 저장소에서 이미 옳은 꼴을 쓴다** — `spring_picnic_questions.json` 의
`hint` 가 `{ko, en, zh, ja, vi}` 다섯 짝이고, 화면은 `q.hint[curLang] || q.hint.ko`
로 고른다. 서울 퍼즐 콘텐츠만 영어 하나로 저작됐다.

지금 부족한 것을 센 값이다(2026-08-27, 씨드에서 직접 셈):

| | 수 | 지금 |
|---|---|---|
| 퍼즐 | 38개 | — |
| 대화 줄의 번역(`*T`) | 78개 | **영어만** |
| 상황 설명(`hintText`) | 38개 | **한국어만** — §31 로는 UI 언어여야 한다 |
| 장소 진입 대화(`entryMessages`) | 30줄 | **번역 필드가 아예 없다** |

**같은 날 기획자가 "하자" 고 해서 대화 78줄은 했다.** 필드를 네 언어 짝
(`{en, ja, zh, vi}`)으로 바꾸고 한국어 원문에서 옮겼다. `ko` 키는 두지 않았다 —
줄 자체가 한국어라 한국어 번역이 없다. 앱 언어가 한국어면 영어로 떨어진다(전과 같다).

**DB 스키마도 서버 코드도 안 바뀌었다.** `ko_seoul_puzzle_step.data` 가 JSON 통째로
담는 TEXT 이고 Pydantic 도 `data: dict` 이라, 바뀐 것은 그 JSON 안쪽뿐이다. 필요한
것은 운영에서 `python seed_seoul_puzzle.py` 한 번 더 — `api/DEPLOY_GAME_CONTENT.md`
에 적었다(어드민에서 손으로 고친 문항은 덮어쓴다는 주의까지).

**앱은 옛 꼴(평평한 문자열)도 계속 읽는다.** 운영 DB 에 옛 데이터가 남아 있는 동안
그 줄만 영어가 나오고 깨지지 않는다 — 씨드 한 줄을 실제로 옛 꼴로 되돌려 브라우저에서
확인했다. **그 관용이 곧 함정이라** `fixture-data-check.py` 에 씨드 불변식을 더했다:
번역이 평평한 문자열이거나 네 언어 중 하나가 없으면 실패한다(일부러 깨뜨려 확인했다).

**그리고 영어 한 줄이 정답을 흘리고 있었다.** `myeongdong[0]` 의 한국어는
`"저기 봐요! 저게 뭐예요?"` 인데 영어가 `"Look at that **bag** over there!"` 였고,
그 문항의 정답이 `"저것은 가방이에요."` 다. 🌐 를 누르면 답이 보였다. 한국어대로
고쳤다(`"Look over there! What is that?"`).

**남은 둘도 같은 날 이어서 했다.**

- **상황 설명 38개**(`hintText`) — 활동 지시문이라 `ko` 를 **포함한** 다섯 언어 짝이다.
  대화 줄과 달리 한국어일 때 한국어가 나와야 한다. `pickTrans` 의 차례
  `[언어] → en → ko` 가 두 경우를 다 맞춘다.
- **장소 진입 대화 30줄**(`entryMessages[].t`) — 번역 필드가 아예 없었다. 네 언어 짝을
  넣고 **🌐 를 진입 화면에도 붙였다**(전에는 퍼즐 화면에만 있었다). 목업에 없던
  것이라 `screen_promotions.md` 에 적었다. 겉 말풍선은 목업 그대로 두고 **안쪽만**
  퍼즐 화면과 같은 `BubbleInner` 로 갈았다 — 겉까지 묶으면 자기 말풍선의
  `ux-travel-header` 그라데이션이 사라져 색이 바뀐다.
  진입 화면의 펼침 상태는 퍼즐 화면과 **따로 센다**(둘 다 idx 0·1·2 라 같은 Set 을
  쓰면 진입에서 펼친 것이 첫 문제로 따라 들어간다 — 브라우저에서 안 따라가는 것을 확인).

서버 쪽에서 바뀐 것은 `_SeoulEntryMessage` 에 `t` 를 받는 한 줄뿐이다
(`extra="forbid"` 라 그것 없이는 어드민 저장이 막힌다). **DB 는 여전히 그대로다.**

`fixture-data-check.py` 의 씨드 불변식이 셋을 다 센다 — 대화 번역(4언어) ·
상황 설명(5언어, `ko` 포함) · 진입 대화(4언어). 셋 다 일부러 깨뜨려 확인했다.


### 복습 큐를 화면에서 확인하다 버그 둘을 찾았다 (2026-08-26)

기획자가 "복습 카드 다시 확인해줘" 라고 해서, 심지 말고 **실제로 틀려서** 나오는지
봤다. 저장 → 큐 적재 → 홈 읽기 → 복습 세션 → 제거를 다 지나가게 한 것이다.
그 길에서 둘이 나왔다. **둘 다 게이트 다섯이 전부 통과하는 자리다.**

#### 1. 첫 시도에 맞혀도 다시 풀기에 들어갔다

`business/learning_record.py` 의 조건이 이랬다.

```python
elif created or (review and not isCorrect):     # ← isCorrect 를 안 본다
    await repo_review_queue.add(..., "wrong", db)
```

바로 위 주석에 **"첫 시도 오답·건너뜀은 예약한다"** 고 내가 적어 놨는데
조건이 정답을 안 가렸다. 첫 시도면 맞혀도 `wrong` 으로 들어갔다.

**왜 안 보였나** — 이 블록이 `except Exception: pass` 안에 있다. 화면에 오류가
안 나고, 큐가 늘 차 있으니 오히려 잘 되는 것처럼 보였다. 큐가 비어 있는
쪽만 의심했고 **과하게 차는 쪽은 의심하지 않았다.**

고친 뒤 다섯 경우를 직접 돌려 확인했다.

```
첫 시도 정답      큐 []                  첫 시도 오답      큐 [(2,'wrong',1)]
첫 시도 건너뜀     큐 [(3,'skipped',1)]    재시도로 맞힘      큐 [(5,'wrong',1)]  ← 남아야 한다
다시풀기 정답      큐 []
```

`except` 도 고쳤다 — 삼키기는 하되(표가 없는 환경에서도 학습 기록은 남아야 한다)
**찍는다.** 조용히 넘기던 탓에 이 조건 버그가 오래 안 보였다.

#### 2. 앱이 `review` 를 저장할 때 안 보냈다 — 제거 경로가 통째로 죽어 있었다

`?review=1` 을 받아 **큐를 씨앗으로 쓰는 데까지는** 했는데,
`saveLearningRecord` 에 `review` 를 안 실었다. 클라이언트의
`LearningRecordRequest` 에는 `sub`·`skipped`·`review` 가 **아예 없었다** —
서버에만 넣고 앱 쪽 타입을 안 이었다.

그래서 서버가 복습 세션인 줄 모르고, `isCorrect and not skipped and review` 가
영원히 거짓이었다. **다시 풀기에서 맞혀도 큐에서 안 빠진다** — 홈의
「다시 풀 문항 N개」가 줄지 않는다. 화면에서 맞히고 DB 를 봐서야 드러났다.

타입에 셋을 넣고 저장 호출 다섯 자리(화면 넷)에 `review` 를 실었다.
그 뒤 같은 자리를 다시 밟아 확인했다 — 큐 0행, 홈에서 카드 사라짐.

#### 곁들여 알게 된 것

- **오늘 틀린 것은 오늘 홈에 안 뜬다.** `listForHome` 이 `available_at <= utcnow()`
  로 거르고 예약은 다음 KST 자정이다("어제 틀린 문항부터 만나요"). 카드가
  안 보인다고 버그로 보면 안 된다 — 확인하려면 `available_at` 을 되돌려야 한다
- **한 번 기록된 문항은 큐에 다시 안 들어간다.** 예약이 `created` 기준이므로,
  큐 행만 지우면 그 문항은 영구히 예약 불가다. 시험하다 큐를 비웠으면
  **기록도 같이** 지워야 같은 문항으로 다시 볼 수 있다
- `:8799` 는 `phase1/game_mockapi.py` 다. `app/.env` 가 여기를 보고 있으면
  홈이 전부 0으로 나온다 — 진짜 서버는 `:8000` 이다

### 9-a-3. 무료 경계와 잠금을 만들었다 — §09 의 1~3단계 (2026-08-26)

기획자가 "1~3 만들자" 고 해서 만들었다. **결제 없이도 여기까지가 온전한 제품**이라는
`access_and_pricing_v1` §09 의 말이 그대로 맞았다 — 결제 계약도 PG 도 가격도 필요 없었다.

| 단계 | 만든 것 |
|---|---|
| 1 경계를 데이터로 | `api/shared/free_scope.py` — 자모 1과 · 교재 {1:[4], 2:[1], 3:[1]} · 게임 둘 · 클립 전부 |
| 2 판정 | `business/entitlement.py` · `accepter/entitlement_accepter.py` · `GET /entitlement` |
| 3 앱에 잠금 | `api/entitlement.ts` · `store/entitlement-store.ts` · `paywall-panel.tsx` · 칩·게임 카드의 자물쇠 · 문구 5개 언어 |

#### `ko_chapter.is_free` 를 못 썼다 — 그 표가 없다

§05 는 `ko_chapter.is_free` 칸을 쓰라고 적었는데 **`ko_chapter` 라는 표가 저장소에
없다.** 세어 봤다 — `model.py` 의 표 목록에 없고, 과 목록 120개는 프런트 번들의
`app/src/shared/data/chapter.ts` 에 있다. 서버는 과를 값(`chapter_seq`)으로만 든다.

§05 의 뜻은 **"예외를 코드가 아니라 데이터로"** 였으므로, 없는 표를 만드는 대신
데이터 파일 하나(`free_scope.py`)로 지켰다. 정본에도 어디로 갔는지 적었다.

#### `source` 를 첫날부터 가른 이유

지금은 누구에게나 무료 범위만 내준다. 그런데 `source` 만은 이미 셋으로 가른다 —
§06 이 **기관 학생에게 결제 화면을 띄우면 안 된다**(학교가 이미 낸 돈이다)고
못 박았기 때문이다. `ko_user.role == "student"` 이고 `school_code` 가 있으면
`school` 을 낸다. 여기서 뭉개면 그 규칙이 **첫날부터** 깨지고, 나중에 고치려면
"왜 결제를 권했나" 를 되짚어야 한다.

학교 **계약 범위**는 아직 담을 곳이 없다(`ko_entitlement` 미착수) — 그래서 기관
학생이 지금 받는 범위는 무료와 같다. 다른 것은 안내 문구다.

#### 서버가 답하지 않으면 잠금을 아예 그리지 않는다

처음엔 "무료 범위로 닫는다(fail closed)" 로 썼다가 **되돌렸다.** 이유 둘이다.

- 무료 범위를 앱에도 적으면 경계가 두 곳에 있게 된다 — 이 저장소가 가장 자주
  어긋난 자리다
- 비운 값으로 닫으면 서버가 잠깐 죽은 사이 **교재 전체가 잠긴 것처럼 보인다.**
  게스트가 첫 화면에서 아무것도 못 하는 쪽이 더 나쁘다

열어 두는 것이 위험하지 않은 이유 — **이 잠금은 아직 표시일 뿐이다.** 콘텐츠를
내주는 라우트에 권한이 안 붙어 있어(§08 의 3번) 잠금이 있어도 주소로는 새 나간다.
막는 것은 서버의 일이고 결제와 같이 온다. 그때 이 자리도 다시 봐야 한다 —
산 사람에게 손해가 되지 않는 재시도·캐시가 필요해진다.

#### 무엇을 어떻게 확인했나

- **서버 다섯 경우를 실제로 불렀다** — 게스트 `guest` · 기관 학생 `school` ·
  개인 계정 `guest` · 없는 계정 `guest` · 토큰 없음 `401`. 탐침 계정은 지웠다
- **갈래 여덟을 직접 돌렸다** — `paywallKind` 의 비로그인/미구독/만료/구독중/기관/
  응답없음 조합. 여덟 다 맞다
- **컴포넌트를 그려서 눈으로 봤다** — 칩 자물쇠, 게스트·기관 안내, 게임 카드.
  **기관 학생 판에 결제 버튼이 0개**인 것을 DOM 으로 셌다(§06 의 규칙)
- 게이트 — typecheck 0 · build 0 · `check:css` 어긋난 이름 0 · **목업 대조 "모두 같다"**

**확인하지 못한 것 둘.** 정직하게 적는다.

1. **실제 앱에서 눌러 보지 못했다.** `app/.env` 를 내 서버로 돌려야 하는데 다른
   세션이 지금 그 앱을 쓰고 있다(전에 401 폭풍을 만든 적이 있다). 잠금은 props 로
   결정되는 그림이라 렌더 확인으로 상당 부분 덮이지만, **`useEntitlement` 의
   한 번만 받기와 `ready` 게이트는 훅이라 안 돌아 봤다**
2. **게임 카드 배치**는 확인용 페이지에 Tailwind 가 없어 깨져 보였다. 구조(잠긴
   클래스·자물쇠·문구)는 DOM 으로 확인했고 배치는 목업 대조가 통과한다

#### 일부러 목업과 다르게 한 곳

`paywall_SOT` 의 「목록의 자물쇠」는 **과 목록**에 자물쇠를 그린다. 앱에는 과
목록이 없다 — 급 탭 → **과 칩** → 그 과의 활동 목록이다. 그래서 자물쇠를 칩에
붙이고, 잠긴 과를 고르면 활동 목록 자리에 안내가 들어오게 했다. §06 의
"숨기지 않고 보이되 잠근다 · 잠긴 과도 제목과 자물쇠를 보여 준다" 는 지켜진다.

`PaywallPanel` 에서 `useNavigate` 를 뺐다 — 이 저장소가 표시와 배선을 갈라 놨고,
안에서 라우터를 부르면 라우터 없이 그릴 수 없어 목업 대조에도 못 올린다.
로그인으로 가는 일은 부르는 쪽(`onSignIn`)이 한다.

### 9-a-3-1. 그 잠금이 **무료 과까지 잠그고 있었다** (2026-08-26)

9-a-3 을 브라우저에서 열어 보고 찾았다. 무료인 **1급 4과에 자물쇠가 붙어 있었고**,
바로 그 아래 결제 안내가 "지금 바로 볼 수 있는 것 — 교재 1급 4과" 라고 적고 있었다.
한 화면이 스스로와 모순됐다.

**원인 하나, 그 위에 겹친 문제 둘.**

| | 무엇 | 고친 곳 |
|---|---|---|
| 원인 | `GET /entitlement` 가 `{result:true, data:{}}` 를 내면 앱이 그 `{}` 를 **자리 채워 받아들였다** — `source ?? "guest"` · `books:[]` · `chapters:{}`. 빈 범위는 "아무것도 못 여는 사용자" 이므로 `ready` 가 참이 되고 **전부 잠겼다** | `api/entitlement.ts` 의 `asEntitlement()` — `source` 와 네 묶음이 다 있어야 답으로 친다. 없으면 `null`, 곧 **잠금을 아예 안 그린다** |
| 겹친 것 ① | 목 서버에 `/entitlement` 가 없어 알 수 없는 경로로 떨어졌다. 그래서 **로컬은 늘 이 갈래를 밟았다** | `phase1/game_mockapi.py` 에 실서버와 같은 값을 내는 경로를 넣었다 |
| 겹친 것 ② | 무료 목록이 **다섯 언어 문구에 박혀** 있었다("1급 4과 · 2급 1과 · 3급 1과"). 경계가 두 곳에 있으니 갈라진 것이 화면에 그대로 보였다 | 처음엔 서버가 준 범위로 목록을 짓게 고쳤고, 2026-08-27 페이월 v1.1에서는 결제 유도와 무관한 긴 무료 목록 자체를 없앴다. 잠금 판정은 계속 서버 응답만 믿는다 |

**이건 `withArrays` 로는 못 잡는 갈래다**(9-c). 거기서는 `Record<string,T>` 의 `{}` 를
"정상적인 빈 맵" 이라 보고 일부러 뺐는데, **이 응답에서는 `{}` 가 정상이 아니다.**
빈 답과 안 온 답이 다른 뜻인 자리에서는 자리를 채우지 말고 **거절해야 한다.**

확인 — 목을 `{}` 로 되돌려 놓고 다시 열어 봤다. 자물쇠가 **하나도** 안 그려진다(fail open).
제 값으로 돌리면 1급 4과·2급 1과·3급 1과·한글 1과와 게임 둘만 열리고 나머지는 잠긴다.

### 게스트에게 결제를 권하고 있었다 — 눌러 보고서야 드러났다 (2026-08-27)

다른 세션이 끝나 `app/.env` 를 내 서버로 돌릴 수 있게 되자마자, **못 했던 확인**
(9-a-3 에 "실제 앱에서 눌러 보지 못했다" 고 적어 둔 것)을 했다. 첫 클릭에서 나왔다.

게스트로 들어가 잠긴 1급 5과를 눌렀더니 **「구독 시작하기」**가 떴다.
§06 은 **게스트는 로그인이 먼저**라고 정해 뒀다 — "계정 없이 결제하면 기기를
바꿀 때 잃는다".

```
paywallKind(ent, isSignedIn)          ← 내가 넘긴 것
isSignedIn = !!getAccessToken()       ← 게스트도 JWT 를 받는다. 그래서 늘 참이다
```

**`isSignedIn` 은 "토큰이 있나" 이지 "계정이 있나" 가 아니다.** 이름만 보고 골랐다.
`sign-provider` 에는 `isLoggedInUser = user !== null` 이 **이미 있었다** — 찾지 않고
가까운 것을 집었다. 인자 이름을 `hasAccount` 로 바꾸고 그것을 넘긴다.

**정적 검사 넷이 다 통과하던 자리다.** 타입도 맞고(둘 다 boolean), 목업 대조는
페이월을 안 그리며, `paywallKind` 만 따로 돌린 여덟 갈래 검사도 통과했다 —
**내가 넣은 입력이 틀렸기 때문이다.** 함수는 맞았고 부르는 쪽이 틀렸다.
9-a-3 이 "훅은 안 돌아 봤다" 고 적어 둔 그 틈이 정확히 여기였다.

#### 눌러서 확인한 것 (2026-08-27)

실제 서버(`:8000`)에 붙여 실제 계정으로 걸었다.

| 누구 | 잠긴 과를 눌렀을 때 |
|---|---|
| 게스트 | 「로그인하고 학습을 이어가세요」 · 「로그인 / 회원가입」 |
| 개인 계정(미구독) | 「한글부터 8급까지 모두 열어보세요」 · 구독 버튼은 **눌리지 않는다**(결제가 없다) |
| 기관 학생(`school_code=YS01`) | 「학교 이용 범위에 포함되지 않아요」 · **결제 버튼 0개 · 가격 없음** — §06 의 규칙 |

그 밖에 — 교재 1급은 4과만 열리고 5~15과에 자물쇠, 한글은 1과만, 게임은
VocaShot·봄소풍 둘만. 잠긴 게임을 누르면 안내가 뜨고 「무료 수업 계속 보기」로
목록에 돌아온다. 잠긴 과에서도 과 제목은 남는다. **콘솔 오류 0.**
탐침 계정 둘은 지웠다.

### 9-a-4. 학생 자체 회원가입 — §09 의 4단계 (2026-08-27)

**이 플로우의 유일한 완전 단절이 풀렸다.** 어제 `user_flow_v1` 에 적은 것 —
"QR 로 들어온 사람은 벽을 만나도 구독자가 될 수 없다" 가 이제 거짓이다.

| 무엇 | 어디 |
|---|---|
| 서버 | `POST /user/sign/up` · `user_business.signUpStudent` · `base.StudentSignupRequest` |
| 앱 | `routes/signup.tsx` · `api/sign.ts` 의 `signUpStudent`·`passwordMisses` · `sign-provider` 의 `signUp` |
| 문구 | 5개 언어 (`signup.*` 31개 키, 다섯 파일 다 빠진 것 0) |

#### `/auth/signup` 과 왜 갈랐나

§08 이 요구한 둘을 그대로 지켰다. 저쪽은 `role="school_admin"` ·
`is_approved=False` 로 만든다 — 어학당 콘솔용이라 개인 가입자가 그 길로 들어오면
**승인 대기로 앉아 아무것도 못 한다.** 새 길은 `role="student"` · **승인 없이
바로 활성** · `school_code` 는 비운다. 마지막 것이 중요하다 — 비어야
`GET /entitlement` 가 `guest` 를 내고 앱이 결제를 안내한다. 학교 코드가 있으면
`school` 이 되어 「학교에 문의」가 뜬다(§06).

#### 게스트 기록 이전을 서버가 가입과 한 번에 한다

로그인은 `loginAsStudent` 뒤에 앱이 `migrateGuestData` 를 따로 부른다. 가입은
`guestId` 를 요청에 실어 **서버가 한 번에** 처리한다. 따로 부르면 계정은 생겼는데
이전이 실패하는 상태가 남고, 가입 화면이 맨 위에서 "지금까지 푼 것은 그대로
옮겨집니다" 라고 약속하므로 그건 화면이 거짓말을 하는 것이 된다.
**이전이 실패해도 가입은 성공으로 낸다** — 계정이 생겼는데 "가입 실패" 라고
말하면 다시 가입할 수도 없다. 실패는 로그로 찍는다.

#### 오류를 문장이 아니라 코드로 낸다

영어 화면에서 중복 이메일을 냈더니 **「이미 등록된 이메일입니다.」** 가 떴다.
이 앱을 쓰는 사람은 전부 한국어를 배우는 중이고 화면은 5개 언어다 — 서버가
문장을 만들면 읽을 수가 없다. 서버는 `emailInvalid`·`nameRequired`·
`passwordWeak`·`emailTaken` 을 내고 앱이 `signup.err_*` 로 옮긴다.
**앱이 모르는 값은 그대로 보여 준다** — 다른 엔드포인트는 아직 한국어 문장을
내므로, 못 옮긴다고 "가입 실패" 로 뭉개면 이유가 사라진다.

> 다른 엔드포인트(로그인·재설정 등)는 **여전히 한국어 문장**이다. 같은 문제가
> 남아 있다 — 손대는 김에 같이 고칠 자리다.

#### 처음에 화면에서 직접 부르다 둘을 빠뜨렸다

`signUpStudent` 를 라우트에서 직접 부르고 `checkSign()` 만 했더니,
`handleCheckSign` 이 토큰만 읽는 함수라 `user` 가 null 로 남았다 —
**가입 직후 인사말이 이름 대신 "반갑습니다"** 였고 게스트 id 도 안 지워졌다.
로그인(`handleLogin`)이 이미 그 둘을 하고 있었다. provider 에 `signUp` 을 두어
같은 자리에서 하게 고쳤다. (`checkSign` 이 게스트 토큰을 새로 발급하는 줄 알고
한 번 의심했는데, 그건 `handleGuestSign` 이었다 — 토큰의 `sub` 를 까 보고 확인했다.)

#### 눌러서 확인한 것

게스트로 들어가 1급 4과를 풀고 → `/signup` 에서 가입 → 홈까지 갔다.

```
가입 직후      인사말 "두번째 님" · guestId 지워짐 · 토큰 sub=11 roles=[student]
기록 이전      학습 기록 1 · 출석 1 · 세션 1 이 계정으로, 게스트에 남은 것 0
홈             "이어서 학습하기 1권 4과 · 빈칸 채우기" 가 바로 보인다
권한           source=guest → 잠긴 과에서 구독 안내(학교 문의가 아니다)
```

규칙 표시(8자·대문자·숫자)가 타이핑에 따라 켜지고, 동의 전에는 버튼이 눌리지
않는다. 서버도 여덟 갈래로 따로 확인했다 — 이메일 형식 · 8자 미만 · 대문자 없음 ·
숫자 없음 · 이름 없음 · 정상 · 중복 · **대소문자만 다른 같은 주소**(소문자로
맞춰 저장하므로 걸린다).

#### 배포를 막는 것 — 약관과 개인정보 처리방침의 **문안**이 없다

**화면은 생겼다(2026-08-27).** `/terms` · `/privacy` 가 있고 가입 화면이 그리로
링크한다. 전에는 "동의합니다" 를 required 로 요구하면서 **읽을 곳을 한 군데도
안 주는 화면**이었다 — 그것이 링크를 안 건 이유였는데, 어디까지 와 있는지
말해 주는 자리로 보내는 것은 안 보내는 것보다 낫다.

**막는 것은 이제 코드가 아니라 문안이다.** 초안은 `phase1/legal_draft_v1.html`
에 조문까지 있는데 **채우지 못한 빈칸이 34개**고, 기획자와 변호사가 정할 값들이다.
그중 둘은 법정 필수 기재다:

| 빈칸 | 왜 코드가 못 채우나 |
|---|---|
| 사업자 | 상호 · 대표자 · 주소 · 사업자등록번호 · 통신판매업 신고번호 |
| 보호책임자 | 개인정보 보호책임자 이름 · 직책 · 연락처 — **법정 필수 기재** |
| 보관기간 | 항목별 기간. 법정 보존의무(전자상거래법 등)와 맞춰야 한다 |
| 14세 미만 | 가입을 받을지. 받으면 법정대리인 동의 절차가 통째로 더 필요하다 |
| 관할 | 준거법과 관할 법원. **이용자 상당수가 국외**라 그냥 정하면 안 된다 |
| 환불 · 가격 · PG | 결제가 붙을 때 같이 정해진다 |

그때까지 두 화면은 **조문을 지어 보여 주지 않는다.** "준비 중입니다 → 문의하기"
라고 말한다 — 없는 약관을 있는 것처럼 보이면 그냥 틀린 것이 아니라 위험하다.

**문안이 오면 `shared/feature-gates.ts` 의 `LEGAL_DOCS_READY` 한 줄**을 켜고
두 화면에 조문을 넣는다. 가입 화면은 그때 고칠 것이 없다 — 같은 깃발을 본다.

**문안 없이 자체 회원가입을 내보내면 안 된다.** 동의를 받는다는 사실은 그대로다.

#### 목업에서 일부러 뺀 것 — 음성 기록 보관 동의

`draft_auth` 의 가입 화면에는 「음성 기록 보관에 동의합니다」가 있고, 그 아래에
**"보관 여부가 아직 정해지지 않았습니다 — 자리만 잡아 둔 항목입니다"** 라고
적혀 있다. 정해지지 않은 것에 동의를 받는 것은 안 받느니만 못하다 —
무엇에 동의했는지 아무도 말할 수 없다. 정해지면 그때 넣는다.

### 학습자 음성이 공개 읽기로 S3 에 올라간다 (2026-08-27 발견)

약관·개인정보 처리방침 초안을 쓰려고 **이 앱이 실제로 무엇을 모아 어디로 보내는지**를
코드에서 캤다. 그러다 나왔다. **초안보다 이것이 먼저다.**

```
util/s3utils.py   public_upload_to_s3(...)  →  obj.put(..., ACL='public-read')
business/stt.py   SHADOW_ENABLED = True · SHADOW_SAMPLE_RATE = 1.0   ← 표본이 아니라 전부
                  s3utils.public_upload_to_s3(audio_bytes, "stt/shadow", f"{uuid4().hex}.webm", ...)
model.py          KoSttShadow(user_id, audio_url, ...)               ← 누구 것인지까지 남는다
버킷              pulley-mock / korean/stt/shadow/
```

**주소를 아는 사람은 로그인 없이 학습자의 목소리를 들을 수 있다.** 파일 이름이
uuid 라 찍어서 맞히기는 어렵지만, 그건 잠근 것이 아니라 **주소를 숨긴 것**이다.
게다가 `ko_stt_shadow` 가 그 주소를 `user_id` 와 함께 들고 있어 누구의 목소리인지
이어진다. 보관 기간 정책은 없다 — 지운 적이 없다.

셋을 같이 봐야 한다.

| | |
|---|---|
| 공개 | `ACL='public-read'` → 비공개로 바꾸고 필요할 때 서명된 임시 주소를 낸다 |
| 전량 | `SHADOW_SAMPLE_RATE = 1.0` → 엔진 비교가 목적이면 표본이면 된다. 목적과 비율을 정한다 |
| 무기한 | 보관 기간을 정하고 파기를 만든다 |
| 버킷 | 이름이 `pulley-mock` 이다. 운영 버킷을 따로 둔다 |

**문서로 덮을 수 없다.** 지금 상태를 정직하게 적으면 처리방침에 "회원님의 음성을
공개된 위치에 기한 없이 보관합니다" 라고 써야 하고, 아무도 동의하지 않을 문장이다.

곁들여 — `draft_auth` 의 가입 화면에는 「음성 기록 보관에 동의합니다」가 **"보관
여부가 아직 정해지지 않았습니다"** 라는 설명과 함께 있다. **정해지지 않은 것이
아니라 이미 전량 보관하고 있다.** 목업이 코드를 모르고 쓰인 자리다.

### 이용약관·개인정보 처리방침 초안을 썼다 (2026-08-27)

`phase1/legal_draft_v1.html`. 9-a-4 가 남긴 배포 조건에 대한 답이다.

**내가 할 수 있는 것과 없는 것을 갈랐다.** 사업자 정보·가격·환불·관할·보관 기간은
비워 뒀다(`채워야 함` 표시). 대신 **수집 항목과 국외 이전은 지어내지 않고 코드에서
캤다** — 여기가 초안의 값어치다.

- **수집 항목** 아홉 갈래를 표에 적고 각각 어느 표에 담기는지 달았다. QR 은
  로그인 전에 **IP·국가·도시·브라우저·기기 지문**을 남긴다(`ko_qr_scan`)
- **처리 위탁 다섯 곳** — OpenAI(미국) · Google(미국) · RTZR(국내) · Tutorus(국내) ·
  AWS 서울. **음성과 대화 문장이 국외로 나간다** — 국내법상 국외 이전은 별도 고지·
  동의가 필요할 수 있어 변호사에게 이 표를 그대로 보이라고 적었다
- 표현클립 329편은 유튜브 임베드라 **유튜브가 자체 쿠키를 심는다** — 우리가 심는
  것이 아니어도 방문자에게는 같은 일이다

**약관 제8조(청약철회·환불)는 초안을 두지 않았다.** 디지털 콘텐츠는 전자상거래법에
특칙이 있어 그럴듯하게 써 두면 그대로 나갈 위험이 크다. 제16조(관할)도 같다 —
이용자 상당수가 국외 거주일 수 있어 서울중앙지법으로 그냥 적으면 안 된다.

**쓰다 드러난 것 둘.** 약관에 "열람·정정·삭제·탈퇴할 수 있습니다" 를 적으려면
그 길이 있어야 하는데 앱에 탈퇴 화면도 열람 화면도 없었다.
그리고 고객센터 주소가 없다 — 재설정 화면도 그것을 기다리고 있다(§7).

> **탈퇴는 만들었다 (2026-08-27)** — 아래 「회원 탈퇴」 절. 열람(내 데이터
> 내려받기)은 아직 없다. 문의로만 받겠다면 그 처리 절차(누가·며칠 안에)를
> 정해 두어야 한다는 지적도 그대로 남아 있다.

### 교재는 서버가 막을 수가 없다 — 문항이 이미 앱 안에 있다 (2026-08-27)

§09 의 7단계("서버 쪽 실제 차단")를 게임처럼 하려고 열어 봤더니, **교재는 막을
라우트가 없다.** 문항이 서버에서 오지 않는다 — `app/src/shared/data/` 의 원장이
**앱 번들에 그대로 실린다.**

빌드 산출물로 확인했다. 5급(유료) 빈칸 문항 "한국 대학교에서 공부하( ) 한국말을
열심히 배워요." 를 `app/dist` 에서 grep 하면 `5476.*.js` 에서 나온다.

| 원장 | 행 | 그중 유료 범위 |
|---|---|---|
| 어휘 · 어휘퀴즈 · 역할극 · 듣고답 · 빈칸 · 읽고답 · 플래시카드 · 미션대화 | 9,373 | **9,164 (98%)** |

즉 **게스트가 앱을 열기만 해도 유료 문항 98% 가 기기에 내려간다.** 자물쇠는
그 위에 덮은 뚜껑이고, 개발자 도구를 열 줄 아는 사람에게는 뚜껑이 없는 것과 같다.

**이건 가드로 못 고친다 — 구조를 바꿔야 한다.** 문항을 서버가 내주게 옮기고
그 라우트에 권한을 걸어야 한다. 그 전까지 §09 의 7단계는 **게임에만 해당한다.**

#### 그래서 지금 할 수 있는 것은 했다 — 기록하는 쪽

읽는 것은 못 막아도 **쓰는 것은 막을 수 있다.** 잠긴 과의 진도가 무료 계정에
쌓이지 않게, 급·과를 받는 쓰기 라우트 넷에 `requireChapter` 를 붙였다.

    POST  /activity/enter        PATCH /activity/progress
    POST  /activity/complete     POST  /learning-record

읽기(`/activity/chapter` · `/learning-record/list`)는 그대로 뒀다 — **자기가
남긴 기록**이지 콘텐츠가 아니다.

**`Depends` 가 아니라 손으로 부른다.** 급·과가 요청 몸에 있는데 의존성이 몸을
또 선언하면 라우트마다 몸 모델이 둘이 되어 스키마가 흐려진다.

#### 하마터면 무료 자모 1과를 막을 뻔했다

처음에는 급·과만 보고 판정하게 썼다. **자모 과는 1급 seq 1~3 이고 일반 과는
seq 4 부터인데, 무료 경계는 따로 잡혀 있다** — `FREE_JAMO_CHAPTERS = [1]` 과
`FREE_CHAPTERS = {1: [4]}`. 그래서 급·과만 보면 **무료인 자모 1과가 402** 다.

처음 재 봤을 때는 안 드러났다. 그때 자모 화면은 서버에 아무것도 안 썼기
때문이다(`components/learn/jamo/` 에 `@/api/` 호출 0 — 실측). 그런데 **같은 날
다른 세션이 `useJamoActivityState` 로 그 쓰기를 붙이고 있었다.** 커밋 전에
작업 트리를 보다 발견했다.

이제 `menuType` 을 같이 본다. 자모 여섯은 `menuType="jamo"` 를 보내므로
(dev_spec §10) 그때는 `jamo_chapters` 로 판정한다. 요청이 이미 들고 오는 값이라
경계를 한 벌 더 적지 않아도 된다.

**확인** — 여덟 경우를 불러 기대와 대 봤다(전부 기대대로).

    교재 1급 4과·2급 1과 무료 → 통과      교재 1급 5과·5급 3과 유료 → 402
    자모 1과 무료          → 통과      자모 2과·3과 유료        → 402
    1급 1과를 word 로       → 402 (일반 경계로 판정되는 것이 맞다)

**토큰이 없거나 망가진 경우에도 402 지 403 이 아니다** — 403 을 내면 앱이
세션을 지워 구독 안 한 사람이 로그아웃된다.

앱 쪽은 이미 준비돼 있었다(다른 세션이 게임 가드를 붙이며 해 뒀다). 402 는
세션을 안 지우고, `enterActivity`·`saveLearningRecord` 가 실패를 삼켜 `null` 을
낸다 — **화면은 안 깨지고 기록만 안 남는다.**

> **주의 — 두 층의 실패 방향이 반대다.** 판정이 죽으면 앱은 **열고**(자물쇠를
> 아예 안 그린다) 서버는 **닫는다**(402). 각각은 그 자리에서 맞는 선택인데,
> 겹치면 "화면에서는 열리는데 눌러도 기록이 안 남는" 상태가 된다. 판정이 죽는
> 일이 드물어서 지금은 이대로 두지만, 결제가 붙으면 다시 봐야 한다.

### 스트릭이 "열어만 봐도" 올랐다 (2026-08-27)

스트릭 기준은 `ko_daily_activity` 에 그날 행이 있느냐였다. 그런데 **그 행을
학습 세션 핑이 만든다** — 핑은 활동 화면에 들어가면 즉시 한 번 돈다
(`use-study-session-ping.ts`). 즉 아무것도 안 풀고 열어만 봐도 올랐다.

기획 확정(2026-08-27): **그날 응답이 하나라도 있고 그 채점 결과를 봤으면** 오른다.
맞든 틀리든, 재시도든 상관없다. **건너뛰기만 한 날은 안 오른다.**

**새 신호가 필요했다.** 화면은 채점을 마친 뒤에 `saveLearningRecord` 를 부르므로
**그 호출이 곧 "응답 + 결과 확인"** 이다. 건너뜀은 기록 행을 안 만든다(같은 날
다른 세션이 그렇게 고쳤다). 그래서 `ko_daily_activity.responded` 를 그 자리에서
켠다.

| | |
|---|---|
| 모델 | `responded` 칸 + `uq_user_date` |
| 마이그레이션 | `api/migration_daily_responded.sql` — 로컬 DB 에 적용함 |
| 켜는 곳 | `business/learning_record.saveRecord` (건너뜀 제외) |
| 읽는 곳 | `repo_daily_activity.findRecentDates` — 응답한 날만 낸다 |

**기존 행은 `responded = 1` 로 채웠다.** 0 으로 두면 지금까지 쌓인 스트릭이
통째로 끊긴다. 앞으로 생기는 행부터 새 기준을 탄다.

**유니크를 걸기 전에 `ensureExists` 를 먼저 고쳤다.** 그 함수는 읽고 나서 쓰는데,
핑과 기록 저장이 같은 순간에 오면 둘 다 "없다" 를 읽고 둘 다 넣는다. 유니크만
걸고 이 자리를 안 고치면 그 경합이 **500 으로 바뀔 뿐**이다. 진 쪽이 예외를 받아
되돌리고 이긴 쪽 행을 다시 읽게 했다.

#### 주석을 믿고 넘어갈 뻔했다

`markResponded` 를 넣으며 "건너뜀은 위에서 일찍 돌아가므로 이 줄에 닿지 않는다"
고 적었다. **틀렸다** — `if skipped:` 갈래는 기록 행만 안 만들고 아래로 그대로
흘러온다. 굴려 보고서야 알았다(건너뛰기만 했는데 스트릭이 1). 여기서 다시 가른다.

**확인 — 진짜 DB 로 네 갈래**(뒷정리까지). 전부 기대대로.

    ① 열어만 봄(핑)   responded=false · 학습시간 120s 는 남음 · 스트릭 0
    ② 건너뛰기만      responded=false · 스트릭 0
    ③ 하나 틀림       responded=true  · 스트릭 1
    ④ 여러 번 부름    하루 행 수 1 (유니크가 지킨다)

### 완료 기준을 바꾸니 "돌아가 풀어도 영원히 미완료" 가 됐다 (2026-08-27)

새 기준(건너뛴 것 없이 모두 응답)을 넣고 브라우저에서 걸어 보니, **6개를 다
건너뛰었다가 [다시 풀기]로 6개를 다 풀어도 서버는 `in_progress` 그대로**였다.
새 기준이 만든 구멍이다. 막는 자리가 **둘**이었다.

| 자리 | 무엇 |
|---|---|
| 화면 | `reported = useRef(false)` — 완료를 **마운트당 한 번만** 보낸다. 돌아가 풀어도 다시 안 보낸다 |
| 훅 | `use-activity-state` 의 `complete()` 가 **다시 풀기 세션에서는 아예 안 부른다**(`retry`) — §3.3 |

화면 쪽은 보낸 **값을 기억**하는 방식으로 고쳤다(수가 달라지면 다시 보낸다).
자모 쪽이 먼저 같은 자리를 밟아 같은 방식으로 고쳐 두었기에 그것에 맞췄다.
`fill-blank` · `listen-answer` · `read-answer` · `ai-roleplay` 넷이다.

훅 쪽은 **손대지 않았다.** 다시 풀기 세션이 `complete` 를 보내면 안 되는 이유가
따로 있다 — 그 세션의 문항 집합은 **부분집합**이라(건너뛴 것만) 세 수를 보내면
원래 성적을 덮는다(5문항 풀었는데 1 로 줄어든다).

그래서 **서버가 스스로 본다.** 문항이 하나 기록될 때마다
`repo_activity_state.markCompletedIfAllAnswered` 가 학습 기록 행 수를
`total_items` 와 견주어 완료로 올린다. **성적은 안 건드린다.**

이게 가능해진 것은 같은 날 다른 세션이 **건너뛴 문항에 학습 기록 행을 안 만들게**
고쳤기 때문이다(`business/learning_record.py`). 그래서 **행 수 = 건너뛰지 않고
응답한 문항 수** 이고, 그것이 새 기준이 요구하는 바로 그 수다.

**`sub` 로 못 거른다 — `ko_learning_record` 에 그 칸이 없다.** 지금은 문제가
안 된다(`sub` 를 여럿 쓰는 것은 자모뿐이고 자모는 이 표에 안 쓴다). **자모가
학습 기록을 남기게 되면 여기부터 고쳐야 한다** — 안 그러면 하나만 다 풀어도
여섯이 전부 완료로 올라간다. 코드 주석에 적어 뒀다.

**확인 — 진짜 DB 로 흐름을 그대로 재현했다**(뒷정리까지).

    문항 3개 · 1·2 를 풀고 3 을 건너뜀 → 학습 기록 2행 · 큐에 (103, skipped)
    화면이 complete(2/3) 를 보냄        → in_progress · 성적 2/1
    다시 풀기로 103 을 풂(complete 없음) → **completed** · 성적 **2/1 그대로**

### 회원 탈퇴 — 처리방침 제6조의 그 길을 만들었다 (2026-08-27)

**앱에 탈퇴하는 길이 한 곳도 없었다.** 그래서 처리방침에 "탈퇴할 수 있습니다" 를
적을 수가 없었다.

| 층 | 무엇 |
|---|---|
| 경계 | `api/shared/withdrawal_scope.py` — **무엇을 지우는지는 여기 한 곳에만 있다** |
| 판정 | `api/business/user_withdraw.py` · `POST /auth/withdraw` |
| 화면 | `app/src/routes/my-withdraw.tsx` · MY 화면 맨 아래 |

**지금은 전부 지운다.** 남길 근거가 되는 표가 아직 하나도 없어서다 — 결제가
없으니 전자상거래법의 보존의무가 걸리는 표가 없고, 학습 기록을 남길 근거인
"품질 개선" 은 범위가 미정이며(§01 의 「전량 보관」), 개인정보 보호법의 기본은
목적을 다하면 지체 없이 파기다. 결제가 붙으면 그때 `ko_payment` 를 예외로 두고
보존기간을 그 파일에 적는다.

지우는 표는 **열다섯 + 계정**이다. 그중 둘은 **저장소 파일이 딸려 있어 순서가 있다** —
파일을 먼저 지우고 행을 지운다. 반대로 하면 주소를 잃어 S3 에 파일만 남는다
(`business/stt.pruneShadow` 와 같은 규칙).

  · `ko_stt_shadow`   학습자 음성. `audio_url` 이 주소다
  · `ko_inquiry_file` 문의에 붙인 화면 캡처. **user_id 가 없다** — `ko_inquiry.id`
                      로 매달려 있어 문의를 지우기 전에 그 문의의 파일부터 찾는다

**계정은 맨 마지막에 지운다.** 앞이 실패해 예외로 빠져도 계정이 남아 다시 시도할
수 있다. 계정을 먼저 지우면 남은 행이 누구 것인지 알 길이 없어진다.

**되돌릴 수 없어 비밀번호를 다시 받는다.** 토큰만 믿으면 남의 기기를 잠깐 만진
사람이 계정을 지울 수 있다. 화면도 두 걸음이다 — 무엇이 지워지는지 먼저 보여
주고 그다음에 비밀번호를 받는다. 한 화면에 다 넣으면 읽지 않고 누른다.

#### 딸려 나온 것 — 게스트 이전이 표 다섯을 놓치고 있었다 (**고쳤다 2026-08-27**)

탈퇴가 지울 목록을 세다 찾았다. 가입할 때 게스트 데이터를 옮기는
`migrateGuestData` 는 표 아홉만 옮기는데, `user_id` 를 가진 표는 **열넷**이다.
빠진 다섯 중 둘이 학습 데이터다:

    ko_activity_state   풀던 활동의 진행 상태
    ko_review_queue     복습 큐
    ko_stt_shadow · ko_error_report · ko_inquiry

2026-08-26 에 `ko_learning_record` 가 빠져 있던 것을 찾아 넣은 그 자리다 —
**같은 자리에 다섯이 더 있었다.** 게스트로 공부하다 가입하면 진행 상태와 복습 큐가
사라졌다.

**고쳤다.** 이제 `_MOVE_PLAIN` 열과 `_MOVE_KEYED` 넷으로 **표 열넷을 다 옮긴다.**
지우는 쪽(`withdrawal_scope`)과 같은 열넷이고, 어긋나면 바로 보이게 이름으로 세었다.

#### 유니크 제약을 실제 DB 로 재 봤더니 파일과 달랐다 (2026-08-27)

충돌 규칙을 정하려고 모델 파일을 읽었는데, **파일만 보고 정하면 틀린다**는 것을
DB 에 물어보고 알았다.

| 표 | 논리적 키 | DB 가 막아 주나 |
|---|---|---|
| `ko_activity_state` | user·book·chapter·menu·sub | **예** (`uq_state`) |
| `ko_review_queue` | 위 + question | **예** (`uq_queue`) |
| `ko_learning_record` | user·book·chapter·menu·question | **아니오** |
| `ko_daily_activity` | user·date | **아니오** |

**앞 판 코드는 `ko_learning_record` 에 유니크가 있다고 보고 IntegrityError 를 잡아
두었다.** 이 DB 에는 없다 — 그래서 그 `except` 는 한 번도 돌 수 없었고, 대신 같은
문항이 두 벌 들어왔다. `findOne` 이 `.first()` 라 어느 쪽이 이길지는 그때그때 달랐다.

**`ko_daily_activity` 는 모델과 SQL 이 서로 다른 말을 했다 — 맞췄다(2026-08-27).**
`migration_dashboard.sql` 은 `UNIQUE KEY uq_user_date` 를 걸어 두었는데 모델에는
없어서, DB 를 어떻게 만들었느냐에 따라 스키마가 갈렸다. 모델에 넣고
`migration_daily_responded.sql` 로 기존 DB 에도 걸었다(중복 0건을 확인하고).
`ko_learning_record` 쪽은 여전히 유니크가 없다 — 그건 논리 키가 다섯 칸이라
별건이고, 코드가 그것을 대신 지킨다(아래).

그래서 코드는 **DB 가 막아 주든 아니든 넷을 똑같이 다룬다** — 논리적으로 하나여야
하는 것은 하나로 만든다.

#### 한 줄짜리 `update()` 를 버렸다

앞 판은 표마다 `query(...).update({"user_id": ...})` 한 방이었다. 그것은 **한 행만
부딪쳐도 문 전체가 되돌려져 그 표가 통째로 안 옮겨진다.** 유니크가 걸린 표에서는
충돌 하나에 그 표의 이전을 전부 잃는다는 뜻이다. 이제 키 있는 넷은 행 단위로 보고,
겹치는 게스트 행만 버린다(첫 시도 보존 — `dev_spec_v1` §2.1).

### 음성 공개를 막고 문의처를 만들었다 (2026-08-27)

앞 절이 찾은 넷을 고치고, 초안이 비워 둔 「문의처」를 만들었다.

#### 음성 — 공개를 막았다

한 함수가 성격이 다른 둘을 같이 올리고 있었다. **TTS 로 만든 소리는 앱이 그대로
재생하므로 공개여야 하고, 학습자가 녹음한 목소리는 아니어야 한다.** 가르지 않아
뒤엣것까지 `ACL='public-read'` 로 나갔다.

| | |
|---|---|
| `private_upload_to_s3` | 새로 뒀다. ACL 을 주지 않고 **키**를 낸다 |
| `object_bytes` · `presign` · `delete_object` | 서버가 읽고, 어드민이 5분짜리 주소로 듣고, 지운다 |
| `_key_of` | **옛 행은 공개 URL 이 들어 있다** — 둘 다 받는다(둘 다 시험했다) |
| `stt.py` | 학습자 음성을 비공개로 올린다. `listShadow` 는 presign 해서 낸다 |
| `tutorus_pron` | HTTP GET 대신 S3 로 직접 읽는다. 죽은 `_download` 는 지웠다 |
| 표본 | `STT_SHADOW_SAMPLE_RATE`(기본 0.1) · `STT_SHADOW_ENABLED` — 코드에 박혀 있던 1.0 을 뺐다 |
| 버킷 | `S3_BUCKET` 으로 받는다. 기본값은 지금 쓰는 것 그대로 |

**두 가지는 내가 못 한다.** 로컬에 AWS 자격증명이 없어 **S3 동작 자체는 여기서
증명하지 못했다** — `_key_of` 와 코드 경로만 확인했다. 그리고 **이미 올라간
파일은 여전히 공개다** — `ACL='public-read'` 로 올라간 객체는 그 ACL 을 계속
들고 있다. `tools/lock_existing_stt_audio.py` 를 자격증명이 있는 데서 돌려야 한다
(학습자 음성 경로만 건드린다 — TTS 를 같이 잠그면 듣기가 통째로 죽는다).
**버킷의 Block Public Access 와 버킷 정책도 같이 봐라** — 정책이 공개면 ACL 과
무관하게 공개다.

보관 기간은 **기계만 만들었다**(`tools/prune_stt_shadow.py` · `STT_SHADOW_RETENTION_DAYS`).
**기본 0 이라 아무것도 안 지운다** — 처리방침에 적을 값이라 기획이 정하기 전에
마음대로 지우지 않는다. 정하면 크론에 걸어야 한다(요청 처리 중에 몰래 돌리면
언제 지워지는지 아무도 모른다).

#### 문의처 — 글로 받아 슬랙으로

전화를 두지 않는다(이용자 상당수가 국외 · 기획 확정 2026-08-27).

```
표  ko_inquiry(user_id · reply_email · topic · message · lang · from_path · notified · status)
API POST /inquiry
슬랙 xternal/slack.py — Incoming Webhook
앱  routes/inquiry.tsx · api/inquiry.ts · MY 와 비밀번호 재설정 화면에서 들어간다
```

**저장이 먼저, 슬랙은 그다음이다.** 웹훅이 죽어 있어도 문의는 남고 `notified=0` 으로
표시된다 — `tools/resend_inquiries.py` 로 다시 보낸다(가짜 웹훅으로 2건을 실제로
다시 보내 확인했다). 웹훅이 아예 없으면 조용히 안 보낸다 — 로컬·검사 환경 때문에
문의 접수가 실패하면 안 된다.

**답장 주소를 본문에서 받는다.** 게스트는 계정 이메일이 없다. 로그인한 사람은
계정 이메일이 미리 채워지지만 고칠 수 있다.

#### 인증을 걸었다가 되돌렸다 — 잠긴 사람이 못 쓰는 문의처

처음에 `POST /inquiry` 에 `JWTBearer` 를 걸었다. 화면에서 눌러 보니 **401** 이었다.
**이 길을 가장 필요로 하는 사람이 비밀번호를 잊어 로그인을 못 하는 사람**이다 —
재설정 화면의 「문의하기」가 죽어 있었던 셈이다. 인증을 선택으로 바꿨다:
토큰이 있으면 누구인지 기록하고, 없으면 `anonymous`.

**열려 있으므로 남용될 수 있다.** 지금은 길이 제한(2000자)과 이메일 형식만 본다.
스팸이 실제로 오면 IP 단위 제한을 붙일 자리다.

#### 화면 캡처 첨부 (2026-08-27)

글로만 설명하기 어려운 것이 많다. `ko_inquiry_file` 과 `POST /inquiry` 의 `files` —
`data:image/png;base64,…` 로 최대 3장, 한 장 5MB, png·jpeg·webp 만.

**비공개로 저장한다.** 학습자가 자기 화면을 찍어 보내는 것이라 이름·이메일·학습
기록이 그대로 담긴다. 음성에서 겪은 일을 되풀이하지 않는다.

**mime 을 머리에 적힌 것으로 믿지 않는다.** `data:image/png` 라고 써 놓고
아무것이나 넣을 수 있어서 **매직 바이트로 다시 본다**(PNG 서명 · JPEG `FFD8FF` ·
RIFF/WEBP). 적힌 것과 실제가 다르면 실제를 쓴다. 열 갈래로 확인했다 —
진짜 셋 · png 이라 쓰고 jpeg(실제로 고침) · png 이라 쓰고 실행파일(거절) ·
허용 안 된 mime · data: 아님 · base64 깨짐 · 빈 파일 · 5MB 초과.

**슬랙은 파일을 올릴 수 없다** — Incoming Webhook 은 링크만 싣는다. 그래서
비공개 객체에 **짧게 사는 주소**(`SLACK_LINK_TTL_SEC`, 기본 하루)를 만들어 붙이고
만료 시각을 메시지에 함께 적는다. 링크가 채널에 남아도 다음 날에는 죽는다.

##### 캡처가 유실되면 양쪽에 말한다

첫 판에서는 저장이 실패해도 **"잘 받았습니다"** 만 떴다. 보낸 사람은 캡처를
보냈다고 알고 받는 쪽은 글만 본다 — 이 저장소가 싫어하는 그 꼴이다.

- 화면: 저장된 수와 보낸 수를 견줘 **「캡처 3장은 저장되지 못했어요」**
- 슬랙: `:warning: 화면 캡처 3장을 보냈는데 저장에 실패했다` + 제목에도 표시

접수 자체는 그대로 성공이다 — 글은 이미 남았고, 계정도 없는 사람이 다시 쓰게
만들 이유가 없다.

##### 확인한 것과 못 한 것

브라우저에서 canvas 로 진짜 PNG 를 만들어 파일 입력에 넣어 확인했다 —
미리보기 2장 · 지우기 · **한도 초과 경고** · **형식 경고**(gif) · 3장을 채우면
「더 올리기」가 사라짐 · 전송.

**실제 업로드는 로컬에 AWS 자격증명이 없어 못 했다.** 그래서 브라우저에서는
유실 경로가 돌았고(그 덕에 위 구멍을 찾았다), **성공 경로는 S3 를 대역으로
세워 증명했다** — 파일 행 2개, 키가 `korean/inquiry/{문의번호}-{uuid}.png`,
슬랙에 임시 주소 2개. 자격증명이 있는 데서 한 번 더 밟아야 한다.

#### 슬랙에 캡처 파일을 올린다 — 웹훅으로는 안 된다 (2026-08-27)

기획자가 쓰려는 채널을 알려 주면서 물었다 — "슬랙에는 화면 캡처가 안 올라가?"
**올라간다. 다만 Incoming Webhook 으로는 안 된다** — 웹훅은 글만 싣는다.
그래서 봇 토큰(Web API) 길을 만들었다.

| 길 | 무엇으로 | 캡처 |
|---|---|---|
| **봇 토큰** | `chat.postMessage` + `files.getUploadURLExternal` → PUT → `files.completeUploadExternal` | **파일이 스레드에 붙는다** |
| 웹훅 | `SLACK_WEBHOOK_URL` | 못 올린다 — 하루 사는 링크만 |

봇 토큰이 있으면 그쪽을, 없으면 웹훅으로 떨어지고, 둘 다 없으면 조용히 안 보낸다.
캡처는 **문의 메시지의 답글**로 붙인다 — 채널에 그림만 따로 떠 있으면 어느
문의인지 알 수 없다. `files.upload` 는 폐기됐으므로 세 단계 방식을 쓴다.

**사람이 해야 하는 일 셋** — Slack 앱을 만들고 봇 스코프 `chat:write`·`files:write`,
그 봇을 채널에 초대, `.env` 에 `SLACK_BOT_TOKEN`·`SLACK_CHANNEL_ID`.
채널 id 는 채널 주소의 마지막 토막이다(`…/archives/C0BSL4ZUVQF` → `C0BSL4ZUVQF`).

##### 저장과 전달을 갈랐다 — 한 번 뒤섞었다

첫 판에서 `withLinks=False` 일 때 키 목록을 통째로 비웠더니, **슬랙에 올라가는
중인데 제목이 "캡처 2장 저장 실패" 라고 말했다.** 우리 S3 에 남았나(저장)와
담당자가 볼 수 있나(전달)는 **다른 사실**이다. 링크만 빼고 개수는 남기게 고쳤다.

같은 뒤섞임이 화면에도 있었다. 저장이 실패하면 「캡처 2장은 저장되지 못했어요」가
떴는데 **슬랙에는 두 장이 다 가 있었다** — 필요 없는 걱정이다. 서버가
`files`(저장)와 `filesDelivered`(전달)를 따로 내고, 화면은 **둘 다 실패했을 때만**
말한다. 지금 로컬이 정확히 그 상황(자격증명 없음 + 슬랙 성공)이라 눌러서 확인했다 —
경고가 안 뜬다.

##### 확인한 것

**진짜 슬랙 토큰이 없어 가짜 Slack Web API 를 세워 확인했다.** 세 엔드포인트를
받는 서버를 띄우고 앱에서 canvas 로 만든 진짜 PNG 두 장을 보냈다.

```
chat.postMessage   channel C0BSL4ZUVQF · Bearer 붙음 · ts 받음
getUploadURL       문의12-1.png 1998바이트 · 문의12-2.png 2399바이트
PUT                application/octet-stream · 바이트 수 일치
complete           channel_id + thread_ts(위 ts) · 파일 제목 그대로
```

**진짜 워크스페이스에서는 못 밟았다** — 토큰은 기획자가 넣을 것이고 나는 만지지
않는다. 스코프가 모자라면 `files.completeUploadExternal` 이 `error` 를 내는데,
그때도 **글은 이미 갔고** 스레드에 "캡처 N장 중 M장을 올리지 못했다" 가 남는다.

##### 처리 위탁 표에 슬랙이 빠져 있었다 — 내 실수다

문의를 만들 때부터 **글이 슬랙(미국)으로 가고 있었는데** 개인정보 처리방침
초안의 §03-2 표에 그 줄이 없었다. 넣었다. 캡처까지 올리면 **사본이 하나 더**
생긴다는 것도 적었다 — 슬랙 쪽 보관은 워크스페이스 설정을 따르고 **우리가
지우지 못한다.** 보관 기간을 정할 때 같이 봐야 한다.

#### 재설정 화면이 이제 거짓말하지 않는다

`draft_auth` 규칙 01 이 지목한 두 화면 중 하나가 풀렸다. 아무것도 안 보내고
"이메일로 보내드렸습니다" 로 넘기던 것을, 목업의 `resetBlocked` 판으로 바꿨다 —
**"지금은 메일로 보내 드릴 수 없습니다"** 와 갈 수 있는 길(문의하기 · 로그인으로).
빈손으로 돌려보내지 않을 수 있게 된 것은 문의처가 생겨서다.
메일 발송이 붙으면 원래 폼이 돌아온다(문구는 `resetPassword.description` 에 남겨 뒀다).

> 남은 하나는 `new-password.tsx` 다 — 비밀번호를 안 바꾸고 바꾼 것처럼 한다.

#### 눌러서 확인한 것

로그아웃 상태에서 재설정 → 문의하기 → 접수 → 슬랙까지 갔다.

```
DB     #6 etc · from-check@example.com · lang ko · from_path /reset-password · notified 1
슬랙   [문의 #6] etc · from-check@example.com
       답장 주소 · 언어 ko · 보낸 화면 /reset-password · 사용자 anonymous
```

`from_path` 도 한 번 틀렸다 — 보낼 때 `location.pathname` 을 읽었더니 그때는 이미
`/inquiry` 라 **자기 자신이 찍혔다.** 슬랙 메시지를 보고 알았다. 부르는 쪽이
`?from=` 으로 명시해 넘긴다.

서버 검증도 갈래별로 확인했다 — 이메일 형식 · 빈 본문 · 2000자 초과 · 모르는 갈래
(`etc` 로 떨어진다) · 정상. 오류는 가입과 같은 규약으로 **코드**를 내고 앱이
5개 언어로 옮긴다.

### 유료 콘텐츠를 서버가 막는다 — 잠금이 표시만이 아니게 됐다 (2026-08-27)

`access_and_pricing_v1` §08 의 3번. 어제 넣은 자물쇠는 **화면에만 있는 잠금**이었다 —
주소를 직접 치면 잠긴 게임 콘텐츠가 그대로 열렸다.

#### 세는 것부터 한 번 틀렸다

"아직 손대지 않은 곳" 을 짚다가 `grep -c '@router.get'` 으로 세고
**"여섯이 아니라 GET 15개"** 라고 문서를 고쳤다. **그것이 틀렸다** —
관리자용 `/edit`·POST·DELETE 까지 센 수였고 그쪽은 `MasterAdminRequired` 로
이미 막혀 있다(유료 셋에서 20개). 학습자용 무인증 GET 은 **정확히 여섯**이다.
원래 문서가 맞았고 내 정정이 틀렸다 — 되돌리고 실측을 적었다.

> 데코레이터를 세면서 **의존성 인자를 안 본 것**이 원인이다. 라우트 수를 셀 때는
> `dependencies=[...]` 까지 봐야 학습자용과 관리자용이 갈린다.

#### 403 이 아니라 402 를 쓴다

이 저장소에서 **403 은 인증 실패**다(`auth.JWTBearer` 가 토큰이 없거나 망가졌을 때
403 을 낸다). 그리고 앱은 401·403 에서 **세션을 지운다**(`api.ts`).
권한 없음으로 403 을 내면 **구독하지 않은 사람이 로그아웃된다.**
그래서 `402 Payment Required` 를 쓴다 — 세션은 멀쩡하고 구독만 없는 상태다.

#### 화면도 같이 막아야 했다

서버가 402 를 내도 콘텐츠를 받는 함수들이 오류를 삼켜 `{}` 를 낸다
(`game-content.ts`). 그대로 두면 주소로 들어온 사람에게 **빈 게임**이 조용히
열린다. `GameGate` 를 세워 유료 게임 라우트 셋을 감쌌다 —
답이 오기 전에는 통과시킨다(교재 쪽 자물쇠와 같은 규약. 진짜 문은 서버다).

#### 확인한 것

```
유료 여섯 GET   무토큰 402 · 게스트 토큰 402   (카드분류 3 · 조사저격 2 · 서울퍼즐 1)
무료 셋 GET     200                          (봄소풍 2 · 보카샷 1)
관리자 경로     401                          (그대로 막혀 있다)
열린 사람       free_scope 에 card-sort 를 잠깐 넣으니 200 — 막기만 한 것이 아니다
```

브라우저로 주소를 직접 쳤다 — 게스트는 「로그인하고 학습을 이어가세요」,
로그인한 개인 계정은 「한글부터 8급까지 모두 열어보세요」+ 눌리지 않는 구독 버튼.
**세션이 안 지워지는 것**(토큰·게스트 id 유지)까지 확인했다. 무료 봄소풍은 열린다.

##### 서버를 안 다시 띄우고 "안 막힌다" 고 볼 뻔했다

첫 브라우저 확인에서 카드분류가 **200** 이었다. 원인은 코드가 아니라 **내가
`free_scope.py` 를 되돌리고 서버를 재기동하지 않은 것**이었다 — 메모리에 아직
`card-sort` 가 무료로 남아 있었다. 파일을 되돌린 것만 보고 넘어갔다.
**값을 바꿔 시험했으면 되돌린 뒤 다시 띄워야 한다.**

##### 결제 안내가 화면 맨 위에 붙었다

`PaywallPanel` 은 교재학습 화면 안(과 제목 아래)에 들어가도록 **위 여백만** 있다.
전체 화면으로 쓰니 맨 위에 붙고 아래가 텅 비었다. 기획자가 보고 바로 지적했다.

`.paywall-page`(flex + 가운데)로 감쌌는데 **그래도 안 됐다.** 부모 높이를 재 보니
라우트 레이아웃이 이미 `.nav-frame h-full` 과 `.scroll`(812px)을 주는데, 내가 그
사이에 높이 없는 `div` 를 한 겹 끼워 `min-height:100%` 가 죽어 있었다.
껍데기를 빼니 가운데로 왔다(위 154 · 아래 178).

#### 출석 점은 「열었다」로 둔다 — 스트릭과 뜻이 다르다 (2026-08-27)

스트릭을 「그날 응답이 있었나」로 고친 판(`c332bc1`)을 굴려 봤다. **스트릭은 맞다** —
열어만 보면 0, 한 문항 풀면 1 이다. 그런데 **출석 점은 안 걸렀다.**

```
repo_daily_activity.findRecentDates   responded 를 본다   ← 스트릭
repo_daily_activity.findByDateRange   안 본다             ← 주간 출석 점 · 학습 시간 차트
```

`business/dashboard.py` 의 `weekDays[i] = d in activity_dates` 가 그 목록을 쓴다.
그 행은 학습 세션 핑이 만들므로 **활동 화면을 열어만 봐도 오늘 점이 칠해진다.**

열어만 본 상태(행 있음 · `responded=0`)로 홈을 열어 재현했다.

```
목요일 칸    <i class="c done">   ← 파란 ✓ (출석 완료)
바로 아래    "0일 연속 학습 중"
학습 현황    오늘 활동 수 0 · 주간 활동 수 0
```

**같은 카드에서 어긋난다** — 오늘은 칠해졌는데 연속은 0 이고 활동 수도 0 이다.

그쪽 커밋의 확인 목록은 「열어만 봄 → 스트릭 0」까지만 보고 점은 보지 않았다.

##### 정한 것 — 출석 점은 「열었다」로 둔다 (기획 확정 2026-08-27)

**고치지 않는다. `findByDateRange` 에 `responded` 필터를 걸지 마라.**

둘의 뜻이 갈리는 것을 알고 두는 것이다.

| | 뜻 | 원천 |
|---|---|---|
| 출석 점 | 그날 앱을 **열었나** | `findByDateRange` — 필터 없음 |
| 스트릭 | 그날 **응답했나** | `findRecentDates` — `responded` |

그래서 열어만 본 날은 **점이 칠해지고 연속은 안 오른다.** 어긋나 보이지만
출석은 "왔다" 의 기록이고 스트릭은 "했다" 의 기록이라 서로 다른 것을 센다.
학습 시간 차트도 같은 이유로 그대로다 — 열어만 본 시간도 학습 시간이다.

> 학습 시간 차트는 그대로 두는 것이 맞다 — 열어만 본 시간도 학습 시간이다.
> 즉 필터를 거는 자리가 **출석 점 하나**이고, 같은 함수를 차트도 쓰므로
> 목록을 나누거나 점만 따로 걸러야 한다.

#### 건너뛴 문항이 그 화면을 벗어나면 사라지고 있었다 (2026-08-27)

결과 화면의 [다시 풀기] 는 건너뛴 문항을 가리켰다. 그런데 그 목록은 **화면 안의
`skipped` 배열**이었다 — 나가면 사라진다. 그래서 다음날 홈의 「다시 풀 문항」에는
**오답만** 떴다.

배관은 이미 다 있었다. 없던 것은 한 줄이었다.

```
서버   saveRecord(..., skipped) → 큐에 reason='skipped'          있었다
타입   LearningRecordRequest.skipped?: boolean                    있었다
큐     reason: "wrong" | "skipped" | "unknown"                    있었다
홈     getHomeReviewQueue → 「다시 풀 문항 N개」                   있었다
화면   skipped: true 를 실제로 보내는 곳                          0개  ← 이것
```

`fill-blank` · `listen-answer` · `read-answer` 의 건너뛰기 핸들러에서 보내게 했다.

##### 기획 확정 둘 (2026-08-27)

| 물음 | 정한 것 |
|---|---|
| 건너뛴 것을 언제 보일지 | **오답과 같이 내일부터**. 안 푼 것이니 오늘 바로 내보낼 수도 있었지만 두 갈래로 갈리면 복잡하다 |
| 자모를 홈 복습에 넣을지 | **넣지 않는다.** `LEARN_ROUTE` 에 `jamo` 가 없어서 큐에 들어와도 홈의 [다시 풀기]가 조용히 아무것도 안 한다. 넣으려면 `sub`·묶음까지 주소로 실어야 하는데 자모만 파라미터가 넷이라 모양이 다르다 |

##### 건너뜀은 학습 기록을 만들지 않는다

`saveRecord` 는 무조건 `upsert` 를 했다. 그러면 건너뛴 문항도 `ko_learning_record`
행이 되는데, **세 곳이 정오답을 안 가리고 행 수를 센다.**

```
getProgressAll        전체 진행률
countTodayMenuTypes   오늘 학습한 메뉴 수
getProgress           그 과의 분모
```

즉 **건너뛰기만 눌러도 진행률이 오른다.** 기획자의 말이 그 기준이다 —
"건너뛴거는 푼 게 아니지." 건너뜀에는 행을 만들지 않고 예약만 남긴다.
"첫 시도인가" 는 행이 있나로 가른다. `word` 의 누적 학습 단어 수도 건너뜀에서 빼야
했다(`created` 만 보면 건너뛴 낱말이 배운 것으로 찍힌다).

**통제군을 붙여 확인했다** — 값이 안 바뀌는 것만 보면 검사가 헛돌아도 통과한다.

```
건너뛰기 전   chapterCompleted 2
건너뛴 뒤     chapterCompleted 2     ← 안 올랐다
실제로 푼 뒤  chapterCompleted 3     ← 올랐다 (이게 없으면 위가 무의미하다)
```

처음엔 `progressPercent` 로 재려 했는데 그 키가 응답에 없어서 `None → None` 이
나왔다 — **없는 키를 같다고 읽은 것**이다. 응답 키를 찍어 보고 알았다.

##### 건너뛴 뒤 돌아가 맞혀도 예약이 남았다

건너뛰기가 예약을 남기게 되자 새 구멍이 생겼다. 학습자가 진행바로 돌아가 그 문항을
풀어도 예약이 그대로라, **내일 홈이 이미 맞힌 문항을 "다시 풀 문항" 으로 센다.**

제거 조건에 `isCorrect and not skipped and created` 를 더했다. `created` 가
안전장치다 — 기존 규칙이 지키려던 "재시도로 맞힌 것" 은 기록 행이 이미 있어
`created` 가 거짓이므로 이 갈래에 오지 않는다. 즉 **첫 시도 정답**뿐이고, 첫 시도
정답인데 큐에 있는 문항은 건너뜀으로 들어간 것밖에 없다.

##### 눌러 본 것 — 네 갈래를 갈랐다

API 로:

```
건너뜀 q101 · 첫 시도 오답 q102   → 101 skipped/a1 · 102 wrong/a1
101 로 돌아가 첫 시도 정답        → 101 빠짐 · 102 남음
102 를 재시도해서 정답            → 102 **그대로**(재시도 정답은 안 뺀다)
102 를 다시 풀기에서 정답         → 비었다
건너뛰기만 한 q103·q104           → 기록 행 없음 · 큐에 있음
```

브라우저로(1급 4과 문법):

```
1번(q=9)을 건너뛴다        → 큐 [(9, skipped, 1)] · 기록 행 0개
                             available_at 2026-08-27 15:00Z = 08-28 00:00 KST
진행바 1번으로 돌아가 "는"  → 피드백 "아주 잘 했어요!" · 큐 [] · 기록 q9 정답
```

헛짚은 것 둘. 선택지 클래스를 `choice` 로 알고 눌렀는데 `chip-opt` 였다 —
"틀렸다" 로 읽힐 로그가 실은 **아무것도 안 누른 것**이었다. 그리고 `fill-blank` 은
맞혀도 자동으로 안 넘어간다(§28 — [다음] 활성화). 문항이 안 바뀐 것을 오답으로
읽을 뻔했다. 정답이 무엇인지는 원장에서 확인했다(`n4_blank_question` q9 = "는").

##### 홈 카드가 오늘 셀 수 없는 것을 세고 있었다 — 갈래를 나눴다 (2026-08-27)

`GET /review-queue?scope=home` 이 `total` 은 **보관 전체**로, `items` 는
`available_at <= now` 로 낸다(명세 §13 이 그렇게 정했다). 홈은 `total` 을 카드에
쓰고 `items[0]` 로 이동한다. 그래서 **오늘 틀리거나 건너뛰면 카드에 "다시 풀 문항
2개" 가 뜨는데 눌러도 아무 일이 없다** — 갈 항목이 내일까지 없다.

```
오늘 두 문항을 건너뛴 직후
  scope=home      total=2  items=0     ← 카드는 2 를 보여 주고 버튼은 죽는다
  scope=activity  total=2  items=2
```

**오답에도 있던 기존 동작이라 건너뛰기가 새로 만든 것은 아니다.** 다만 건너뛰기가
오답보다 흔하니 더 자주 보일 것이다.

**기획 확정 — 개수는 그대로 두고 문구로 말한다.** 카드를 두 갈래로 나눴다.

| | 오늘 낼 문항이 있다 | 없다(내일부터) |
|---|---|---|
| 태그 | `<button class="task">` | `<div class="task waiting">` |
| 화살표 | 있다 | **없다** — 누를 수 있다는 신호다 |
| 문구 | 어제 틀리거나 건너뛴 문항부터 만나요 | 오늘 틀리거나 건너뛴 문항은 내일 만나요 |

`reviewCount`(보관 전체)와 `reviewReady`(오늘 낼 것이 있나)를 갈라 넘긴다.
`<button disabled>` 이 아니라 `<div>` 다 — 비활성 버튼은 초점을 못 받아
스크린리더가 건너뛰므로, 개수를 알려 주는 이 카드가 안 읽힌다.

기존 문구도 절반만 참이었다 — 「어제 **틀린** 문항부터」였는데 이제 건너뛴 것도
들어온다. 다섯 언어와 목업 정본(`screens_SOT` · `screens_ref/nav__home__review`)을
같이 옮겼다(`screen_promotions.md`).

**재서 골랐다.** `.t2` 는 한 줄 고정(`nowrap` + 말줄임)이라 긴 문구가 잘린다.
기다리는 갈래만 줄바꿈을 허용했는데, 처음 문구는 두 줄이 되며 둘째 줄에 "있어요"
한 단어만 남았다. 그리고 **한글이 단어 중간에서 끊겼다** — `word-break:keep-all` 이
없으면 음절 어디서나 끊긴다("풀 수 있 / 어요"). `keep-all` 을 넣고, 다섯 언어가
**모두 한 줄에 들어가는** 문구를 브라우저에서 재서 골랐다(칸폭 253px).

```
ko 191   en 229   ja 218   zh 151   vi 223
```

##### 눌러 본 것

```
기다리는 갈래   <div class="task waiting"> · 화살표 없음 · cursor default · 한 줄
                "다시 풀 문항 2개 / 오늘 틀리거나 건너뛴 문항은 내일 만나요"
오늘 갈래       <button> · 화살표 있음 · 잘림 없음
                누르면 /learn/grammar?level=1&lesson=4&review=true 로 간다
```

#### 활동 상태를 나머지 넷에 붙였다 — 그리고 진행바 셋이 틀린 것을 봤다 (2026-08-27)

`menuType` 은 여덟이다(dev_spec §10) — `word`·`fill-blank`·`listen-answer`·
`read-answer`·`roleplay`·`flashcard`·`mission-chat`·`jamo`. 넷만 붙어 있었다.

**처음에 "훅이 없는 파일 11개" 로 셌는데 단위가 틀렸다.** `word-quiz-card` 는
`word-learning` 의 부품이고, `flashcard-result`·`mission-report` 는 결과 **뷰**다 —
위치를 쥐는 쪽은 각각 `word-learning`(이미 붙음)·`routes/learn/flashcard.tsx`·
`mission-dialog` 다. 그러면서 정작 **`routes/learn/flashcard.tsx` 를 빠뜨리고 있었다.**
활동으로 세면 남은 것은 넷이다.

| `menuType` | 붙인 곳 | 문항 단위 | `graded`/`correct` |
|---|---|---|---|
| `roleplay` | `ai-roleplay.tsx` | **내 차례 1번** | 0 — STT 는 채점하지 않는다(§28) |
| `flashcard` | `routes/learn/flashcard.tsx` | 카드 1장 | 0 — 자기 평가다 |
| `mission-chat` | `routes/learn/mission-chat.tsx` | **대화 1개**(1/1) | 미션 수 / 달성 수 |
| `jamo` `sub=1~6` | 자모 여섯 | 글자·낱말 1개 | 0 — 아래 |

##### 롤플레잉의 단위를 원장으로 갈랐다

목업 진행바 칸이 둘이라 "시나리오 둘" 로 읽고 그렇게 붙였다. **틀렸다** — 그
대화의 "나" 줄을 세 보니 둘이었다. `shell_spec_v1` 은 §1·§3.4·§28 **세 곳에서** "문항 = 대화 턴"
이라고 적어 두었고, 원장을 세면 결정적이다.

```
롤플레잉이 있는 과            117
그중 시나리오가 하나뿐인 과    74      ← 시나리오로 세면 분모가 1 이다
과별 내 차례 수              2~4 가 대부분
```

그래서 문항은 내 차례로 세고, **이어하기는 시나리오 경계까지만** 되돌린다 —
시나리오에 들어가면 첫 턴부터 TTS 가 순서대로 재생되므로 중간 턴은 되살릴 지점이
아니다. 세는 기준은 시나리오의 `mode` 고 학습자가 뒤집는 방향 탭이 아니다(탭을
뒤집으면 내 차례가 2 ↔ 3 으로 바뀌는데 분모가 흔들리면 안 된다).

##### 연습 세션이 원래 성적을 덮고 있었다

훅이 `practice` 를 **돌려주기만 하고 아무도 쓰지 않았다** — 네 화면 다 안 받았고
훅도 막지 않았다. 그래서 완료한 활동에 다시 들어가 한 번 더 풀면 `complete` 가
나가서 `answered/graded/correct` 를 덮었다. **복습하면 원래 성적이 사라진다.**
§32 는 연습 세션에서 진행 저장도 완료도 부르지 말고 위치는 0 부터라고 정해 두었다.

양쪽을 다 막았다 — 클라이언트(`use-activity-state.ts`)가 1차, 서버
(`repo_activity_state.complete`)가 2차다. 한쪽만으로는 못 막는다.

##### 자모는 채점하지 않는다 — 기획 확정 2026-08-27

**여섯 다 `gradedCount = 0` 이고 정답률은 "—" 다. 이것이 끝 상태고 숙제가 아니다.**

처음에 나는 이것을 "지금은 0, 첫 시도 기록은 따로 할 일" 로 적었다. **틀렸다** —
기획자가 "굳이 측정할 필요 없어서 `—` 로 표기하기로 하지 않았나, 0 이라고 하면 다
틀린 것 같으니까 아예 안 재는 것으로" 라고 확인해 주었다. 그래서 정본을 고쳤다.

| 어디 | 초안 | 지금 |
|---|---|---|
| `shell_spec_v1` §3.3 · §28 | 발음(`sub` 1·3)과 플래시카드만 "—" | **자모 여섯 전부**와 플래시카드·롤플레잉 |
| `shell_spec_v1` §28 판정표 | 자모 쓰기·고르기 `gradedCount` = 문항 수 | **0** |
| `dev_spec_v1` §11 | 같은 초안 | 같이 고쳤다 |

이유는 둘이다. **0% 로 보이는 것이 안 재는 것보다 나쁘다** — 분모만 있고 분자가
없으면 화면이 "정답률 0%" 로 그리고 그건 "다 틀렸다" 는 말이다. 그리고 **자모는
한글을 처음 익히는 자리라 점수를 매길 자리가 아니다.**

구현 쪽도 같은 방향이었다 — 자모 여섯 중 `saveLearningRecord` 를 부르는 것이 **0개**다.
그것은 빠뜨린 것이 아니라 이 결정과 맞는 상태다. **여기에 첫 시도 기록을 넣지 마라.**

##### `answeredCount` 는 건너뛴 것을 뺀다 — 같은 날 뒤늦게 고쳤다

처음엔 `answeredCount` 로 **문항 수 전부**를 보냈다. "과하게 세는 쪽으로 기울었다"
고 적고 남은 일로 미뤘는데, **같은 날 다른 세션이 완료 기준을 바꾸면서 그 값이 곧
완료 판정이 됐다**(9-b 아래 · `repo_activity_state.complete`) — 즉

> 다 건너뛰고 끝까지 넘긴 자모 활동이 **완료**로 남는다.

그 기준을 세운 이유가 자모에서만 사라진다. 그래서 훅이 `countAnswered()` 를
**필수로** 받고 화면이 실제 응답 수를 센다. 타입이 필수라 빠뜨리면 컴파일이 막힌다
(실제로 넷을 잡아 줬다).

| `sub` | 세는 지점 |
|---|---|
| 5 고르기 | 선택지를 한 번이라도 눌렀으면 — **오답이어도 센다** |
| 2·6 조합 | 따라쓰기를 끝냈으면. 이 판(`HangulTracingCanvas`)에는 **"틀린 제출" 이 없다** — 획이 맞을 때까지 다시 긋거나 건너뛰는 것뿐이라 제출이 곧 성공이다 |
| 3 따라 말하기 | 녹음했으면 — STT 판정과 무관 |
| 4 읽고 쓰기 | 음절 칸이 다 채워졌으면 — **OCR 이 못 읽어도 센다.** 낱말 id 로 센다(탭을 옮겨 다니므로 번호로는 안 맞는다) |
| 1 발음 | 완료를 안 보내므로 0 |

**한 번만 보내면 안 됐다.** 처음엔 `reported` 불리언으로 완료를 한 번만 보냈는데,
그러면 건너뛴 문항을 **돌아가 풀어도 다시 안 보내서 영원히 미완료**다 — 명세
시나리오 14 가 자모에서 성립하지 않았다. 브라우저에서 `a=4` 로 멈추는 것을 봤다.
보낸 **값**을 기억하고 응답 수가 늘면 다시 보내게 고쳤다.

```
1번 문항 건너뛰고 나머지 넷 풀기   → a=4 total=5 in_progress
진행바 1번 칸으로 돌아가 그것 풀기 → a=5 completed
```

##### 발음(`sub=1`)은 진입만 알린다 — 완료는 다른 세션이 정하는 중이다

이 화면은 순서대로 푸는 활동이 아니다 — 묶음 탭에서 낱말을 골라 다니는 자리라
정본 목업(`activity__speak.html`)에 진행바가 **0칸**이고, 끝났다는 신호도 없다
(`setIsExit(true)` 가 주석으로 남아 있다). 없는 위치를 지어내 저장하면 이어하기가
엉뚱한 자리를 가리키고, 없는 완료를 지어내면 스트릭이 오른다. "열었다" 만 남긴다.
**이 활동의 완료를 무엇으로 볼지는 기획 결정이고, 2026-08-27 에 다른 세션이
그 일을 하고 있다** — 그쪽이 정하면 이 `mode: "enter-only"` 를 걷어내면 된다.

##### 배선 밖에서 본 것 셋 — 고치지 않았다

셋 다 진행바이고, **대조가 못 잡는 자리**다. 대조는 View 에 픽스처를 넣어 그리므로
`totalScenarios={2}` 같은 값을 자기가 만들어 통과한다 — 제품이 그 View 에 무엇을
넘기는지는 안 본다.

| 무엇 | 지금 | 목업 정본 |
|---|---|---|
| 롤플레잉 진행바 | `totalScenarios={scenarios.length}` — **74과에서 칸이 1개** | 내 차례 수만큼 |
| 자모 조합(`sub=2`) | `current/total` 을 아예 안 넘긴다 → 칸 0 | `activity__write` 칸 3 |
| 자모 3단 조합(`sub=6`) | 같음 | `activity__write3` 칸 1 |

그리고 `getChapterStates()` 는 **아무도 안 쓴다** — 교재학습 목록의 완료 표시가
아직 소비자가 없다. 활동 상태를 넣는 이유의 절반이 그 표시인데 그쪽이 비어 있다.

##### 학습 화면 셋에도 같은 "한 방" 제약이 있다 — 안 고쳤다

`fill-blank` · `listen-answer` · `read-answer` 는 `reported` 불리언으로 완료를
**한 번만** 보낸다. 셋 다 진행바에 `onJump={setCurrentIndex}` 가 있어 건너뛴 문항으로
돌아갈 수 있는데, 돌아가 풀고 끝에 다시 닿아도 **다시 보내지 않는다.** 새 완료 기준
아래에서는 그 활동이 미완료로 남는다 — 시나리오 14 가 성립하지 않는 자리다.
자모는 위에서 고쳤고 고치는 방법도 같다(보낸 값을 기억한다). **이 셋은 완료 기준을
세운 세션의 자리라 손대지 않았다.**

`word-learning` 은 이 문제가 없다 — 확인했다. `answeredCount` 로 `quizzes.length` 를
그대로 보내는데, **[결과 보기] 가 `quizzes.every(답했다)` 일 때만 켜지므로**
답 안 한 문항이 있는 채로 결과에 갈 수가 없다. 처음에 이것을 버그로 적을 뻔했다 —
보내는 값만 보고 켜지는 조건을 안 읽었다.

##### 눌러 본 것 — 게이트는 "안 깼나" 만 말한다

서버는 게스트 토큰으로 직접 눌렀다.

```
자모 sub=1~6        상태 행이 여섯으로 갈린다
sub=2 만 완료       sub=1·3 은 in_progress 그대로 · sub=2 재진입은 practice=true
연습 complete       a=6 g=0 c=0 그대로 (a=1 g=99 를 보내도 안 덮인다)
mission-chat 완료   a=1 g=3 c=2
1급 2과(유료)       402
```

브라우저에서는 실서버(:8000)로 돌려 눌렀다.

```
자모 sub=5   칸 5 · 눌러 넘기니 progress idx=1,2,3,4 (sub:5 포함) · 끝에서 complete a=5
             그 뒤 새로고침 → enter 하나뿐 · progress·complete 없음 · 위치 0 (연습)
자모 sub=1   진행바 0칸 · enter 하나뿐 (progress·complete 없음)
자모 sub=4   위치 3 을 심으면 "오이"(4번째) · 5 를 심으면 "위"(6번째) · 목록 안에서 켜져 있다
             범위 밖(7)을 심으면 따르지 않고 첫 낱말 — 실제 문항이 6개다
롤플레잉     total_items=4 (시나리오 2 가 아니라 내 차례 4)
플래시카드   total_items=27 (부분집합 아님) · 두 장 넘기니 idx=2
             위치 9 를 심고 다시 열면 10/27 — 옛 유추(저장 2장)라면 3/27 이었다
미션대화     total_items=1 · idx=0 · progress 없음
```

> **못 한 것** — 미션대화의 `complete`(a=1 · g=미션수 · c=달성수)는 서버 쪽만 눌렀다.
> 화면에서 그것을 내려면 AI 대화를 끝까지 해야 하고 그 백엔드는 키가 필요하다.
> 플래시카드도 카드 넘기기가 framer-motion 이라 창이 숨으면 애니메이션이 멈춰
> 자동으로 여러 장을 넘길 수 없었다 — 그래서 이어하기 쪽으로 갈라 확인했다.

#### 하단바가 사라지고, 놀이판이 먼저 스쳤다 (2026-08-27)

기획자가 잠긴 게임에서 둘을 지적했다 — **하단바가 안 보인다**, 그리고
**큰 아이콘 그림이 순간순간 스쳐 지나간다.**

##### 하단바 — 잠긴 게임은 "하는 중" 이 아니다

`TabBar` 가 `/main/game/*` 에서 스스로 숨는다(놀이판을 60px 넓히려고).
그런데 결제 안내는 놀이판이 아니다 — 나갈 길이 안내 안의 버튼 하나뿐이라 갇힌다.
경로만 보지 않고 **그 게임이 열려 있는지**까지 보게 했다. 경로→키는 `GAMES` 표가
정본이다(보카샷은 라우트 `vocashot-solo` 인데 키가 `vocashot` 이다).

##### 스쳐 지나가는 것 — 재서 찾았다

무엇이 깜빡이는지 추측하지 않고 재 봤다.

```
런타임 <style>        0개          (2026-08-26 에 걷어낸 CDN Tailwind 는 되살아나지 않았다)
svg·img > 90px        전환 중 0건
layout-shift          375x60 이 4.1초에 들어옴 · 합 0.224
```

**375x60 은 하단바다.** 그리고 그것이 단서였다 — 문(gate)이 판정을 미루는 동안
**잠긴 게임의 놀이판이 먼저 그려지고**, 몇 초 뒤 결제 안내와 하단바로 바뀐다.
게임 시작 화면에는 큰 그림·이모지가 있으므로(봄소풍 🍱🐰🐻 처럼) 그것이 스친다.

처음에 "답이 오기 전에는 통과시킨다" 로 둔 것이 원인이다 — 교재 쪽 자물쇠와 같은
규약으로 맞췄는데, **무엇이 먼저 그려지는가가 다르다.** 교재는 자물쇠만 늦게 붙으면
되지만 게임은 놀이판 전체가 잘못 그려진다. 답이 오기 전에는 아무것도 그리지 않게
고쳤다.

```
layout-shift 합   0.224 → 0
```

대신 게임을 열 때 잠깐 빈 화면이 지나간다 — SPA 안에서는 열린 범위가 이미 손에
있어 거의 없고 전체 새로고침일 때만 보인다.

> **못 한 것** — 기획자가 본 "커다란 아이콘" 을 그 화면에서 직접 재현하지는 못했다.
> 위 셋을 재서 같은 자리(놀이판이 먼저 그려짐)를 찾아 고쳤을 뿐이다. 여전히 보이면
> 어느 화면에서였는지 알려 주면 그 화면을 다시 잰다.

##### 결제 안내가 화면 맨 위에 붙은 것도 같이

`PaywallPanel` 은 교재학습 화면 안(과 제목 아래)에 들어가도록 **위 여백만** 있다.
전체 화면으로 쓰니 맨 위에 붙고 아래가 텅 비었다. `.paywall-page`(flex + 가운데)로
감쌌는데 **그래도 안 됐다** — 부모 높이를 재 보니 라우트 레이아웃이 이미
`.nav-frame h-full` 과 `.scroll`(812px)을 주는데, 내가 그 사이에 높이 없는 `div` 를
한 겹 끼워 `min-height:100%` 가 죽어 있었다. 껍데기를 빼니 가운데로 왔다.

### 9-b. 이미 해결된 여섯 — 리뷰가 몰랐던 것

| 항목 | 어디서 |
|---|---|
| 2초 준비 · 1초 추가의 정확한 동작 | `RECORD_PREPARE_MS=2000` · `RECORD_TAIL_MS=1000`(`activity/record.tsx`).<br>`draft_recording_interaction.html` v2.6 이 규칙을 적었다 — **"버튼을 누르는 즉시 캡처하되 화면만 2초 준비를 보여 준다"** |
| 자동 저장인가 들어본 뒤 제출인가 | 같은 문서 — **"1초 뒤 자동 저장·분석. 별도 저장 버튼은 두지 않는다"** |
| 조금 틀려도 넘어가기 | `onSkip` 이 **17개 화면**에 붙어 있다 |
| STT 실패 시 대체 흐름 | `75e4afd` "비정상 STT 응답으로 롤플레잉이 중단되지 않게 한다" |
| 마이크 권한 거부 | `NotAllowedError` 처리 — `components/audio/audio-provider.tsx` |
| 손글씨 캔버스를 키보드로 끝낼 수 없다 | `word-write.tsx` 에 `onSkip`. 주석이 이유를 적어 뒀다 — 다음으로 가는 유일한 길이 손으로 칸을 채우는 것이라 손을 못 쓰는 사람이 갇힌다 |

**리뷰의 권고 "발음이 나빠도 막지 마라" 는 코드상 이미 그렇다**(`onSkip` 17개).
다만 **문서로 확정되지 않았다** — 정하려면 `shell_spec_v1` 에 한 줄이 필요하다.

### 9-c. 절반만 된 일곱

| 항목 | 어디까지 |
|---|---|
| ~~게스트 기록 이전~~ | **됐다 (2026-08-26).** §07 의 2번을 "서버에 남긴다" 로 확정하고, 돌려 보니 `ko_learning_record` 만 이전 목록에서 빠져 있어 넣었다 — §6-d |
| 오디오 재생 실패 | 두 화면에만 |
| STT·AI 지연 | 비정상 응답은 처리됐고 **타임아웃 상수가 없다** |
| 앱 전환·백그라운드 | `hooks/use-study-session-ping.ts` 하나 |
| 녹음·쓰기 업로드 실패 | `api/analyzeApi.ts` 가 서버 오류를 던지고 `FailedScreen` 이 받는다.<br>**재시도·오프라인 보관은 없다** — 실패를 보여 줄 뿐이다 |
| 게임·자모 피드백 `aria-live` | 5파일. 게임은 `particle-sniper` 하나뿐, 자모는 `HangulCanvas` 경유 |
| 복습 큐 자동 제거 | `dev_spec_v1` §16 의 2번 — **권고는 확정**(한 트랜잭션, `DELETE` 는 어드민 수동용). 개발 판단 대기 |
| 진행 저장 방식 | 같은 §16 의 3번 — **권고는 확정**(매 문항 + `keepalive`). 실측까지 있다 |
| 결과 화면 | **뜬다**(2026-08-26). 문법·읽기·듣기·어휘 넷이 `ResultScreen` 을 쓴다.<br>전에는 「결과 보기」 가 `router.history.back()` 이었다. 남은 것은 아래 두 줄 |
| 결과 화면 새로고침 | 오답 목록이 **화면 안 상태로만 산다.** 네 활동 다 첫 시도 오답을 서버로 보내지만(`fill-blank` 는 2026-08-26 에 마지막으로 붙었다),<br>받는 쪽 `getLearningRecords` 처리는 `is_correct` 인 것만 복원한다. 되살리려면 그 자리를 고쳐야 한다 |
| 목업 대조 사각지대 | **닫혔다**(2026-08-26). 대조가 화면 조합을 손으로 다시 짜고 있어서 `components/learn/*` 를 하나도 안 봤다.<br>표시/배선을 갈라 제품 컴포넌트를 그리게 했다. 대조 화면 50 |
| ㄴ 어휘 퀴즈 | **닫혔다.** 정본이 그리던 낱말+발음듣기 갈래는 원장에 0건이라 뺐고, 그림 갈래(image-to-word 196)를 정본·목업에 넣었다.<br>대조 화면이 하나 늘었다(activity__wordQuiz_image) |
| ㄴ 진행바 prop 층 | **닫혔다**(2026-08-26). 목업 10개를 승격해 칸이 `<button class="seg">` 가 됐다.<br>flash·loading 은 제품도 안 넘기므로 그대로다. 쌍둥이 `RoleplayScreen` 이 점프를 안 넘기던 것도 같이 고쳤다 |
| 응답 모양을 안 보는 API | **닫혔다**(2026-08-26). `api.ts` 에 도우미 둘을 두고 **열여덟 자리**를 막았다.<br>`asArray<T>()` — 통째로 배열인 자리 열(`flashcard` 셋 · `game-content` 둘 · `chat` 둘 · `game-progress` · `learning-record` · `report`).<br>`withArrays<T>()` — **겉은 객체인데 안의 필드가 배열**인 자리 여덟(`chat` 여섯 · `game-content` 둘). `{}` 를 받으면 겉은 통과하고 필드가 undefined 로 흘러가던 갈래다.<br>`Record<string,T>` 꼴은 넣지 않았다 — `{}` 가 **정상적인 빈 맵**이라 도는 쪽이 안 터진다 (`ParticleSniperSentences` · `CardSortVocab` · `LearningProgress` 등).<br>막는 자리는 **부르는 쪽이 아니라 래퍼** 다 |
| 눌러 보지 못한 곳 둘 | 2026-08-26 에 활동 열셋을 표시/배선으로 가르고 **화면을 열어 확인했는데 둘은 못 했다.**<br>① `SyllableRow` 의 **그림 갈래** — 캔버스에 실제로 획을 그어야 `<img>` 가 붙는데 합성 이벤트로는 캔버스가 반응하지 않는다. 코드 경로만 봤다.<br>② `LoadingScreen` — 제품이 아직 아무 데서도 안 쓴다(위 “제품에 그 경로가 없다” 와 같은 건) |
| 오답 해설 원장 | 문법만 `grammar_focus_revised` 를 들고 온다(836행 중 835행).<br>읽기·듣기·어휘 원장에는 해설 칸이 없어 결과 화면이 `player.answerIs`("정답은 X예요")로 대신한다 |

### 9-d. 아직 아무것도 없는 것

**출시 안전장치 여섯이 전부 0 이다.**

```
app/src 의 테스트 파일 0 · E2E 도구 없음 · 테스트 러너 없음
.github/workflows 없음 · 에러 수집 없음 · 분석 이벤트 없음
```

> 이걸 잴 때 한 번 틀렸다. `glob("app/**/*.spec.ts")` 가 `node_modules` 안의
> 남의 테스트를 집어 "있다" 가 나왔다. **탐침을 `app/src` 로 좁혀야 한다** —
> 틀린 탐침은 없는 것보다 나쁘다. 문서에 "검사 통과" 도장을 달고 거짓말한다.

**공통 예외 화면 셋이 방금 생겼다** — `activity/state-screens.tsx` 의 `LoadingScreen` ·
`FailedScreen` · `MicDeniedScreen` 을 `fill-blank` · `read-answer` · `listen-answer` ·
`mission-dialog` 넷이 쓴다(2026-08-26, 다른 세션 작업 중). 리뷰 4번이 그 자리다.

그 밖에 없는 것 — 무음 녹음 감지 · 네트워크 복귀 ·
저장 중 나가기(`beforeunload` 0곳) · 모달 초점 가둠(`focus-trap`·`inert` 0곳) ·
라우트 전환 후 초점 이동 · 다시 녹음 횟수 제한 · STT 가 판정 근거인지 ·
음성 파일 보관과 그 기간·삭제·동의·탈퇴.

### 9-e. 무엇부터 하면 제일 많이 움직이나

리뷰의 권고 순서는 맞다 — **범위 고정 → 정책 확정 → 세로 흐름 하나 → 상태 전수 → 나머지 목업.**

**0번은 끝났다 (2026-08-26).** 서버가 로컬에서 뜨고 학습 기록 왕복이 돈다 — §6-b.
그것도 **외부 키 하나 없이** 된다. 개발자에게 `.env` 를 받아야 한다고 적었던 것이
이제 필요 없다 — 로컬 MySQL 과 `api/.env.example` 이면 누구나 띄운다.

**그래서 세로 흐름을 지금 시작할 수 있다.** 남은 것은 서버 쪽 저장 모델이지
환경이 아니다 — 9-a 의 스물둘이 그것이다.

---

## 확인된 상태

인계 시점에 아래는 통과한다.

```bash
cd app
pnpm typecheck        # 통과
pnpm parity:activity  # "모두 같다"
pnpm build            # 통과 — §1 에서 고쳤다
```

**`npx biome check src` 는 아직 통과하지 않는다 — 505 → 220 건까지 줄였고(2026-08-24),
그 뒤 화면들을 다시 쓰면서 저절로 더 줄었다. 2026-08-27 에 재니 49건이다.**
전에 여기 "통과" 라고 적혀 있었는데 실제로 재 보니 **505건**(에러 404 · 경고 101)이었다.
셋을 했고 남은 220건은 사람이 판단할 것만 남았다.

| | 무엇 | 결과 |
|---|---|---|
| ① | `biome.json` 에 **데이터 폴더 제외** | 505 → 467 |
| ② | `biome check --write` → `--write --unsafe` | 467 → 226 |
| ③ | 효과 없는 `biome-ignore` 4건 정리 | 226 → **220** (경고 0) |

②는 **타입 · 목업 대조 · 빌드를 매 단계 확인했다.** 셋 다 그대로 통과한다.
대조가 안 깨지는 이유가 있다 — 클래스 순서를 바꾸는 `useSortedClasses` 가 97건인데
비교기가 이미 클래스 토큰을 정렬한다(`activity-parity-diff.py` 의 `class` 처리).

**① 이 왜 필요했나 — biome 이 데이터를 보고 있었다.** 네 파일이 1MB 상한을 넘어
`biome` 이 **아예 처리하지 못한다**(`clip.ts` 11MB · `dialog.ts` 1.9MB ·
`n7_mission_chat.json` 1.5MB · `n1_word_list.json` 1.1MB). 그 파일마다 "크기 초과"
진단 4건씩 나오므로 **제외하지 않으면 통과가 원리적으로 불가능하다.**
제외한 곳은 셋이고 전부 코드가 아니라 데이터·자산이다.

    src/shared/data                        원장 산출물 + 구 앱에서 온 데이터표
    src/assets/complete_mission_ani.json   Lottie(After Effects) 내보내기

`src/components/main/game/data` 도 여기 있었는데, **아무도 안 읽는 사본이라
2026-08-25 에 지웠다**(앱은 게임 콘텐츠를 서버에서 받는다). 제외 목록에서도 뺐다.

`src/screens_ref/screens.ts` 가 이미 같은 이유로 제외돼 있었다.

**딸려 오는 것 — `src/shared/data` 의 작은 로더 모듈들이 검사 밖으로 나갔다.**
`word-list.ts` · `learn-data-check.ts` 처럼 JSON 을 읽어 타입을 붙이는 파일 열한 개다.
**정확히 말하면 잃은 것이 아주 조금 있다** — 제외 직전에 `book.ts` · `dialog_word.ts` ·
`flashcard.ts` 에 `format` 진단이 각 1건 걸려 있었고 그것은 이제 영구히 적용되지 않는다
(서식뿐이라 해는 없다). 더 중요한 것은 **앞으로 이 파일들이 틀려도 아무도 안 잡는다**는 것이다.
폴더 하나를 통째로 빼는 대신 파일을 스물 몇 개 나열할 수도 있었는데, 새 데이터표가
생길 때마다 목록을 고쳐야 해서 폴더로 갔다. 그 폴더는 자기 README 가 "이 폴더의 JSON 은
산출물이다" 라고 밝혀 둔 곳이다.

**내가 틀렸던 것 하나 — 생성 JSON 은 churn 이 아니다.** "`--write` 를 돌리면 다음
생성에서 되돌아간다" 고 적었는데, `build-content.py` 가 `json.dumps(indent="\t")` 로
써서 **biome 의 서식과 이미 같다.** 실제로 대 보니 `n1_*`~`n6_*` 중 바뀌는 것이 없다.

### 남은 220건 — 사람이 판단할 것

| 규칙 | 건수 | 왜 기계가 못 하나 |
|---|---|---|
| `useExhaustiveDependencies` | **95** | 의존성을 잘못 넣으면 무한 렌더가 된다. 훅마다 "빠뜨린 건가, 일부러 뺀 건가" 를 봐야 한다. 이미 여섯 곳은 `biome-ignore` 에 이유를 적어 재워 뒀다 |
| a11y 여섯 규칙 | **73** | `useKeyWithClickEvents` 25 · `useButtonType` 22 · `noSvgWithoutTitle` 12 · `useAltText` 7 · `useSemanticElements` 4 · `useMediaCaption` 3. 봄소풍 🔊 를 버튼으로 만든 것과 같은 종류의 일이다 |
| 그 밖 열 규칙 | 52 | `noArrayIndexKey` 16(정당하게 재우는 것도 섞여 있다) · `noExplicitAny` 10 · `noInvalidPositionAtImportRule` 6 · `noUnsafeFinally` 2 등 |

`noUnsafeFinally` 2건과 `noInvalidPositionAtImportRule` 6건은 **취향이 아니라 버그
종류다** — `finally` 안의 `return` 이 예외를 삼키고, CSS `@import` 가 규칙 뒤에 있으면
무시된다. 여기부터 봤고, **@import 쪽에서 진짜 버그가 나왔다.**

### @import 6건을 봤더니 폰트가 안 실려 있었다 (2026-08-24)

`globals.css` 의 `@import` 여섯이 다른 규칙 뒤에 있었다. 다섯(`tokens`·`activity`·
`nav`·`game`·`vocashot`)은 **Tailwind 가 인라인해 줘서 결과가 맞았다** — 빌드 CSS 에
`.pc-wrong-play` 같은 규칙이 실제로 들어 있는 것으로 확인했다.

**여섯째가 진짜였다.** 파일 맨 끝에 Pretendard 를 부르는
`@import url("…jsdelivr…/pretendard.css")` 가 있었는데,

* 빌드 CSS 의 `@font-face` 가 **0개**였다
* `index.html` 에 폰트 `<link>` 가 없었다
* 저장소에 폰트 파일(`.woff*`)이 하나도 없었다

즉 **프로덕션에서 Pretendard 가 아예 로드되지 않았다.** 목업이 지정한 폰트이고
`tokens.css` 의 `--font-sans` 첫 항목인데, 실제로는 그다음 폴백
(`Apple SD Gothic Neo` → 시스템 sans)으로 그려지고 있었다. macOS 에서는 한글이
멀쩡해 보여서 눈에 안 띄었다.

**맨 위로 올리는 것으로는 안 고쳐졌다.** 옮긴 뒤 빌드 산출물이 **바이트까지 같았다**
(파일 해시 동일) — Tailwind v4 의 import 해석기가 **원격 `@import` 를 위치와 무관하게
버린다.** 그래서 `index.html` 의 `<link>` 로 옮겼다(+`preconnect`). 목업 캡처들도
Exo 2 를 `<link>` 로 부른다.

확인한 것 — CDN 이 `@font-face` 9개를 200 으로 주고, 브라우저에서
`document.fonts.size` 가 **0 → 9**, 실제로 쓰는 400·500·700 이 `loaded`,
`document.fonts.check("700 16px Pretendard")` 가 `true`, 캔버스 글자폭이 폴백과 다르다.

`biome check` 는 220 → **214건**이 됐다. `noInvalidPositionAtImportRule` 0.

### noUnsafeFinally 2건 — 죽은 코드였다 (2026-08-24)

`api/report.ts` 의 두 함수가 `finally { return result }` 로 값을 돌려줬다.
**finally 의 return 은 try·catch 의 return 을 덮어쓰고 던져진 예외까지 삼킨다** —
그래서 try 안의 `return []` 과 catch 안의 `return null` 이 전부 **죽은 코드**였다.

마침 `result` 의 초기값이 그 return 들과 같은 값이라 **결과는 우연히 맞았다.**
옛 모양과 새 모양을 여덟 경로(정상 · `result=false` · `data=null` · 빈 배열 ×
예외 유무)로 대 봤고 전부 같은 값을 냈다.

위험한 것은 지금이 아니라 다음이다. `catch` 에서 되던지도록 고치는 순간
그 예외가 **조용히 사라지고 `null` 이 반환된다** — 실측했다.

    옛: catch 에서 되던져도 → 값 null 반환 (예외가 사라졌다)
    새: catch 에서 되던지면 → throw boom

값을 그 자리에서 바로 돌려주도록 바꿨다. 호출부 둘 다 안전하다 —
`listReport` 는 배열의 `.length` 만 보고, `createReport` 는 반환값을 쓰지 않는다.
`biome check` 214 → **212건**.

### a11y 73건을 화면 단위로 끝냈다 — 212 → 137건 (2026-08-24)

**기계적인 34건.** `useButtonType` 22 → `type="button"`, `noSvgWithoutTitle` 12 →
`aria-hidden="true"`. 둘 다 비교기의 `DROP_ATTRS` 에 있어 **목업 대조에 안 보인다.**
svg 열둘은 전부 장식이었다(수치·목록이 곁에 글자로 있다). 버튼 스물둘이 든 파일에는
`<form>` 이 하나도 없어 `submit` 의도가 없는 것을 확인했다.

**`useAltText` 7 → `alt=""`.** 책 표지·아바타·상황 그림인데 제목·대화 글이 곁에 있어
읽어 주면 같은 말을 두 번 한다.

**`useMediaCaption` 3 → 재웠다.** 숨은 재생 전용 `<audio>` 다 — 저장소가 이미 같은
이유로 여덟 곳을 재워 뒀다.

**`useKeyWithClickEvents` 25 → 진짜 `<button>` 으로.** `role="button"` +
`tabIndex` + `onKeyDown` 으로도 이 규칙은 사라지지만 **그러면 `useSemanticElements`
가 새로 뜬다**(실측했다). biome 은 native 버튼을 원한다. 클래스는 한 글자도 빼지 않고
옮기고, display 지정이 없던 곳엔 `block`, 왼쪽 정렬 글자가 있던 곳엔 `text-left` 를
더했다 — 버튼은 기본이 `inline-block` · 글자 가운데 정렬이라서다.

**`useSemanticElements` 4 중 3은 재웠다.** `role="status"` → `<output>`,
`role="group"` → `<fieldset>` 인데 **보조기술에는 차이가 없다** — 그 요소들의 암묵
role 이 곧 status·group 이다. 얻는 것이 없는데 목업을 갈라야 하고 배치도 흔들린다.

#### 여기서 나온 것들

**서울 퍼즐이 키보드로 아예 닿지 않았다.** 장소 카드가 `div` 였고, 지도 핀은
SVG `<g>` 라 초점을 못 받는다 — 즉 장소로 들어갈 방법이 마우스뿐이었다. 카드가
유일한 경로라 **목업을 고쳤다**(`game__sp_map.html` 카드 10개 → `<button>`,
잠긴 곳은 `disabled`). 🔊 때와 같은 판단이다. 딸려서 `sp_entry`·`sp_puzzle` 목업도
고쳤다 — `.sp-loc-card` 의 버튼 기본값 되돌림이 **공유 `<style>` 블록**에 들어가고
그 블록이 대조 대상이라서다. `TWIN_ALLOW` 에 셋 다 적었다.

**교재 목록에 실제 버그가 있었다.** `routes/book/index.tsx` 의
`onClick={() => goBook(1)}` 이 2~8권 버튼까지 감싸는 **래퍼**에 붙어 있어서,
3권을 눌러도 이벤트가 위로 올라가 **`/book/1` 로 갔다.** 1권 카드로 내렸다.
같은 화면의 `routes/book/list.tsx` 가 처음부터 올바른 꼴이었다. 같은 자리의
`map` 에 `key` 가 없던 것도 함께 넣었다(HEAD 에도 없었다).

**죽은 코드 하나.** `components/main/course-list/scene-header.tsx`(36줄)는 참조가
0곳이었다. a11y 를 고치는 대신 지웠다.

**모달에 Esc 가 없었다.** `components/ui/dialog.tsx` 는 바깥을 마우스로 누르는 것이
닫는 유일한 방법이라 **키보드만 쓰면 모달에 갇혔다.** Esc 로 닫히게 하고, 열릴 때
초점이 모달로 가고 닫힐 때 원래 자리로 돌아오게 했다. `role="dialog"` 도 화면을 덮는
오버레이에서 **상자 쪽으로 옮겼다**(전에는 어두운 배경까지 대화상자로 읽혔다).

**남은 것 — 초점 갇힘(focus trap)이 없다.** Tab 을 계속 누르면 초점이 모달 밖으로
나가므로 `aria-modal="true"` 가 아직 정확한 말이 아니다. native `<dialog>` +
`showModal()` 로 가면 브라우저가 갇힘·Esc·top layer 를 다 해 준다. 이 모달을 쓰는
두 화면(`learn/jamo/word-write` · `book/chapter/unit/write2`)이 로그인 뒤라 눈으로
확인할 수 없어 미뤘다 — **볼 수 있을 때 하는 것이 맞다.**

**눈으로 못 본 것.** 고친 화면 대부분이 로그인·서버 데이터가 있어야 열린다.
목업 대조가 통과하는 것으로 갈음했고, 대조 밖 레거시 화면은
`div`→`button` 전환에서 display·text-align 만 지켰다. 그 화면들을 열 수 있게 되면
한 번 훑는 것이 좋다.

### useExhaustiveDependencies 93건을 끝냈다 — 137 → 43건 (2026-08-24)

**먼저 알아야 하는 것 — 이 작업은 저장소의 게이트가 지켜 주지 못한다.**
`pnpm typecheck` · `pnpm parity:activity` · `pnpm build` 는 **훅이 언제 다시 도는지를
검사하지 않는다.** 목업 대조는 `react-dom/server` 로 그리므로 `useEffect` 가 아예
실행되지 않는다. 즉 의존성을 잘못 넣어 무한 렌더를 만들어도 셋 다 통과한다.
그래서 규칙을 하나 세우고 시작했다 — **넣어도 안전하다는 것을 근거로 말할 수 없으면
넣지 말고, 지금 코드가 왜 맞는지 이유를 적어 재운다.**

#### 재우지 않고 진짜로 해소한 것

| 무엇 | 어떻게 |
|---|---|
| `confetti-provider` 의 `randomInRange` | 상태를 안 읽는 순수 헬퍼라 **모듈 스코프로 올렸다** |
| `content4` 의 `timeStringToSeconds` | 같음 |
| `HangulCanvas` 의 `getCoordinates` | `canvasRef` 만 읽어 `useCallback([])` 로 안정화하고 두 콜백 의존성에 넣었다 |
| `circular-progress` 의 `onEnd` | **ref 로 최신 콜백을 잡았다.** 의존성에 넣으면 부모가 리렌더될 때마다 애니메이션이 처음부터 다시 시작한다 |
| `sign-provider` 의 `handleCheckSign` | `useCallback(…, [])` 로 이미 안정해서 그냥 넣었다(옛 eslint 주석 걷음) |

#### 진짜 버그 둘

**`mission-report.tsx` 가 리포트를 한 번만 받았다.** 의존성이 `[]` 라서 이 컴포넌트를
다시 쓰면 **앞 대화의 리포트가 그대로 남았다.** `dialogId`·`missions?.length` 는
원시값이라 넣어도 루프가 날 수 없어 넣었다.

**`seoul-puzzle.tsx` 의 `resolvedPuzzle` 이 `puzzlesMap` 을 안 보고 있었다.**
하이드레이션(`setCurrentLoc`)이 콘텐츠 로드(`setPuzzlesMap`)보다 먼저 끝나는
경합에서 메모가 빈 `{}` 로 `null` 을 물고 굳는다 — 콘텐츠가 도착해도 다시 계산되지
않는다. 지금은 `contentLoading` 이 퍼즐 화면을 막아 눈에 안 보이지만 화면 전이가 한 줄만
바뀌면 드러나는 자리다. 의존성에 넣어 닫았다.

#### 되돌린 것 하나 — 의존성을 좁혔다가 원복했다

세 화면(`fill-blank` · `read-answer` · `listen-answer`)의 **답 복원 효과**에서
`[currentIndex, question?.id, savedAnswers]` 를 `[question, savedAnswers]` 로 좁혔다.
근거는 "`question` 이 `useMemo` 배열의 원소라 `currentIndex` 가 바뀌면 `question` 도
바뀐다" 였다. 그런데 그것이 참이려면 **배열에 같은 객체가 두 번 들어가지 않는다**는
보장이 필요하고, 그 보장을 코드로 확인할 방법이 없었다. 틀리면 **문항을 넘겨도 저장한
답이 복원되지 않는다** — 학습자가 바로 알아차리는 동작이다.

**원래 배열로 되돌리고 재웠다.** 린트 결과는 같고(진단 0) 동작 변경은 사라졌다.
얻는 것이 없으면 바꾸지 않는 것이 맞다. 규칙은 `CLAUDE.md` 에 적었다 —
**의존성은 더하기만 하고, 뺄 일이면 재운다.**

남은 "더하기" 다섯은 근거를 직접 확인했다: `bookId?: number` · `chapterSeq?: number` ·
`dialogId: string` · `missions?.length` 는 원시값이라 값 비교이고,
`handleCheckSign` · `getCoordinates` 는 `useCallback(…, [])` 로 안정하며,
`puzzlesMap` 은 **`resolvedPuzzle` 을 의존성으로 쓰는 훅이 하나도 없어**(핸들러 4곳 ·
렌더 2곳뿐) 연쇄가 불가능하다.

#### "불필요한 의존성" 은 대부분 지우면 안 되는 것이었다

biome 은 **"몸통에서 읽지 않는다"** 만 본다. 그런데 이 저장소에서 걸린 것은 거의 다
**일부러 넣은 재실행 방아쇠**였다. 지웠다면 이렇게 됐다.

| 어디 | 지웠다면 |
|---|---|
| `dialog-input` `textareaValue` | 글을 쳐도 입력칸이 늘어나지 않는다 |
| `book-tabs` · `chapter-chips` `activeId` | 탭·칩을 바꿔도 그것이 화면 안으로 안 들어온다 |
| `word-learning` `currentPage` | 이전 페이지의 자동 이동 타이머가 그대로 터진다 |
| `routes/learn/flashcard` `currentIndex` | 카드를 넘겨도 앞 카드 소리가 계속 난다 |
| `mockup-parity.stories` `screen.name` | 스토리를 바꾸면 다음 화면이 통째로 비어 보인다 |
| `particle-sniper` `currentQuestionIndex` | 문항이 넘어가도 이전 타이머가 이어진다 |
| `particle-sniper` `verdictKey` | MISS 가 연달아 나면 두 번째 표시가 일찍 사라진다 |

`verdictKey` 는 **의존성 자리에만 존재하는 순수 재장전 카운터**다(선언 1곳 · 사용 2곳,
둘 다 의존성 배열). 이런 것은 지우는 것이 아니라 왜 있는지 적어 두는 것이 맞다.

#### 나머지는 근거를 적어 재웠다

대부분 두 꼴이다. ① **마운트 1회 · 언마운트 정리** 효과 — 넣으면 매번 떼고 붙거나
매번 치운다. ② **매 렌더 새로 만들어지는 함수 + 몸통에서 `setState`** — 넣으면
무한 렌더다. `audio-provider.tsx` 는 진단이 11건이었는데 주석 셋으로 끝났다.
그 파일은 `useState` 가 **0개**이고 `useRef` 가 12개라 모든 클로저가 ref 만 읽어
낡지 않는다 — **파일 전체의 상태 관리 방식을 먼저 본 것이 판단을 갈랐다.**

### 이 작업이 드러낸 것 — `use-sound-effects` 가 아무것도 memo 하지 않는다

`src/components/effect/use-sound-effects.ts` 에 `useCallback` · `useMemo` 가
**하나도 없다**(실측 0). 즉 호출마다 새 객체와 새 함수를 돌려준다. **이 훅을 18개
파일이 쓴다.** 그래서 `sound.playCorrect` 같은 것을 의존성에 넣으면 그 콜백이 매 렌더
새로 만들어져 `useCallback` 이 무의미해진다 — `fill-blank` · `read-answer` ·
`card-sort` 의 억제가 전부 이 한 가지 원인에서 나왔다.

**그쪽을 안정화하면 억제 몇 개가 필요 없어진다.** 공용 훅이라 18개 파일에 영향이
가므로 이번 범위(훅 의존성)에서 손대지 않았다. 따로 잡을 일이다.

### 확신이 낮아 재운 자리 — 사람이 다시 볼 것

- **`flashcard-result.tsx:78`** — `cardType` 이 zustand 스토어에서 온다. 결과 화면이
  떠 있는 동안 카드 타입을 바꿀 경로가 있으면 집계가 갱신되지 않는다. 지금 UX 에는
  그 경로가 없다고 봤지만 확신은 중간이다.
- **`combine.tsx` · `combine3.tsx` 의 채점 효과** — 문항이 넘어갈 때 `init()` 이
  자모 선택을 `undefined` 로 되돌리는 것에 기대고 있다. 넘어가는 순간 이미 전부
  `undefined` 였다면 `isSucceed` 가 낡을 수 있다(실제로는 조합을 맞춰야 쓰기 단계로
  가므로 지금은 안 터진다).
- **`seoul-puzzle.tsx:916` 저장 훅의 경합** — 하이드레이션이 콘텐츠보다 먼저면 이미
  완료한 과가 `stageScore = 0` 으로 저장된다(`locations` 가 아직 `[]`). 의존성으로는
  못 고치고 로직을 바꿔야 한다.
- **`read-answer` · `listen-answer` 의 `handleSelect`** — 의존성에 매 렌더 새
  `Set` 인 `currentWrongSet` 이 이미 들어 있어 `useCallback` 이 메모 역할을 못 한다.

### 덤으로 눈에 띈 것 (손대지 않음)

`combine.tsx` · `combine3.tsx` 가 `_problem.content_sound` 를 **바로 다음 줄의
`if (_problem)` 널 검사보다 먼저** 읽는다. `problemIndex` 가 항상 범위 안이라 지금은
안 터지지만 순서가 뒤집혀 있다.

### 한 사실은 한 문서에만 — A안을 했다 (2026-08-24)

문서가 낡는 원인은 **개수가 아니라 한 사실이 여러 곳에 적힌 것**이었다. 세어 보니
"목업 대조 화면 수" 가 **일곱 문서**에, 자모 529행이 다섯, 문서 수가 네 곳에 있었다.
오늘 "31 → 47" 을 고칠 때 다섯 곳을 손댄 것이 그 결과다.

**사실마다 주인 문서를 하나 정했다.** 주인 밖에서는 숫자를 적지 않고 질적으로 쓴다
("목업 캡처 전부가 일치한다"). 어기면 `check_docs.py` 의 `[사실 중복]` 이 잡는다.

| 사실 | 주인 | 전에 몇 곳 |
|---|---|---|
| 목업 대조 화면 수 | `README.md` | 7 → 1 |
| phase1 정본 문서 수 | `phase1/INDEX.md` | 7 → 1 |
| `_superseded` 문서 수 | `phase1/INDEX.md` | 2 → 1 |
| 인계 메모 수 | `phase1/INDEX.md` | 2 → 1 |
| i18n 로케일 수 · VocaShot 문항 은행 · n1_word_quiz 행 | 지금 자리 그대로 | 각 1 (우연을 규칙으로 굳힘) |

`BLOCKERS.md` 의 검증 기록도 숫자를 뺐다 — 명령 옆에는 스크립트의 실제 성공
메시지를 적는다(`pnpm parity:activity  # "모두 같다"`). **화면이 늘어도 안 낡는다.**

#### 검사기가 넣자마자 두 곳을 더 찾았다

**패턴이 불완전했다.** `games_spec_v1` 은 `parity:activity <수>화면` 꼴로 적고 있었는데
그 문구가 목록에 없어서, 오늘 낡은 수를 잡을 때 이 파일은 **조용히 통과**했다
(사람이 grep 해서 찾았다).

*(여기 예시에 실제 숫자를 쓰지 않는 것도 이유가 있다 — 쓰는 순간 이 검사에 자기가
걸린다. 실제로 한 번 걸렸다.)*
문구 셋을 채우자 곧바로 걸렸다 —

* `masterplan_v3` 의 상태 표에 **낡은 "27화면"** 이 남아 있었다. 오늘 스윕을 통과한 낡음이다
* `BLOCKERS.md` 의 시점 기록 네 곳

**교훈은 검사기 자체에도 적용된다** — 패턴이 없으면 검사는 통과한다. 새 문구로
숫자를 쓰면 `claims()` 의 패턴 목록에 같이 추가해야 한다. 이것도 `check_docs.py`
안에 주석으로 적어 뒀다.

### A안을 "끝났다" 고 한 뒤 다시 검증했더니 셋이 더 나왔다

기획자가 "제대로 한 거 맞는지 다시 검증해라" 고 해서, 내 검사기로 통과를 확인하는
대신 **틀렸을 가능성을 찾는 쪽으로** 다시 봤다. 셋이 나왔다.

**① 세지 않는 사실이 있었다.** "캡처 N개" 를 문서 다섯 곳이 손으로 적고 있었는데
`check_docs` 가 목업 캡처를 아예 세지 않아 낡아도 몰랐다. `mockup_captures()` 를
넣어 세게 하고 주인을 README 로 정했다.

**② 검사기가 첫 자리만 봤다.** `re.search` 는 패턴당 한 번만 찾는다. 그 첫 자리가
예외(`CLAIM_ALLOW`)에 걸리면 **뒤의 진짜 위반을 건너뛴다** — 실제로 `BLOCKERS.md`
두 곳을 그렇게 놓쳤다. `finditer` 로 모든 자리를 보게 고쳤다.

**③ 새 패턴이 시점 기록까지 잡았다.** "그때는 30화면이었다" 나 다른 문서의 옛
문장을 옮긴 인용은 **고치면 안 되는 것**이다. 정규식으로는 현재 주장과 가를 수
없어서 `CLAIM_ALLOW` 에 문구와 이유를 손으로 적어 봐준다(`TWIN_ALLOW` 와 같은 꼴).

검증 방법도 남겨 둔다 — **주인마다 일부러 어겨 보고 잡히는지 세는 것**이다.
지금 14가지 문구를 넣어 보면 14가지 다 잡고, 원복하면 통과한다. 문구를 새로
쓰면 이 시험에 한 줄 더 넣어라.

**교훈은 매번 같다.** 검사기가 "통과" 라고 해도 그것은 *지금 세는 축에서* 어긋난
게 없다는 뜻이다. 새 축(캡처 수)과 구현 결함(첫 자리만 보기)은 사람이 찾았다.

### 목업 대조 화면 전체를 접근성 감사했다 (2026-08-24) — 고치기 전 기록

린트가 0이 된 뒤에 **화면 단위로** 다시 봤다. 규칙 단위로 보면 묶어서 판단하게 되고,
실제로 그러다 서울 퍼즐 지도의 눌리는 핀을 놓쳤다.

네 번 훑었다 — ① 그려진 마크업(이름·`aria-hidden`·`alt`·`tabindex`)
② 소스(대화형이 아닌 요소에 붙은 핸들러) ③ 이름의 품질·중복·라벨 없는 입력·
안 보이는데 초점 가능 ④ 초점 순서와 `aria-live`.

#### 찾은 것

| # | 무엇 | 어디 | 왜 문제인가 |
|---|---|---|---|
| 1 | **이름 없는 뒤로가기 버튼** | 게임 9화면 — `cs_level` `cs_play` `pc_title` `pc_select` `ps_level` `sp_map` `sp_entry` `sp_puzzle` `vocashot__start` | 버튼 안에 라벨 없는 화살표 svg 뿐이라 스크린리더가 **"버튼" 이라고만** 읽는다. **앱·목업 양쪽 다** 그렇다(9화면 각각 열어 확인) |
| 2 | **뒤면이 안 보이는데 초점을 받는다** | `flash` | `.flash-face { backface-visibility:hidden }` 라 카드 뒤면이 DOM 에 남는다. 키보드로 **안 보이는 버튼에 닿고** 같은 이름("사과 발음 듣기")이 두 번 읽힌다 |
| 3 | **같은 이름, 다른 일** | `speak` | 듣기 버튼과 말하기 대상이 **둘 다 "어"** 로 읽힌다. `flash` 는 같은 자리를 "사과 발음 듣기" 로 붙여 뒀다 — 그 방식이 맞다 |
| 4 | **초성과 받침이 구분되지 않는다** | `write3` | `data-type="consonant"` 인 ㄱ 과 `data-type="final"` 인 ㄱ 이 같은 이름이다. 여덟 자모가 전부 그렇다 |
| 5 | **묶음을 알 수 없다** | `nav__jamo__resume` | 묶음 셋(모음1·자음1·자음2)이 같은 이름의 버튼 다섯씩을 갖는다. 묶음 이름은 글자로만 있어 **어느 묶음의 것인지 알 수 없다** |
| 6 | **키보드 없는 `div onClick`** | `flash` · `wordPreview` · `write` `write3` `write_canvas` `write3_canvas` | `jamo-write.tsx:85` 는 네 화면이 공유한다. "손가락으로 긋는 판" 이라는 주석이 이미 있으니 그 판단이 지금도 맞는지 화면별로 봐야 한다 |
| 7 | **장식 아이콘이 노출된다** | `game__list` | 카드에 `aria-label` 이 있는데 안의 lucide 아이콘 10개가 `aria-hidden` 없이 남아 있다 |

#### 확인했고 문제 아니었던 것

* **결과 화면 일곱에 `aria-live` 가 없다** — 오탐이다. `aria-live` 는 화면 안에서
  값이 바뀔 때 필요하고 결과 화면은 화면 자체가 바뀌므로 그냥 읽힌다.
  활동 화면 열하나는 피드백 띠에 제대로 붙어 있다.
* **`game__cs_intro` 에 초점 받을 것이 하나도 없다** — `setInterval` 로 자동
  전환되는 2초 오버레이라 갇히지 않는다. 다만 `aria-live` 가 없어 그 2초 동안
  뜨는 카테고리 안내를 보조기술이 놓칠 수 있다.
* `tabindex` 가 양수인 곳 0 · `alt` 없는 `<img>` 0 · `aria-hidden` 안에 들어간
  대화형 요소 0(MapSvg 를 고친 뒤).

#### 고치려면 목업을 같이 고쳐야 하는 것

1번은 `aria-label` 을 붙이는 일인데 **`aria-label` 은 비교기가 무시하지 않는다**
(`DROP_ATTRS` 에 없다). 즉 앱만 고치면 대조가 깨진다 — 🔊 · `sp-loc-card` 때처럼
목업 9개를 같이 고치고 `TWIN_ALLOW` 에 적어야 한다. 2번의 `tabindex`/`inert` 도 같다.
7번의 `aria-hidden` 은 비교기가 무시하므로 **앱만 고치면 된다.**

#### 정적 마크업으로는 볼 수 없어 남긴 것

화면이 바뀔 때 **초점이 어디로 가는가**. 활동 → 결과처럼 화면이 갈릴 때 초점이
사라진 요소에 남아 있으면 보조기술이 아무것도 읽지 않는다. 이건 실제로 돌려 봐야 한다.

### 게임·자모를 실제로 돌려서 본 것 (2026-08-24) — 정적 마크업이 못 보는 쪽

목 API 를 띄우고 게스트로 들어가 손으로 조작했다. **`useEffect` 가 실제로 도는 상태**라
목업 대조로는 볼 수 없던 것들이 나왔다.

#### 진짜로 막히는 것

**자모 `단어 쓰기`(sub=4)는 키보드로 끝낼 수 없다.** `Next` 는
`checkSucceed()` 가 여는데, 그 조건이 `imageSlots` 가 글자 수만큼 차는 것이고
슬롯을 채우는 유일한 경로가 `returnImage(base64Img)` — **캔버스에 그린 그림**이다.
캔버스는 `tabindex` 도 `aria-label` 도 없고 `onPointerDown` 으로만 받는다.
슬롯 버튼까지는 키보드로 가지만 거기서 끝이다.

#### 화면이 바뀌어도 초점이 따라가지 않는다

게임에 들어갈 때 · 급을 고를 때 · 판이 끝나 결과가 뜰 때 — 세 번 다
`document.activeElement` 가 `BODY` 였다. 스크린리더는 **새 화면을 아무것도 읽지 않는다.**
결과 화면에는 `aria-live` 도 없어서 판이 끝난 것 자체가 알려지지 않는다.

#### 피드백이 안 읽히는 화면

`ps_play` 에는 `role="status"` 가 있지만 그 안에 든 것은 명중/빗나감뿐이고
**문항 자체는 라이브 영역 밖**이라 다음 문항이 안 읽힌다. 서울 퍼즐 퍼즐 화면과
자모 화면들에는 `aria-live` 가 아예 없다 — 낱말을 놓아도 채점이 돼도 조용하다.

#### 이름 문제 (실행해야 보이는 것)

* 서울 퍼즐 이름 입력칸에 **`placeholder` 만 있고 접근 가능한 이름이 없다**
  (`aria-label` · `id`+`label` 둘 다 없음). placeholder 는 입력하면 사라진다
* 서울 퍼즐 퍼즐 화면의 번역 토글 둘이 **"🌐" 이모지만 이름**이다
* 자모 `sub=1` 의 헤더 오디오 버튼과 연습 목록 첫 항목이 **둘 다 "아"**,
  `sub=4` 의 글자 슬롯 셋이 **모두 "이"** 다

#### 확인했더니 문제가 아니었던 것 — 두 번 잘못 볼 뻔했다

* **"게임을 키보드로 못 한다"** — 아니다. 선택지 버튼은
  `onClick={(e)=>{ if (e.detail===0) onAnswer(choice) }}` 로 키보드 경로를 갖고 있고,
  실제로 점수가 0 → 100 으로 올랐다. 처음에 "안 된다" 고 본 것은 두 가지 때문이었다 —
  내 도구의 키 입력이 페이지에 안 닿았고(`keydown` 조차 안 왔다), 클릭 **직후
  동기적으로** 상태를 읽어 리렌더 전 값을 봤다.
* **"자모 조합 화면도 그려야 넘어간다"** — 아니다. ㅇ+ㅓ 로 맞추자 캔버스 획 0개로
  `Check` 가 열렸다. 처음에 안 열린 것은 정답이 "어" 인데 내가 "아" 를 만들어서였다.
  **앱이 맞고 내 시험이 틀렸다.**

두 번 다 한 단계 더 가서 막았다. 정적 마크업만 보고 적었으면 없는 버그를 적었을 것이다.

#### 다시 돌리려면

```bash
python3 phase1/game_mockapi.py &   # 8799 — 앱의 .env 가 보는 주소. api/seed_data 를 낸다
                                   # (씨드가 서버와 같은지는 --live 로 먼저 보라)
cd app && pnpm dev                 # 3000
```
로그인 화면에서 **Browse without signing in** 을 누르면 게스트 토큰이 나온다.
(좌표 클릭은 안 먹을 때가 있다 — 요소를 직접 `.click()` 하는 편이 확실하다.)

### 자모 화면에 건너뛰기가 빠져 있었다 — 목업에는 처음부터 있었다 (2026-08-24)

`단어 쓰기`(sub=4)가 키보드로 끝나지 않는 문제를 파다가 원인이 다른 데 있었다.
**목업 네 개**(`activity__write` · `write3` · `wordrep` · `jamoListen`)에 상단
건너뛰기 버튼(`data-action="skip"`)이 있는데 **실제 자모 화면 여섯 개는 아무도
`onSkip` 을 넘기지 않는다.** `ActivityAppBar` 는 그 자리를 44px 빈 칸으로 둔다.

#### 목업 대조가 왜 못 잡았나 — 알아 둘 한계

`scripts/activity-parity.tsx` 는 `@/components/learn/jamo` 에서 **아무것도
import 하지 않는다.** 활동 22화면은 셸 컴포넌트(`ActivityShell` · `ProblemCard` …)에
props 를 손으로 넣어 조립한 **픽스처**다. 즉 —

> **목업 대조가 통과해도 실제 자모 화면이 목업과 다를 수 있다.**
> 대조가 보는 것은 "셸 컴포넌트가 목업대로 그리는가" 지
> "그 화면이 셸을 목업대로 쓰는가" 가 아니다.

같은 사정이 `learn/*` 의 다른 활동 화면에도 해당한다.

#### 고친 것 — sub=4 만

`word-write.tsx` 에 `onSkip={next}` 를 넣었다. `next()` 는 이미 다음 낱말로
넘기는 일을 하고 있어서 그대로 쓰면 됐다. 브라우저에서 확인했다 —
키보드로 초점을 주고 눌러 `이 → 아이 → 오` 로 넘어간다.

**왜 "쓰기를 건너뛰게" 가 맞나.** 이 화면에서 다음으로 가는 유일한 길이
`imageSlots` 를 손으로 써서 채우는 것이라, 손으로 쓸 수 없으면 갇힌다. 그런데
이 활동이 가르치는 것이 **손글씨 자체**라 키보드로 흉내 내는 것은 활동을 없애는
것이다. 그래서 가짜 완료를 만들지 않고 **넘어갈 길**을 준다.

같이 한 것 — **그리기 모달을 따라쓰기 화면과 같은 꼴로 맞췄다.** 전에는 여기만
둥근 아이콘 버튼 둘이 좌우로 갈라져 있어서, 같은 "손으로 쓰는 판" 인데 다르게
보였다. 목업(`activity__write_canvas`)이 정한 모양을 따랐다 —

* 캔버스 아래 오른쪽에 글자 도구 둘(`.tools > .tool`) — **되돌리기 · 전체 지우기**
* 확정은 아래쪽 `.primary` **확인**
* 채점 표시는 손으로 만들지 않고 **다른 화면과 같은 피드백 알약**
  (`components/main/activity/feedback.tsx` 의 `FeedbackMessage`)을 자리
  (`.feedback-slot`)까지 그대로 쓴다
* 캔버스에 `role="img"` 와 이름

**되돌리기는 이 판에 없던 기능이라 새로 만들었다.** 따라쓰기의 `undo` 는 목표 글자의
**정해진 획 순서**를 되감는 것이라 자유 필기에는 뜻이 다르다 — 그래서 가져오지 않고,
획마다 점을 모아 두고(`strokesRef`) 되돌릴 때 남은 획을 처음부터 다시 그리게 했다.
그릴 때와 **같은 곡선식**으로 다시 그려야 모양이 안 바뀐다.
브라우저에서 두 획을 긋고 확인했다 — 잉크가 24708 → 12354 → 0 으로 정확히 절반씩 준다.

> 만드는 중에 두 번 헛다리를 짚었다. ① 점을 모으는 한 줄이 편집에서 빠졌는데
> "되돌리기가 전부 지운다" 는 증상만 보고 다른 원인을 뒤졌다 — **넣은 편집이 실제로
> 들어갔는지 세어 보니** 바로 나왔다. ② 알약 자리로 `.feedback-slot` 을 그대로
> 가져다 썼다. 그것은 활동 화면 **바닥에 깔리는 띠**라 회색 배경(`--color-background-base`)을
> 갖고 `flex:0 0 44px` 로 서는데, 흰 모달은 flex 열이 아니라 **회색 박스가 떴다 줄었다**
> 했다. 알약만 같은 것을 쓰고 자리는 `.canvas-feedback`(높이 고정 · 배경 없음)으로 따로 뒀다.

**오답 뒤에 도구가 잠기던 것도 고쳤다.** `saveSignature` 가 정답·오답을 가리지 않고
`setHasLine(false)` 를 불러서, "Try again" 이 떴는데 **획은 그대로 남고 되돌리기·전체
지우기만 잠기는** 상태가 됐다 — 고칠 수도 지울 수도 없으니 사실상 갇힌다.
맞았을 때만 내린다(그때는 1초 뒤 슬롯에 넣고 닫히므로 다시 누르는 것을 막는 값이기도 하다).

#### 나머지 다섯도 붙였다 — 그런데 "같은 한 줄" 이 아니었다

여섯 화면 전부 `onSkip` 이 들어갔다. 다만 **넘어가는 방법이 화면마다 달랐다.**

| 화면 | 건너뛰기가 하는 일 |
|---|---|
| `word-write` | `next()` 를 그대로 — 다음 낱말 |
| `combine` · `combine3` | **전용 `skip()`.** `next()` 는 조합 단계에서 **정답 소리를 내고** 따라쓰기로 넘긴다 — 건너뛸 때 낼 소리가 아니다. 셸의 건너뛰기는 "이 문항을 건너뛴다" 이므로 단계와 상관없이 다음 문항으로 |
| `choose` | **전용 `skip()`.** `next()` 는 "정답 표시 후 1초 딜레이" 용이라 선택지를 잠그고 1초를 기다린다 — 건너뛸 때는 멈춘 것처럼 보인다 |
| `pronounce` · `word-repeat` | 하단 CTA 가 아예 없다. 연습 목록에서 낱말을 골라 진행하므로 "다음" 은 **목록의 다음 낱말**이고 마지막이면 나간다 |

브라우저에서 여섯 다 눌러 확인했다 — sub1 `아→어` · sub2 `외→아→위` ·
sub3 `이→아이` · sub4 `이→아이→오` · sub5 선택지가 400ms 안에 바뀜(전엔 1초 지연) ·
sub6 `밖→앞`.

`HangulCanvas.tsx` 의 문구가 한국어로 박혀 있다(`여기에 손가락으로 쓰세요.`).
UI 언어가 영어여도 그대로 나온다 — i18n 대상인데 아직 아니다.

### 목업 캡처를 상태별로 늘렸다 — 47 → 49화면 (2026-08-25)

**메우려는 구멍** — 한 화면에 상태가 여럿이면 **캡처가 담은 상태만 견줘진다.**
담지 못한 상태는 영영 안 본다. 오늘 나온 버그 여럿이 그 자리에 있었다 —
`.r-row`(놓친 단어 있는 결과) · `.typed`(직접 입력 모드) 둘 다 캡처에 없는 상태였다.

**캡처를 어떻게 떴나 — 지어내지 않았다.** 정본 프로토타입
`phase1/screens_SOT.html` 을 `mockup-preview` 로 띄우고 **실제로 몰아서**
`#screen-voca` 의 DOM 을 그대로 떴다. 기존 목업(`vocashot__start`)과 시작 상태를
견줘 **같은 자리에서 나온 것임을 먼저 확인**했다.

> **붙여넣기 오류를 잡는 법.** 브라우저에서 뜬 글을 손으로 옮기면 틀릴 수 있다.
> 그래서 브라우저에서 **길이와 해시**를 같이 받아, 저장한 파일을 같은 식으로 해싱해
> 맞춰 봤다. 첫 판은 길이가 12 글자 어긋났는데 **낱말이 다음 문항으로 넘어간 것**이었고,
> 얼린 상태로 다시 재니 정확히 같았다.

| 새 화면 | 담은 상태 |
|---|---|
| `vocashot__play_type` | 플레이 — **직접 입력** 모드(기존은 4개 중 고르기뿐) |
| `vocashot__result_best` | 결과 — **신기록 + 놓친 단어 있음**(기존은 0점·놓친 단어 없음) |

#### 새 캡처가 바로 잡아낸 것 넷

| | 앱 | 정본 |
|---|---|---|
| 게임 오버 제목 색 | `r-ttl` 만 — **회색** | `r-ttl no` — 빨강. `.r-ttl.no` 규칙은 있었다 |
| 끝난 사유 문구 | 늘 "N문항을 하트 M개 남기고 끝냈습니다" | 하트 0이면 **"하트가 0이 되어 끝났습니다."** |
| 입력 칸 안내 | "한국어로 입력" | "한국어 단어를 입력하세요" |
| 제출 버튼 | "쏘기" | **"발사!"** |

`autoCapitalize="off"` 도 빠져 있어 더했다 — 모바일에서 첫 글자가 대문자로 바뀐다.
넷 다 **정본에 맞췄다**(이 저장소는 구현이 목업과 다르면 목업이 기준이다).

#### 일부러 갈라 둔 것

**직접 입력 줄을 `<div>` 대신 `<form>` 으로 감쌌다** — Enter 로 제출된다. 정본은
`<div>` 라 키보드만 쓰면 버튼까지 Tab 해야 한다. `mockups/` 를 고치고 `captured/` 는
날것으로 두고 `TWIN_ALLOW` 에 적었다.

**비교기가 `value` 를 안 본다.** React 는 제어 입력에 늘 `value` 를 붙여 내보내는데
(빈 칸이면 `value=""`) 목업은 그냥 `<input>` 이다. 디자인 차이가 아니라 렌더러
차이라 `DROP_ATTRS` 에 이유와 함께 넣었다.

#### 아직 안 늘린 것

게임 넷(조사 스나이퍼·어휘 카드·봄소풍·서울 퍼즐)의 상태는 **그대로다.** 그쪽
캡처는 정본 프로토타입이 아니라 **배포된 화면에서 뜬 것**이라, 상태를 늘리려면
그 화면을 다시 띄워야 한다. 활동 22화면도 마찬가지다.

### 아무것도 안 하던 클래스 둘을 지웠다 — 그리고 죽은 tailwind.config.js (2026-08-25)

`pnpm check:css` 가 "이 이름의 규칙이 어디에도 없다" 로 남겨 뒀던 둘이다.
**어디에 쓰이는지 보고 지웠다.**

| 이름 | 쓰이던 곳 | 무엇이었나 |
|---|---|---|
| `items-top` | `chat/chat-text.tsx` 2곳 · `dialog/dialog-scenario.tsx` 1곳 | **Tailwind 어느 판에도 없는 이름.** `items-start` 오타로 보인다 |
| `font-inter` | `draw/HangulCanvas.tsx` 1곳 | 이 이름의 글꼴 규칙이 앱에도 Tailwind 에도 없다 |

**지워도 화면이 안 바뀐다.** 규칙이 없으니 원래 아무 일도 안 하고 있었다.
`items-start` 로 고치는 쪽은 **안 골랐다** — 그러면 정렬이 실제로 달라지는데
견줄 목업이 없다. 셋 다 **목업 캡처에도 대조 산출물에도 없어서**(그 요소들이 대조가
보는 범위 밖이다) 지워도 대조가 안 깨진다.

#### 딸려 나온 것 — `app/tailwind.config.js` 는 죽어 있다

`font-inter` 를 만들 만한 자리를 찾다가 나왔다. 이 저장소는 **Tailwind v4** 이고
설정은 CSS 의 `@theme` · `@plugin` 으로 한다(`globals.css` 가 이미 그렇게 한다).
v4 가 v3 형식 설정을 읽으려면 CSS 에 `@config` 를 적어야 하는데 **없다.**

그래서 그 파일의 내용은 아무 효력이 없다 —

| 설정 | 지금 상태 |
|---|---|
| `fontFamily.sans = Pretendard` | 안 읽힌다(폰트는 `globals.css` 가 따로 잡는다) |
| `perspective-1000` · `transform-style-3d` | 안 읽히고 **코드도 안 쓴다** |
| `backface-hidden` · `rotate-y-0` · `rotate-y-180` | 안 읽히지만 **v4 내장이라 이미 실려 나간다.** 코드는 안 쓴다 |
| `require('@tailwindcss/forms')` | 안 읽힌다 — 이 플러그인은 **적용되고 있지 않다** |

**지우지 않고 남겨 뒀다.** 두 클래스와 달리 이건 설정 파일이라 지우는 판단을
따로 받는 것이 맞다. 지금 상태로도 빌드는 이것을 안 읽는다.

> **작업 중 실수 하나.** 표에서 `USED_ALLOW` 만 비우려고
> `s.index('USED_ALLOW = {')` 를 썼는데 **`UNUSED_ALLOW` 안에 걸려** 그쪽 표
> 열넷을 통째로 날렸다. `pnpm check:css` 가 갑자기 열 건을 뱉어서 알았다.
> `git diff` 로 그 파일의 미커밋 변경이 그 실수 하나뿐임을 확인하고 되돌렸다.
> 부분문자열로 앵커를 잡지 마라 — 줄 시작(`^`)을 걸어야 한다.

### VocaShot 요격 연출 — 그리고 `.log` 는 기능이 아니었다 (2026-08-25)

#### `.log` — 만들지 않았다. 부스러기였다

앞 항목에서 나는 이것을 "VocaShot 요격 기록 목록" 이라고 적었다. **틀렸다.**
정본에서 `.log` 는 `<aside>` 안 `<h2>이번 판 기록</h2>` 아래에 있는
**프로토타입 문서 자신의 조율 벤치**다(`.tune` · `.note-section` · `--bench-*` 와
한 묶음). 앱 화면이 아니다.

앱 CSS 에는 그 묶음 중 **`.log .w` 한 줄만** 딸려 와 있었다 — `.log` 도
`.log div` 도 `.log .t` 도 없고 `--bench-*` 변수도 앱에 없다. 만들 기능이 아니라
**지울 부스러기**였다. 지웠다.

> **내가 성급했다.** 이름이 정본에 나온다는 것만 보고 "앱 기능인데 안 그렸다" 로
> 분류했다. **어디에 나오는지**를 봤어야 했다. 검사기가 자리를 알려 주지는 않는다.

#### `.destroying` — 요격 연출을 만들었다

정답을 맞히면 방어선에서 운석까지 광선이 뻗고, 맞은 자리에 불꽃이 터지고,
운석이 부서진다. 정본의 `playHitEffect()` 다.

**절반만 이관돼 있었다.** `.defense-emitter` · `.earth-surface` · `.sky` 는 앱이
그리고, keyframes(`laserShot` · `impactRing` · `meteorDestroy`)도 다 있는데
**`.laser-beam` · `.impact-burst` 선택자만 빠져 있었다.** 그래서
`.meteor-shell.destroying` 도 붙일 데가 없어 규칙만 남았다. 네 규칙을 정본에서
그대로 옮기고 연출을 붙였다.

**정본과 다르게 한 것 하나** — 정본은 DOM 을 직접 만들어 `.sky` 에 붙이지만,
여기서는 잰 값을 상태로 두고 React 가 그리게 했다. 렌더러와 다투지 않는 쪽이 낫다.

**타이머를 정리 함수가 아니라 ref 에 둔다.** 정리 함수에 두면 `feedback` 이
null 로 바뀔 때 React 가 그것을 먼저 부르는데, 되먹임은 900ms 뒤 지워지고 연출은
560ms 라 **먼저 끊길 수 있다.** 그러면 불꽃이 안 사라진다.

**브라우저에서 좌표까지 확인했다.**

| | 값 |
|---|---|
| 광선 시작 | `left:187.5px top:566px` — 방어선 중심 `(188, 566)` |
| 광선 길이 | `498px` — 방어선 566 에서 운석 68 까지 |
| 불꽃 자리 | `left:187.5px top:68px` — 운석 중심 `(188, 68)` |
| 불꽃 살 | 8개 · `.meteor-shell.destroying` 붙음 · 되먹임 "정답 +10" |

스틸로는 560ms 를 놓쳐서, 확인할 때만 **페이지에서** 제거 타이머를 잠깐 막고
찍었다(코드 변경 아님, 곧 되돌렸다). 광선이 방어선에서 뻗는 것이 보인다.

### VocaShot 최고 점수 경신 배지 (2026-08-25)

`pnpm check:css` 가 "`.r-new` 규칙은 있는데 아무도 안 쓴다" 로 잡아 낸 것이다.
정본은 신기록이면 이전 최고 점수 대신 배지를 띄운다.

```
${G.isBest ? `<span class="r-new">최고 점수 경신</span>`
           : `<p class="r-prev">최고 점수 ${G.prev}</p>`}
```

**왜 그냥 못 그렸나 — 판정할 값이 그 시점에 없다.** `endRun()` 은 점수를
**저장한 뒤** 최고 점수를 다시 받아 온다. 그래서 결과를 그릴 때의 `best` 는 이미
새 점수이고, 경신인지 동점인지 갈리지 않는다.

**저장 전 값을 ref 로 들고 있다가 저장 직전에 판정한다.**
기준은 정본과 같은 `score > (이전 최고 ?? 0)` 이다 — 기록이 없어도 0점은 경신이
아니다.

> **`best` 를 `endRun` 의 의존성에 넣지 않은 이유**는 이 파일이 이미 적어 둔 것과
> 같다. `endRun` 신원이 바뀌면 그것을 쥔 `serveNext` 의 낙하 타이머가 매 렌더 다시
> 걸려 **문항이 순간이동한다.** 그래서 `clearTimer` 와 같은 방식(ref)으로 갔다.

**브라우저에서 양쪽을 다 봤다.** 기록이 없는 3급으로 10점 → **「최고 점수 경신」**.
이어서 다시 하기로 같은 10점 → 배지 없이 **「최고 점수 10」**. 동점은 경신이 아니다.

목업 대조 픽스처(`vocashot__result`)는 캡처가 0점·최고 0 인 상태라 `isBest={false}`
를 명시했다.

### CSS 이름과 코드 이름이 맞는지 보는 검사 (2026-08-25)

**메우려는 구멍** — 이 앱의 CSS 는 정본 프로토타입(`phase1/screens_SOT.html`)과
목업에서 **통째로 이관해 왔고**, 컴포넌트는 그것을 보고 **손으로 다시 적었다.**
이름이 어긋나면 규칙은 있는데 아무것도 안 입는다. `.r-row` 로 한 번 겪고, 검사를
만들자마자 `.typed` 로 한 번 더 나왔다.

**목업 대조로는 원리적으로 못 잡는다.** 대조는 캡처가 담은 상태만 견준다.
놓친 단어가 있는 결과도, 직접 입력 모드도 캡처에 없다.

```
cd app && pnpm build && pnpm check:css
```

**두 방향을 본다.**

| | 보는 것 | 기준 |
|---|---|---|
| ① | CSS 에 있는데 코드가 안 쓴다 | `src/styles/*.css` ↔ 코드(주석 뺀) |
| ② | 코드가 쓰는데 **어떤 CSS 에도 없다** | `dist/static/css` 의 **실려 나간** 클래스 + 컴포넌트 안 `<style>` |

②가 빌드 산출을 기준으로 하는 이유는 **Tailwind 가 만들어 내는 것까지 포함해
브라우저에 실제로 실려 나간 전부**가 거기 있기 때문이다. 거기 없으면 그 이름은
아무것도 안 입는다. 그래서 `pnpm build` 뒤에 돌려야 한다.

**목업 표식은 자동으로 봐 준다.** 목업·캡처가 달고 있는 클래스는 규칙이 없어도
있어야 한다 — 대조가 클래스 이름을 견주기 때문이다(`ps-blank-value`·`ux-exit` 등).

**되돌려서 확인했다.** `typerow` 를 `typed` 로 되돌리니 **양쪽에서 같이** 잡혔다 —
①에 「`.typerow` 를 아무도 안 쓴다」, ②에 「`.typed` 규칙이 어디에도 없다」.

#### 검사가 바로 찾아낸 것

**① 직접 입력 칸이 아무 스타일도 없었다.** `className="typed"` 인데 그 이름의
규칙은 어디에도 없다. 정본은 `typerow` 다. 고치고 나니 **버튼이 줄을 다 먹는**
문제가 또 드러났다 — 제출 버튼에 `g-go`(4지선다 쪽 전폭 버튼, `width:100%`)를
달아 놔서 입력 칸이 34px 로 찌그러지고 줄이 프레임 밖으로 넘쳤다. 정본은 그
버튼에 클래스를 안 준다(`.typerow button` 이 맡는다). 지금은 입력 269 · 버튼 66 이다.

**② 카운트다운 덮개가 새까맸다.** `bg-black bg-opacity-75` 인데 **Tailwind v4 에는
`bg-opacity-*` 가 없다.** 규칙이 안 실려 75% 가 아니라 불투명 검정이었다.
v4 문법 `bg-black/75` 로 고쳤다.

#### 아직 안 그린 것 (표에 TODO 로 적어 뒀다)

다섯을 적어 뒀었는데 **다섯 다 정리됐다.** `.r-new` · `.destroying` 은 만들었고,
`.log` 는 기능이 아니라 부스러기였고, `.items-top` · `.font-inter` 는 지웠다
(각각 아래 항목).

`.r-new` 는 **만들었다** — 바로 아래 항목.

**이 검사가 못 보는 것** — **이름이 맞는지만 본다. 모양이 맞는지는 안 본다.**
그리고 코드가 클래스를 조립하면(`is-${shotResult}`) ①이 못 본다 — 그런 자리는
표에 이유와 함께 적는다.

### VocaShot 결과 화면이 깨져 있었다 — 프레임 높이·놓친 단어 (2026-08-25)

둘 다 **목업 대조가 원리적으로 못 보는 자리**였다.

**① 프레임이 화면을 안 채웠다.** `.vocashot-frame` 에 `height` 가 없었다.
안쪽은 `.r-body { flex:1 1 auto; overflow-y:auto }` · `.sky { flex:1 1 auto }` 처럼
**부모가 높이를 준다는 전제**로 짜여 있어서, 프레임이 내용 높이로 줄면 채울 것이
없어진다. 결과 화면이 568px 로 끝나고 아래가 하얗게 남았고 긴 목록도 안 굴렀다.
`game.css` 의 `.game-frame` 은 `width:100%; height:100%` 를 받고 있었는데
`vocashot.css` 에만 없었다. 같게 맞췄다.

**못 본 이유** — 프레임은 대조가 **벗기는 껍데기**다. 목업 캡처가 프레임 안쪽만
담았으므로 프레임 자신의 크기는 대조 대상이 아니다.

**② 놓친 단어가 붙어 나왔다** — `녹차green tea다시 맞힘`. 컴포넌트가
`r-missed` · `r-miss` · `<b>` · 이름 없는 `<span>` 으로 그렸는데 **그 이름에는
규칙이 하나도 없다.** 쓸 CSS 는 이미 다 이관돼 있었다 —
`.r-wrong` · `.r-row` · `.r-row .w` · `.r-row .m` · `.r-row .re` · `.r-row.got`.
정본(`phase1/screens_SOT.html`)의 마크업으로 맞췄다. 머리 글자도 정본대로
"놓친 단어 N개 · 다시 맞힘 M" 이다.

**못 본 이유** — **목업 캡처가 "놓친 단어가 없습니다" 상태만 담았다.**
대조는 `r-empty` 만 견주므로 목록 쪽 마크업은 한 번도 견줘진 적이 없다.

> 앞서 `59bcbd9` 에서 "이관한 CSS 에 규칙이 다 있는데 컴포넌트가 안 그리고
> 있었다" 를 잡았는데, **`.r-row` 한 벌이 더 남아 있었다.** 그때는 목업에 보이는
> 것만 봤기 때문이다. **CSS 에 있는데 코드가 안 쓰는 이름**을 세어 보는 축이 없다.

### 게임 목록도 초점을 받는다 (2026-08-25)

게임에서 나오면 목록으로 돌아오는데 초점이 `<body>` 에 떨어져 있었다. 목록은
화면이 하나라 옮길 일이 없어 보이지만 **돌아오는 자리**라서 필요하다.
`routes/main/game/index.tsx` 가 `useScreenFocus("list")` 로 ref 를 만들고
`list-view.tsx` 의 `.game-frame` 이 받는다. 이름은 `t("game.list.title")` 이라
UI 언어를 따른다.

브라우저에서 왕복을 봤다 — 목록(초점 `Games`) → 봄소풍 진입(`봄소풍 숫자미션`) →
나가기 → 목록(`Games`).

### 게임 다섯을 초점 훅 하나로 모았다 (2026-08-25)

`use-screen-focus.ts` 를 만들어 놓고 **아무도 안 쓰고 있었다.** 게임 넷은 각자
`useRef` + `useEffect` 를 인라인으로 짰고, VocaShot 만 훅을 썼다. 그래서 **같은
일을 두 가지로 하고 있었고 동작도 갈렸다.**

**갈린 자리 — 첫 마운트.** 훅은 "첫 마운트에는 옮기지 않는다" 고 적혀 있었고,
`particle-sniper` 는 반대로 "첫 마운트에도 옮긴다" 는 이유를 달아 두었다.
**particle-sniper 쪽이 맞다.** 이 다섯은 전부 라우트 컴포넌트라
(`routes/main/game/*`) 마운트된다는 것이 곧 **사람이 그 주소로 이동했다**는 뜻이고,
그건 보통 웹에서 페이지가 새로 로드되며 초점이 옮겨지는 바로 그 자리다. 게임
목록에서 게임을 누르면 누른 버튼이 사라지며 초점이 `<body>` 로 떨어지는데, 옮기지
않으면 아무것도 안 읽히고 Tab 이 문서 맨 처음으로 간다.

그래서 훅을 그쪽으로 맞추고 다섯을 다 모았다. `ready` 인자를 더했다 — 콘텐츠를
받는 동안은 참는다(로딩 칸에 줬다가 진짜 화면으로 갈아 끼워지면 도로 잃는다).
게임 넷은 `!contentLoading` 을 넘긴다.

**VocaShot 은 이 결정으로 동작이 바뀌었다.** 어제는 첫 진입에 초점을 안 옮겼고
그렇게 확인해 적었는데, 지금은 옮긴다. 다시 확인했다.

**브라우저에서 다섯 다 봤다.** 첫 진입과 화면 전환 전부 프레임으로 옮겨 가고
이름도 맞다.

| 게임 | 첫 진입 | 전환 |
|---|---|---|
| 어휘 카드 마스터 | `cs_level` ✓ | → `cs_intro` ✓ → `cs_play` ✓ |
| 조사 스나이퍼 | `ps_level` ✓ | → `ps_lesson` ✓ → `ps_play` ✓ |
| 봄소풍 숫자미션 | `pc_title` ✓ | → `pc_select` ✓ → `pc_game` ✓ |
| 서울 여행 퍼즐 | `sp_map` ✓ | → `sp_entry` ✓ → `sp_puzzle` ✓ |
| VocaShot | `vs_start` ✓ | → `vs_play` ✓ → `vs_result` ✓ |

**중간에 걸린 것 — 목 API 가 지운 자리를 보고 있었다.** 8799 를 잡고 있던 것은
`/tmp/mockapi.py`(예전에 `SEED` 를 `app/…/game/data` 로 바꾼 사본)였고, 그 자리는
방금 지웠다. 어휘 카드 마스터가 "해당 과까지 카테고리 없음" 으로 START 가 비활성인
채였다. **초점 문제로 보였지만 데이터 경로 문제였다.** 정본
`phase1/game_mockapi.py` 로 바꿔 띄우니 다섯 갈래가 다 내려왔다.

~~아직 남은 것 — 게임 목록(`list`) 화면은 초점을 안 받는다.~~
**고쳤다(같은 날, 위 「게임 목록도 초점을 받는다」).** 게임에서 나오면 초점이
`<body>` 로 떨어지던 것을 `useScreenFocus("list")` 로 잡았다.

### VocaShot 화면 전환 초점 (2026-08-25)

VocaShot 은 화면 셋(`start`·`play`·`result`)이 **각자 `.vocashot-frame` 을 그린다.**
감쌀 자리가 하나가 아니라서 게임 넷과 달리 미뤄 뒀던 것이다. 프레임에
`frameRef` 를 받는 자리를 열고, 판을 굴리는 `vocashot-solo.tsx` 가
`useScreenFocus(view)` 로 만든 ref 를 셋에 다 넘긴다.

이름은 화면별로 다르게 준다 — `VocaShot — 시작` · `— 문제` · `— 결과`.
초점이 옮겨 갔을 때 **어디로 갔는지** 들려야 뜻이 있다.

**브라우저에서 네 자리를 다 확인했다.**

| 때 | 화면 | 초점 |
|---|---|---|
| 첫 진입 | `vs_start` | `BODY` — 안 뺏는다 ※ **이날 늦게 뒤집혔다** — 아래 「초점 훅 하나로 모았다」에서 첫 마운트에도 옮기기로 정했고, 지금은 프레임으로 간다 |
| 시작 → 문제 | `vs_play` | `.vocashot-frame` `VocaShot — 문제` |
| 문제 → 결과 | `vs_result` | `.vocashot-frame` `VocaShot — 결과` |
| 결과 → 시작 | `vs_start` | `.vocashot-frame` `VocaShot — 시작` |

**중간에 잡힌 것.** 편집 스크립트가 중간에 멈춰 `frameRef` 가 **셋 중 하나에만**
넘어가 있었다. 타입도 대조도 통과했다 — 넘기지 않으면 `undefined` 라 타입이 맞고,
프레임은 대조가 벗기는 껍데기라 마크업도 같다. **브라우저에서 결과 화면 초점이
`BODY` 인 것을 보고서야 알았다.**

**대조 정규식 주의 — `class` 가 첫 속성이어야 한다.** 껍데기를 벗기는 정규식이
`^<div class=` 로 시작한다. `tabIndex` 를 `className` 앞에 두었더니 껍데기가 안
벗겨져 VocaShot 세 화면이 통째로 갈렸다. `activity-parity.tsx` 에 적어 뒀다.

~~아직 안 한 것 — 게임 넷은 훅을 안 쓰고 첫 마운트에도 초점을 뺏는다.~~
**고쳤다(같은 날, 위 「게임 다섯을 초점 훅 하나로 모았다」).** 넷을 훅으로 모으면서
**첫 마운트 정책을 뒤집었다** — 라우트 컴포넌트라 마운트가 곧 이동이므로 옮기는
쪽이 맞다. 다섯 다 브라우저로 확인했다.

### 씨드는 정본이 아니다 — 서버와 갈려 있었다 (2026-08-25)

**정본은 어드민이다.** 게임 콘텐츠는 사람이 어드민에서 고치고, 앱은 그 내용을
API 에서 받는다. `api/seed_data/` 는 **그때 떠 온 스냅숏**일 뿐이다.

여덟 갈래를 서버와 견줘 보니 **넷이 갈려 있었다.** 사람이 고친 자리가 씨드에는
없었다.

| 갈래 | 갈린 내용 |
|---|---|
| `spring-picnic/questions` | 서버 70 · 씨드 63 — `tel05`~`tel11` 이 씨드에 없다. 그리고 7건이 내용이 다르다: `age05.correct` **마흔셋 → 마흔세** · `cnt02.tmpl` **강아지 ___ → 강아지가 ___** |
| `card-sort/vocab` | 2·3·4·5급 **전부** 여러 과가 다르다 |
| `card-sort/rare` | 서버는 `examples` 만 준다(씨드의 `설명` 은 서버가 안 내려줘서 지켰다) |
| `seoul-puzzle` | `locations` 는 같고 `puzzles` 가 다르다 |

전부 서버 것으로 맞췄고, 다시 견줘 여덟 갈래 모두 같음을 확인했다.

**그래서 검사를 만들었다.**

```
cd app && python3 scripts/fixture-data-check.py --live
```

서버의 게임 콘텐츠 여덟 갈래를 받아 씨드와 견주고 다르면 실패한다. 네트워크가
필요해서 평소 게이트에는 안 붙였다 — **씨드를 손대거나 어드민에서 콘텐츠를 고친
뒤에 돌려라.** 씨드 하나를 고치기 전 값으로 되돌려 실제로 잡히는지 확인했다.

> **어제 이 `--live` 를 문서에만 적고 만들지 않았다.** 주석에 "「--live」 로 맞춰
> 볼 수 있다" 고 써서 커밋했는데 그런 코드가 없었다. 문서에 적는 순간 그것은 약속이다.

**같이 없앤 것 — 서울 퍼즐 픽스처의 손글씨.** `SP_LOCATIONS` 는 열 곳 중 **여덟이
`x:0, y:0`** 이었고 경복궁 id 가 `gyeongbokgung`(씨드는 `gyeongbok`)이었다. 핀
자리는 이름 표(`PIN_FULL`)가 덮어써서 **목업 대조에는 안 보였다.** 이제 픽스처가
`seoul_puzzles.json` 을 직접 읽고, 퍼즐도 진짜 `resolveToken()` 으로 푼다 — 값을
지어낼 자리가 없다. 그래서 서울 퍼즐은 픽스처 검사 표에서 뺐다(대조할 손글씨가
없다). 대조는 그대로 "모두 같다" 다.

### 픽스처가 실제 데이터와 같은 규약인지 보는 검사 (2026-08-25)

**메우려는 구멍** — 목업 대조는 픽스처를 그려서 목업 캡처와 견준다. 그런데
**픽스처 값은 사람이 목업을 보고 손으로 적은 것**이다. 목업과 실제 데이터의 규약이
다르면 픽스처는 목업 쪽에 맞고, **대조는 계속 통과하는데 실제 화면만 깨진다.**
조사 스나이퍼가 정확히 그랬다(바로 아래 항목).

**하는 일** — `activity-parity.tsx` 에서 데이터를 흉내 낸 값을 `fromData(키, 값)`
으로 등록하면 `.parity-out/_fixtures.json` 으로 나간다.
`app/scripts/fixture-data-check.py` 가 `api/seed_data/` 의 **진짜 레코드에서
불변식을 캐서** 픽스처에 들이민다. 규칙을 손으로 적지 않고 캐는 이유는, 손으로
적으면 그것도 사람이 지어낸 값이라 같은 함정이기 때문이다.

| 축 | 잡는 것 |
|---|---|
| 빠진 필드 | 진짜 레코드 전부에 있는 필드가 픽스처에 없다 ← **조사 스나이퍼 `blank`** |
| 없는 필드 | 픽스처에만 있고 진짜엔 없다 |
| 타입 | 같은 필드인데 타입이 다르다 |
| 표식 | 진짜 값 전부에 든 토막(`[?]` 등)이 픽스처엔 없다 ← **`___` 를 썼다면 여기서** |
| 어휘 | 값이 닫힌 집합인데 픽스처가 없는 값을 쓴다 ← **`cat: "age"`** |
| 관계 | 진짜 레코드에서 늘 성립하는 필드 간 포함관계가 깨졌다 |

`pnpm parity:activity` 뒤에 붙어 같이 돈다.

**되돌려서 확인했다.** 픽스처를 버그 나던 그때 모양으로 되돌리니 둘 다 잡혔다 —
`blank` 를 지우면 「빠진 필드」, `blank` 에 `___` 를 적으면 「표식」.

**바로 잡은 것 — 봄소풍 픽스처가 지어낸 값이었다.** `friend.id="sol"` ·
`cats=["age","price"]` · `desc2=""` · `round.cat="age"` · `id="q-age-19"`. 진짜 씨드는
`somi` · `["나이","가격"]` · `"나이"` · `age02` 다. 목업 어디에도 `age`·`price` 는
없다 — **내가 지어낸 값**이고, 화면에 안 그려지는 필드라 대조가 못 봤다. 씨드
레코드(somi·age02) 그대로로 바꿨고 대조는 그대로 "모두 같다" 다.

**면제 — 목업이 낡은 자리.** 조사 스나이퍼 급 선택 목업은 7·8급 설명을 자리표로
담았다(`고급 조사 연습`·`심화 조사 연습`). 진짜는 `마다· 대로· 에 따라` ·
`조차· 로서· 으로서· 에 의하면` 다. 픽스처는 대조를 위해 목업을 따라야 하므로
그대로 두고 **갈렸다는 사실을 검사기 표에 적었다.** 목업을 다시 뽑을 때 같이 고칠 것.

**이 검사가 못 보는 것** — 값이 그럴듯한지는 안 본다. 통과는 "픽스처가 옳다" 가
아니라 **"모양이 데이터와 어긋나지는 않는다"** 뿐이다. 그리고 씨드가 서버와
어긋나면 어긋난 것을 기준으로 삼는다.

**같이 고친 것 — 목 API 가 추적 안 되는 낡은 씨드를 봤다.**
`phase1/game_mockapi.py` 의 `SEED` 가 `koreanapi-master/…/seed_data` 를
가리켰는데 그 자리는 **git 이 추적하지 않고 6급에 멈춰 있다.** 그래서 이 목 API 를
띄운 사람만 6급까지 보였다. 추적되는 `api/seed_data` 를 읽도록 바꿨다.

### 조사 스나이퍼 — 빈칸이 안 뚫렸고 씨드가 낡아 있었다 (2026-08-24)

**① 문제 화면에 정답이 그대로 보였다.** 화면이 `question.sentence` 를 `"___"` 로
잘랐는데, 데이터에 `___` 를 쓰는 문항이 **하나도 없다**(79문항 전부 `blank` 의
`[?]` 다). `sentence` 는 **정답이 든 완성 문장**이라 자를 것이 없어 문장이 통째로
나오고 빈칸이 뒤에 붙었다 — `누가 왔어요 ?` 인데 선택지가 `이/가/를` 이었다.
화면이 `blank` 를 쓰도록 고쳤다.

이 코드는 **2026-08-19 `9bf6bb2` 에서 목업에 맞춰** 들어온 것이다. 목업 캡처의
문장이 `저는 한국어___공부해요` 라 코드가 그것을 따랐고, **목업과 실제 데이터의
규약이 처음부터 달랐다.**

> **왜 대조가 못 잡았나 — 내가 픽스처를 손으로 지어냈기 때문이다.**
> `activity-parity` 의 `game__ps_play` 는 내가 넣은 값으로 그린다. 목업을 보고
> `___` 가 든 문장을 적었으니 당연히 맞았다. **실제 데이터로는 한 번도 안 돌렸다.**
> 나중에 게임을 직접 돌렸을 때 "누가 왔어요 ?" 를 보고도 이상하다고 여기지 않았다.
> 픽스처가 실제 데이터와 같은 규약인지 보는 축이 없었다. **지금은 있다** —
> 바로 위 항목의 `fixture-data-check.py` 다.

**② 씨드 파일이 6급까지였다.** `games_spec_v1` 이 이미 적어 둔 것이다 —
"씨드 파일은 낡았다. 정본은 어드민이다." 실서버
(`koreanapi-live.pulleyai.co.kr`)에서 받아 갱신했다.

| | 급 | 과 | 문항 |
|---|---|---|---|
| 전 (씨드) | 6 | 24 | 79 |
| 후 (서버) | **8** | **32** | **173** |

`api/seed_data/` 를 갱신했다. **`app/src/components/main/game/data/` 도 같이 갱신했는데
그건 헛수고였다** — 아무도 안 읽는 사본이었고 2026-08-25 에 지웠다.

**주의 — 앱은 씨드 파일을 읽지 않는다.** `getParticleSniperSentences()` 로 서버에서
받는다. 6급까지만 보였던 것은 **로컬 확인용 목 API**(`phase1/game_mockapi.py`)가
씨드를 읽기 때문이었다. 씨드는 그 목 API 와 서버 초기화용이다.

### 남은 43건 — 손대기 전에 의도를 먼저 정한다

이번에 배운 것 하나: **"진단 수를 0으로 만든다" 를 목표로 잡으면 판단이 숫자에
끌려간다.** 남은 것은 규칙마다 우리가 원하는 끝 상태를 먼저 적어 두고 시작한다.

| 규칙 | 수 | 어디가 많나 | 원하는 끝 상태 (제안) |
|---|---|---|---|
| `noArrayIndexKey` | 16 | `seoul-puzzle-view` 5 · `content4` 2 | **대부분 재우기.** 칸·획처럼 위치가 곧 정체성인 목록이 많다. 다만 순서가 바뀔 수 있는 목록이 섞여 있으면 그것만 진짜 열쇠로 |
| `noExplicitAny` | 10 | `api/api.ts` 4 · `confetti-provider` 4 | **타입 붙이기.** `api.ts` 의 넷은 `post<T>` 의 `data?: any` 라 제네릭으로 좁힐 수 있다. confetti 는 외부 라이브러리 타입이라 재울 수도 |
| `noForEach` | 6 | `spring-picnic` 2 · `mic-permission-provider` | **바꾸기.** `for…of` 로 옮기면 끝난다. 동작 동일 |
| `noNonNullAssertion` | 5 | `HangulTracingCanvas` 2 · `card-sort` | **자리마다.** `!` 가 실제로 보장되는지 봐야 한다 — 보장이 없으면 그게 버그다 |
| `noDangerouslySetInnerHtml` | 4 | `seoul-puzzle-view` 4 | **재우기.** 목업에서 캡처한 정적 마크업 주입이다(`mockup-parity.stories` 에 같은 이유의 주석이 이미 있다). 다만 **주입하는 값이 정말 정적인지** 한 번 확인 |
| `useJsxKeyInIterable` | 1 | `activity/shell.tsx` | 진짜 열쇠 넣기 |
| `noShadowRestrictedNames` | 1 | `list-view.tsx` | lucide 의 `Map` 아이콘이 전역 `Map` 을 가린다 — `Map as MapIcon` 으로 |

**이 표를 먼저 합의하고 시작하는 것이 이번과 다른 점이다.** 지난번에는 규칙을
만나서야 "이건 재울까 고칠까" 를 정했고, 그때 판단이 몇 번 헐거웠다.



i18n 은 5개 로케일 **300키**가 일치한다(en·ja·ko·vi·zh 전부 300).
