# ── macOS ──
.DS_Store

# ── 회사 제품 소스 사본 ──────────────────────────
# 각자 원본 저장소가 있다. 여기 복제하면 이력이 두 벌이 된다.
korean-master/
korean-admin-master/
koreanapi-master/
writeapi-master/
*.zip

# ── 교재 파생 자산 ───────────────────────────────
# 연세 교재에서 추출·가공한 것. 라이선스 범위 밖으로 나가면 안 된다.
illust/
book/
*.xlsx

# ── 참고 자산 (용량) ─────────────────────────────
# 획순 안내 PNG 187개 · 5MB. 코드가 참조하지 않는 디자인 참고물이다.
handwriting/

# ── 파이썬 ──
__pycache__/
*.pyc
