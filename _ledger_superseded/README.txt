콘텐츠 원장(글로벌_교재기반_콘텐츠_v*.xlsx)의 옛 판을 모은다.
2026-08-25 에 저장소 루트에서 옮겼다 — 루트 66개 항목 중 40개가 xlsx 였다.

정본은 여기 있는 것이 아니다. 정본은 저장소 루트의 가장 높은 번호이고,
app/scripts/build-content.py 가 max() 로 고른다. 이 폴더는 루트가 아니라
하위 폴더이므로 그 glob 에 걸리지 않는다 — 생성기가 여기를 보지 않는다.

*** 지우지 마라. 다시 만들 수 없다. ***

  · 빌드 사슬이 끊긴다 — 26→27 · 28→29 · 30→31 을 만든 build_vN.py 가 없다
  · 최신 스크립트(stage_images.py · strip_*.py)는 "가장 높은 판 + 1" 로 쓴다.
    버전이 코드에 없으니 어느 것을 어떤 순서로 돌렸는지 기록이 남지 않는다
  · 즉 v2 와 스크립트 전부가 있어도 중간 판을 되짚을 수 없다

실제로 필요했던 적이 있다 — 2026-08-24 에 build_v24.py 가 자모 400행을 날렸고,
v23 이 남아 있어서 복구했다(BLOCKERS.md §2-b).

xlsx 는 교재 파생이라 .gitignore(*.xlsx)가 막는다. 이 README 만 추적된다.
