# AI 미션 대화 — 개발 요청

**원본은 기획자가 쓴 `미션대화_개발요청_v53.md`(2026-09-01)다.** 저장소 밖에 있고
`docs/INDEX.md` 에도 없어서 **아무도 못 찾았다** — 2026-09-03 에 「미션대화 발화
피드백 프롬프트가 필요하다」는 물음이 다시 나왔을 때, 이 문서가 그 답을 이미
갖고 있었는데 찾지 못해 「없다」고 답한 일이 있었다. 그래서 정본 자리로 들였다.

**원본 파일은 지우지 않았다.** `미션대화_개발요청_v53.md` 는 기획자의 개인 작업
파일이고(권한 `600`, git 미추적) 남의 파일을 옮기거나 지우는 것은 내 몫이 아니다.
**정본은 이 문서다** — 원본을 다시 고쳐 쓰지 마라. 고칠 것이 있으면 여기를 고친다.
원본은 기획자가 지워도 된다.

**기획자 원문은 고치지 않았다.** 2026-09-01 이후 사실이 달라진 자리에만
`> **[2026-09-03 갱신]**` 을 달았다. 그렇게 한 이유 — 원문을 고쳐 쓰면 「기획자가
정한 것」과 「그 뒤에 확인된 것」이 섞이고, 이 저장소가 오늘 하루 그것 때문에
세 번 헛돌았다(`BLOCKERS.md` §5-c).

## 지금 살아 있는 것 · 닫힌 것 (2026-09-03)

| 절 | 상태 |
|---|---|
| §1 `ko_chat_dialog` UPSERT | **닫혔다 — 다른 방법으로.** `DEV-13` 이 그 표를 버리고 `repo_chat.getDialog` 가 `ko_mission_chat` 을 `legacy_id` 로 읽게 했다(`6c5ec6a`) |
| §2 힌트/완료 분리 | **살아 있다.** `hint_opened_at_briefing` 기록이 아직 없다 |
| §3 미션 상태의 주인 | **살아 있다. 가장 크다** — 대화 AI(`ai_persona_prompt`)와 판정 AI(`missionPrompt`)가 서로를 모른다 |
| §4 STT 오인식 | **살아 있다.** 다만 **발음 평가 API 는 이미 서버에 있다**(아래 갱신) |
| §5 실패 모드 규칙 | **살아 있다. 기획자만 정할 수 있다** |
| §6 observability | **살아 있다.** `PROMPT_VERSION` 부터 넣는다 |
| §7 발화 길이 | **살아 있다. 기획자만 정할 수 있다** |

---

## 원문 — 이 아래는 기획자가 2026-09-01 에 쓴 것이다

작성: 2026-09-01 · 대상: 미션 대화 백엔드/앱 담당
콘텐츠(시트)로는 못 하고 **코드가 있어야 하는 것**만 모았다. 우선순위 순.

---

## 0. 지금 상태 한 줄

`글로벌_교재기반_콘텐츠_v53.xlsx` 의 `n7_mission_chat`(117행)과 `n7_mission_hint`(354행)이 정본이다.
**그런데 이 시트는 아직 대화 화면에 닿지 않는다.** 브리핑 화면만 원장을 쓰고, 실제 대화·완료 판정은 백엔드 `ko_chat_dialog` 테이블(검수 전 내용)을 본다 — `BLOCKERS.md` §8.

> **[2026-09-03 갱신] 이 줄은 더 이상 참이 아니다.** `DEV-13`(2026-09-01, `6c5ec6a`)이
> `repo_chat.getDialog` 를 고쳐 **대화·판정도 원장(`ko_mission_chat`)을 읽는다.**
> `ko_chat_dialog` 는 0행 사문이 됐다. 원장도 **v53 → v57** 로 갔다 —
> `n7_mission_hint` 354행은 `reviewed 340 · fixed_v56 14` 이고,
> `n7_mission_chat` 117행은 **여전히 `reviewed` 가 0개**다(`fixed_v29` 103 ·
> `fixed_v56` 14). 즉 **`ai_persona_prompt` 는 아직 사람 검수를 안 받았다**(`BLOCKERS.md` §8).

---

## ~~1. `ko_chat_dialog` UPSERT — 출시 blocker~~ **닫혔다**

> **[2026-09-03 갱신] 이 절은 해소됐고, 방법이 여기 적힌 것과 다르다.**
> UPSERT 를 하지 않았다 — `DEV-13` 이 **읽는 곳을 옮겼다**(`ko_mission_chat` 을
> `legacy_id` 로 조회). 그 표를 버린 이유가 이 절의 마지막 항목이 걱정한 그것이다:
> `ko_chat_dialog.id` 가 `int AUTO_INCREMENT` 라 MySQL 이 `WHERE id = 'C4'` 를
> **`id = 0`** 으로 견줬다(`'C10'` 도 같은 행). 아래 원문은 기록으로 남긴다.

원장 v53 의 `ai_persona_prompt` · `ai_first_line` · `mission_detail` · `situation_ko` 를 백엔드 테이블에 반영한다.

- **v53에서 `ai_first_line` 이 4행 바뀌었다**: `MC-5-10` · `MC-7-11` · `MC-8-09` · `MC-8-15`. 첫 대사가 미션의 답을 미리 말하고 있어서 고친 것이라, 이 4행은 반드시 같이 넘어가야 한다.
- `mission_detail` 의 **라벨**은 완료 판정이 문자열 그대로 대조하는 값이다. 6~8급은 v51에서 영어 지시문이 한국어 환기 질문으로 바뀌었지만 **라벨은 그대로**다. 다만 **`MC-8-01` 은 슬롯이 4개→3개로 줄었다**(「기준」 삭제) — 그 과의 판정 배열 길이가 달라진다.
- 확인 필요: `id`(Integer) ↔ 프론트가 넘기는 `dialogId`(문자열, 예 `"C4"`) 매칭. `accepter/chat_accepter.py` 와 `persistence/repo_chat.py` 의 타입이 str/int 로 갈려 있어 코드만 봐서는 실제로 맞물리는지 확신할 수 없었다.
- 어느 환경(스테이징/프로덕션)에 언제 넣을지, 백업 절차부터 정할 것.

---

## 2. 힌트 사용과 미션 완료를 분리한다 — **지표 blocker**

v52에서 **힌트 버튼**이 생겼다. 미션마다 모범 문장 하나와 그 번역을 보여 준다(`n7_mission_hint`).

문제는 이렇다. 힌트는 **정답에 가까운 문장**이다. 학습자가 버튼을 누르고 그대로 읽어도 판정 AI는 완료로 볼 가능성이 높다. 그러면 아래 셋이 전부 `mission_completed=true` 로 합쳐진다.

- A. 스스로 만들어 낸 발화
- B. 힌트를 본 뒤 자기 말로 바꾼 발화
- C. 힌트를 그대로 읽은 발화

그런데 이 기능의 성공 정의 2번은 **「학습자가 스스로 말한다」**다. 지금 구조로는 **힌트 도입 뒤 달성률이 올라가도 콘텐츠가 좋아진 것인지 정답 노출이 늘어난 것인지 구별할 수 없다.**

### 필요한 필드
```
hint_used              : bool      # 그 미션에서 힌트를 열었는가
hint_opened_at_turn    : int       # 몇 번째 턴에 열었는가
mission_completed      : bool
completion_after_hint  : bool      # 힌트를 연 뒤에 완료됐는가
mission_evidence       : string    # 완료로 본 실제 사용자 발화
```

### 지표
`independent_completion_rate` 와 `assisted_completion_rate` 를 나눠 본다.
힌트를 그대로 읽었는지(C)까지 보고 싶으면 `mission_evidence` 와 `hint_ko` 의 문자열 유사도를 사후에 재면 된다 — 별도 필드는 필요 없다.

---

## 3. 미션 상태의 주인을 하나로 정한다 — **대화 품질 blocker**

지금은 **대화 AI가 어떤 미션이 남았는지 스스로 추론**하고, 판정 AI가 따로 `completed_missions` 를 계산한다. 두 시스템이 각자 상태를 갖고 있으니 어느 한쪽이 틀리는 순간 **AI가 이미 끝난 미션을 다시 묻는다.**

> **[2026-09-04 갱신] 「각자 상태를 갖는다」는 이제 절반만 맞다 — 주입이 이미 된다.**
> `business/chat.py` 의 대화 AI 호출이 판정 결과를 시스템 메시지로 넣는다:
>
>     chat_all = create_chat_history(…, chat.completed_missions, True)
>     → {"role":"system", "text":"현재 완료된 미션 : [\"인사\"]"}
>
> **그래도 이 절이 살아 있는 이유 넷** —
>
> ① **완료된 것만 주고 남은 것은 안 준다.** 대화 AI 가 `mission_detail` 셋에서
>    완료된 것을 빼야 한다. 아래 제안이 `remaining_missions` 를 주자는 것은
>    **그 뺄셈을 없애자**는 뜻이다 — 뺄셈은 AI 가 틀릴 수 있는 자리다.
> ② **판정이 실패하면 상태가 안 늘어난다.** 그러면 대화 AI 는 낡은 목록을 보고
>    이미 한 미션을 다시 묻는다. **이 절이 말하는 증상의 직접 원인일 수 있다.**
>    2026-09-03 에 판정 실패를 화면에서 눈에 보이게 만들었으니(`DEV-16`) 그 진단이
>    전보다 쉽다.
> ③ `ko_chat.completed_missions` 가 **`String(200)`** 이다. 한글 라벨 셋이면
>    여유롭지만 상한이다.
> ④ **`target_grammar_used` 가 없다** — 아래 「같이 정할 것」이 요구한 관찰값이다.
>    2026-09-04 에 **목표 문법을 판정 프롬프트에 넣고**(「완료 조건이 아니다」를
>    같이 못 박았다) 출력 스키마에 `target_grammar_used`·`target_grammar_evidence`
>    두 칸을 더했다. **원장 값이 판정까지 닿는 것을 실제로 확인했다.**
>    남은 것은 그 값을 **리포트에서 쓰는 것**이다.

프롬프트에 「이미 나온 미션은 다시 묻지 마세요」라고 적어 두는 것보다, **상태를 주입하는 편이 훨씬 강하다.**

### 제안
판정 AI가 매 사용자 발화 뒤에 반환:
```
completed_missions        : [label]
newly_completed_missions  : [label]
remaining_missions        : [label]
evidence_by_mission       : {label: 사용자 발화}
```
다음 대화 AI 호출에 **`remaining_missions` 를 시스템 상태로 직접 넣는다.**

### 같이 정할 것 — 문법은 완료 조건이 아니다
v52부터 방침이 바뀌었다. **목표 문법은 「쓸 수 있게 유도할 것」이지 「반드시 쓰게 만들 것」이 아니다.** 프롬프트에는 「사용자가 뜻이 통하게 말했으면 그 미션은 채워진 것이고 목표 문법을 썼는지는 보지 마라」를 넣었지만, **판정 쪽이 따라오지 않으면 실효가 없다.**

```
mission_completed        : bool     # 뜻이 통했는가 — 진행의 기준
target_grammar_used      : bool     # 별도 관찰값
target_grammar_evidence  : string
```

> **[2026-09-04 갱신] 아래 둘은 들어갔다.** `missionPrompt` 에 `Target Grammar` 슬롯과
> 「이 값으로 status 를 정하지 마세요」를 넣고, 출력 스키마에 `target_grammar_used` ·
> `target_grammar_evidence` 를 더했다(`_normalizeCheckMission` 이 빠진 키를 메운다).
> **`mission_completed` 는 따로 만들지 않았다** — `completed_missions` 배열이 이미
> 그 일을 하고, 프롬프트가 「문법이 틀려도 뜻이 통하면 달성」이라고 이미 말한다.
> **원장의 `target_grammar` 가 판정에 안 닿고 있던 것이 진짜 문제였다** —
> `repo_chat._Dialog` 가 다섯 칸만 노출했다. 이제 닿는다.
대화 진행은 `mission_completed` 로만 판단하고, 문법은 사후 피드백에서 따로 다룬다.
「대화는 잘했는데 목표 문법은 안 썼다」가 그 자체로 유용한 학습 데이터다.

---

## 4. STT — 오인식 처리

음성 입력이다. 지금 프롬프트에는 「음성 환경이니 짧게 말하라」 한 줄뿐이고 **입력 쪽 규칙이 없다.**

### 알려진 오류
- 「뭐라고요?」에 **인사 미션이 체크**됨 → 판정이 단어 유사도가 아니라 **대화 행위**를 봐야 한다. 「뭐라고요?」는 되묻기(clarification request)이지 인사가 아니다.
- 「김풀리예요」를 **통째로 이름으로 인식** → 완료 여부와 슬롯 값 추출을 분리한다.
  ```
  mission_completed = true
  raw_evidence      = "김풀리예요"
  normalized_value  = "김풀리"
  ```

> **[2026-09-03 갱신] 발음 평가 API 가 이미 서버에 있다.**
> `POST /tutorus/pronunciation`(`api/accepter/tutorus_accepter.py:31`)이
> `score.overall`(30~100) · `weakWords[]` · `weakPhones[]` 를 낸다. `api/.env` 에
> `TUTORUS_*` 6줄이 이미 있고, **기준 문장이 없는 자유 발화를 채점하는 선례까지**
> 있다(`evaluateShadow`, `api/business/tutorus_pron.py:129`). 없는 것은 **앱 배선**뿐이다.
> 그리고 **학습자가 STT 결과를 키보드로 고칠 수 있다**
> (`app/src/components/dialog/dialog-input.tsx:106-109`) — 그래서 전송되는 텍스트는
> 말한 것이 아닐 수 있고, 발음 채점의 기준 문장을 무엇으로 잡을지가 판단거리가 된다.
> **다만 Tutorus 는 dev 엔드포인트·공용 자격증명이고 서버에 ffmpeg 가 필요하다** —
> 운영 승격이 발음 축의 진짜 blocker다.

### 필요한 것
- STT 의 `transcript` · `confidence` · `alternatives` 를 판정 AI에 전달. **confidence 가 낮은 텍스트를 LLM이 그럴듯하게 보정해서 곧바로 완료 처리하는 것을 막아야 한다.**
- 불명확 입력 처리 규칙(프롬프트가 아니라 앱/백엔드 단):
  1회차 → 고정 재요청 「다시 한번 말해 주세요.」 · 2회차 → 더 짧은 질문이나 선택지 · 3회차 → 같은 미션을 계속 밀지 말고 다음 미션으로 이동하거나 다시 말하기 UI 노출.
  ※ 「다시 한번 말해 주세요」는 **v53에서 대화 운영 표현 whitelist 에 넣어** 초급에서도 쓸 수 있게 해 두었다.

---

## 5. 실패 모드 규칙

지금 어디에도 정의돼 있지 않다. 프롬프트로 일부는 되지만, 반복 횟수 같은 것은 앱이 세야 한다.

| 상황 | 행동 |
|---|---|
| 「네」만 반복 | 같은 질문 반복 금지 → 더 좁은 질문 |
| 「몰라요」 | 다른 방식으로 1회만 유도 → 그래도 실패하면 다음 미션 |
| 침묵 / `...` | 짧게 한 번 재질문 → 선택지 |
| 영어·모국어 사용 | 한국어로 짧게 다시 질문, 계속되면 UI 힌트 |
| 주제 이탈 | 한 번 받아 준 뒤 남은 미션으로 연결 |
| **AI에게 질문** | **질문에 먼저 답한 뒤** 남은 미션으로 — hard rule 로 둘 것 |
| 공격적 발화 | 중립적으로 경계를 세우고 역할극 복귀 |
| 종료 요청 | 즉시 종료 |

「사용자의 질문에 먼저 답하라」는 특히 중요하다. 질문을 무시하고 미션으로 돌아가면 **성공 기준 1번(계속 말하게 만들기)을 직접 해친다.** 실제로 관찰된 문제다 — 사용자의 「왜요?」에 답하지 않고 미션 질문만 던졌다.

### 진척 없는 턴 기준
「8~12턴」 같은 총량보다 **미션별 진척 없는 연속 턴 수**가 낫다.
> 같은 미션에서 사용자 발화가 두 번 연속 진척되지 않으면 질문을 더 직접적으로 바꾼다. 세 번째에도 진척이 없으면 그 미션을 건너뛰고 다른 미션으로 이동한다. 나머지를 다 본 뒤 한 번만 다시 시도한다.

3슬롯이면 **5~8 사용자 턴**이 자연스러운 목표로 보인다. 20턴은 이미 실패로 분류하는 편이 맞다.

---

## 6. observability — 이후 개선을 측정하려면

이게 없으면 「v54가 v53보다 좋아졌나」를 다시 사람이 감으로 판단하게 된다.

```
item_id
prompt_version            # v53 …
model_version
learner_level
stt_transcript / stt_confidence
ai_utterance
completed_missions        # 매 턴 이후
evidence_turns
hint_used / hint_opened_at_turn
target_grammar_observed
termination_reason        # 자연 종료 / 사용자 이탈 / 미완료
```

---

## 7. 발화 길이 기준 — 데이터가 모이면

프롬프트가 「한 번에 1~3문장, 120자 이내」로 제한하고 있는데 **근거 없는 숫자**다(v50에서 물려받았다). TTS/STT 대화에서는 글자 수보다 **발화 초 수**가 맞는 기준이다. 감으로는 한 발화 3~7초 정도.
**TTS 로그에서 실제 발화 시간을 모은 뒤에 다시 잡자.** 지금 바꾸면 근거 없는 숫자를 다른 근거 없는 숫자로 바꾸는 셈이다.

---

## 처리 순서 제안

1. **`ko_chat_dialog` UPSERT** — 이게 없으면 나머지가 화면에 안 보인다
2. **`remaining_missions` 주입 + `mission_completed`/`target_grammar_used` 분리** — 대화 품질의 뿌리
3. **`hint_used` 계열** — 힌트 기능을 켜는 순간부터 필요하다. 나중에 붙이면 그전 데이터는 못 쓴다
4. STT confidence 전달 · 실패 모드
5. observability
6. 그 상태가 되면 **독립된 학습자/대화 모델 호출로 실제 대화 A/B** 를 돌린다 — 지금까지의 검증은 전부 정적 텍스트 검사였고, 대화를 돌려 본 적이 한 번도 없다
