# i18n 다국어 검수 — 반영 결과 (2026-08-28)

대상: `app/src/i18n/locales/{ko,en,ja,zh,vi}.ts` · 관련 컴포넌트 12개 · 목업 정본 3개

> **2026-08-28 — 이 대상에 `hooks/` 가 빠져 있었다.** 그래서
> `app/src/hooks/useRecording.ts` 의 토스트 **일곱 줄**이 한국어로 박힌 채 남았다.
> 그 훅은 미션 대화 화면이 쓰는데, 같은 화면의 다른 토스트는 번역돼 있어서
> **한 화면에서 언어가 갈렸다.** 같이 찾은 것이 하나 더 있다 —
> `api/business/user_business.loginAsStudent` 와 `app/src/api/sign.ts` 가
> 로그인 오류를 **한국어 문장으로** 냈고 `login.tsx` 가 그것을 그대로 그렸다.
> 둘 다 코드 반환으로 바꾸고 `login.err_*` · `activity.rec_*` 열한 키를
> 다섯 언어에 넣었다.
>
> **훑을 때 컴포넌트만 보면 안 된다** — `hooks/` · `api/` 에도 화면에 닿는
> 문자열이 있다. 저장소 전체를 다시 훑어 화면에 나오는 것은 이 둘뿐임을 확인했고,
> 나머지(요일 폴백 · 신고 payload · `console.error` · 자모/게임 콘텐츠 · 죽은 컴포넌트)는
> 번역 대상이 아니다.

입력은 **이 문서의 §5(원 리포트)** 다 — 2026-08-29 에 합치기 전에는 별도 파일이었다. 그 리포트가
"무엇을 고쳐야 하나" 를 적었고, 이 문서가 "무엇을 고쳤나" 를 적는다.

---

## 0. 실행한 검증과 반환값

| 검사 | 명령 | 결과 |
|---|---|---|
| 타입 | `tsc --noEmit` | **0** |
| 린트·서식 (내가 건드린 17개 파일) | `biome check <17 files>` | **0** |
| 목업 대조 | `activity-parity.tsx` + `activity-parity-diff.py` | **50화면 "모두 같다" / 0** |
| 픽스처 데이터 | `fixture-data-check.py` | **0** |
| 표현클립 데이터 | `clip-data-check.py` | **0** |
| CSS 클래스 | `css-class-check.py` | **0** |
| 고정폭 글자칸 넘침 | `fixed-box-check.py` | **0** ("넘칠 만한 고정폭 칸 없다") |
| 문서 참조 | `check_docs.py` | **0** ("통과 — 끊어진 참조 없음") |
| 키·플레이스홀더 정합 | 자체 스크립트 | **637키 × 5언어, 누락·초과·불일치 0** |

**돌리지 못한 것 — `rsbuild build` (번들).** `node_modules` 가 macOS(darwin-arm64)로 설치돼 있어
리눅스 VM에서 `@rspack/binding` 이 로드되지 않는다. `build` 의 앞 절반인 `tsc --noEmit` 은 통과했다.
`biome` 과 `esbuild` 는 같은 문제를 겪었으나 리눅스 바이너리를 `/tmp` 에 따로 받아 돌렸다
(저장소의 `node_modules` 는 건드리지 않았다). **번들 빌드는 맥에서 한 번 확인이 필요하다.**

---

## 1. 첫 리포트의 숫자를 바로잡는다

앞서 "하드코딩 한국어 89건"이라 적었으나, **실제로 사용자에게 보이는 것은 19곳**이었다.
한 단계 더 들어가지 않고 센 숫자였다. 빠진 근거:

| 제외 | 건수 | 근거 |
|---|---|---|
| `HangulTracingCanvas.tsx` | 17 | `statusText` 를 13번 `set` 하지만 **어디서도 읽지 않는다.** 이 컴포넌트의 `return` 은 `<canvas>` 하나뿐이다 (`grep -i statustext` → 선언 1줄 외 없음) |
| `card-sort-view.tsx`·`card-sort.tsx` | 13 | `"2급"`·`${i}과` 는 **원장 조회 키**다 (`vocab["2급"]["4과"]`, `stage_id="2급_4과"`). 화면에는 `gameLevelLabel`/`gameLessonLabel` 을 거쳐 나간다 — 파일 주석에 명시돼 있다 |
| `seoul-puzzle.tsx` | 12 | 장소명(`북한산`·`경복궁`)과 문법 템플릿(`${name}이에요`) — 학습 대상 |
| `content4.tsx` | 5 | 동영상 오류 문구가 `reportError()` 텔레메트리로만 간다. 화면에 뜨지 않는다 |
| `chat-message.tsx` L75 · `audio-recorder.tsx` L147 | 2 | `throw new Error(...)` · `console.error(...)` |
| `score-box.tsx` · `assess-chart.tsx` | 3 | 임포터 0 — 죽은 코드 |

---

## 2. 반영한 것

### 2-1. 하드코딩 한국어 → i18n (12개 파일, 신규 키 30개)

| 파일 | 옮긴 것 |
|---|---|
| `learn/flashcard-result.tsx` | 결과 화면 전체 — 제목·본문·통계 라벨·버튼·바텀시트 제목 (`flashResult.*` 10키) |
| `learn/mission-report.tsx` | 평가 4축 빈 데이터 문구 (`report.empty*`) |
| `learn/mission-dialog.tsx` | 토스트 2종 (`missionChat.errTooShort`·`errNoRecording`) |
| `learn/ai-roleplay.tsx` | 빈 상태 · 화자 라벨 "나" (`activity.noData`·`speakerMe`) |
| `chat/chat-message.tsx` | 응답 없음 대체 문구 (`missionChat.noMessage`) |
| `problem/audio-recorder.tsx` | 분석 실패 토스트 (`activity.analyzeFailed`) — 원문의 "다시시도" 띄어쓰기도 함께 고침 |
| `problem/scene/header.tsx` | 헤더 2곳 `"과 - "` (`player.chapterUnit`) |
| `dialog/dialog-input.tsx` | 시각화 `aria-label` (`missionChat.recordingWave`) |
| `main/activity/briefing-screen.tsx` | 이미지 자리표 (`activity.sceneImageAlt`) |
| `routes/__root.tsx` | 앱 업데이트·오류 화면 5문구 (`app.*`) |
| `routes/learn/flashcard.tsx` | 앱바 라벨 (`player.chapterActivity`) |
| `routes/learn/mission-chat.tsx` | 급·과 라벨 3곳 → **기존 `player.lessonTitle` 재사용**, 브리핑 부제 (`missionChat.briefSubtitle`) |

### 2-2. 일본어
- **오역**: `signup.emailHint` 의 `この住所に` → `このメールアドレスに` (`住所` 는 집 주소)
- **반말**: `springPicnic.subLine2` `やりきろう！` → `やりきりましょう！`
- **나가기 / 마치기 미구분** — `終了`(중단)와 `終える`(완료)가 뜻이 겹쳐 데이터가 날아가는 쪽을 구분할 수 없었다. 중단 쪽을 `中断` 으로 갈랐다 (`missionChat.exitTitle`·`exit`, `state.exitConfirmChat`)
- 용어 통일: `メール`→`メールアドレス`, `私の`→`自分の`(3곳), `友達`→`友だち`, `購読`→`定期購読`
- 배지 품사: `挑戦できます`→`挑戦可能`, `ロック`→`ロック中`
- `{{name}} さん` → `{{name}}さん` (조사 앞 공백 제거)
- `nav.my` `MY` → `マイ`
- 전각 문장부호: `例:`·`正解:`·`前回のミッション:` → `：`, `名前 (…)` → `（…）`
- 직역 교정: `続けて言う` → `真似して言う`(2곳), `よく間違えます` → `間違えやすい`, `応用学習` → `発展学習`

### 2-3. 중국어
- `您`(8곳) → `你` 전부 통일
- 라틴 문자 앞뒤 공백: `包含AI对话`·`AI正在说话`·`和AI一起`·`AI对话报告`·`听AI说` → 전부 `AI ` 띄어쓰기
- 전각 콜론: `答案:`·`上次任务:`·`例:` → `：`
- 어휘: `我的页面`→`个人中心`, `姓名`→`名字`(4곳)

### 2-4. 베트남어
- **게임명 이중화 해소**: `Thợ săn tiểu từ` → `Thợ bắn trợ từ` (목록과 게임 안이 달랐다). 문법 용어도 `tiểu từ`/`trợ từ` → `trợ từ` 로 통일
- 성조 표기 통일: `xoá`→`xóa`(11곳), `khoá`→`khóa`(2곳). `tài khoản` 13곳 무결 확인
- `Hangeul` → `Hangul`
- 표현: `Clip biểu đạt`→`Clip diễn đạt`, `Sổ lỗi`→`Sổ lỗi sai`, `Kết thúc học`→`Kết thúc bài học`, `Hết lượt`→`Trò chơi kết thúc`, `Phim ảnh`→`Phim · Truyền hình`
- 게임명 표기 통일 (`— ` 유무)

### 2-5. 영어
- **sentence case 통일** (사용자 결정): `Reset Password`→`Reset password` 외 16곳
- **`Login` → `Sign in`** (`Sign up` 과 짝을 맞춤), `Logging in`→`Signing in`, `Login failed`→`Sign-in failed`
- 짝 맞추기: `I know it`/`Not yet` → `Know it`/`Don't know`, `Lessons`/`Levels` → `Choose a lesson`/`Choose a level`
- `L{{seq}}` → `Lesson {{seq}}`
- 문법: `we send`→`we'll send`, `You will not redo it.`→`You won't have to do it again.`, `You missed nothing.`→`You didn't miss any.`, `Email address cannot`→`The email address cannot`, `final you`→`final, you`

### 2-6. briefing 헤더 — 목업 승격 동반
`briefing.scenario`/`keyword` 는 **ko 를 포함한 5개 언어 전부 영어**였다(`Conversation Scenario`).
5개 언어로 옮기면서 **목업 정본도 같이 고쳤다** — `screens_ref/activity__briefing.html`,
`screens_ref/screens.ts`, `screens_SOT.html`. 대조가 `changeLanguage("ko")` 로 그리고
목업의 한국어와 i18n 한국어를 견주기 때문이다(`activity-parity.tsx` 153행).
`docs/screen_promotions.md` 에 승격 사유를 적었다. 대조는 `✓ briefing` 으로 통과했다.

---

## 3. 회의적인 시니어의 반론과 답

> **"`t` 를 useCallback 의존성에 넣었다가 뺐다. 왜 오락가락하나."**

biome 의 `useExhaustiveDependencies` 가 4곳을 잡아 기계적으로 `t` 를 넣었는데,
**넣은 뒤 인접 기능을 짚어 보니 두 곳이 회귀였다.**
- `chat-message.tsx`: `fetchData` 는 아래 `useEffect` 의 의존성이고 그 몸통이 `postChat` 을 부른다.
  `t` 를 넣으면 **언어를 바꿀 때 같은 말이 서버로 한 번 더 날아간다.**
- `mission-report.tsx`: `useEffect[dialogId]` 안에서 `getReport` 를 부른다. `t` 를 넣으면
  언어 변경마다 리포트를 다시 받아 빈 골격으로 깜빡인다.

둘은 `biome-ignore` 에 이유를 적어 되돌렸다(저장소의 기존 관행과 같은 꼴).
`mission-dialog.tsx` 의 두 곳은 **이벤트 핸들러 전용**이라(어느 `useEffect` 도 물고 있지 않다 — deps 4개 전수 확인) `t` 를 남겼다.

> **"목업 정본을 건드린 것은 범위를 넘은 것 아닌가."**

`ko.ts` 의 값을 바꾸면 목업도 같이 바꿔야 대조가 통과한다 — 저장소가 그렇게 만들어져 있다.
같은 절차의 선례가 둘 있다(`빈칸 채워 말하기`→`빈칸 채우기` 66b0e94, `주간 활동`→`주간 학습 시간` 08-26).
다만 **정본을 고치는 일이라 사용자에게 물어 승인을 받고 진행했다.**

> **"베트남어가 길어 버튼이 넘치지 않나."**

`fixed-box-check.py` 가 "고정폭 글자칸 70개 · 변수 자리 23곳 — 넘칠 만한 칸 없다" 로 통과했다.
다만 이 검사는 **고정폭으로 선언된 칸만** 본다. 유동폭 버튼의 줄바꿈은 실기로만 확인된다.

---

## 4. 하지 않은 것 · 남은 것

- **실행 화면을 열어 보지 못했다.** 이 리눅스 VM 에서 `rsbuild` 가 돌지 않는다. 맥에서 `pnpm dev` 로 5개 언어를 한 번 훑어야 한다 — 특히 영어 `Practice what you missed`·`Lesson {{seq}}` 와 베트남어 긴 문장.
- **`rsbuild build` 미실행** (위 사유). `tsc --noEmit` 은 통과.
- **영어 약관 동의문 `<Trans>` 전환 — 사용자 요청으로 보류.** `signup.terms + and + privacy + agreeSuffix` 4키 조립이 `"Terms of Service and Privacy Policy — I agree"` 가 되어 영어 어순이 깨진다. 문구 치환으로는 못 고치고 JSX 구조를 바꿔야 한다. 나중에 다시 짚기로 했다.
- **복수형 `_one`/`_other` 미적용 — 이번 범위 밖.** `"{{count}} screenshot(s)"` 의 `(s)` 꼼수가 남아 있고, `"{{count}} results"`·`"{{count}} days"` 는 1일 때 `1 results`·`1 days` 가 된다.
- **일본어 `わかる`/`わからない`(플래시카드 버튼) — 일부러 두었다.** 주변이 전부 `ですます` 라 형식상 어긋나지만, 일본 플래시카드 앱의 관용 표기이기도 하다. 결정이 필요한 자리라 임의로 바꾸지 않았다.
- **`routes/book/list.tsx` 의 `"3주완성 연세한국어"`** — 교재 고유명으로 보아 두었다. 번역 대상으로 볼 여지가 있다.
- **`report.title`·`missionChat.completeTitle` 의 한국어 — 설계대로 유지.** `h2`(한국어) + `p`(모국어) 2단 구조이고 부제는 이미 각 언어로 번역돼 있다.

---

## 5. 원 리포트 — 무엇을 고쳐야 했나 (2026-08-28)

**전에는 이것이 `i18n_검수리포트_20260828.md` 라는 별도 파일이었다.** 2026-08-29 에
여기로 합쳤다 — 위 §2 는 **무엇을 고쳤나**이고 아래는 **무엇을 고쳐야 했나**다.
같은 날 같은 감사의 입력과 출력인데 파일이 둘이라 색인이 둘을 따로 설명해야 했고,
읽는 사람은 어느 쪽이 최신인지 매번 물어야 했다.

**아래는 그때 적힌 그대로다.** 절 번호에 `5-` 를 붙인 것 말고는 손대지 않았다 —
시점 기록이므로 지금 상태와 다른 곳이 있으면 §1·§2 가 정정한다.

- 대상: `app/src/i18n/locales/{ko,en,ja,zh,vi}.ts` — 각 607키, 5개 언어
- 기준 원문: `ko.ts`
- 일자: 2026-08-28
- **본 리포트는 점검 결과만 담는다. 파일은 하나도 수정하지 않았다.**
- **여기서 나온 것을 실제로 반영한 기록은 [`i18n_검수_20260828.md`](i18n_검수_20260828.md) 다.**
  이 문서는 입력(무엇을 고쳐야 하나), 그쪽이 출력(무엇을 고쳤나)이다.

---

### 5-0. 기계 검증 결과 (통과)

실행한 검증과 반환값:

| 검사 | 결과 |
|---|---|
| 키 개수 | ko/en/ja/zh/vi 모두 **607** |
| 누락 키 | en 0 · ja 0 · zh 0 · vi 0 |
| 초과 키 | en 0 · ja 0 · zh 0 · vi 0 |
| `{{placeholder}}` 불일치 | **0건** (ko 대비 전 언어 일치) |
| `\n` 줄바꿈 개수 불일치 | **0건** |
| 파일 인코딩 | 5개 파일 전부 UTF-8, U+FFFD(깨진 문자) 0건 |
| 중국어 번체자 혼입 | **없음** (검출된 `示`·`語`는 각각 간·번 동형 / `日本語` 정상) |

> 검증 방법: `ko.ts` 기준으로 5개 파일을 Node로 로드→flatten 후 키·플레이스홀더·개행 대조,
> `file -bi` 인코딩 확인, 번체 후보 문자셋 대조.
> 초기 출력에서 `ja.springPicnic.hardMission`이 깨져 보였으나 hexdump 확인 결과
> `e3 82 80 e3 81 9a e3 81 8b e3 81 97 e3 81 84` = 「むずかしい」로 **정상**. 터미널 표시 문제였다.

**구조는 깨끗하다. 문제는 전부 문구 층위와 i18n 누락 층위에 있다.**

---

### 5-1. 🔴 최우선 — 번역 파일 밖의 하드코딩 한국어 (i18n 우회)

번역 파일 검수만 하면 놓치는 부분이다. **컴포넌트에 한국어가 직접 박혀 있어 언어를 바꿔도 한국어가 그대로 노출된다.**
`.stories.tsx`(스토리북)와 학습 대상 한국어(서울퍼즐 장소명·문법 템플릿)는 제외한 실사용 건이다.

| 파일 | 건수 | 예시 | 임포터 |
|---|---|---|---|
| `components/learn/flashcard-result.tsx` | 12 | "단어를 다 외웠어요!" / "모르는 단어" / "아는 단어" / "학습 끝내기" — **결과 화면 전체** | 1 |
| `components/draw/HangulTracingCanvas.tsx` | 17 | "경로를 벗어났습니다!" / "모든 획을 완벽하게 그렸습니다! 🎉" / "아직 학습되지 않은 글자입니다." | 6 |
| `components/main/content4.tsx` | 5 | "동영상을 찾을 수 없거나 비공개 설정되었습니다." 등 재생 오류 5종 | 1 |
| `components/learn/ai-roleplay.tsx` | 5 | "데이터가 없습니다" / 화자 라벨 "남"·"나" | 2 |
| `components/learn/mission-report.tsx` | 4 | "평가할 발음 데이터가 없어요." 외 3 | 1 |
| `routes/learn/mission-chat.tsx` | 4 | "상황에 맞는 대화를 연습하세요." / `${level}급 ${lesson}과` | — |
| `components/learn/mission-dialog.tsx` | 2 | "메시지를 2자 이상 입력해 주세요." / "녹음된 내용이 없습니다." | 3 |
| `components/chat/chat-message.tsx` | 2 | "TTS 요청 실패" / "메시지 없음" | 1 |
| `components/problem/audio-recorder.tsx` | 2 | "분석에 실패했습니다. 다시시도해 주세요" (※ "다시시도" 띄어쓰기 오류도 있음) | 4 |
| `components/problem/scene/header.tsx` | 2 | `"과 - "` | 23 |
| `routes/__root.tsx` | 2 | "앱 업데이트 중입니다" / "오류가 발생했습니다" | — |
| `components/main/game/card-sort-view.tsx`, `card-sort.tsx` | 13 | `"2급"~"5급"`, `${i}과` 칩 | 1 |
| `components/dialog/dialog-input.tsx` | 1 | `aria-label="녹음 중인 소리"` (스크린리더 전용) | 1 |
| `components/main/activity/briefing-screen.tsx` | 1 | L55 `<span aria-hidden>상황 이미지</span>` (이미지 없을 때 노출) | — |
| `routes/learn/flashcard.tsx` | 1 | "플래시카드" | — |

**이미 존재하는 키와 중복되는 것들** — 키를 새로 만들 필요 없이 연결만 하면 되는 건:
- `mission-chat.tsx`의 `${level}급 ${lesson}과` → 이미 `player.lessonTitle`이 있다
- `card-sort-view.tsx`의 `${i}과` / `2급` → 이미 `catalog.chapterChip` / `catalog.bookTab`이 있다
- `score-box.tsx`의 "완료한 키워드"·"점수" → 이미 `report.keywordsDone` / `report.score`가 있다

**죽은 코드 (임포터 0 — 수정 대상에서 빼도 된다)**: `components/report/score-box.tsx`, `components/ui/assess-chart.tsx`

신뢰도: **높음** (파일·라인 직접 확인, 임포터 수까지 확인)

---

### 5-2. 🟡 확인이 필요한 설계 판단 3건

수정 전에 **의도인지 누락인지 결정**이 필요하다. 코드를 열어 확인한 내용을 함께 적는다.

#### 5-2-1. `report.title` — 5개 언어 전부 "AI 대화 리포트"
`report-screen.tsx:186-187`에서 `<h2>{report.title}</h2>` + `<p>{report.subtitle}</p>` 2행 헤더다.
`report.subtitle`은 ja/zh/vi가 제대로 번역되어 있다(`AI会話レポート` / `AI对话报告` / `Báo cáo hội thoại AI`).
→ **"한국어 원문 + 모국어 부제" 라는 일관된 이중 표기 설계로 보인다. 버그 아닐 가능성이 높다.**
다만 en의 경우 `AI 대화 리포트` / `AI Chat Report`가 되는데, 라틴 문자권 사용자에게 한글 제목이 적절한지는 별개 판단.

#### 5-2-2. `missionChat.completeTitle` — 5개 언어 전부 한국어
`chat-text.tsx:217-231`에서 `completeTitle`(굵게) + `completeSub`(작게) 2단 구성.
`completeSub`는 ja/zh/vi 정상 번역됨. **2-1과 동일한 설계 패턴 — 의도로 판단된다.**

#### 5-2-3. `briefing.scenario` / `briefing.keyword` — 5개 언어 전부 영어
`"Conversation Scenario"` / `"Mission Keyword"`. `briefing-screen.tsx:55,74`의 `<h3>` 섹션 헤더다.
ko.ts조차 영어라서 **디자인 언어로서의 영어일 수도** 있으나,
같은 화면의 다른 헤더는 전부 번역되어 있어 **누락일 가능성이 더 높다.**
→ 일본어·중국어·베트남어 사용자는 이 두 섹션 제목만 영어로 본다.

신뢰도: 2-1·2-2 **높음**(의도) / 2-3 **중간** — 2-3은 진행 전 확인 필요.

---

### 5-3. 🇯🇵 일본어 (ja.ts)

#### 5-3-1. 반말(常体) 혼입 — 요청하신 항목
| 키 | 현재 | 문제 |
|---|---|---|
| `game.springPicnic.subLine2` (L307) | `韓国語の数字ミッションをやりきろう！` | **의지형 반말**. 같은 파일이 전부 `〜ましょう`체다 (`身につけましょう`·`練習しましょう`·`やり直しましょう`). ko는 "완수해요!"로 존댓말. |
| `activity.flashKnown` / `flashUnknown` (L647-648) | `わかる` / `わからない` | **사전형(반말)**. ko는 "알아요/몰라요". 다만 일본 플래시카드 앱은 관용적으로 반말 라벨을 쓰기도 해서 판단 여지 있음. |

#### 5-3-2. 오역 — 원어민만 잡아내는 건
| 키 | 현재 | 문제 |
|---|---|---|
| `signup.emailHint` (L82) | `この**住所**に再設定リンクを送ります` | **`住所`는 물리적 주소(집 주소)다.** 한국어 "이 주소로"를 직역한 결과. 이메일은 `このアドレス` 또는 `このメールアドレス`. **명백한 오역.** |

#### 5-3-3. 용어 일관성
| 항목 | 불일치 |
|---|---|
| 이메일 라벨 | `メールアドレス`(L4·81·131) vs `メール`(L196, `mypage.email`) |
| 1인칭 | `私の`(L652 `myRecording`·L684 `myPronunciation`·L727) vs `自分の`(L367·L667·L681) — 같은 "내 녹음"이 `私の録音`(L652)과 `自分の録音`(L681) 두 가지. 일본어 UI는 `自分の`가 표준. |
| 친구 표기 | `友だち`(L306·312) vs `友達`(L461) |
| 구독 | `定期購読`(6곳) vs `購読`(L576 `expiredCta`) |
| 잠금 배지 | `canTry: 挑戦できます`(정중체 동사) vs `locked: ロック`(명사) — 배지 라벨끼리 품사가 다르다. `ロック`도 단독으로는 어색(`ロック中`이 자연스러움, 실제로 L557은 `ロック中の課`) |
| 그림 | `activity.picture: 画像` vs `game.vocashot.picture: （絵）` |

#### 5-3-4. UX 명확성 — 「나가기」와 「마치기」가 구분되지 않는다
en은 `Leave` / `Finish`, zh는 `离开` / `结束`, vi는 `Rời đi` / `Kết thúc`으로 구분되는데 ja만 뭉개진다:
- `missionChat.exitTitle` (L754) = `会話を終了しますか？` ← 실제로는 **나가기(내용 저장 안 됨)**
- `missionChat.finishTitle` = `会話を終えますか？` ← **마치기(리포트 생성)**
- `missionChat.exit`(L759) = `終了`, `player.exit`(L592) = `終了`

`終了`와 `終える`는 일본인에게 거의 같은 뜻이라, **"내용이 날아가는 나가기"와 "리포트가 나오는 마치기"를 구분할 수 없다.** 실제 데이터 손실로 이어질 수 있는 건.

#### 5-3-5. 기타
- `home.userName` (L534) `{{name}} さん` — **`さん` 앞 공백은 일본어 표기법상 틀렸다.** 한국어 `{{name}} 님`의 공백을 그대로 옮긴 흔적. 같은 파일 L254는 `{{name}}さんの`로 공백이 없어 자체 모순.
- `activity.instrSpeak` `聞いて続けて言ってみましょう` — `続けて言う`는 "따라 말하다"의 직역. `catalog.jamoAct.listen-repeat`는 `真似する`를 쓴다. 어색 + 불일치.
- `result.hardItem` `よく間違えます` — 배지/칩 라벨에 정중체 동사. `間違えやすい` 같은 명사·형용사형이 자연스럽다.
- `nav.my` = `MY` — zh는 `我的`, vi는 `Của tôi`로 현지화했는데 ja만 라틴 문자. `マイ`/`マイページ`가 자연스럽다.
- 반각 문장부호 혼용: `例:`(L291)·`正解:`(L315)는 일본어 조판상 `例：`·`正解：`(전각). `名前 (Korean or English)`(L290)는 반각 괄호인데 `（任意）`·`（絵）`는 전각.
- `catalog.sectionAdvanced` `応用学習` — "심화"는 `発展学習`이 더 가깝다. (경미)

신뢰도: 3-2 **높음** / 3-1·3-3·3-4 **높음** / 3-5 **중간**

---

### 5-4. 🇨🇳 중국어 (zh.ts)

#### 5-4-1. 您 / 你 혼용 — 원어민이 바로 느끼는 부분
- `您` 8곳: 로그인·문의·비밀번호 재설정 등 **거래성 화면** + `home.greeting: "您好，"`
- `你` 5곳: 게임·학습 화면 (`你选的`, `你想学的`, `等待轮到你`, `你的发音与句子很匹配`, `你完成了所有任务`)

"거래 화면은 격식, 학습 화면은 친근" 이라는 의도된 분리로 볼 수도 있으나, **`home.greeting`의 `您好`는 바로 아래 학습 콘텐츠와 붙어 있어 톤이 튄다.** 학생 대상 학습 앱이면 전체 `你`로 통일하는 편이 일반적이다.

#### 5-4-2. 라틴 문자 앞뒤 공백 불일치 — 중문 조판 관례
| 공백 있음 | 공백 없음 |
|---|---|
| `AI 对话内容与批改` | `包含AI对话和角色扮演的全部学习活动` |
| `AI 角色扮演` | `AI正在说话` |
| `AI 任务对话` | `和AI一起练习对话` |
| | `AI对话报告`, `听AI说` |

같은 파일 안에서 규칙이 갈린다. 중문 조판 관례는 **CJK와 라틴 사이 공백**이므로 공백 있는 쪽으로 통일이 맞다.

#### 5-4-3. 반각 콜론
`answerLabel: "答案:"`, `lastMission: "上次任务: ..."`, `namePlaceholder: "例: ..."` — 중국어 문장부호는 전각 `：`. 같은 파일의 `。`·`，`는 전각으로 잘 쓰고 있어 콜론만 튄다.

#### 5-4-4. 어휘
- `mypage.title: "我的页面"` — "마이 페이지"의 직역. 중국 앱은 `我的` 또는 `个人中心`을 쓴다. (`nav.my`는 이미 `我的`로 되어 있어 자체 불일치이기도 하다)
- `signup.name: "姓名"` — `姓名`은 법적 성명. `nameHint`가 "학습 화면에 표시됩니다"라고 하니 `名字`/`昵称`이 맞다.
- `game.vocashot.picture: "（图）"` vs `activity.picture: "图片"` (경미)

신뢰도: 4-1·4-2·4-3 **높음** / 4-4 **중간**

---

### 5-5. 🇻🇳 베트남어 (vi.ts)

#### 5-5-1. 🔴 같은 게임 이름이 두 가지 — 명백한 버그
| 위치 | 값 |
|---|---|
| `game.particleSniper.title` (L418) | `Thợ **bắn** **trợ từ**` |
| `game.list.particleSniper.name` (L469) | `Thợ **săn** **tiểu từ**` |

게임 목록에서 보이는 이름과 게임 안에서 보이는 이름이 다르다. 더해서 **조사(助詞)를 `trợ từ`(L418·427·430)와 `tiểu từ`(L469·470) 두 용어로 부른다.** 문법 용어가 화면마다 달라지는 건 학습 앱에서 치명적이다.

#### 5-5-2. `xóa` / `xoá` 성조 표기 혼용
- `xoá` 계열 10곳 (L63·165·166·173·175·180·181·185·334·335)
- `xóa` 계열 2곳 (L609 `eraseAll: "Xóa tất cả"`, L750 `discard: "Xóa bản ghi"`)

둘 다 통용되지만 **한 파일 안에서 섞이면 안 된다.** 참고로 현대 베트남어 표기 규범과 대부분의 소프트웨어는 `xóa` 쪽이다. 같은 계열로 `khoá`(L254·554)와 `khóa`(L722 `Từ khóa`)도 갈린다 — 단, `từ khóa`(키워드)는 원래 `khóa`가 맞으므로 `Đang khoá`→`Đang khóa` 쪽을 맞추는 게 옳다.

#### 5-5-3. `Hangul` / `Hangeul` 혼용
- `catalog.jamoTab` (L500) = `Hangeul`
- `paywall.benefitAll` (L556), `paywall.memberBody` (L567) = `Hangul`

#### 5-5-4. 어색한 표현
| 키 | 현재 | 문제 |
|---|---|---|
| `clip.title` / `nav.clip` | `Clip biểu đạt` | `biểu đạt`은 추상명사라 UI 라벨로 어색하다. `Clip mẫu câu`(예문 클립) 또는 `Clip diễn đạt`이 자연스럽다. |
| `game.springPicnic.wrongHead` | `Sổ lỗi` | ko "오답 노트". `Sổ lỗi`만으로는 뜻이 안 선다. `Sổ lỗi sai` / `Các câu sai`. |
| `activity.roleFinish` | `Kết thúc học` | 목적어가 비어 어색. `report.finishLearning`은 `Kết thúc bài học`로 되어 있어 **불일치이기도 하다.** |
| `game.vocashot.gameOver` | `Hết lượt` | "차례가 끝남". Game over는 `Trò chơi kết thúc`. 바로 다음 문장 `overDesc`가 "하트를 다 썼다"라 의미가 어긋난다. |
| `clip.catFilm` | `Phim ảnh` | ko는 "영화·드라마". `Phim ảnh`은 영화 일반이라 드라마가 빠진다. |
| `game.springPicnic.aria` vs `game.list.springPicnic.name` | `Nhiệm vụ số — Dã ngoại mùa xuân` vs `Nhiệm vụ số dã ngoại mùa xuân` | 표기 불일치 (경미) |

#### 5-5-5. 톤
`bạn` 2인칭이 전체적으로 일관되게 쓰였다. **경어 톤 문제는 발견되지 않았다.**

신뢰도: 5-1·5-2·5-3 **높음** / 5-4 **중간**

---

### 5-6. 🇬🇧 영어 (en.ts)

#### 5-6-1. 대소문자 규칙이 반반
| Title Case | sentence case |
|---|---|
| `Reset Password`, `Send Email`, `Check Your Email`, `New Password`, `Confirm Password`, `My Page`, `Language Settings`, `Sign Out`, `Edit Profile`, `Change Password`, `Change Nationality`, `Current Password` | `Create an account`, `Create a new password`, `Delete account`, `Back to sign in`, `Sign up and start`, `Contact us`, `Add screenshot`, `Remove {{name}}` |

같은 화면 안에서도 갈린다(`mypage`의 `Edit Profile`·`Change Password` vs `Delete account`). 한쪽으로 정해야 한다.

#### 5-6-2. sign in / log in 용어 혼용
`login.title`·`login.loginButton` = `Login`(명사를 버튼에) / `signup.goLogin` = `Sign in` / `resetPassword.blockedBack` = `Back to sign in` / `signup.goSignUp` = `Sign up` / `login.guestButton` = `Browse without signing in`.
**`Sign up`과 짝이 되는 건 `Sign in`이다.** `Login`은 명사형이라 버튼 라벨로는 `Log in`이어야 한다.

#### 5-6-3. 🔴 `signup.agreeSuffix` — 문자열 연결이 영어 어순을 깬다
`signup.terms` + `signup.and` + `signup.privacy` + `signup.agreeSuffix`
→ **"Terms of Service and Privacy Policy — I agree"**
한국어/일본어 어순(`~에 동의합니다`)을 전제로 한 조립이라 영어에서 무너진다. 자연스러운 영어는 `I agree to the Terms of Service and Privacy Policy`로 **앞에 와야** 한다.
같은 이유로 zh `，我同意`, vi ` — Tôi đồng ý`도 어순이 어색하다. (중국어는 서명 문서에 `《서비스 약관》` 겹낫표 관례도 있다)
→ 이건 문구 수정이 아니라 **`<Trans>` 컴포넌트로 문장 전체를 한 키로 만드는 구조 변경**이 필요한 건.

#### 5-6-4. 복수형 하드코딩
`inquiry.doneShotsLost`: `"{{count}} screenshot(s) could not be saved."`
i18next는 `_one` / `_other` 복수 처리를 지원하는데 `(s)` 꼼수를 쓰고 있다. 다른 count 키(`clip.resultCount: "{{count}} results"`, `home.streakDays: "{{count}} days"`)도 1일 때 `1 results`·`1 days`가 된다.

#### 5-6-5. 문법·자연스러움
| 키 | 현재 | 지적 |
|---|---|---|
| `signup.emailHint` | `If you forget your password, we send a reset link here` | 시제. `we'll send` |
| `signup.carryBody` | `You will not redo it.` | 어색. `You won't have to do it again.` |
| `game.vocashot.noMissed` | `You missed nothing.` | 어색. `You didn't miss any.` |
| `mypage.profileInfo.noticeText` | `Email address cannot be changed.` | 관사 누락. `The email address cannot be changed.` |
| `legal.pendingBody` | `Once they are final you will be able to…` | `final,` 뒤 쉼표 |
| `activity.flashKnown` / `flashUnknown` | `I know it` / `Not yet` | 짝이 안 맞는다(1인칭 문장 vs 부사구). `I know it` / `Not yet` → `Know it` / `Don't know` |
| `game.cardSort.pickLesson` | `Lessons` | 짝인 `pickGrade`는 `Choose a level`. 명령형/명사형 불일치 |
| `catalog.chapterChip` | `L{{seq}}` | `L1`은 관용 약어가 아니다. `Ch. 1` 또는 `Lesson 1` |

신뢰도: 6-1·6-2·6-3·6-4 **높음** / 6-5 **중간**

---

### 5-7. 우선순위 제안

| 순위 | 항목 | 근거 |
|---|---|---|
| 1 | §1 하드코딩 한국어 (특히 `flashcard-result.tsx` 전체 화면, `HangulTracingCanvas.tsx` 피드백 17건) | 번역 품질 이전에 **언어 전환 자체가 작동하지 않는다** |
| 2 | §3-4 일본어 「나가기/마치기」 미구분 | **데이터 손실**로 이어질 수 있는 유일한 건 |
| 3 | §5-1 베트남어 게임명·문법용어 이중화 | 학습 앱에서 용어 불일치는 치명적 |
| 4 | §3-2 일본어 `住所` 오역 | 단일 명백 오역 |
| 5 | §2-3 `briefing.scenario/keyword` 영어 노출 | 의도 확인 후 결정 |
| 6 | §6-3 영어 약관 동의문 어순 | 구조 변경(`<Trans>`) 필요 — 공수 있음 |
| 7 | §4-1·4-2·4-3, §5-2·5-3, §6-1·6-2, §3-3 표기·용어 통일 | 일괄 치환으로 처리 가능 |

---

### 5-8. 회의적인 시니어의 반론과 답

> **"§2의 한국어 노출 3건은 그냥 미번역 버그 아닌가? 왜 '확인 필요'로 미루나."**

`report-screen.tsx`와 `chat-text.tsx`를 열어 확인했다. 둘 다 **h2/strong(한국어) + p/span(모국어)** 2단 구조이고,
`report.subtitle`·`missionChat.completeSub`는 ja/zh/vi가 정상 번역되어 있다.
같은 화면의 `briefing-screen.tsx`도 `content.title` / `content.titleTranslated` 쌍을 쓴다.
즉 **"한국어를 먼저 보여주고 모국어로 받쳐주는" 일관된 설계**가 존재한다 — 한국어 학습 앱에서 합리적인 선택이다.
반면 `briefing.scenario`/`keyword`는 **UI 크롬(섹션 제목)인데 ko.ts조차 영어**라 이 패턴에 안 맞는다. 그래서 이 셋을 갈라 등급을 다르게 매겼다.

> **"§3-1의 `わかる/わからない`를 반말이라고 지적하는 건 과한가?"**

절반은 그렇다. Anki를 비롯한 일본 플래시카드 앱은 관용적으로 반말 라벨을 쓴다. 그래서 `やりきろう`(명백한 불일치)와 등급을 나눠 적었다. `わかる/わからない`는 **"의도면 두고, 아니면 `わかります/わかりません`"** 로 결정만 하면 되는 건이다.

> **"기계 검증이 전부 0건이면 번역이 잘 된 것 아닌가?"**

구조 검증(키·플레이스홀더·개행)이 통과한 것과 문구 품질은 다른 층위다. 실제로 구조는 깨끗한데
§1처럼 **번역 파일 밖에서 i18n을 우회하는 89건**이 존재했고, 이건 키 대조로는 절대 안 잡힌다.

---

### 5-9. 이 검수에서 하지 않은 것

- **파일 수정 없음.** 5개 로케일 파일과 컴포넌트 전부 원본 그대로다.
- **실행 화면 확인 안 함.** 앱을 띄워 실제 렌더링·줄바꿈·말줄임(truncation)은 보지 않았다.
  특히 독일어처럼 긴 언어는 없지만 **베트남어가 한국어 대비 1.5~2배 길어** 버튼 넘침 가능성이 있다. 이건 실기 확인이 필요하다.
- **학습 콘텐츠(교재 지문·단어 뜻) 번역은 대상 밖.** `shared/data/*` 의 한국어는 학습 대상이라 검사에서 제외했다.
- **§1의 89건 중 서울퍼즐 장소명·문법 템플릿**(`북한산`, `${name}이에요`)은 학습 대상 한국어로 판단해 제외했다. ko.ts 주석에도 "학습 대상이라 한국어를 그대로 쓴다"고 명시되어 있다.
