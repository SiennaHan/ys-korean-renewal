# 개발자 인계서 — 출시까지 남은 제품 배선

<!-- 관찰: api/persistence/model.py, api/xternal, app/src/shared/feature-gates.ts, .github/workflows @ 36c7830 -->
<!-- 왜: 카드의 「현재」는 전부 이 코드를 읽고 적은 관찰이다. 원본이 바뀌면 카드가 낡는다.
     경로를 넷으로 좁혔다 — 표(DEV-04·09·14) · 외부 호출 타임아웃(DEV-12) ·
     기능 게이트(DEV-03·09) · CI(DEV-10). 자주 우는 검사는 안 보고 넘기게 된다 -->

**기준일:** 2026-08-29 (카드별 「현재」를 코드로 다시 재서 맞췄다)  
**대상:** 서버·외부 연동·배포를 맡는 개발자  
**역할:** 남은 일을 그대로 이슈로 옮길 수 있는 **실행 목록**  
**화면·문구 정본:** `screens_SOT.html` · `paywall_SOT.html` · 앱의 다섯 로케일  
**필드 계약과 근거:** `dev_spec_v1.html` · `access_and_pricing_v1.html` · `clip_spec_v1.html` · `BLOCKERS.md`

> 이 문서와 과거 기록이 충돌하면 임의로 고르지 않는다. 현재 제품 코드로 사실을 확인하고,
> 제품 정책이 필요한 충돌은 아래 **기획 결정 대기**로 돌려보낸다. 개발자가 빈 정책을
> 추측해서 구현하지 않는다.

---

## 0. 역할 경계

| 담당 | 정하는 것 | 하지 않는 것 |
|---|---|---|
| 기획·제품 책임자 | 가격·환불·보관기간·노출 게이트·정확도 기준·최종 법률 문구, 화면 승인 | DB 구조·웹훅 검증·재시도 정책을 감으로 지정하지 않는다 |
| 개발자 | DB 마이그레이션·API·권한·보안·외부 연동·관측·테스트·배포·롤백 | 미정인 가격·보관기간·정답률을 대신 결정하지 않는다 |
| 디자인 | 시각 정본·상태별 문구·다섯 언어 레이아웃 승인 | 결제 성공·권한 부여를 클라이언트 상태만으로 확정하지 않는다 |

앱 코드를 고쳐야 하는 작업도 있다. 다만 개발자가 앱에서 맡는 범위는 **API 배선·오류 상태·접근성·분석 이벤트**다.
승인된 컴포넌트의 색·간격·문구를 바꾸는 것은 별도 디자인 승인을 받는다.

---

## 1. 현재 제품에서 이미 된 것 — 다시 만들지 않는다

| 영역 | 현재 상태 |
|---|---|
| 학습 상태 | `ko_activity_state`와 활동 enter/progress/complete/chapter API, 이어하기, 복습 큐가 연결됐다 |
| 학습 기록 | 문항 기록과 정답률은 **첫 시도 기준**, 위치·완료는 별도 상태 기준이다 |
| 회원 | 자체 회원가입과 게스트 기록 이전이 연결됐다 |
| 기관 코드 | **끝났다(2026-08-28).** 학교가 어드민에서 코드를 발급하고 학생이 `/join` 에서 그것으로 가입한다. 표 셋·API 일곱·화면 둘이 들어갔다 — `BLOCKERS.md` §10. **다시 만들지 마라** |
| 회원 탈퇴 | **끝났다(2026-08-27).** `POST /auth/withdraw` · `business/user_withdraw.py` · 지우는 범위는 `shared/withdrawal_scope.py` 한 곳이 정한다. 앱 진입점과 5개 언어 문구까지 있다 — **다시 만들지 마라** |
| 학기 종료 | **끝났다(2026-08-28).** 어드민에서 학생을 골라 접근을 끊으면 `ko_user.access_ended_at` 이 찍히고 **다음 요청부터** 무료 범위로 내려간다. `is_active` 를 쓰지 않은 이유가 있다 — 토큰이 30일이라 그 칸으로는 안 끊기고, 로그인이 막혀서 설명할 기회도 없어진다. 학생은 앱 진입 때 모달로 한 번 안내받고 페이월이 다시 말한다. **자동 만료는 없다** — `access_and_pricing_v1` §12. **다시 만들지 마라** |
| 학생 현황 | **끝났다(2026-08-28).** 어드민 학생 목록에 활동 일수·학습 시간·완료 수·마지막 활동일·최근 급·과가 붙는다. 활동 일수는 `responded` 기준이다(행이 있다 ≠ 공부했다). 진도는 **가장 최근에 끝낸** 과다 — `access_and_pricing_v1` §12.3 |
| 어드민 | **학교별로 갈렸다(2026-08-28).** 역할 셋 · 학교 경계는 토큰의 학교로 **덮어쓴다**. 그 전에는 `batch`·`upload` 가 뚫려서 아무 학교 관리자가 남의 학교 학생을 다룰 수 있었다 — `access_and_pricing_v1` §10.2. **단 어드민에는 게이트가 없다**(DEV-14) |
| 권한 표시 | `GET /entitlement`, 앱 권한 스토어, 잠금 표시, 페이월 상태는 있다. **2026-08-28 부터 기관 학생은 전 급을 받는다**(`shared/full_scope.py`) — 여기 오래 「서버는 아직 무료 범위만 낸다」로 적혀 있었다. **개인 결제만 무료 범위다** — `ko_entitlement` 가 없어 담을 곳이 없다(DEV-04). 되돌리는 길이 있다 — `SCHOOL_FULL_SCOPE=false` |
| 게임 | 다섯 게임의 진행·점수 저장과 게임 콘텐츠 서버 게이트가 연결됐다 |
| 자모 | 여섯 화면이 통합 라우트와 `jamoProblems`를 쓴다 |
| 음성 | 신규 STT 표본은 비공개 저장, 기본 표본 비율은 10%다. 보관기간과 자동 삭제만 미정이다 |
| 시각 회귀 | 활동·내비게이션·VocaShot·게임의 정본 대조 장치가 있다 |

이 표의 기능을 큰 구조부터 다시 짜는 제안은 별도 기술 제안서로 분리한다. 버그 수정 때문에
계약이 달라지면 `dev_spec_v1.html`과 실제 테스트를 같은 변경 묶음에서 갱신한다.

---

## 2. 기획이 먼저 답해야 하는 것

아래 값이 없으면 해당 작업은 **막힘**이다. 개발자는 임시 값을 운영 환경에 넣지 않는다.

| ID | 결정할 것 | 필요한 답 | 막히는 작업 |
|---|---|---|---|
| **PD-01** | 결제 상품 | ~~월 가격~~ **월 6달러로 확정(2026-08-28)**. 남은 것 — 통화 표기(학습자가 다섯 언어다), 부가세·해외 VAT 처리, 무료 체험 유무, 환불·해지·실패·유예 정책 | DEV-04 |
| **PD-02** | 결제 사업자 | PG, 상점 계정, 테스트/운영 키, 웹훅 주소 등록 담당 | DEV-04 |
| **PD-03** | 유료 콘텐츠 보호 수준 | 잠금이 UX 안내만인지, 유료 교재 JSON 자체도 서버 인증 뒤 내려야 하는지 | DEV-05 |
| **PD-04** | 음성 보관 | STT 표본 보관 일수, 기존 공개 객체를 모두 잠글지·삭제할지 | DEV-06 |
| **PD-05** | 콘텐츠 공개 게이트 | `deleted`만 빼는 지금 규칙을 유지할지, `reviewed`만 노출로 좁힐지. 좁힌다면 `draft` 로 남은 시트를 어떻게 할지 | DEV-07 |
| **PD-06** | MY 정확도 | 첫 시도 정답률인지 마지막 답 기준인지. 현재 학습 기록 규칙과 맞추려면 첫 시도를 권장 | DEV-08 |
| **PD-07** | 법률·사업자 정보 | 최종 약관/개인정보 URL, 상호·대표자·주소·사업자번호·통신판매번호, 개인정보 보호책임자, 국외 이전·보관기간 문구 | DEV-09 |
| **PD-08** | 가입 가능 연령 | 만 14세 미만을 받지 않을지, 받을 경우 법정대리인 동의를 어떻게 받을지 | DEV-11 |
| **PD-09** | B2C 판매 권리 | 교재 파생 콘텐츠를 개인에게 유료 판매할 수 있는지 연세 MOU·라이선스 범위 확인 | DEV-04 |
| **PD-10** | 개인정보 열람·내보내기 | 앱에서 직접 제공할지, 문의 처리라면 담당자·처리 기한·전달 형식 | DEV-11 |

개발자는 PD-01·02에 대해 선택지가 없다면 **비교안 1장**을 먼저 낸다. 비교안에는 지원 통화,
정기결제·환불 웹훅, 개발/운영 비용, 국내외 카드, 테스트 환경, 정산·세금계산서만 담고
사업자를 임의로 계약하지 않는다. PD-09 확인 전에는 유료 판매를 운영에 열지 않는다.

### 개발 과업과 별도로 제품팀이 끝낼 것

아래는 개발자에게 넘겨 해결할 문제가 아니다. 다만 출시 체크리스트에서 빠지면 안 된다.

- ~~자모 받침·겹받침 67행을 교재와 대조하고 529행의 검수 상태를 실제 이력과 맞춘다.~~ **끝났다(2026-08-28)** — 원장 v41 에서 529행 전부 `reviewed` 다.
- 미션대화 6·7·8권 45항목은 사람 검수가 들어왔다(원장 v42). **1~5권 72항목이 남았고**, 45항목 중 여섯은 키워드만 바뀌고 영어 설명이 옛 라벨의 것이다 — `BLOCKERS.md` §8.
- `i18n_검수_20260828.md`의 번역 계층 밖 하드코딩과 언어 품질을 담당 언어별로 승인한다.
- 약관·개인정보 처리방침의 법률 번역은 기계 번역본을 그대로 정본으로 쓰지 않는다.
- MY 화면과 표현클립 재생 실패 상태처럼 시각 정본이 없는 화면은 디자인 승인 뒤 개발한다.

---

## 3. 개발 작업 카드

상태 표기: **READY**는 바로 시작 가능, **PARTIAL**은 기반 작업까지만 가능,
**BLOCKED**는 위 결정 전 구현 금지다. 번호 순서가 권장 실행 순서다.

### DEV-01 · QR을 홈으로 보내기 — READY · P0

**목적:** 책의 QR은 권한 키가 아니라 유입 경로다. 로그인 여부와 무관하게 게스트 세션을
보장한 뒤 홈(`/main`)으로 보낸다.

**현재:** `app/src/routes/qr.tsx`는 `PUBLIC_QR_WEB_REDIRECT_URL`의 기본값 `/`을 쓴다.
비로그인 사용자가 `/`을 거치면 로그인으로 갈 수 있어 확정 정책과 다르다.

**구현 범위**

1. 스캔 기록 요청은 한 번만 보낸다. 실패해도 사용자를 빈 화면에 두지 않는다.
2. 로그인 토큰이 없으면 기존 게스트 인증 흐름으로 게스트 세션을 만든다.
3. 언어 설정과 QR 추적 정보는 보존하고 `/main`으로 이동한다.
4. QR은 무료 범위 이상의 권한을 만들지 않는다.

**완료 판정**

- 새 브라우저·기존 게스트·로그인 계정 세 경우가 모두 홈에 도착한다.
- 새 브라우저는 도착 즉시 `GET /entitlement`와 학습 기록 API를 호출할 수 있다.
- 잘못된 QR 코드·추적 API 실패·중복 탭에서도 무한 리다이렉트나 중복 스캔이 없다.
- 한국어·영어를 포함한 언어 설정이 이동 전후 동일하다.

### DEV-02 · 표현클립 신고를 안전하게 저장하기 — READY · P0

**목적:** 신고가 실제로 저장되고, 신고 한 건이 영상을 전 사용자에게서 즉시 숨기지 않게 한다.

**현재**

- `ko_error_report.target_id`는 `varchar(10)`인데 유튜브 ID는 11자라 `POST /report`가 500이다.
- 신고 영상 제외 로직은 살아 있어 컬럼만 먼저 늘리면 익명 한 명이 영상을 전원에게서 숨길 수 있다.
- `POST /report` 와 `GET /report/list/{category}` 가 **둘 다 무인증**이고(라우트에 `Depends` 가 없다), 클라이언트는 실패를 성공처럼 닫는다.

**반드시 지킬 구현 순서**

1. 앱의 신고 결과를 **제외가 아니라 하위 정렬**로 바꾼다. 재생 불가 코드 100·101·150만 제외한다.
2. 그다음 DB를 마이그레이션한다. 신고 단위는 영상이 아니라 **영상 구간**이며 `target_id`는 최소 32자다.
3. 구간 시작 초·검색어·맞은 대본 줄·신고 사유 코드를 저장한다.
4. 두 신고 API 모두 로그인/게스트 토큰을 받고 토큰 없는 호출은 거부한다.
5. `inappropriate`는 기존 문의 채널로 알리되, 슬랙 실패가 DB 저장을 되돌리지 않게 한다.
6. 앱은 저장 실패 시 실패 상태와 재시도를 보여 준다.

**완료 판정**

- 같은 영상의 서로 다른 구간을 별도로 신고할 수 있다.
- `audio_quality` 신고 후 해당 구간은 사라지지 않고 같은 우선순위 안에서 뒤로 간다.
- `inappropriate` 신고는 DB에 먼저 남고 조치 가능한 시점 링크와 맥락이 알림에 포함된다.
- 토큰 없는 요청은 401/403, 게스트·회원 요청은 정상 처리된다.
- 중복 전송은 중복 행·중복 알림을 만들지 않거나 명시한 멱등 규칙으로 처리된다.
- 마이그레이션 전후 데이터와 롤백 절차가 PR에 포함된다.

상세 근거는 `BLOCKERS.md` §6-e와 `clip_spec_v1.html` §05~§06이다.

### DEV-03 · 비밀번호 재설정과 메일 발송 — PARTIAL · P0

**목적:** 현재의 안내 화면을 실제 단일 사용·만료형 재설정 흐름으로 연결한다.

**현재:** `MAIL_RESET_READY=false`이고 화면만 있다. 서버에 재설정 요청/확정 API와 메일 발송 수단이 없다.

**지금 시작할 수 있는 기반 작업**

1. 메일 사업자 비교안과 발신 도메인 DNS 요구사항을 제출한다.
2. 재설정 토큰 표를 만든다: 사용자, **해시된 토큰**, 만료 시각, 사용 시각, 생성 시각.
3. 요청 API는 존재하지 않는 이메일에도 같은 응답과 유사한 처리 시간을 쓴다.
4. 확정 API는 서버에서 회원가입과 같은 비밀번호 규칙을 검증한다.
5. 토큰은 24시간 이내의 기획 승인값, 단일 사용, 새 요청 시 이전 미사용 토큰 무효화를 기본안으로 제시한다.
6. 다섯 언어 메일은 같은 딥링크 계약을 쓰고 토큰·비밀번호를 로그에 남기지 않는다.
7. 이메일·IP 기준 속도 제한과 감사 로그를 넣는다.

**완료 판정**

- 존재/부재 이메일의 화면 응답으로 가입 여부를 추정할 수 없다.
- 정상·만료·이미 사용·변조 토큰을 각각 테스트한다.
- 성공 후 기존 비밀번호는 실패하고 새 비밀번호는 성공한다.
- 운영 메일의 SPF/DKIM/DMARC와 반송 처리 결과를 기록한다.
- 운영 발송까지 검증한 뒤에만 `MAIL_RESET_READY=true`로 바꾼다.

결제 메일은 DEV-04에서 같은 발송 기반을 재사용하며, 마케팅 메일은 범위 밖이다.

### DEV-04 · 월 구독 결제와 실제 권한 부여 — BLOCKED(PD-01·02) · P0

**목적:** 결제 안내 화면의 버튼을 실제 월 구독으로 연결하고, 서버가 결제 사실만으로 권한을 판정한다.

**현재**

- 앱의 잠금·페이월·복원 진입과 `GET /entitlement`는 있다.
- `ko_entitlement`·`ko_purchase`·결제 생성 API·웹훅은 없다.
- ~~`GET /entitlement`는 기관 학생 여부만 구분하고 실제 계약/개인 구독 범위는 읽지 않는다.~~ **2026-08-28 에 절반이 바뀌었다** — 기관 학생은 이제 전 급을 받는다(`shared/full_scope.py`). **개인 구독은 그대로 남았다** — `ko_entitlement` 가 없어 `purchase` 를 담을 곳이 아직 없다.

**구현 범위**

1. `ko_purchase`: 사용자, PG 거래/구독/웹훅 ID, 상품, 금액, 통화, 상태, 원본 이벤트, 생성·갱신 시각.
2. `ko_entitlement`: 사용자, 출처(school/purchase), 시작·만료 시각, 상태, 근거 거래, 생성·갱신 시각.
3. 결제 세션 생성 API, 웹훅 서명 검증, 이벤트 ID 유일 제약과 멱등 처리를 만든다.
4. 권한은 성공 리다이렉트가 아니라 **검증된 웹훅과 서버 조회**로만 연다.
5. 월 갱신·실패·해지·환불·만료가 PD-01 정책대로 권한에 반영된다.
6. 기관 학생은 개인 결제 CTA가 아니라 학교 문의 상태를 유지한다. **「계약 급만」이 아니다** — 계약 학교는 전 급이라는 확정(2026-08-28)에 따라 `school_code` 가 있으면 전 급이다. 학기가 끝난 학생은 무료 범위로 내려가고 페이월이 「범위」가 아니라 「기간」을 말해야 한다.
7. 결제 완료·실패·환불·만료 메일을 다섯 언어로 보낸다.
8. “이미 구독했어요”는 PG와 서버 상태를 재조회하는 복원 동작으로 연결한다.

**완료 판정**

- 동일 웹훅을 여러 번 보내도 결제·권한 기간이 중복 증가하지 않는다.
- 위조 서명, 금액/통화/상품 불일치, 순서가 뒤바뀐 웹훅을 거부하거나 안전하게 수렴한다.
- 신규 구독·갱신·실패·해지·환불·만료·복원을 테스트 환경에서 끝까지 재현한다.
- 결제 후 새로고침·재로그인·다른 기기에서도 `GET /entitlement`가 같은 범위를 낸다.
- CTA는 테스트 결제와 롤백 검증 전까지 운영에서 활성화하지 않는다.

### DEV-05 · 유료 교재 콘텐츠의 서버 권한 강제 — BLOCKED(PD-03) · P0

**목적:** 주소 직접 입력과 번들 파일 접근으로 유료 콘텐츠가 열리는 범위를 합의한 보호 수준에 맞춘다.

**현재:** 게임 콘텐츠 라우트에는 서버 게이트가 있지만, 교재 문항은 앱 번들 JSON에도 있어
화면 잠금만으로는 다운로드 자체를 막을 수 없다.

**PD-03이 ‘서버 보호’일 때 구현 범위**

1. 교재 콘텐츠를 인증·권한 검사 API 뒤로 옮기고 앱 번들에는 무료 범위와 최소 색인만 남긴다.
2. 게스트·기관·개인 구독의 같은 권한 응답을 콘텐츠 API에서도 강제한다.
3. URL 직접 진입, 이전 북마크, 캐시된 응답, 만료 직후의 동작을 정의한다.
4. 권한이 줄었을 때 기기에 남은 민감 콘텐츠를 영구 오프라인 저장하지 않는다.

**완료 판정**

- 잠긴 과의 원문 문항은 비권한 사용자의 네트워크 응답과 배포 번들에 없다.
- 무료·학교 계약·개인 구독·만료 계정의 API 테스트가 있다.
- 권한 API 장애 시 유료 범위를 넓히는 실패 개방(fail-open)이 없다.

PD-03이 ‘UX 잠금’이면 이 작업은 하지 않고, 그 제한을 출시 문서에 명시한다.

### DEV-06 · STT 표본 보관·삭제 운영 — BLOCKED(PD-04) · P0

**목적:** 코드에만 있는 비공개 저장 규칙을 실제 버킷과 정기 삭제 운영으로 완성한다.

**현재**

- 신규 표본은 비공개 업로드이고 기본 표본 비율은 10%다.
- `STT_SHADOW_RETENTION_DAYS=0`은 삭제 없음이며 `prune_stt_shadow.py`는 자동 실행되지 않는다.
- 과거 공개 객체를 잠그는 도구는 있지만 운영 버킷 실행 증거가 없다.

**구현 범위**

1. PD-04 보관일을 환경값과 개인정보 문서에 동일하게 반영한다.
2. 삭제 도구를 스케줄러에 연결하고 성공·실패·삭제 수를 관측한다.
3. 기존 객체는 dry-run 목록과 영향 수를 제출한 뒤 승인받아 잠금/삭제한다.
4. 버킷 Public Access Block, 객체 ACL, 접근 주체, 서명 URL 만료를 점검한다.

**완료 판정**

- 보관일을 넘긴 표본이 다음 스케줄에서 삭제되고 최근 표본은 남는다.
- 삭제 실패가 알림으로 올라오고 재실행해도 안전하다.
- 공개 URL로 신규·기존 음성에 접근할 수 없다는 점을 표본 검사한다.
- 운영 실행 로그와 복구 불가능성 고지가 남는다.

### DEV-07 · 콘텐츠 공개 게이트를 빌드에서 강제 — PARTIAL(PD-05) · P0

**목적:** 검수 상태가 문서 문구가 아니라 실제 배포 산출물을 막는 규칙이 되게 한다.

**현재: 2026-08-28 에 절반이 들어갔다.** `app/scripts/build-content.py` 가 `review_status`를 읽어
`deleted` 행을 빼고(지금 54행), **모르는 상태값을 만나면 빌드를 실패시킨다**(`KNOWN_STATUS`).
전에 여기 적혀 있던 두 가지는 이제 사실이 아니다 — 자모 529행은 원장 v41에서 `reviewed`가 됐고,
`PORTED_AS_IS` 예외는 없앴다.

**아직 없는 것:** 규칙이 `deleted`만 빼는 뺄셈이라 `draft`는 그대로 나간다. 산출물 매니페스트도,
이름 있는 예외의 사유·승인자·만료도 없다. PD-05는 **좁힐지 말지**의 결정으로 남았다.

**구현 범위**

1. PD-05에 따라 허용 상태와 이름 있는 예외 목록을 한 파일에서 정의한다.
2. 빌드가 허용되지 않은 문항을 발견하면 목록과 수를 출력하고 실패한다.
3. 산출물 매니페스트에 시트별 상태·포함·제외·예외 수를 남긴다.
4. 예외에는 사유·승인자·승인일·만료/재검수 조건을 둔다.

**완료 판정**

- 의도적으로 draft 문항 하나를 넣은 회귀 테스트가 빌드를 실패시킨다.
- 예외 없는 draft/auto_checked/deleted가 학생 번들에 없음을 검사한다.
- 빌드 결과 수가 원장과 매니페스트에서 재현 가능하다.

### DEV-08 · MY 누적 학습 요약 — BLOCKED(PD-06) · P1

**목적:** 이미 쌓이는 학습 기록을 MY 화면에서 누적 요약으로 조회한다.

**현재:** `/learning-summary`와 누적 화면이 없고, 정확도를 첫 시도/마지막 답 중 무엇으로 볼지 미정이다.

**구현 범위:** PD-06 결정 후 `my_learning_summary_v1.html`의 데이터 계약대로 기간·급·활동별
요약 API를 만들고, 빈 상태·부분 데이터·탈퇴/게스트 이전을 처리한다. 원문 응답이나 음성은 요약 응답에 싣지 않는다.

**완료 판정**

- 같은 샘플 데이터에서 API 합계와 원장 행을 손으로 대조한 표를 PR에 첨부한다.
- 첫 시도 오답 후 재시도 정답, 건너뛰기, 채점 없는 활동의 계산 결과가 정책과 일치한다.
- 타임존 경계와 기간 필터가 테스트된다.

### DEV-09 · 법률 링크와 동의 증거 — BLOCKED(PD-07) · P0

**목적:** 회원가입과 결제에서 사용자가 실제 문서를 열 수 있고, 어떤 판에 동의했는지 증명한다.

**현재** (2026-08-29 에 코드로 확인)

- **읽을 자리는 생겼다** — `/terms` · `/privacy` 화면이 있고 가입 화면이 링크한다(2026-08-27).
  전에는 「동의합니다」를 필수로 요구하면서 **읽을 곳을 한 군데도 안 주는 화면**이었다.
- **조문이 아직 없다.** 두 화면은 `LEGAL_DOCS_READY` 가 꺼져 있어 "준비 중입니다 → 문의하기"
  라고 **사실대로 말한다.** 지어낸 조문을 보여 주지 않는 것이 의도다. 문안이 오면 그 한 줄을 켠다.
- **동의를 남기는 자리가 0곳이다** — `model.py` 에 `consent` 라는 이름이 없다.
  즉 지금은 **누가 어느 판에 동의했는지 증명할 수 없다.** 이 카드의 2·4번이 통째로 미구현이다.
- **필수/선택 동의 구분도 없다** — i18n 에 동의 관련 키가 `agreeSuffix` 하나뿐이다.

**구현 범위**

1. 최종 약관·개인정보 문서를 고정 URL에 게시하고 가입/결제 화면에 연결한다.
2. 사용자 ID, 문서 종류, 버전, 동의 시각, 필요한 경우 로케일을 저장한다.
3. 필수 동의와 선택 동의를 분리하고 선택 동의를 서비스 이용 조건으로 쓰지 않는다.
4. 문서 버전이 바뀔 때 재동의 정책을 적용한다.

**완료 판정**

- 다섯 언어 화면에서 두 링크가 열리고 뒤로 갔을 때 입력값이 보존된다.
- 동의하지 않으면 가입/결제가 진행되지 않으며, 동의 기록은 서버에서 조회 가능하다.
- 개인정보 문구의 음성 보관일·외부 처리자·국외 이전 내용이 실제 운영값과 일치한다.

### DEV-10 · 출시 안전장치와 운영 관측 — READY · P0

**목적:** 기능이 한 번 동작한 것을 넘어, 변경과 장애를 배포 전에 잡고 운영에서 원인을 찾는다.

**현재** (2026-08-29 에 코드로 확인)

- ~~CI 가 없다.~~ **2026-08-29 에 만들었다** — `.github/workflows/gates.yml`.
  푸시·PR 마다 **여섯**이 돈다(문서 · typecheck · 목업 대조 · build · check:css · biome).
  새로 만든 검사는 없다. 있던 것을 묶었을 뿐이다.
- **일곱째(`build-content.py --check`)는 CI 에서 못 돈다** — 원장 xlsx 를 읽는데
  **원장이 저장소에 없다**(`.gitignore` 의 `*.xlsx`, 교재 파생이라 일부러 뺐다).
  스크립트가 "원장을 찾지 못했다" 로 죽는다. **콘텐츠를 건드리면 사람이 돌려야 한다** —
  이것을 CI 에 넣으려면 원장을 시크릿 저장소에서 받아 오는 단계가 필요하고,
  **그러면 교재 파생 콘텐츠가 CI 러너를 지나간다.** 그 결정은 기획·법무 쪽이다.
- **biome 은 0 을 요구하지 않게 넣었다.** `biome check src` 가 아직 통과하지 않으므로
  **늘어나면 실패**하는 톱니다(기준 60). 남은 것이 대부분 `useExhaustiveDependencies` 라
  한 건씩 사람이 봐야 하는데, 0 을 걸면 그 판단을 서두르게 만든다.
  **줄이면 워크플로의 `BASELINE` 도 같이 내려야 한다** — 안 내리면 톱니가 헐거워진다.
- **어드민은 아직 못 들어간다** — `typecheck` 가 통과하지 않고 빌드가 유틸리티 CSS 를
  내지 않는다(DEV-14). 그 카드가 먼저다.
- **남은 것은 관측과 E2E 다.** 오류 수집·분석 도구가 없어 운영에서 무엇이 깨졌는지
  알 길이 로그뿐이고, 가입 → 무료 학습 → 벽 → 복습의 세로 흐름을 자동으로 걷는 것이 0개다.
- **오류 수집·분석 도구가 없다.** 운영에서 무엇이 깨졌는지 알 길이 로그뿐이다.
- **E2E 가 없다.** 가입 → 무료 학습 → 벽 → 복습의 세로 흐름을 자동으로 걷는 것이 0개다.

**최소 테스트**

- API: 게스트 생성과 가입 이전, 첫 시도 기록, 이어하기, 복습 큐, 권한 네 유형, 신고 인증.
- 결제가 붙은 뒤: 웹훅 서명·멱등·갱신·환불·만료.
- 메일이 붙은 뒤: 재설정 정상·만료·재사용·열거 방지.
- 브라우저 세로 흐름: QR → 홈 → 무료 활동 → 가입 → 잠긴 과 → 결제 안내 → 권한 갱신 → 학습 재개.

**CI 필수 단계**

1. `cd app && pnpm typecheck && pnpm check && pnpm check:css && pnpm parity:activity && pnpm build`
2. `python3 docs/check_docs.py`
3. API 단위/통합 테스트와 `api/smoke_test.py`
4. DB 마이그레이션 사전 검사

**운영 관측**

- 서버 오류 수집: 요청 ID, 라우트, 상태 코드, 사용자/게스트의 비식별 ID. 토큰·비밀번호·음성 원문은 금지.
- 핵심 이벤트: paywall_view, checkout_start, purchase_success/fail, lesson_start/complete,
  review_start/complete. 이벤트 이름·속성·동의 범위를 문서화한다.
- 결제 웹훅 실패, 메일 반송, STT 삭제 실패, 신고 저장 실패는 운영 알림을 둔다.

**완료 판정**

- 기본 브랜치의 모든 변경은 CI를 통과해야 병합된다.
- 의도적으로 실패시킨 API/웹훅/삭제 작업이 오류 수집과 알림에서 식별된다.
- 장애 대응 문서에 담당자, 확인 화면, 재시도와 롤백 절차가 있다.

### DEV-11 · 계정 데이터 열람·내보내기와 가입 연령 — BLOCKED(PD-08·PD-10) · P0

**목적:** "요구할 수 있고 탈퇴할 수 있습니다"를 문구가 아니라 실제로 처리 가능한 절차로 만든다.

**현재:** **탈퇴는 끝났다**(§1). 없는 것은 그 옆의 둘이다.

- 개인정보 **열람·내보내기** API가 없다. 앱에도 요청 창구가 없다.
- 생년월일·연령을 받는 자리가 코드에 **0곳**이다. 만 14세 미만을 걸러낼 방법도, 법정대리인 동의를 남길 자리도 없다.

**구현 범위**

1. PD-10이 "앱에서 직접 제공"이면 본인 데이터 내보내기 API를 만든다. "문의 처리"면 창구·처리 기한·전달 형식을 화면과 개인정보 문서에 적는다.
2. 내보내기 응답에 다른 사용자의 데이터나 음성 원문 URL이 섞이지 않게 한다.
3. PD-08에 따라 가입 화면에 생년월일 또는 연령 확인을 넣고, 거부 시 문구와 경로를 정한다.
4. 법정대리인 동의를 받는다면 동의자·방법·시각을 DEV-09의 동의 증거와 같은 표에 남긴다.

**완료 판정**

- 내보내기 결과에 그 사용자의 것만 들어 있고 재현 가능한 형식이다.
- 만 14세 미만 가입 시도가 정책대로 처리되고 그 판정 근거가 남는다.
- 탈퇴 뒤 같은 계정으로 내보내기를 요청하면 데이터가 없다.

### DEV-12 · 외부 AI 호출의 타임아웃과 실패 화면 — READY · P0

**목적:** 외부 응답이 늦으면 학생이 몇 분을 빈 화면에서 기다리는 지금 상태를 없앤다.

**현재:** `api/xternal/` 여섯 중 **`openai.py`와 `gcloud.py`만 타임아웃이 0곳이다**
(`gemini.py`·`rtzr.py`·`tutorus.py`·`slack.py`에는 있다). 그런데 그 둘을 지나는 것이
**STT·TTS·미션 대화 응답·미션 완료 판정 전부**다. SDK 기본값에 맡겨져 있어 실질적으로 멈춘 것과 같다.
재시도는 `gemini.py` 한 곳에만 있다.

**구현 범위**

1. `openai.py`·`gcloud.py`의 모든 호출에 연결·읽기 타임아웃을 준다. 값은 화면이 기다릴 수 있는 시간에서 거꾸로 정한다.
2. 스트리밍과 비스트리밍을 갈라 잡는다 — 첫 토큰까지의 시간과 전체 시간은 다르다.
3. 재시도는 **멱등한 호출에만** 넣고 횟수·간격을 적는다. 채팅 응답을 말없이 두 번 만들지 않는다.
4. 앱은 타임아웃을 오류가 아니라 **다시 시도할 수 있는 상태**로 그린다. 녹음·입력 내용을 잃지 않는다.

**완료 판정**

- 외부를 일부러 늦춰 놓고 각 활동이 정해진 시간 안에 실패 화면에 도달한다.
- 실패 뒤 재시도가 되고, 재시도로 중복 기록·중복 과금이 생기지 않는다.
- 타임아웃과 그 뒤의 재시도가 운영 관측(DEV-10)에서 구분되어 보인다.

### DEV-13 · 미션 대화 DB를 검수된 원장으로 다시 채우기 — BLOCKED(DB 환경) · P1

**목적:** 브리핑은 검수된 내용을 보여 주는데 실제 대화는 구 앱 덤프로 도는 지금의 갈라짐을 없앤다.

**현재:** 브리핑(`/learn/mission-chat`)은 원장 JSON을 읽지만, **대화 화면(`mission-dialog.tsx`)은
구 앱 덤프(`dialog.ts`·`dialog_keyword.ts`)를 쓰고 서버 `ko_chat_dialog`는 v29 이전 내용을 들고 있다.**
미션 완료 판정이 프론트 `missionList`의 라벨로 도는데, 원장은 그 뒤로 두 번 더 나아갔다(v29 라벨 재정의,
v42의 6·7·8권 45항목). 헬퍼 `parseMissionDetail` 은 **브리핑 화면이 이미 쓴다**(`routes/learn/mission-chat.tsx:48`, 2026-08-24 부터). **대화 화면이 안 쓴다** — 그쪽은 여전히 `dialog_keywords` 로 `missionList` 를 만든다.<br>**여기 「부르는 곳이 0곳」이라고 적혀 있었다.** 그 문장은 2026-08-28 의 「인계서를 코드와 다시 맞춘다」 커밋에서 들어왔는데, 그때 이미 나흘째 불리고 있었다 — **코드와 맞춘다고 하면서 코드를 안 본 자리다.**
자세한 것은 `BLOCKERS.md` §8.

**구현 범위**

1. 어느 환경(스테이징·프로덕션)에 적용할지와 **백업 절차를 먼저 정한다.**
2. `ko_chat_dialog`에 원장의 `ai_persona_prompt`·`ai_first_line`·`mission_detail`·`situation_ko`를 UPSERT한다.
3. `id`(Integer)와 프론트가 넘기는 `dialogId`(문자열, 예 `"C4"`)가 실제로 맞물리는지 확인한다 — `chat_accepter.py`와 `repo_chat.py`의 타입이 갈려 있다.
4. 위 둘이 끝난 뒤에 화면을 `@/shared/data/mission-chat`으로 옮긴다.

**완료 판정**

- 브리핑에 뜬 미션 키워드와 대화 화면의 완료 판정이 **같은 라벨**을 본다.
- 라벨이 바뀐 과에서 완료 판정이 조용히 깨지지 않는다(바뀐 과를 골라 실제로 완료까지 해 본다).
- 되돌리기 절차와 적용 전후 행 수가 PR에 있다.

### DEV-14 · 어드민의 조용한 데이터 어긋남과 없는 게이트 — READY(일부 PD 대기) · P0

**목적:** 어드민이 2026-08-28 에 학교가 직접 쓰는 도구가 됐는데, **화면을 보고는 알 수 없는
어긋남 둘**과 **어긋남을 알려 줄 장치가 없는 것 셋**이 남아 있다. 명세는
`access_and_pricing_v1.html` §10~§12 — 아래는 거기 적지 않은 **위험 다섯**을 작업 카드로 옮긴 것이다.

**현재**

- `PATCH /school/{id}` 로 `school_code` 를 바꿀 수 있고 어드민 화면이 그 칸을 편집으로 내보인다
  (`school.tsx` 가 `editCode` 를 그대로 보낸다). **`ko_user.school_code` 는 따라오지 않는다** —
  FK 가 아니라 값으로 잇고 있어서 **그 학교 학생 전원이 조용히 고아가 된다.** 막지도 경고하지도 않는다.
- `ko_signup_code_use` 가 `shared/withdrawal_scope.py` 의 지우기 범위에 없다. 탈퇴한 학생의
  `user_id` 가 남아 이미 없는 계정을 가리킨다 — **재현했다(2026-08-28).**
- `admin` 의 `pnpm typecheck` 가 통과하지 않는다 — 오류가 `routes/game/vocashot/host.$pin.tsx`
  한 파일에 있다. **리뉴얼 전부터다.**
- `admin` 의 `pnpm build` 가 Tailwind 유틸리티 CSS 를 내지 않는다. 개발 서버에서는 보이므로
  **배포본에서만 어긋난다.** 이것도 이전부터고, 기존 화면의 클래스도 같이 없다.
- 운영 도구(QR 통계·STT 표본·TTS 시험)가 학교 계정에 화면으로 보이는지 **눌러서 확인하지 않았다.**
  서버 쪽 문(`MasterAdminRequired`)은 있다.

**구현 범위**

1. `school_code` 편집을 **막거나**, 바꿀 때 `ko_user.school_code` 와 `ko_signup_code.school_code`
   를 같은 트랜잭션에서 따라 옮긴다. **어느 쪽인지는 기획이 정한다** — 학교 코드를 정말 바꿀 일이
   있는지에 걸린다. 막는 쪽이면 한 줄이고, 옮기는 쪽이면 되돌리기 절차가 필요하다.
2. `ko_signup_code_use` 를 지우기 범위에 넣거나, 행은 남기고 학생 식별자만 비운다.
   **처리방침 제4조가 탈퇴 시 파기를 약속하므로** 남기는 쪽을 고르면 그 조문도 같이 고친다.
   `used_count` 는 **되돌리지 않는다** — 소비된 자리가 돌아오면 안 된다.
3. `admin` 의 타입 오류를 없애고 `typecheck` 를 게이트로 세운다.
4. `admin` 빌드의 유틸리티 CSS 누락을 고친다. **고쳤는지는 빌드 산출물에서 클래스를 grep 해서
   본다** — 개발 서버 화면으로는 판정할 수 없다.
5. 학교 계정으로 로그인해 운영 도구 주소를 직접 입력해 본다. 서버가 막아도 화면이 보이면
   내비게이션에서 숨긴다.

**완료 판정**

- 학교 코드를 바꾼 뒤 그 학교 학생의 `GET /entitlement` 가 여전히 `source:"school"` 이다
  (막는 쪽을 골랐으면 **바꾸려는 시도가 거절되고 이유가 화면에 뜬다**).
- 코드로 가입한 학생을 탈퇴시킨 뒤 `ko_signup_code_use` 의 상태가 **정한 대로**다 —
  0행이거나, 행은 있고 학생 식별자가 비어 있다. **재현 절차와 전후 행 수가 PR 에 있다.**
- `cd admin && pnpm typecheck` 가 0 이다.
- `admin` 빌드 산출물에서 학생 화면이 쓰는 유틸리티 클래스가 실제로 grep 된다.
- 학교 계정으로 운영 도구 주소를 직접 쳤을 때의 결과가 PR 에 적혀 있다.

---

---

## 4. 권장 실행 순서

| 단계 | 기획 | 개발 |
|---|---|---|
| A · 즉시 | PD-01~07 답변 준비 | DEV-01 QR, DEV-02 신고, DEV-10 CI·관측 기반, DEV-12 AI 타임아웃 |
| B · 외부 연동 기반 | 메일 사업자·보관일 승인 | DEV-03 재설정, DEV-06 음성 운영 |
| C · 접근과 과금 | 가격·PG·보호 수준 확정 | DEV-04 결제·권한, DEV-05 콘텐츠 보호 |
| D · 콘텐츠와 법률 | 게이트·정확도·최종 문서·연령·열람 방식 확정 | DEV-07 게이트, DEV-08 MY, DEV-09 동의 증거, DEV-11 계정 데이터·연령, DEV-13 미션 대화 DB |
| E · 어드민 정리 | 학교 코드를 바꿀 일이 있는지 · 코드 사용 기록을 지울지 | DEV-14 어드민 데이터 어긋남과 게이트. **앞 단계와 병렬로 할 수 있다** — 다른 카드에 걸리지 않는다 |
| E · 출시 후보 | 상태별 화면 최종 승인 | 전체 세로 흐름·부하·보안·롤백 검증 |

작업은 카드별 PR로 나눈다. DB 마이그레이션과 그 마이그레이션을 쓰는 API는 같은 배포 계획에
묶되, DEV-02는 **클라이언트 하위 정렬을 먼저 배포한 뒤** DB 폭을 늘리는 순서를 지킨다.

---

## 5. 개발자가 각 작업에서 돌려줘야 하는 것

“완료했습니다”만으로 인계가 끝나지 않는다. 작업 카드마다 아래를 반환한다.

1. **변경 요약:** 사용자에게 무엇이 달라졌는지 3줄 이내.
2. **계약:** API 요청·응답·오류 코드, DB 변경, 환경변수. 비밀값은 쓰지 않는다.
3. **마이그레이션:** 적용 순서, 기존 데이터 처리, 되돌리기 또는 되돌릴 수 없는 이유.
4. **검증 증거:** 자동 테스트 결과와 상태별 화면/응답. “직접 눌러 봄”만 적지 않는다.
5. **운영:** 대시보드·알림·배치 일정·담당자·재시도 방법.
6. **배포/롤백:** 기능 플래그, 배포 순서, 롤백 시 데이터 호환성.
7. **남은 위험:** 임시 처리, 알려진 제한, 다음 작업. 없으면 “없음”이라고 쓴다.
8. **문서 갱신:** 이 문서의 카드 상태와 관련 정본 문서. 과거 기록을 새 사실처럼 덮어쓰지 않는다.

### 상태 보고 형식

```text
[DEV-__] 작업명
상태: READY / IN PROGRESS / BLOCKED / REVIEW / DONE
이번에 끝낸 것:
검증:
운영 반영:
막힌 결정 또는 외부 의존:
다음 행동과 담당자:
관련 PR/마이그레이션/문서:
```

---

## 6. 전체 출시 완료의 정의

- QR 신규 사용자가 로그인 벽이 아니라 홈에서 무료 학습을 시작한다.
- 가입·기록 이전·비밀번호 재설정이 실제 서버와 메일로 끝까지 동작한다.
- 잠금, 결제, 웹훅, 권한 복원, 만료·환불이 서버 상태 하나로 수렴한다.
- 합의한 보호 수준에서 잠긴 콘텐츠가 주소 직접 입력으로 열리지 않는다.
- 표현클립 신고가 저장되고 한 사람의 신고로 전역 삭제되지 않는다.
- 콘텐츠 공개 상태와 음성 보관기간이 빌드·배치에서 자동 강제된다.
- 약관·개인정보 문서가 실제 운영값과 맞고 동의 증거가 남는다.
- 외부 AI가 늦거나 끊겨도 학생이 정해진 시간 안에 다시 시도할 수 있는 화면에 도달한다.
- 미션 대화가 검수된 원장 내용으로 돌고, 브리핑과 완료 판정이 같은 라벨을 본다.
- 계정 데이터 열람·내보내기 요청과 가입 연령 정책이 정해진 방법으로 처리된다.
- CI·오류 수집·핵심 운영 알림·롤백 절차가 있으며 출시 후보 세로 흐름을 통과한다.

이 조건 중 정책 결정이 비어 있으면 개발 완료로 표시하지 않고 해당 PD 번호로 **BLOCKED** 처리한다.
