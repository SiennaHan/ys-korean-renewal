# 목업 장부 — 화면 승격 이력과 버전 이력

**2026-08-29 에 `mockup_version_history.md` 를 여기로 합쳤다.** 둘 다 「목업 파일을
어떻게 다루나」였는데 파일이 갈려 있어서, 화면 하나를 승격할 때 **두 장부에 각각
적어야 했다.** 실제로 한쪽만 적힌 적이 있다.

## 무엇을 승격하나 — 기준

**「확정한 디자인이니까」로 승격하지 않는다. 제품이 그 컴포넌트를 실제로 쓰는지를 본다.**

    확정 컴포넌트를 제품이 안 쓰는 채로 승격하면,
    검사가 지키는 것이 제품 화면이 아니라 **디자인 쪽 사본**이 된다.

2026-08-25 Claude Code 세션 협의에서 정했다. 그날 상태 감사가 승격 후보 21개를 냈는데
**전부 보류**로 바꿨다 — 확정 컴포넌트 여섯(`ChatScreen`·`ReportScreen`·`RoleplayScreen`·
`FlashcardScreen`·`ResultScreen`·`BriefingScreen`)을 제품 라우트가 안 쓰고 있었기 때문이다.
경위와 7단계 순서는 `docs/state_audit/README.md` 의 「승격은 보류다」 절이다
(1·3·4·5 는 「했다」로 표시돼 있고 2·6·7 이 남았다. 마지막이 「그 뒤에 **승인된 상태만**
정본에 더한다」다).

> **이 기준을 여기 적는 이유.** 2026-09-03 에 「승격 기준이 저장소에 한 줄도 없다」는
> 판정이 나왔다. 실제로는 `state_audit/README.md` 에 있었지만 **이 장부에는 없었고**,
> 승격을 하려면 먼저 펼치는 문서가 이쪽이다. 기준과 장부가 갈려 있으면 장부만 읽고
> 승격한다.

## `_snapshots/` 와 `screens_ref/` 가 갈라진 곳

두 폴더의 뜻이 다르다.

| | 무엇 | 규칙 |
|---|---|---|
| `app/src/screens_ref/` | **대조의 기준.** `pnpm parity:activity` 가 이것과 견준다 | 정본이 앞으로 나가면 여기를 다시 뜬다 |
| `docs/_snapshots/` | **시점 기록.** 처음 목업을 떴을 때의 날것 | **덮어쓰지 않는다** |

그래서 둘은 **앞으로 영원히 갈라진다.** 갈라지는 것 자체는 잘못이 아니고,
**왜 갈라졌는지 적히지 않은 것**이 잘못이다. 아래 표가 그 기록이고,
`check_docs.py` 의 `[화면 승격]` 이 표에 없는 화면을 잡는다.

전에는 이 표가 `check_docs.py` 안의 `TWIN_ALLOW` 라는 파이썬 사전이었다.
검사 이름도 `[목업 쌍둥이]` 로 "둘이 같아야 한다" 는 뜻이었는데, 실제로는
승격할 때마다 항목이 하나씩 느는 **장부**였다. 2026-08-26 에 이름과 자리를
하는 일에 맞췄다.

## 표

| 화면 | 왜 갈라졌나 |
|---|---|
| `activity__result` | 목업 v3.0 승격(2026-08-27). 제목이 상태를 말한다 — `잘 했어요!` / `아직 남았어요`. 전에는 셋 다 「다 했어요!」 하나였는데, **완료 기준이 바뀌면서 거짓말이 됐다**(기획 확정 2026-08-27: 건너뛴 것 없이 모두 응답해야 완료). 목업 표본이 마침 3/4 인 부분 상태라 「다 했어요! · 4개 중 3개 풀었어요」로 **한 화면이 스스로와 어긋나 있었다**. 정본 §28 은 원래부터 결과 화면을 상태 셋으로 갈라 뒀는데(완전 정답 · 내일 복습 · 미해결 있음) 제목만 하나를 돌려쓰고 있었다.<br>다 풀었을 때도 「다 맞았어요」라고는 하지 않는다 — 채점하지 않는 활동(자모 여섯·롤플레잉·플래시카드)이 있어 맞았는지를 이 자리에서 단정할 수 없다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__chat` | 목업 v3.0 승격(2026-08-26). **정본이 제품보다 한 세대 뒤였다.** 제품은 08-25 에 시나리오를 파란 배너로 바꾸고 상황 그림 펼치기·미션 접기 패널·키보드 입력을 얹었는데(`2ec44ff`) 정본은 회색 접기 배너에 평평한 미션 줄 그대로였다. `screens_SOT.html` 의 `missionChat()` 을 지금 화면으로 다시 쓰고 (`.mission-chat-header` 껍데기 · `button.mission-title` · `.mission-list` · `.mission-panel` · `.chat-compose` · `.chat-end-anchor`) 여기를 다시 떴다.<br>**반대 방향도 하나 있다** — 말풍선은 제품이 뒤졌다. 정본이 `.msg`/`.bubble` 로 정한 자리를 제품은 tailwind 유틸(`rounded-[15px]`·`bg-[#F6F7F8]`)로 그리고 있었는데, 그 유틸은 이미 `activity.css` 의 `.mission-*` 규칙이 전부 덮어서 **죽은 글자**였다. 죽은 채로 두면 정본이 이 화면을 적을 수가 없다(정본에는 tailwind 가 없다). 유틸을 걷고 이름으로 옮겼다 — 눈에 보이는 변화는 없다.<br>`aria-label` 과 `data-action="chatFold|chatMissions"` 도 양쪽을 맞췄다. `.record-limit` 은 이 화면에서 1x1·opacity 0 이라 목업이 안 그린다 — 대조에서 뺀다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__role` | 목업 v2.9→v3.0 승격(2026-08-26). 먼저 진행바 칸을 `<i>`에서 누를 수 있는 `<button class=seg>`로 바꿔 이전 문항 탐색을 열었다. 이어 대본 줄의 소리 아이콘을 `<button aria-label="AI|나">`로 바꿨다 — 제품의 `handleReplayTurn`은 지나온 줄을 다시 듣게 하지만 정본은 눌리지 않는 그림이었다. `RoleplayLayout` 연습순서 버튼의 `data-action="roleAi|roleMe"`도 되돌렸다.<br>**대사 줄 세로 리듬 보정(2026-08-28).** 오른쪽 스피커 칸이 버튼 없는 줄까지 `min-height:32px`로 벌려 한 줄 대사가 위로 치우쳤다. 칸은 24px, 실제 버튼만 32px와 `margin:-4px 0`을 지켜 위아래를 맞췄다. 행은 49→41px이고 여러 줄에서도 배지와 첫 글줄이 맞는다. 앱과 정본을 함께 옮겼다. _snapshots/는 당시 날것이라 그대로 둔다 |
| `activity__flash` | 목업 v3.0 승격(2026-08-26). `.flash-card` 를 `.flash-motion` 한 겹이 감싼다. **제품이 이 화면을 그리게 만들면서 생긴 층이다** — 카드를 끌어 넘기려면 framer-motion 이 붙을 자리가 하나 있어야 한다. 눈에는 아무 차이가 없다: activity.css 에서 `.flash-motion` 도 `.flash-card` 도 똑같이 `position:absolute; inset:0` 이라 겹쳐도 같은 자리다. 전에는 라우트가 같은 마크업을 손으로 한 벌 더 갖고 있었고, 그래서 대조는 **아무도 안 보는 쪽**을 보고 있었다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__briefing` | **v2.7(2026-08-26)** 상황 이미지 자리표를 `<span aria-hidden>`으로 감쌌다. **v2.8(2026-08-28)** 섹션 제목을 `Conversation Scenario`→`대화 상황`, `Mission Keyword`→`미션 키워드`로 옮기고 다섯 로케일과 정본을 함께 맞췄다.<br>**v3.1(2026-09-01)** 둘이 바뀌었다. ① 「미리 볼 낱말」 띠(`word-strip`)를 걷었다. 채우는 데이터가 원장·서버에 없고 라우트가 `words: []`를 넘겨 실제 앱에서는 한 번도 보이지 않았기 때문이다. 목업만 약속하던 빈 구조라 규칙·타입·호출부까지 같이 걷었다. ② 아래 `activity__briefing_hint`의 힌트 버튼이 그 자리를 대신한다. _snapshots/는 각 시점의 날것이라 그대로 둔다 |
| `activity__briefing_hint` | **새 상태다**(2026-09-01) — `_snapshots/` 에 짝이 없는 것이 맞다. 미션 브리핑에 **힌트 버튼**이 생겼다. 미션 키워드 카드 **밖, 그 아래**에 작은 알약으로 붙고, 누르면 그 아래에 카드가 하나 더 열려 미션마다 모범 문장과 번역을 보여 준다. 닫힘·열림 두 상태라 화면이 하나 늘었다 — `write`·`write3` 이 조합/따라쓰기를 나눠 뜬 것과 같은 이유다.<br>**대화 화면에는 이 버튼이 없다.** 그것이 이 설계의 요점이다 — 대화 중에는 베낄 문장이 화면에 없어서 미션 발화가 전부 기억에서 나온다. 그래서 「스스로 말한다」를 재는 자리가 브리핑 한 곳으로 좁아진다(개발 요청 v53 §2 는 대화 중 힌트를 전제로 필드 다섯을 요구했는데, 이 설계에서는 `hint_opened_at_briefing` 하나로 준다). 지원을 버튼 뒤 하나로 모은 것도 같은 이유다 — 늘 보이는 낱말 띠가 남아 있으면 전원이 답의 절반에 노출돼 그 구분이 사라진다.<br>정본(`screens_SOT.html`)의 `briefingView()` 를 먼저 고치고 여기를 다시 떴다 — `.hint-toggle` · `.hint-card` · `.kw-hint` 와 `state.briefHint`(자모 쓰기의 `hintOn` 과 이름이 겹치지 않게 골랐다), `data-action="briefHint"`. 힌트 줄의 키워드 칩은 `kw-line` 과 같은 100px 폭이라 두 카드의 왼쪽 기둥이 맞는다.<br>**열림 상태를 대조에 넣지 않으면 모범 문장이 어떻게 그려지는지 아무도 안 본다** — 힌트는 브리핑에만 있는 길이라 볼 자리가 여기밖에 없다 |
| `activity__wordQuiz_image` | 새 화면이다(2026-08-26) — _snapshots/ 에 짝이 없는 것이 맞다. 원장의 어휘 문제는 갈래가 둘인데(meaning-to-word 950 · image-to-word 196) 정본은 그림 갈래를 그리지 않았고, 대신 원장에 0건인 낱말+발음듣기 갈래를 그리고 있었다. 그 갈래를 빼고 그림 갈래를 넣으면서 화면이 하나 늘었다 |
| `activity__jamoListen` | 목업 v2.9 승격(2026-08-26). 진행바 칸이 `<i>` 에서 누를 수 있는 `<button class=seg>` 가 됐다 — 앱이 35589a2(08-20)에서 먼저 정한 것을 정본이 따라갔다. 하단 도크가 목업의 전폭 버튼을 지키느라 앞 문항으로 돌아갈 길이 없어서 그 길을 진행바로 옮긴 것이다. 그 커밋이 '대조는 영향을 받지 않는다' 고 적어 둔 자리라 여태 안 보였다. 플래시카드·로딩은 제품도 안 넘기므로 그대로다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__reading` | 목업 v2.9 승격(2026-08-26). 진행바 칸이 `<i>` 에서 누를 수 있는 `<button class=seg>` 가 됐다 — 앱이 35589a2(08-20)에서 먼저 정한 것을 정본이 따라갔다. 하단 도크가 목업의 전폭 버튼을 지키느라 앞 문항으로 돌아갈 길이 없어서 그 길을 진행바로 옮긴 것이다. 그 커밋이 '대조는 영향을 받지 않는다' 고 적어 둔 자리라 여태 안 보였다. 플래시카드·로딩은 제품도 안 넘기므로 그대로다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__wordQuiz` | 목업 v2.9 승격(2026-08-26). 진행바 칸이 `<i>` 에서 누를 수 있는 `<button class=seg>` 가 됐다 — 앱이 35589a2(08-20)에서 먼저 정한 것을 정본이 따라갔다. 하단 도크가 목업의 전폭 버튼을 지키느라 앞 문항으로 돌아갈 길이 없어서 그 길을 진행바로 옮긴 것이다. 그 커밋이 '대조는 영향을 받지 않는다' 고 적어 둔 자리라 여태 안 보였다. 플래시카드·로딩은 제품도 안 넘기므로 그대로다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 같은 판에 표본을 낱말+발음듣기 갈래에서 뜻 갈래로 바꿨다 — 원장에 있는 갈래다. |
| `activity__write3_canvas` | 목업 v2.9 승격(2026-08-26). 진행바 칸이 `<i>` 에서 누를 수 있는 `<button class=seg>` 가 됐다 — 앱이 35589a2(08-20)에서 먼저 정한 것을 정본이 따라갔다. 하단 도크가 목업의 전폭 버튼을 지키느라 앞 문항으로 돌아갈 길이 없어서 그 길을 진행바로 옮긴 것이다. 그 커밋이 '대조는 영향을 받지 않는다' 고 적어 둔 자리라 여태 안 보였다. 플래시카드·로딩은 제품도 안 넘기므로 그대로다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__write_canvas` | 목업 v2.9 승격(2026-08-26). 진행바 칸이 `<i>` 에서 누를 수 있는 `<button class=seg>` 가 됐다 — 앱이 35589a2(08-20)에서 먼저 정한 것을 정본이 따라갔다. 하단 도크가 목업의 전폭 버튼을 지키느라 앞 문항으로 돌아갈 길이 없어서 그 길을 진행바로 옮긴 것이다. 그 커밋이 '대조는 영향을 받지 않는다' 고 적어 둔 자리라 여태 안 보였다. 플래시카드·로딩은 제품도 안 넘기므로 그대로다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__wordrep` | 목업 캡처가 정본보다 낡았다(2026-08-26 재캡처). shell_spec 이 2026-08-25 에 '자모 발음·단어 듣고 따라 말하기·단어 읽고 쓰기 = 진행바 숨김(내부 탐색)' 으로 확정했고 정본 프로토타입도 이미 진행바를 안 그린다 — 캡처만 그 전 것이었다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__speak` | 목업 캡처가 정본보다 낡았다(2026-08-26 재캡처). shell_spec 이 2026-08-25 에 '자모 발음·단어 듣고 따라 말하기·단어 읽고 쓰기 = 진행바 숨김(내부 탐색)' 으로 확정했고 정본 프로토타입도 이미 진행바를 안 그린다 — 캡처만 그 전 것이었다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__readwrite` | 목업 캡처가 정본보다 낡았다(2026-08-26 재캡처). shell_spec 이 2026-08-25 에 '자모 발음·단어 듣고 따라 말하기·단어 읽고 쓰기 = 진행바 숨김(내부 탐색)' 으로 확정했고 정본 프로토타입도 이미 진행바를 안 그린다 — 캡처만 그 전 것이었다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__listen` | 목업 v2.9 승격(2026-08-26). 둘 다 앱이 먼저 정한 것을 정본이 따라갔다 — ① O/X 카드 라벨 '들은 문장' → '제시 문장'(f8bd0aa 08-20). 들은 것은 오디오뿐이고 그 글자는 맞는지 가릴 대상이라 뜻이 달랐다. ② 선택지를 .listen-response 로 한 겹 감싸 위 여백을 줬다(66b0e94 08-25). _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 같은 판에 진행바 칸도 눌러 갈 수 있는 button 이 됐다(35589a2 승격). |
| `activity__grammar` | 목업 v2.9 승격(2026-08-26). 빈칸을 <u> </u> 에서 <span class=blank-slot> 으로 — 앱이 66b0e94 '빈칸 표기를 통일한다'(08-25)에서 먼저 정한 것을 정본이 따라갔다. 폭이 고정이고 보조기술이 투명한 전각 공백을 읽지 않는다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 같은 판에 진행바 칸도 눌러 갈 수 있는 button 이 됐다(35589a2 승격). |
| `activity__failed` | 목업 v2.8 승격(2026-08-26). 한 줄 문장만 떠 있던 것을 아이콘 + 제목 + 설명으로 바꿨다 — draft_activity_controls 에 시안으로만 그려져 있고 정본에 올라온 적이 없던 디자인이다. _snapshots/ 는 v2.7 시점 기록이라 그대로 둔다 |
| `activity__micdenied` | 위 activity__failed 와 같은 승격. 문구도 정본을 따라 바뀌었다 — '마이크가 꺼져 있어요' → '마이크를 사용할 수 없어요' + 설명. _snapshots/ 는 v2.7 시점 기록이라 그대로 둔다 |
| `activity__report` | 목업 v2.7 승격(2026-08-26). 탭을 <div> 에서 진짜 <button role=tablist·aria-selected> 로 바꾸고, 카드·평가 행의 인라인 style 을 .report-card·.report-rows 로 뺐다. _snapshots/ 는 v2.6 시점 기록이라 그대로 둔다 |
| `vocashot__play_type` | 직접 입력 줄을 <div> 대신 <form> 으로 감쌌다 — Enter 로 제출된다. 정본은 <div> 라 키보드만 쓰면 버튼까지 Tab 해야 했다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `nav__book__resume` | 활동 이름을 기획자가 확정했다(2026-08-25, 66b0e94 '빈칸 표기를 통일한다') — '빈칸 채워 말하기' → '빈칸 채우기'. 앱이 쓰는 i18n(ko.ts 'fill-blank')과 screens_ref 를 같이 바꿨고, _snapshots/ 는 그때 뜬 날것이라 그대로 둔다. 차이는 이 한 줄뿐이다 |
| `nav__home__none` | 캡처는 탭바가 위·홈 비활성. screens_ref 가 아래·활성으로 고친 판(08-20). **그리고 차트 제목을 기획자가 확정했다(2026-08-26)** — 「주간 활동」→「주간 학습 시간」. 차트가 그리는 값은 `study_seconds // 60`, 즉 분(分)인데 제목이 활동이라 바로 위 「오늘 활동 수·주간 활동 수」와 같은 것으로 읽혔다. 영어는 `chartTitle` 과 `weeklyActivities` 가 아예 같은 문자열이었다("This week"). 이름만 사실에 맞추고 데이터는 그대로 뒀다 — 5개 언어와 `screens_SOT` · 홈 세 화면의 screens_ref 를 같이 고쳤다. `_snapshots/` 는 그때 뜬 날것이라 그대로 둔다 |
| `nav__home__resume` | 같음 |
| `nav__home__review` | 위와 같은 탭바 사정. **2026-08-27 에 문구가 한 번 더 나아갔다** — 다시 풀기 목록에 건너뛴 문항이 들어오게 되면서 「어제 틀린 문항부터 만나요」가 절반만 참이 됐다. 「어제 **틀리거나 건너뛴** 문항부터 만나요」로 5개 언어와 `screens_SOT` · 이 캡처를 같이 옮겼다. **그리고 이 카드에 기다리는 갈래가 생겼다** — 오늘 틀리거나 건너뛴 문항은 `available_at` 이 다음 날 0시라 개수는 뜨는데 낼 문항이 없었다(눌러도 아무 일이 없는 버튼). 그때는 `<div class="task waiting">` 로 그리고 화살표를 빼고 「오늘 틀리거나 건너뛴 문항이에요 · 내일부터 풀 수 있어요」를 낸다. **그 갈래는 캡처를 따로 만들지 않았다** — 같은 카드의 문구와 화살표만 다른 변형이라 화면 수를 늘리지 않는 것이 낫다고 봤다. 대조 픽스처는 `reviewReady` 를 켜서 목업이 그린 갈래(오늘 풀 수 있는 쪽)를 그린다.  **그리고 기획 확정으로 정본이 한 번 더 나아갔다(2026-08-26)** — 홈의 '오늘 할 일' 이 한 자리를 세 갈래가 나눠 쓰다 보니 다시 풀 것이 하나라도 있는 동안 **이어하기 버튼이 사라졌다.** 앱을 열자마자 하려는 일은 '하던 데서 계속' 인데 그 자리를 다시 풀기가 이긴 것이다. 맨 위를 최근 학습 바로가기로 고정하고 다시 풀기를 그 아래로 내렸다 — 그래서 홈 셋 중 이 화면만 카드가 둘이다(resume + review). `screens_SOT.html` 도 같은 판에 고쳤으니 앱이 목업을 앞지른 것이 아니라 둘을 같이 옮겼다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `game__pc_result` | 캡처의 🔊를 `aria-label`이 있는 버튼으로 감쌌다(2026-08-24). 이어 오답 노트 숫자 알약을 고정 38×38에서 내용을 감싸는 형태로 바꿨다(2026-08-27). 실제 `num`은 `20,000원`·`102동 804호`처럼 길 수 있어 고정 정사각형에서는 문장 위로 넘쳤다. 짧은 값은 여전히 38px이고 긴 값만 옆으로 자란다. |
| **전 화면 (간격)** | **2026-08-28 에 간격을 4의 배수 눈금으로 스냅했다**(기획자 확정, `DESIGN.md` 5-b). 가장 가까운 4의 배수, 같은 거리면 내림(375px 틀이라 넓히는 쪽이 위험하다). 1·2px 과 파형 막대 간격은 면제다 — 리듬이 아니라 광학 보정이라 4로 올리면 눈에 띈다. **앱 166곳과 목업 142곳을 같이 옮겼으므로 갈라지지 않는다.** 목업은 `screens_SOT.html` 의 `<style>` 안에서 **출처 배너로 구간을 잘라** 활동·내비 구간만 옮겼다 — 그 파일은 프로토타입 자체 UI·VocaShot·게임 CSS 가 한 블록에 섞여 있고 클래스 이름까지 겹쳐서(`.g-go`·`.hearts`·`.caption`) **이름으로는 못 가른다.** 통째로 돌리면 484곳이 걸린다. VocaShot 배너 이후 구간이 바이트까지 같은 것을 확인했다. **목업 대조는 어차피 이 차이를 못 본다**(마크업만 견준다) — 그래서 적어 둔다. 다시 어긋나지 않게 `app/scripts/spacing-grid-check.py` 를 `pnpm check:css` 에 붙였다 |
| `game__sp_map` | 장소 카드 10개가 캡처에는 div 였다 — 장소로 들어가는 유일한 경로인데 키보드로 닿지 않아 screens_ref 를 button 으로 고친 판(08-24). 지도 핀은 SVG <g> 라 초점을 못 받는다 |
| `game__sp_entry` | 위 sp_map 과 같은 사정 — .sp-loc-card 의 버튼 기본값 되돌림(text-align·font·color)이 공유 <style> 블록에 들어갔다. **그리고 2026-08-27 에 진입 대화 세 줄에 🌐 를 넣었다** — 퍼즐 화면(`game__sp_puzzle`)에는 있는데 여기만 없었고, 그래서 장소에 들어설 때 나오는 대화는 한국어를 읽을 길이 없었다. 씨드에 번역을 넣으면서(`entryMessages[].t`) 같이 붙였다. 겉 말풍선은 목업 그대로 두고 **안쪽만** 퍼즐 화면과 같은 `BubbleInner` 로 갈았다 — 겉까지 묶으면 자기 말풍선의 `ux-travel-header` 그라데이션(game.css 581행)이 사라져 색이 바뀐다. 🌐 마크업은 `game__sp_puzzle` 캡처에 있는 것을 글자 그대로 옮겼다. `_snapshots/` 는 그때 뜬 날것이라 그대로 둔다 |
| `game__sp_puzzle` | 같음 |
| `activity__write` | 조합 문제의 머리를 고쳤다(08-24) — 작은 동그란 버튼 하나뿐이라 소리를 들어야 하는 문제인 줄 몰랐다. 다른 듣기 문제(AudioRow)처럼 큰 재생 버튼 + 파형으로 바꾸고 힌트 버튼을 더했다. 재생 버튼 라벨도 바꿨다 — 전에는 "가 발음 듣기" 라 스크린리더에 정답이 그냥 읽혔다. 그 뒤 한 번 더 고쳤다 — 흉내 내지 말고 다른 화면과 같은 AudioRow 컴포넌트를 쓰고, 풀어야 할 글자는 점선 박스(.combo-target)로 줄을 나눴다 |
| `activity__write3` | 같음 |
| `nav__jamo__resume` | 활동 이름을 기획자가 다시 정했다(08-24) — "자음-모음 조합하고 쓰기" → "자음-모음 조합하기". 앱이 쓰는 module.ts 와 screens_ref 를 같이 바꿨고, _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__wordPreview` | **한 칸짜리 진행막대를 뺐다(2026-08-27).** 앱도 프로토타입(`screens_SOT`)도 안 그리는데 **캡처만 갖고 있었다** — 칸이 하나면 늘 꽉 찬 줄이라 어디쯤인지를 말해 주지 못한다. 대조는 `drop_single_progress()` 로 **양쪽에서 지워** 넘기고 있었다.<br>**캡처를 정본대로 고치고 그 건너뛰기 코드를 지웠다** — 눈감던 자리가 하나 없어졌다. `write3` · `write3_canvas` 도 같다. _snapshots/ 는 그때 뜬 날것이라 그대로 둔다 |
| `activity__*` 열한 화면 | **앱바 라벨을 「나가기」로 통일했다(2026-08-27).** i18n 이 `player.exit`(나가기) 하나로 모았고 **앱은 24화면 전부 나가기**인데, 캡처 열하나가 「닫기」로 남아 있었다. 대조는 둘을 같은 값으로 뭉개(`EXIT_LABELS`) 넘기고 있었다.<br>**캡처를 고치고 그 뭉개기도 지웠다.** 프로토타입도 같은 판에 맞췄다(`screens_SOT` 의 `shell()`·`activityAppbar()`) |
| `clip__empty` | 검색 전 — 무엇을 찾을지 안내만 한다.<br>**새로 들어온 화면이다(2026-08-27) — `_snapshots/` 에 짝이 없는 것이 맞다.** 표현클립은 명세도 목업도 없던 자리라(`masterplan_v3` §2 "의도되지 않은 공백") 처음 뜨는 캡처다. **목업을 새로 그리지 않고 지금 화면을 옮겼다** — `CLAUDE.md` 가 "화면은 거의 다 됐다, 새로 그리자고 제안하지 마라" 로 정해 둔 자리다. 게임 절을 만들 때와 같은 방식이고, 마크업은 `ClipView` 를 `renderToStaticMarkup` 으로 그려 넣었다 |
| `clip__results` | 결과 있음 — 머리글의 개수만큼 끝까지 열린다.<br>**새로 들어온 화면이다(2026-08-27) — `_snapshots/` 에 짝이 없는 것이 맞다.** 표현클립은 명세도 목업도 없던 자리라(`masterplan_v3` §2 "의도되지 않은 공백") 처음 뜨는 캡처다. **목업을 새로 그리지 않고 지금 화면을 옮겼다** — `CLAUDE.md` 가 "화면은 거의 다 됐다, 새로 그리자고 제안하지 마라" 로 정해 둔 자리다. 게임 절을 만들 때와 같은 방식이고, 마크업은 `ClipView` 를 `renderToStaticMarkup` 으로 그려 넣었다 |
| `clip__noresult` | 결과 없음 — 검색어를 되짚어 준다. **2026-08-27 에 생긴 화면**이다(전에는 검색 전과 같은 말을 냈다).<br>**새로 들어온 화면이다(2026-08-27) — `_snapshots/` 에 짝이 없는 것이 맞다.** 표현클립은 명세도 목업도 없던 자리라(`masterplan_v3` §2 "의도되지 않은 공백") 처음 뜨는 캡처다. **목업을 새로 그리지 않고 지금 화면을 옮겼다** — `CLAUDE.md` 가 "화면은 거의 다 됐다, 새로 그리자고 제안하지 마라" 로 정해 둔 자리다. 게임 절을 만들 때와 같은 방식이고, 마크업은 `ClipView` 를 `renderToStaticMarkup` 으로 그려 넣었다 |
| `clip__playing` | 재생 중 — 카드 자리가 플레이어로 바뀐다. 유튜브 플레이어는 순수하지 않아 **빈 상자로 그린다** — 대조하는 것은 플레이어가 아니라 그 자리에 무엇이 놓이는가다.<br>**새로 들어온 화면이다(2026-08-27) — `_snapshots/` 에 짝이 없는 것이 맞다.** 표현클립은 명세도 목업도 없던 자리라(`masterplan_v3` §2 "의도되지 않은 공백") 처음 뜨는 캡처다. **목업을 새로 그리지 않고 지금 화면을 옮겼다** — `CLAUDE.md` 가 "화면은 거의 다 됐다, 새로 그리자고 제안하지 마라" 로 정해 둔 자리다. 게임 절을 만들 때와 같은 방식이고, 마크업은 `ClipView` 를 `renderToStaticMarkup` 으로 그려 넣었다 |
| `clip__*` — 없는 것 | **「재생 실패」는 아직 캡처가 없다** — 그 화면이 코드에 없기 때문이다(`clip_spec_v1` §07). 만들 때 같이 그린다 |

---

## 목업 파일 운영 규칙

- 시각 정본 파일명은 `screens_SOT.html`·`paywall_SOT.html`처럼 `_SOT`로 역할을 드러낸다.
- 같은 정본의 수정마다 `v2`, `v3` 파일을 복제하지 않는다. 문서 안의 버전과 Git 이력으로 관리한다.
- `_snapshots/`와 `state_audit/`의 상태 ID가 있는 감사 캡처는 당시 제품 상태를 남긴 자료다. 정본이 바뀌어도 덮어쓰지 않는다. 단, 결론에 쓰이지 않은 캡처 실험물은 `state_audit/README.md`의 보존 규칙에 따라 제거한다.
- 프로토타입에서 확정한 공통 컴포넌트·상태 문법은 같은 작업에서 정본 목업과 기획 문서에도 반영한다.

---

## 목업 버전 이력

### v3.1 — 2026-09-01

#### 반영 파일

- `screens_SOT.html` — 전체 시각 정본
- `app/src/screens_ref/activity__briefing.html` · `activity__briefing_hint.html`
- 미션대화 브리핑 컴포넌트와 힌트 데이터 연결

#### 변경 내용

- 원장과 서버에 값이 없어 실제 제품에서 한 번도 보이지 않던 「미리 볼 낱말」 띠를 제거했다.
- 미션 키워드 아래에 힌트 토글과 열린 상태를 추가해 모범 문장·번역의 표시 규칙을 정본에 넣었다.
- 2026-09-01 승격 이후에도 문서 머리가 v3.0에 머물던 것을 v3.1로 맞췄다.

**위 「표」가 화면 하나하나의 이유**라면 여기는 **판(版) 단위의 기록**이다.
개별 승격 이유는 표가 주인이므로 여기서 되풀이하지 않는다.

### v3.0 — 2026-08-27

#### 변경 내용

- 결과 화면 제목을 완료 여부에 따라 `잘 했어요!` / `아직 남았어요`로 분리했다.
- 미션대화의 시나리오·미션 패널·입력 구조를 현재 제품과 맞추고, 롤플레잉 지난 대사의 다시 듣기를 실제 버튼으로 승격했다.
- 플래시카드의 드래그 모션 래퍼와 어휘 그림 문제 상태를 정본·대조 화면에 반영했다.
- 활동 정본 대조 단위는 23화면이 됐다. 개별 승격 이유는 `screen_promotions.md`가 주인이다.

### v2.9 — 2026-08-26

#### 변경 내용

- 선형 활동의 진행 세그먼트를 누를 수 있는 버튼으로 바꿔 이전 문항 탐색을 열었다.
- 듣기 O/X 라벨, 빈칸 표기, 어휘 문제 표본을 실제 데이터와 현재 제품에 맞췄다.
- 내부 탐색형 자모 활동과 어휘 미리보기의 무의미한 진행바를 제거했다.

### 페이월 v1.1 — 2026-08-27

#### 반영 파일

- `paywall_SOT.html` — 게스트 · 개인 미구독 · 구독 만료 · 기관 학생 · 목록 잠금의 시각 정본
- `app/src/components/main/textbook/paywall-panel.tsx` · `app/src/styles/nav.css`
- `app/src/i18n/locales/*.ts` — 한국어 · 영어 · 일본어 · 중국어 · 베트남어

#### 변경 내용

- 실제 프로토타입에서 승인한 결제 유도형 디자인을 `draft_paywall.html`에서 시각 정본으로 승격했다.
- 무료 범위의 긴 목록, 가격 미정 카드, 중복 설명을 제거하고 구독 가치 세 가지와 주 행동 하나를 남겼다.
- 게스트는 로그인, 개인 회원은 구독, 기관 학생은 학교 이용 범위로 돌아가도록 다음 행동을 분리했다.
- 320·375폭과 다섯 언어를 같은 벤치에서 확인할 수 있게 했다.
- 결제 API가 아직 없으므로 개인 구독 CTA가 비활성이라는 현재 제품 상태를 숨기지 않았다.

### v2.8 — 2026-08-26

#### 반영 파일

- `screens_SOT.html` — 전체 시각 정본
- `app/src/screens_ref/activity__failed.html` · `activity__micdenied.html` — 목업 재캡처
- `app/src/components/main/activity/state-screens.tsx` · `icons.tsx` · `mic-blocked.tsx`
- `app/src/styles/activity.css` — `.state-icon` 추가, `.state-view` 정렬
- `app/src/i18n/locales/*.ts` — 다섯 로케일에 `loadFailedBody` · `micDeniedBody` 추가

#### 변경 내용

- 로드 실패·마이크 거부가 한 줄 문장만 떠 있던 것을 **아이콘 + 제목 + 설명**으로 바꿨다.
  `draft_activity_controls.html` 에 시안으로만 그려져 있고 정본에 올라온 적이 없던 디자인이다.
- 마이크 문구를 정본대로 고쳤다 — "마이크가 꺼져 있어요" → "마이크를 사용할 수 없어요" + 설명.
  전체 화면과 알림(`MicBlockedDialog`)이 **같은 키**를 쓰므로 둘이 함께 바뀐다.
- 제목과 설명을 키로 갈랐다. 전에는 한 키를 `\n` 으로 쪼개 제목을 만들었다.
- `.state-view` 가 늘 위에 붙어 있던 것을 가운데로 되돌렸다. `flex:1` 을 적어 두었지만
  부모 `.scroll-area` 가 `display:block` 이라 아무 일도 하지 않았다 — `min-height:100%` 으로
  고쳤고, **정본 프로토타입도 같은 증상이라 같이 고쳤다.**
- 실패 화면을 손으로 짜 두었던 활동 셋(빈칸·읽기·듣기)을 공용 `FailedScreen` 으로 모았다.
  그 셋은 하단 버튼이 아예 없어 나가기 말고는 길이 없었다 — 정본이 그리는 다시 시도가 생겼다.

### v2.6 — 2026-08-26

#### 반영 파일

- `screens_SOT.html` — 전체 시각 정본
- `draft_recording_interaction.html` — 녹음 상태 확인용 시안
- `app/src/components/main/activity/record.tsx` — 공통 녹음·듣기 조작
- `app/src/components/learn/ai-roleplay.tsx` — 실제 롤플레잉 상태 연결

#### 변경 내용

- AI 차례에는 듣기 상태, 내 차례에는 녹음 상태, 발음 판정 중에는 확인 상태만 보이게 역할을 분리했다.
- 동작하지 않는 녹음 버튼이 활성 상태처럼 보이던 문제를 없앴다.
- 아직 오지 않은 대사는 비활성 버튼 색이 아니라 읽을 수 있는 보조 텍스트 대비를 사용한다.
- 대본의 다시 듣기 터치 영역을 32px로 넓히고, 발음 결과 행동 버튼 높이를 42px로 보정했다.
- 확인용 녹음 시안도 실제 컴포넌트와 같은 `56px 버튼 + 오른쪽 상태 문구` 구조로 맞췄다.

### v2.5 — 2026-08-25

#### 반영 파일

- `screens_SOT.html` — 전체 시각 정본
- `draft_recording_interaction.html` — 녹음 상태 확인용 시안
- `shell_spec_v1.html` — 활동 셸·조작 규칙
- `dev_spec_v1.html` — 진행·다시 풀기 규칙

#### 변경 내용

- 롤플레잉 녹음을 `대기 → 2초 준비 → 녹음 → 1초 마무리 → 자동 분석`으로 통일했다.
- 분석 결과는 목표 대사 바로 아래 카드에 `인식된 문장`으로 표시한다.
- STT가 대본과 달라도 진행을 막지 않고 `다시 녹음`과 `다음 대사`를 함께 제공한다.
- 결과 카드가 보이는 동안 하단 녹음 조작을 숨겨 같은 기능이 두 번 나오지 않게 했다.
- 시나리오 진행 표시는 앱바 아래 공통 진행 표시를 사용한다.
- 기존 감사 캡처는 과거 상태 증거로 보존하고 정본 링크와 변경점을 감사 문서에 남겼다.
