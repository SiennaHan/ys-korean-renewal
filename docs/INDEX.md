# 문서 색인 — 어느 문서가 무엇에 답하나

**이 저장소에서 문서 목록은 여기 하나뿐이다.** 전에는 README 표와
`masterplan_v3` 두 곳에 같은 목록이 있었고, 그래서 한쪽만 고쳐지는 일이
반복됐다(문서 수가 25 와 26 으로 갈렸고, 목업 대조 화면 수는 22·24·27 로 갈렸다).
목록을 늘리지 마라 — 다른 문서는 **여기를 가리키기만** 한다.

`python3 docs/check_docs.py` 가 **정본·확인용 HTML 19개가 모두 이 색인에 있는지** 검사한다.
**이 문서가 문서 수·폐기본 수의 주인이다** — 다른 문서에 그 숫자를 적지
마라(`[사실 중복]` 이 잡는다). 화면 수의 주인은 `README.md` 다.
문서를 새로 만들면 여기 한 줄을 넣어야 검사기가 통과한다.

> 여기 있는 것은 **문서 목록**이다. **무엇이 비었나**(영역 × 산출물, 빈 칸이 곧
> 빠진 기획)는 `masterplan_v3.html` **§2 덮개 지도** 에 있다 — 그것도 한 곳뿐이다.

---

## 처음 30분 — 이 셋만 순서대로

| | 무엇 | 왜 먼저 |
|---|---|---|
| 1 | [`status.generated.md`](status.generated.md) | 코드와 공통 상태 계약에서 생성한 현재 판정. 손으로 고치지 않는다 |
| 2 | [`developer_tasks.md`](developer_tasks.md) | 기획 결정 대기와 개발 실행 카드. 지금 무엇을 할지는 여기서 정한다 |
| 3 | `masterplan_v3.html` | 제품 목적·덮개 지도·게이트·결정 근거. 날짜별 구현 상태를 판정하지 않는다 |

---

## 1. 계획과 결정

"기획서" 라 부를 것이 둘이다. **질문이 서로 다르다.**

| 문서 | 답하는 질문 |
|---|---|
| `masterplan_v3.html` | **정본 기획서 + 인계.** **§0 이 제품 자체를 설명하고**, §2가 영역별 빈 기획, §3·§5·§12·§13이 구조·게이트·계획 원칙·리스크를 기록한다.<br>§15·§16·§19는 화면 현황·목업을 벗어난 여섯 곳·건드리면 깨지는 것이다. 현재 상태는 `status.generated.md`, 실행 순서는 `developer_tasks.md`가 쥔다 |

| [`status.generated.md`](status.generated.md) · [`project_status.json`](project_status.json) | **지금 상태와 정책 계약.** JSON은 완료·부분완료·미구현·검증 안 됨 네 상태와 무료 콘텐츠 전달 방식을 기계 판독 값으로 쥔다. 생성 문서는 실제 CI job·스텝과 JSON을 역대조해 사람이 읽는 표로 만든다. 손으로 고치지 마라(`docs/gen_status.py`가 만들고 CI가 낡으면 실패시킨다).<br>`check_docs.py` 통과는 참조·색인·등록 계약의 **구조 검사**이며 의미 전체의 보증이 아니다 |
| [`doc_review_v1.md`](doc_review_v1.md) | **문서를 어떻게 검수하나.** 검사기는 톱니고 **새 거짓을 찾는 것은 읽기**다 — 그 둘을 어떻게 나눠 쓰는지.<br>무엇을 읽고(비용으로 자른다) · 언제 읽고(`[관찰 기준]` 이 방아쇠다) · 무엇을 의심하고 · 찾은 것을 어떻게 톱니로 넘기는지.<br>**2026-08-30 에 문서를 한 줄씩 읽어 의미가 틀린 곳 열 곳 넘게 나온 뒤에 썼다** |

---

## 2. 시각 정본 — 목업

**구현이 목업과 다르면 목업이 기준이다.** 이름 앞이 무엇인지를 말한다 —
접두사가 없는 것이 정본, `draft_` 는 확인용 시안, `legacy_` 는 옛 세대다.

| 문서 | 무엇의 시각 정본 |
|---|---|
| `screens_SOT.html` | **시각 정본 v3.1** — 활동 화면 20종(`#mk-act` · **대조 정본 24화면**) · 홈·교재학습·자모 목록(`#mk-nav`) ·<br>VocaShot 혼자 하기(`#mk-voca`) · 게임 넷 17화면(`#mk-game`).<br>현재 대조 검사가 확인하는 화면 수는 **`README.md` 가 쥔다** — 여기 적어 두면 화면이 늘 때 같이 낡는다(실제로 「50」으로 낡아 있었다. 그때 참값은 54였다). 승인 뒤 제품과 정본을 함께 바꾼 내역도, 버전 운영 규칙도 `screen_promotions.md` 한 곳이다(2026-08-29 에 합쳤다) |
| `draft_activity_controls.html` | **확인용 시안** — 활동 하단 도크·주/보조 조작·선택지 상태를 비교한다. 제품 정본을 대체하지 않는다 |
| `draft_recording_interaction.html` | **확인용 시안** — 녹음 6상태와 2초 준비·1초 마무리, 둥근 스피커 아이콘만 검증한다. 학습목록 등 주변 화면은 정본이 아니다 |

> 목업을 열면 **삽화가 깨져 보인다.** `illust/images/` · `handwriting/` 은 교재 파생이라
> `.gitignore` 가 막는다 — 고장이 아니다.

---

## 3. 셸 명세 넷 — 층이 다르다

넷이 겹쳐 보이지만 **겹치지 않는다.** 같은 셸을 네 층에서 말한다.
값을 대조해 봤고 어긋난 곳이 없다. 하나를 열었을 때 나머지 셋에 무엇이 있는지는 이 표로 안다.

| 층 | 어디 | 무엇 |
|---|---|---|
| **행동** | `shell_spec_v1.html` §0~§10 | 상태 머신 · 라우트 통일 · 정상 흐름 밖의 화면 · 접근성 · 용어. **가장 많이 인용된다** |
| **구조** | 같은 문서 §11~§20 | 컴포넌트별 상태와 슬롯 · 320 폭 실측 · **라인업** 8종이 어느 슬롯에 무엇을 넣나. **셸 결정 `S1`~`S3` 의 정의처**(§19.0) |
| **값** | 같은 문서 §21~§34 | 토큰 · 타이포 · 색 · 간격 · 채점·피드백 수치 · 검증 |
| **순서** | `dev_spec_v1.html` | 최초 구현 순서와 현재 필드 계약 · 검증 기준. 현재 우선순위는 `developer_tasks.md` |

> `S3` 가 두 가지를 가리킨다 — `shell_spec_v1` 에서는 **셸 결정 3번**,
> `access_and_pricing_v1` 에서는 **AWS S3** 다. `S1`~`S3` 는 기획서의 `D1`~`D7` 과 다른 계열이다.

**셸 밖까지 보려면 [`../DESIGN.md`](../DESIGN.md) 다.** 위 `값` 층은 *학습 활동 셸의
명세*고, DESIGN.md 는 **전 화면군(활동·내비·인증·표현클립·레거시 교재·MY)의 현황**을
코드에서 재어 적은 것이다 — 무엇이 갈렸고 무엇을 정해야 하는지가 §8 에 표로 있다.
값이 둘에서 갈리면 `shell_spec_v1` 이 이긴다. 게임 다섯은 DESIGN.md 범위 밖이다.

---

## 4. 화면·기능별 명세

| 문서 | 답하는 질문 |
|---|---|
| `textbook_tab_spec_v1.html` | **교재학습 탭** — 활동으로 들어가는 유일한 길. 상태 컴포넌트 5종 · 라우트 사상 · 권한 잠금. 복습 N 상태의 모양은 있으나 현재 목록 데이터 연결은 안 됐다 |
| `access_and_pricing_v1.html` | **누가 무엇까지 볼 수 있나, 그리고 그것을 누가 정하나** — 무료/유료 경계 · 구현된 권한 판정·자물쇠·서버 게임 게이트와 아직 없는 결제. **B2C 는 여기부터**.<br>**§10~§12 가 어드민이다**(2026-08-29 에 합쳤다) — 역할 셋과 문 둘이 어긋나는 자리 · 학교 경계를 **검사가 아니라 덮어쓰기**로 둔 이유 · 기관 발급 코드 · 학기 종료.<br>하루 동안 `admin_spec_v1.html` 로 따로 있었는데 **합쳤다** — 「누가 무엇까지 보나」와 「누가 정하나」가 갈리면 `GET /entitlement` 가 무엇을 내는지를 두 곳에 적게 된다 |
| `dev_spec_v1.html` | **최초 개발 명세와 API 계약을 합쳤다.** 핵심 활동 DB·API·라우트·셸은 구현됐으므로 “신설” 표시는 과거 계약 라벨이다. §10~§16의 필드 계약과 회귀 기준을 현재 용도로 쓴다 |
| `clip_spec_v1.html` | **표현클립 탭** — 명세·목업·API 세 칸이 모두 비어 있던 자리를 채운다. 구현을 읽고 재서 규칙으로 고정했고 **어긋난 것 여덟**과 **기획 결정 여섯**을 가려냈다.<br>**무료·비로그인으로 열리는 첫 화면인데 gzip 3.0 MB 를 받고 그 절반을 아무도 읽지 않는다** — §02 · §04 |
| `my_learning_summary_v1.html` | MY 누적 학습 기록에 필요한 데이터 계약. 정답률은 첫 시도로 확정됐고, 현재 `/learning-summary`와 누적 화면은 미구현이다 |
| `jamo_authoring_spec_v1.html` | 자모 콘텐츠 저작 · `n8_jamo` 스키마. **맨 위의 실측 정정을 먼저 읽어라** |
| `games_spec_v1.html` | **둘을 합쳤다** — §1~§10 게임 넷에서 손볼 것(서울 퍼즐 38단계 문장 검수 포함) ·<br>§11~§20 VocaShot 혼자 하기 설계. **구현됐다** — `/main/game/vocashot-solo` |
| `user_flow_v1.html` | **진입에서 결제까지 한 장.** 규칙을 새로 정하지 않는다 — 진입 넷 · 무료 둘러보기 · 벽 · 갈림 셋 · 가입 · 결제를 **순서로** 잇는다.<br>자체 가입은 구현됐고 **기관 발급 코드로 들어오는 길이 2026-08-28 에 붙었다**(`access_and_pricing_v1` §11). **나가는 길도 하나 생겼다 — 학기 종료**. **QR은 게스트 세션을 보장한 뒤 홈(`/main`)으로 보내도록 2026-09-01 구현됐다.** 결제·메일 발송은 아직 외부 연동이 필요하다 |
| `draft_auth.html` | **로그인·회원가입·비밀번호 재설정 확인용 시안.** 로그인·회원가입은 현재 구현을 기준으로 하며, 가입은 **2026-08-27 에 구현됐다**(`POST /user/sign/up` · `/signup`). 제품에 없는 임시 음성 동의 체크박스는 제거했다.<br>재설정 화면은 구현됐지만 실제 메일 발송은 아직 없다. `BLOCKERS.md` §7 |
| `paywall_SOT.html` | **결제 안내(페이월) 시각 정본 v1.1.** 실제 프로토타입에서 승인한 게스트 · 개인 미구독 · 구독 만료 · 기관 학생 넷과 목록의 자물쇠. **다섯째 「학기 종료」는 2026-08-28 에 구현에서 올라왔다** — 목업을 새로 그리지 않고 앱 i18n 문구를 그대로 옮겼다. 개인 결제 화면은 구독 가치 셋과 주 행동 하나로 정리했고, 320·375폭과 다섯 언어를 같은 벤치에서 확인한다.<br>**기관 학생에게는 결제 버튼을 두지 않는다** — `textbook_tab_spec_v1` §06 의 규칙 |
| `legal_draft_v1.html` | **이용약관·개인정보 처리방침 초안.** 변호사 검토 전이고 사업자 정보 등은 비워 뒀다 — 다만 **수집 항목·처리 위탁·국외 이전은 코드를 읽고 적었다**.<br>음성 신규 업로드는 비공개로 바뀌었고 표본 비율도 기본 10%로 낮아졌다. **기존 공개 객체 이관 여부·보관기간(현재 기본 0일=자동 삭제 없음)·최종 법률 문구**는 여전히 출시 전 결정이 필요하다 |
| `G1_content_gate_v1.html` | **G1 게이트가 확정한 것 둘.** 저작 **라인업** 8종을 어떻게 정했나·저작 물량(§1~5) · 문항 스키마 공통 컬럼 규약(§6~11). 공개 정책은 `deleted`만 제외하고 모르는 상태값은 경고 후 포함하며, 기계 판독 정본은 `project_status.json`이다. §8~9의 `reviewed` 전용 노출은 과거 제안으로 표시했다 |
| `design_decisions_SOT.html` | **디자인 결정판.** `../DESIGN.md` 의 「정해야 할 물음」을 눈으로 보고 고르는 판이다 — 후보 수치는 전부 실제 CSS 에서 뽑았고, 고르면 미리보기가 그 값으로 그려진다. **미리보기 글자는 자리표다**(교재에서 온 글이 한 자도 없다). 진짜 화면 위에서 바꿔 보려면 개발 빌드의 우하단 「디자인」 버튼을 쓴다 |
| `i18n_검수_20260828.md` | **다섯 언어 검수 — 입력과 출력이 한 문서다**(2026-08-29 에 합쳤다). §2 가 무엇을 고쳤나, **§5 가 원 리포트**(무엇을 고쳐야 했나)다.<br> 607키의 누락·초과·플레이스홀더는 0건이지만, 번역 계층을 우회하는 제품 코드의 하드코딩 문구와 언어별 표현 품질 문제를 따로 기록한다. 이 문서는 기획 정본이 아니라 2026-08-28 시점 감사 자료다 |

---

## 5. 실측과 근거 — 왜 그렇게 정했나

결론은 위 문서들에 이미 들어가 있다. **근거를 직접 확인할 때만** 열면 된다.

| 문서 | 무엇 |
|---|---|
| `asis_v1.html` | 리뉴얼 **전** 앱 전체의 실측 — 탭 다섯을 순서대로 걷는다.<br>§2 홈 · §3 교재학습 · §4 게임 · §5 표현클립 · §6 MY · §8 이 세 탭의 판단 |

---

## 6. 개발자에게 넘길 묶음 — 실행 목록과 근거

지금 목표는 **화면은 내가 마무리하고, 개발자는 서버·외부 API·배포 안전장치**를
확실히 해서 출시하는 것이다. 개발자는 먼저 실행 목록 한 장을 읽고, 필요한 근거만 따라간다.

| 순서 | 문서 | 왜 필요 |
|---|---|---|
| 1 | [`developer_tasks.md`](developer_tasks.md) | **실제 작업 목록.** 이미 된 것, 기획 결정 대기 `PD-01~10`, 개발 작업 `DEV-01~15`, 카드별 구현 범위·완료 판정·테스트·반환 산출물이 있다 |
| 2 | [`../BLOCKERS.md`](../BLOCKERS.md) **§6-e · §7** | 표현클립 신고의 위험한 배포 순서와 비밀번호 메일의 현재 결손을 확인하는 실측 근거 |
| 3 | `dev_spec_v1.html` | 구현된 활동 상태·복습 큐의 요청·응답 필드와 검증 기준(§10~§16). 변경 시 서버·화면 회귀 계약으로 쓴다 |
| 4 | `access_and_pricing_v1.html` | 권한 판정·무료 경계·기관 학생·월 구독의 확정 규칙. 가격·PG와 실제 권한 저장은 아직 남았다 |

**어드민 쪽 위험은 `developer_tasks.md` 의 DEV-14 가 들고 있다** — 학교 코드 편집이 학생을 조용히 고아로 만드는 것과 배포 CSS 누락이다. 타입 검사는 통과하지만 어드민은 아직 CI에 없다. 현재 판정은 `status.generated.md`의 공통 상태표가 쥔다.

`developer_tasks.md`만 읽어도 일을 나눌 수 있어야 한다. 나머지 문서는 작업 카드가
가리키는 부분의 **근거와 필드 계약**이며, 개발자가 과거 기록 전체를 읽고 현재 과업을 추측하게 하지 않는다.

외부 API 는 이미 붙어 있는 것과 새로 붙일 것이 갈린다 —
붙어 있는 것은 `api/TUTORUS_INTEGRATION.md`(발음평가·비원어민 STT) · OpenAI · Gemini TTS,
새로 필요한 외부 연동은 **메일 발송(SES 등)** 과 **결제**다. QR 홈 착지와 표현클립 신고는
구현됐고, 음성 보관 운영·콘텐츠 산출물 감사·CI와 관측까지 포함한 전체 범위는 인계서가 맡는다.

---

## 옛 이름 — 2026-08-26 에 바꾼 것

`_uiux` 는 정본에도 시안에도 폐기 예정에도 똑같이 붙어 있어서 이름만으로는
아무것도 알 수 없었다. 하는 일을 이름 앞에 적는 것으로 바꿨다.
**`_superseded/` 와 `state_audit/`의 보존 대상 안에서는 옛 이름을 바꾸지 않는다** —
완전히 대체된 원문과 캡처 실험물은 각각 `ARCHIVE_MANIFEST.md`와
`state_audit/README.md`의 규칙으로 정리했다. 남은 옛 이름을 만나면 이 표로 찾는다.

| 옛 이름 | 지금 | 무엇 |
|---|---|---|
| `screens_uiux.html` | `screens_SOT.html` | 시각 정본 |
| `shell_mockup_uiux.html` | `legacy_shell_mockup.html` |  옛 세대 셸 목업. **2026-08-29 에 `_superseded/` 로 갔다** — 여기 오래 「폐기본이 아니다」로 적혀 있었는데, 그 근거였던 masterplan 의 줄은 **2026-08-26 에 스스로 정정됐다**(앱 반영 0). 근거가 사라진 뒤에도 이 줄만 남아 있었다 |
| `activity_controls_uiux.html` | `draft_activity_controls.html` | 확인용 시안 |
| `recording_interaction_uiux.html` | `draft_recording_interaction.html` | 확인용 시안 |
| `auth_uiux.html` | `draft_auth.html` | 확인용 시안 |
| `paywall_uiux.html` | `paywall_SOT.html` | 시각 정본 v1.1로 승격 |
| `draft_paywall.html` | `paywall_SOT.html` | 승인된 프로토타입 디자인으로 승격(2026-08-27) |
| `admin_spec_v1.html` | `access_and_pricing_v1.html` | **§10~§12 로 합쳤다. 하루만 살았다**(2026-08-29). 어드민 명세로 새로 만들었다가 같은 날 합쳤다 — 「누가 무엇까지 보나」와 「누가 정하나」를 가르면 `GET /entitlement` 동작이 두 곳에 적힌다 |

폴더 둘도 같은 날 바꿨다 — 이름이 역할과 반대였다.

| 옛 이름 | 지금 | 무엇 |
|---|---|---|
| `app/src/mockups/` | `app/src/screens_ref/` | **대조의 기준.** parity 가 이것과 견준다 |
| `docs/captured/` | `docs/_snapshots/` | **시점 기록.** 처음 떴을 때의 날것, 덮어쓰지 않는다 |

둘이 갈라진 곳은 `screen_promotions.md` 가 적는다.

---

## 옛 판은 어디에

legacy_shell_mockup.html → _superseded/ (2026-08-29)

정본이 아닌 31개는 `_superseded/` 에 있다. 그 폴더의 `README.txt` 가 항목마다
**[대체됨]**(더 새 판이 있다)인지 **[근거]**(다른 문서의 주장을 받치는 산출물)인지 적는다.
완전히 대체돼 Git 이력으로만 보존하는 파일은 `ARCHIVE_MANIFEST.md`가 기록한다.

과거 인계 메모 셋은 현재 정본에 판단 근거를 흡수한 뒤 삭제했다. 원문 대신
`_superseded/ARCHIVE_MANIFEST.md`가 옛 파일명·대체 정본·보존할 판단 근거를 기록한다.
