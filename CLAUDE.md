# 이 저장소에서 일할 때

연세 글로벌 한국어 학생용 앱 리뉴얼. **문서가 여러 개라 백지에서 뒤지면 틀린 것을 집는다.**
아래 규칙과 색인을 먼저 보고 시작해라.

## 지금의 목표 — 이걸 알고 판단해라

기획자가 **혼자 할 수 있는 것을 최대한** 하고, 나머지를 개발자가 확실히 해서
배포하는 것이 목표다. **역할 경계의 정본은 `phase1/developer_tasks.md` §0 이다** —
거기서 개발자 몫은 백엔드·외부 연동만이 아니라 **관측·테스트·배포·롤백까지**다.
여기 「백엔드와 외부 API 만」으로 좁게 적혀 있었다. 그래서:

- **화면은 거의 다 됐다.** 목업대로 들어가 있고 목업 대조가 통과한다.
  새로 그리자고 제안하지 마라
- **서버도 됐다 (2026-08-29 갱신).** `api/business/` 에 리뉴얼 모듈이 줄줄이 있고
  (`entitlement` · `review_queue` · `activity_state` · `learning_record` ·
  `user_withdraw` · `dashboard` · `signup_code_business` · `student_business`).
  **수를 여기 적지 않는다** — 전에 "라우터가 28개" 로 박아 뒀다가 내가 하나 늘리면서
  낡았다. 세려면 `grep -c include_router api/server.py` 다.
  `ko_activity_state` · `ko_review_queue` · `ko_signup_code*` 셋도 신설됐다 —
  `BLOCKERS.md` §6 · §10.
  **이 파일은 세션마다 자동으로 읽힌다** — 여기 낡은 한 줄이 있으면 모든 새 세션이
  거기서 시작한다. 실제로 "서버는 아직 없다" 가 오래 그랬다
- **기관 코드와 학기 종료는 끝났다 (2026-08-28).** 학교가 어드민에서 코드를 발급하고
  학생이 그것으로 가입한다. 학기가 끝나면 어드민에서 접근을 끊고, 그러면 무료 범위로
  내려가며 학생에게 모달로 알린다. **계약 학교는 전 급이다.** 명세는
  `phase1/access_and_pricing_v1.html` §10~§12 — **전에는 어드민 문서가 아예 없었다**
- **남은 것은 결제와 교재 콘텐츠 DB 이전 둘이다.** 결제는 코드 0줄이고 가격만
  정해졌다(월 6달러, 2026-08-28) — PG·통화·세금은 아직. 교재 문항은 여전히 앱
  번들이다(`dev_spec_v1` §16 의 5번)
- **어드민에는 게이트가 없다.** `cd admin && pnpm typecheck` 가 통과하지 않고
  (`host.$pin.tsx` 한 파일) `pnpm build` 가 유틸리티 CSS 를 내지 않는다. **둘 다
  리뉴얼 전부터다.** 그래서 어드민을 고칠 때는 앱과 달리 **검사가 아무것도 말해 주지
  않는다** — 눌러서 확인해라. 남은 위험은 `developer_tasks.md` DEV-14
- 개발자에게 넘길 묶음은 `phase1/INDEX.md` §6 이 정한다. **여기 「셋뿐」이라고 적혀 있었는데 그 절은 넷을 세고 있었다** — 수를 두 곳에 두지 않는다

## `phase1/` 은 폴더 이름일 뿐이다 — 지금은 Phase 1 이 아니다

**Phase 는 기간이 아니라 무엇을 만드느냐다.** 정의처는 `masterplan_v3.html` §15 다.

| Phase | 무엇 | 지금 |
|---|---|---|
| 0 | 보안 핫픽스 | 끝 |
| **1** | 공통 셸 · 상태 모델 · 라우트 | **끝** (§8 에 항목별로) |
| **2** | 콘텐츠 DB · API · 어드민 편집 | **거의** — 게임 콘텐츠 10개 표는 갔고 **교재 문항만** 남았다 |
| **2.5** | B2C · 페이월 | 화면·권한 판정은 됨 · **결제 코드 0줄** |
| **3** | 복습 큐 · 결과 화면 · 리포트 | **들어와 있다** (`review_queue.py` · `result-screen` · `report-screen`) |

**`phase1/` 폴더는 작업 초기에 지어진 이름이고, 그 뒤 모든 문서가 phase 와 무관하게
거기 쌓였다.** 이름만 보고 "아직 1단계" 로 읽지 마라 — 실제로 그렇게 읽혀서 한 번
물음이 나왔다(2026-08-28). 폴더 이름은 참조가 수백 곳이라 바꾸지 않았다.

## 화면 수가 넷이다 — 어느 것을 말하는지 밝혀라

이 저장소에서 가장 많이 어긋난 것이 이 숫자다. **넷 다 참이고 세는 단위가 다르다.**
그냥 "화면 N종" 이라고 쓰지 마라 — 어느 단위인지 붙여야 다음 사람이 안 헷갈린다.

| 수 | 무엇 | 어디서 나오나 |
|---|---|---|
| **20** | **목업의 활동 화면** | `screens_SOT.html` 활동 절의 버튼 수. `write`·`write3` 은 조합/따라쓰기를 한 버튼 안에서 상태로 넘긴다 |
| **23** | **활동 컴포넌트** | `app/scripts/activity-parity.tsx` 의 `SCREENS` 활동 항목. 위의 두 단계를 따로 센다 |
| **26** | **이식한 화면** | `masterplan_v3.html` **§15 의 표 합계** — `/learn/*` 7 · 자모 6 · 게임+목록 6 · 홈 등 4 · 클립·MY 2 · 코드 입력 1 |
| **목업 대조 화면** | `pnpm parity:activity` 가 실제로 대조하는 수. **목업 캡처 전부**가 들어가 있다 — 지금 수와 갈래별 분해는 `check_docs.py` 가 찍는다 |

`check_docs.py` 가 대조 화면 수와 활동 컴포넌트 수를 **세어서** 검사한다
(`[숫자 주장]`). 20·23 은 문서 안의 정의라 `screens_SOT` 와 `masterplan_v3` §15 가 정본이다.

**이 표에 숫자를 다시 적지 마라.** 지금 수가 필요하면 세라 —
`python3 phase1/check_docs.py` 가 첫 줄에 찍는다. 숫자의 주인은 `README.md` 다.

## 무엇이 정본인가 — 어길 수 없는 넷

| | 정본 | 뜻 |
|---|---|---|
| **콘텐츠** | 원장 `글로벌_교재기반_콘텐츠_v*.xlsx` | `app/src/shared/data/n*.json` 은 **산출물이다. 손으로 고치지 마라** — 고칠 것은 원장이고, `app/scripts/build-content.py` 로 다시 만든다 |
| **화면** | `app/src/screens_ref/*.html` (목업 캡처) | **구현이 목업과 다르면 목업이 기준이다.** 일부러 벗어난 여섯 곳은 `phase1/masterplan_v3.html` §16 에 이유까지 있다 |
| **문서 목록** | `phase1/INDEX.md` | 목록을 **다른 곳에 새로 만들지 마라.** 전에 두 곳에 있어서 계속 어긋났다 |
| **결정의 이유** | 커밋 메시지 | `git log --format='%h %s%n%b' -- <경로>` |

## 어디서 시작하나

| 알고 싶은 것 | 열 것 |
|---|---|
| 지금 막힌 것 · 정해지지 않은 것 | `BLOCKERS.md` **§0 현재 스냅샷** — 거기부터. **통째로 읽지 마라, 4천 줄이 넘는다**(대략 13만 토큰). §0 이 표 한 장으로 요약하고, 필요한 절만 이름으로 찾아 들어간다 |
| 어느 문서가 무엇에 답하나 | `phase1/INDEX.md` |
| 저장소에 무엇이 있고 어떻게 돌리나 | `README.md` |
| 무엇이 아직 기획되지 않았나 | `phase1/masterplan_v3.html` §2 덮개 지도 |

**셸 명세는 한 문서 안의 세 층이다** — `shell_spec_v1.html` 의
행동(§0~§10 상태 머신·라우트) · 구조(§11~§20 컴포넌트 상태와 슬롯) ·
값(§21~§34 치수·색·채점 수치). 전에는 파일이 셋이라 하나만 보고 판단하는 일이
잦았다. 만드는 순서는 따로다 — `dev_spec_v1.html`. 자세한 것은 `phase1/INDEX.md` §3.

## 끝냈으면 돌려라 — 그런데 CI 가 대신 돈다

문서가 작업을 저절로 따라오지 않는다 — **이 저장소의 가장 큰 문제였다.**
기계가 세는 숫자는 **주인 문서 하나에만** 적는다(`[사실 중복]` 이 잡는다).

**2026-08-29 부터 게이트 여섯을 CI 가 돌린다** — `.github/workflows/gates.yml`.
푸시·PR 마다 문서 · typecheck · 목업 대조 · build · check:css · biome 이 돈다.
그러니 평소에 손으로 다 돌릴 필요는 없다. **다만 둘은 여전히 사람 몫이다:**

```bash
cd app && python3 scripts/build-content.py --check   # 콘텐츠 — CI 가 못 돈다(원장이 저장소에 없다)
cd admin && pnpm typecheck                            # 어드민 — 통과하지 않아 CI 에 못 넣었다(DEV-14)
```

빠른 되먹임이 필요하면 손으로도 돌린다 —
`python3 phase1/check_docs.py` · `cd app && pnpm parity:activity`.

`check_docs.py` 는 **문서에 적힌 수를 실제로 세어 비교한다.** 화면을 늘리거나
문서를 만들거나 옮겼으면 이게 잡아 준다. 걸리면 **문서를 고쳐라** — 숫자를
맞추려고 코드를 고치지 마라.

`[관찰 기준]` 이 걸리면 **그 문서를 다시 읽어라.** 코드가 바뀌었으니 그 문서의
관찰 문장이 낡았을 수 있다는 뜻이다. 고칠 것이 없으면 기준 커밋만 지금으로 올린다.
**기준만 올리고 안 읽으면 이 검사는 없는 것과 같다** — 그러려고 경로를 좁게 골랐다.

`[문구 지뢰]` 가 걸리면 그 자리에 적힌 "지금 참인 것" 을 읽고 고쳐라. 시점 기록이라
일부러 남기는 것이면 `STALE_PHRASES` 의 `allow` 에 문서 이름과 이유를 적는다.

일부러 어긋나게 두는 것은 **이유를 적어야** 통과한다 — 화면 승격은
`phase1/screen_promotions.md` 표, 시점 기록의 옛 이름은 문서 안에
`옛이름.html → _superseded/` 줄.

## 한 단계 더 가면 알 수 있는 것을, 가서 확인해라

확인에는 늘 **싼 것**과 **진짜**가 같이 있다. 싼 것은 대부분 맞고 **가장자리에서만
틀린다** — 그런데 버그는 항상 가장자리에 있다. 그래서 싼 것으로 확인하면 평소엔
티가 안 나고 결정적일 때만 틀린다.

**어느 쪽을 볼지 스스로 고르지 마라. 한 단계 더 간 쪽을 봐라.**
출력 끝줄이 아니라 종료코드, 보고서가 아니라 diff, 추론이 아니라 사본에 돌려 보기,
한 자리가 아니라 자리마다. 원칙은 하나다 — **가면 알 수 있는데 안 가고 "됐다" 고
하는 것이 눈속임이다.**

**"됐다" 고 말하기 전에 한 번 더:** 내가 맞다고 확인해 주는 검사 말고,
**내가 틀렸다면 드러날 검사**를 돌려라.

내 검사기가 통과했다는 것은 *그 검사기가 세는 축에서* 어긋난 게 없다는 뜻일 뿐이다.
세지 않는 축은 늘 있고, 그건 사람이 찾아야 한다.

### 게이트는 "안 깼나" 고, "도나" 는 따로 봐야 한다

`typecheck` · `parity` · `build` 를 돌려 0 이 나온 것은 **내가 무엇을 깨지 않았다**
는 뜻이다. **내가 바꾼 것이 실제로 도는지**는 그 셋이 말해 주지 않는다.

2026-08-26 에 활동 컴포넌트 열셋을 표시(View)와 배선으로 갈랐다. 자모 여섯은
`typecheck` + `parity` 만 보고 끝냈는데, **대조는 View 에 픽스처를 넣어 그릴 뿐이라
내가 다시 연결한 props 를 하나도 보지 않는다.** 배선이 다 틀려도 통과했을 것이다.
나중에 여섯을 다 열어 보니 멀쩡했지만 **그건 운이었지 확인한 것이 아니었다.**

그러니 **배선·상태·이동을 건드렸으면 화면을 열고 눌러라.** 그리고 커밋에는
"검사 0" 만 적지 말고 **무엇을 눌러 봤는지**를 적어라 — 다음 사람이 무엇이
확인됐고 무엇이 안 됐는지 알 수 있게.

**화면을 열었다고 끝이 아니라 무엇을 보고 있는지도 확인해야 한다** — 조합 화면에서
학습자가 만든 글자를 목표로 알고 "버튼이 안 켜진다" 고 볼 뻔한 적이 있다.
어겨서 틀린 사례들은 `masterplan_v3.html` §16-b 에 있다.

## 다른 세션이 같은 저장소에서 동시에 일한다

**이 저장소는 거의 항상 세션이 둘 이상이다.** 작업 트리도 `app/.env` 도 공유물이고,
`HEAD` 는 내가 일하는 중에 움직인다. 사고 기록은 `masterplan_v3.html` §13 에 있다 —
2026-08-28 에 한쪽이 `git add .` 로 **다른 쪽의 다국어 변경을 자기 커밋에 담았다.** 그래서:

- **커밋 전에 `git status` 를 봐라.** 내가 안 건드린 파일이 섞여 있으면 남의 것이다.
  `git commit --only -- <내 경로>` 로 **내 것만** 낸다. `git add -A` 는 남의 미완성 작업을
  같이 커밋한다
- **남이 지금 고치고 있는 파일은 손대지 마라.** 같이 쓰면 한쪽이 사라진다.
  고쳐야 하면 멈추고 말해라 — 실제로 masterplan 을 그렇게 비켜 갔다(2026-08-29)
- **`app/.env` 를 바꿔야 하면 백업하고 바이트까지 되돌려라.** 그 파일을 옮기면
  다른 세션이 띄워 둔 앱까지 같이 옮겨 간다 — `BLOCKERS.md` 의 8000/3000 사고
- **같은 것을 두 세션이 동시에 만들 수 있다.** 시작 전에 `git log --oneline -5` 로
  방금 무엇이 들어왔는지 보고, 커밋 직전에 한 번 더 봐라

## 주어진 작업 단위와 범위를 바꾸지 마라

"화면 단위로 고쳐라" 를 규칙 단위로 하고 제목만 요청대로 쓴 적이 있다 — 그 때문에
한 화면을 안 열어 보고 실제 버그를 놓쳤다. **다른 단위가 낫다고 생각하면 바꾸기
전에 말해라.** 범위도 같다 — 넓힐 이유가 보이면 멈추고 물어라.

## 숫자를 0으로 만드는 일은 목표가 아니다 — 먼저 끝 상태를 정해라

`biome check` 진단처럼 **숫자가 붙는 정리 작업**이 많다. 숫자를 목표로 잡으면
**판단이 숫자에 끌려간다.** 시작 전에 규칙마다 "고친다 / 이유를 적어 재운다 /
안 건드린다" 를 정해 두고 들어가라 — 만나서 정하면 헐거워진다.
남은 것에 대한 표는 `BLOCKERS.md` §12 다.

## 검사기가 못 잡는 것을 알고 있어라

| 검사 | 잡는 것 | **못 잡는 것** |
|---|---|---|
| `pnpm typecheck` | 타입 | 실행 시점의 동작 전부 |
| `pnpm parity:activity` | 정적 마크업 구조.<br>2026-08-26 부터 **제품이 실제로 그리는 컴포넌트**를 그린다(손조합을 걷어냈다) | **`useEffect` 가 아예 실행되지 않는다**(`react-dom/server`). 훅 재실행·타이머·초점.<br>**픽스처 값이 실제 데이터와 같은 규약인지.**<br>그리고 **배선** — 대조는 View 에 *픽스처* props 를 넣는다. 제품이 그 View 에 무엇을 넘기는지는 안 본다. 배선을 통째로 갈아도 통과한다 |
| `fixture-data-check.py` | 픽스처가 씨드 레코드와 같은 **모양**인지 | 값이 **그럴듯한지**. 씨드가 서버와 어긋나면 어긋난 것을 기준으로 삼는다 |
| `fixture-data-check.py --live` | 씨드가 **서버(어드민 정본)와 같은지** | 네트워크가 필요해 평소 게이트엔 없다 — 씨드나 어드민을 건드린 뒤 직접 돌려라 |
| `pnpm build` | 빌드 가능성 | 위 전부 |
| `pnpm check:css` | **CSS 이름과 코드 이름이 맞는지** 양방향 | 모양이 맞는지. **`pnpm build` 뒤에 돌려라** — 실려 나간 CSS 를 기준으로 본다 |
| `check_docs.py` | 문서의 참조·숫자, 그리고 **한 번 거짓이라 밝혀진 표현**(`STALE_PHRASES`)과<br>**관찰 기준이 낡은 문서**(`<!-- 관찰: … @ 커밋 -->`) | 선언하지 않은 출처. 문서가 **맞는 말인지**.<br>그리고 **인용과 주장을 못 가른다** — 거짓 표현을 예시로 적으면 그것도 걸린다 |

**대조의 값은 내가 지어낸 것이다.** `activity-parity.tsx` 의 픽스처는 목업을 보고
손으로 적는다. 목업과 실제 데이터의 규약이 다르면 **대조는 통과하고 화면만 깨진다** —
조사 스나이퍼가 그랬다(`BLOCKERS.md`). 그래서 데이터를 흉내 낸 값은 `fromData()` 로
등록하고, `fixture-data-check.py` 가 씨드에서 캔 불변식과 맞춰 본다.
**새 픽스처가 씨드 레코드를 흉내 내면 반드시 등록해라.**

**셋 다 통과하는데 화면이 깨질 수 있다.** 훅 의존성 · ARIA · 초점 · 타이머를
건드릴 때는 근거를 말로 설명할 수 있어야 한다. 못 하겠으면 하지 마라.

로그인·서버 데이터가 필요한 화면은 눈으로 볼 수 없다는 것도 같이 알아 둬라 —
대조 밖의 화면(어드민 · MY · 문의 · 리포트)이 대부분 그렇다.

## "목업이 정본" 은 최신인지 묻지 않아도 된다는 뜻이 아니다

값이 앱과 목업에서 갈렸으면 **git 으로 언제 누가 정했는지 한 번 봐라.** 목업이
기준인 것은 맞지만, 앱 쪽이 나중에 정한 것이면 되돌리는 것이 남의 결정을 지우는
일이 된다(실제로 한 번 그럴 뻔했다 — `masterplan_v3.html` §16).

```bash
git log --format='%h %ci %s' -S'<그 값>' -- app/src
```

## 훅 의존성 배열에서 무언가를 빼는 것은 동작 변경이다

`biome` 의 `useExhaustiveDependencies` 는 **"몸통에서 읽는지" 만 본다.** 이 저장소에서
걸린 "불필요한 의존성" 은 거의 다 **일부러 넣은 재실행 방아쇠**였다.

| | |
|---|---|
| **더하기** | 원시값 · `useRef` 객체 · `useCallback/useMemo(…, [])` 로 안정한 것임을 확인했을 때만. 연쇄로 다시 도는 훅이 없는지도 봐라 |
| **빼기** | **하지 마라.** 대신 `biome-ignore` 에 왜 지금이 맞는지 적어라 |

의존성 배열을 고친 작업을 받으면 이렇게 확인해라 — 남이 "배열만 고쳤다" 고 해도
**배열을 고치는 것이 곧 동작 변경**이다.

```bash
git show <커밋> --unified=0 -- 'app/src/**/*.tsx' | grep -E '^[-+].*\}, \['
```

사례와 실제 목록은 `BLOCKERS.md` §12, 어긴 기록은 `masterplan_v3.html` §16-b.

## 손대면 깨지는 것

- **`npm install` 을 하지 마라.** 이 프로젝트는 `pnpm` 이다. `package-lock.json` 이
  다시 생기면 프로덕션 빌드가 깨진다 — `BLOCKERS.md` §1
- **라우터를 올리지 마라.** 1.136.8 위쪽은 React 19 의 `use` 를 써서 죽는다
  (이 앱은 React 18.3.1). **셋 중 둘만 캐럿이 없다** — `router-devtools` 는
  `^1.114.13` 이고 **지금 막고 있는 것은 락파일이다** — `BLOCKERS.md` §1
- `app/src/shared/data/n*.json` — 산출물이다. 위 표를 봐라.
  그리고 **여기서 과(課) 수를 세지 마라** — 과 구조의 정본은 `chapter.ts` 다.
  왜 틀리는지와 검사는 그 폴더의 `README.md` 가 쥔다
- 자모는 **검수도 배관도 끝났다** — 529행 전부 `reviewed`(원장 v41, 2026-08-28),
  라우트도 하나다(`/learn/jamo?level&lesson&group&sub`). `BLOCKERS.md` §2 · §2-c

## 공개 금지

교재에서 뽑은 문장·어휘·듣기 지문이 저장소에 추적된 채로 있다(약 21MB).
**이 저장소를 공개로 돌리면 안 된다.** 부록 PDF · 원장 xlsx · 삽화는 `.gitignore` 가 막는다.
