# 이 저장소에서 일할 때

연세 글로벌 한국어 학생용 앱 리뉴얼. **문서가 12개라 백지에서 뒤지면 틀린 것을 집는다.**
아래 규칙과 색인을 먼저 보고 시작해라.

## 지금의 목표 — 이걸 알고 판단해라

기획자가 **혼자 할 수 있는 것을 최대한** 하고, **개발자는 백엔드와 외부 API 만**
확실히 해서 배포하는 것이 목표다. 그래서:

- **화면은 거의 다 됐다.** 목업대로 들어가 있고 목업 대조가 통과한다.
  새로 그리자고 제안하지 마라
- **막힌 것은 대부분 서버다.** `api/` 에 리뉴얼 코드가 아직 없다 — `BLOCKERS.md` §6
- 개발자에게 넘길 문서는 **셋뿐**이다 — `phase1/INDEX.md` §6

## 화면 수가 넷이다 — 어느 것을 말하는지 밝혀라

이 저장소에서 가장 많이 어긋난 것이 이 숫자다. **넷 다 참이고 세는 단위가 다르다.**
그냥 "화면 N종" 이라고 쓰지 마라 — 어느 단위인지 붙여야 다음 사람이 안 헷갈린다.

| 수 | 무엇 | 어디서 나오나 |
|---|---|---|
| **20** | **목업의 활동 화면** | `screens_uiux.html` 활동 절의 버튼 수. `write`·`write3` 은 조합/따라쓰기를 한 버튼 안에서 상태로 넘긴다 |
| **22** | **활동 컴포넌트** | `app/scripts/activity-parity.tsx` 의 `SCREENS` 활동 항목. 위의 두 단계를 따로 센다 |
| **24** | **이식한 화면** | `masterplan_v3.html` **§15 의 표 합계** — `/learn/*` 6 · 자모 6 · 게임+목록 6 · 홈 등 4 · 클립·MY 2 |
| **47** | **목업 대조 화면** | `pnpm parity:activity` 가 실제로 대조하는 수 = 활동 22 + 내비 5 + VocaShot 3 + 게임 17. **목업 캡처 전부다** |

`check_docs.py` 가 **47 과 22 는 세어서 검사한다**(`[숫자 주장]`). 20·24 는 문서 안의
정의이니 `screens_uiux` 와 `masterplan_v3` §15 를 정본으로 본다.
전에는 이 자리가 27(활동 22 + 내비 5)이었다 — 2026-08-24 에 VocaShot 과 게임 넷·목록을
표시/상태로 갈라 캡처 47개를 다 넣었다.

## 무엇이 정본인가 — 어길 수 없는 넷

| | 정본 | 뜻 |
|---|---|---|
| **콘텐츠** | 원장 `글로벌_교재기반_콘텐츠_v*.xlsx` | `app/src/shared/data/n*.json` 은 **산출물이다. 손으로 고치지 마라** — 고칠 것은 원장이고, `app/scripts/build-content.py` 로 다시 만든다 |
| **화면** | `app/src/mockups/*.html` (목업 캡처) | **구현이 목업과 다르면 목업이 기준이다.** 일부러 벗어난 여섯 곳은 `phase1/masterplan_v3.html` §16 에 이유까지 있다 |
| **문서 목록** | `phase1/INDEX.md` | 목록을 **다른 곳에 새로 만들지 마라.** 전에 두 곳에 있어서 계속 어긋났다 |
| **결정의 이유** | 커밋 메시지 | `git log --format='%h %s%n%b' -- <경로>` |

## 어디서 시작하나

| 알고 싶은 것 | 열 것 |
|---|---|
| 지금 막힌 것 · 정해지지 않은 것 | `BLOCKERS.md` — **여기부터** |
| 어느 문서가 무엇에 답하나 | `phase1/INDEX.md` |
| 저장소에 무엇이 있고 어떻게 돌리나 | `README.md` |
| 무엇이 아직 기획되지 않았나 | `phase1/masterplan_v3.html` §2 덮개 지도 |

**셸 명세는 한 문서 안의 세 층이다** — `shell_spec_v1.html` 의
행동(§0~§10 상태 머신·라우트) · 구조(§11~§20 컴포넌트 상태와 슬롯) ·
값(§21~§34 치수·색·채점 수치). 전에는 파일이 셋이라 하나만 보고 판단하는 일이
잦았다. 만드는 순서는 따로다 — `dev_spec_v1.html`. 자세한 것은 `phase1/INDEX.md` §3.

## 무엇이든 끝냈으면 이 셋을 돌려라

문서가 작업을 저절로 따라오지 않는다. **이것이 이 저장소의 가장 큰 문제였다** —
다른 세션에서 홈 화면을 셋으로 쪼갰는데 README 는 "홈은 아직 안 들어갔다" 로
남아 있었고, 목업 대조 화면 수가 문서마다 22·24·27 로 갈려 있었다.
한 번 더 겪었다 — 화면을 27 → 47 로 늘리자 문서 넷이 동시에 "31" 로 낡았다.

```bash
python3 phase1/check_docs.py            # 문서 — 참조 · 절 인용 · 숫자 주장 · 색인
cd app && pnpm parity:activity          # 화면 — 구현이 목업과 같은지
cd app && python3 scripts/build-content.py --check   # 콘텐츠 — 원장과 JSON 차이
```

`check_docs.py` 는 **문서에 적힌 수를 실제로 세어 비교한다.** 화면을 늘리거나
문서를 만들거나 옮겼으면 이게 잡아 준다. 걸리면 **문서를 고쳐라** — 숫자를
맞추려고 코드를 고치지 마라.

일부러 어긋나게 두는 것은 **이유를 적어야** 통과한다 — 목업 쌍둥이는
`check_docs.py` 의 `TWIN_ALLOW`, 시점 기록의 옛 이름은 문서 안에
`옛이름.html → _superseded/` 줄.

## 숫자를 0으로 만드는 일은 목표가 아니다 — 먼저 끝 상태를 정해라

`biome check` 진단 · 미이식 화면 수처럼 **숫자가 붙는 정리 작업**이 이 저장소에 많다.
숫자를 목표로 잡으면 **판단이 숫자에 끌려간다** — 2026-08-24 에 505건을 43건으로
줄이면서 여섯 번 틀렸고, 그중 둘은 검사기가 못 잡는 동작 변경이었다
(`masterplan_v3` §16-b 에 전부 적어 뒀다).

**시작 전에 규칙마다 원하는 끝 상태를 적어라.** "이건 고친다 / 이건 이유를 적어
재운다 / 이건 안 건드린다" 를 만나서 정하지 말고 먼저 정한다. 남은 43건에 대한
표는 `BLOCKERS.md` §3-b 에 있다.

**숫자를 말하기 전에 세는 도구로 한 번 더 세라.** 잘린 화면에 `grep -c` 를 걸어
"에러 14건" 이라고 보고한 적이 있는데 실제는 404건이었다. `biome` 은
`--reporter=json --max-diagnostics=5000` 으로 세고, 자동수정의 영향은
**사본에 돌려서** 재라(`cp -R src` 로 충분하다).

## 검사기가 못 잡는 것을 알고 있어라

| 검사 | 잡는 것 | **못 잡는 것** |
|---|---|---|
| `pnpm typecheck` | 타입 | 실행 시점의 동작 전부 |
| `pnpm parity:activity` | 정적 마크업 구조 | **`useEffect` 가 아예 실행되지 않는다**(`react-dom/server`). 훅 재실행·타이머·초점 |
| `pnpm build` | 빌드 가능성 | 위 전부 |
| `check_docs.py` | 문서의 참조·숫자 | 문서가 **맞는 말인지** |

**셋 다 통과하는데 화면이 깨질 수 있다.** 훅 의존성 · ARIA · 초점 · 타이머를
건드릴 때는 근거를 말로 설명할 수 있어야 한다. 못 하겠으면 하지 마라.

로그인·서버 데이터가 필요한 화면은 눈으로 볼 수 없다는 것도 같이 알아 둬라 —
목업 대조 47화면 밖의 레거시 화면이 대부분 그렇다.

## "목업이 정본" 은 최신인지 묻지 않아도 된다는 뜻이 아니다

값이 앱과 목업에서 갈렸으면 **git 으로 언제 누가 정했는지 한 번 봐라.**

```bash
git log --format='%h %ci %s' -S'<그 값>' -- app/src
```

게임 목록 아이콘 색이 그랬다. 목업으로 맞추는 것이 결론이었지만, 확인해 보니
`65d3eb5` 가 카드를 데이터 배열로 합치면서 **값을 조용히 흘린 것**이었다(커밋
메시지에 색 이야기가 없다). 반대였다면 남의 결정을 되돌리는 것이 됐다.

## 린트를 맞추려고 동작을 바꾸지 마라 — 훅 의존성 배열

**`useEffect`·`useCallback`·`useMemo` 의 의존성 배열에서 무언가를 빼는 것은
동작 변경이다.** 그리고 이 저장소의 게이트 셋(`pnpm typecheck` ·
`pnpm parity:activity` · `pnpm build`)은 **훅이 언제 다시 도는지를 검사하지 못한다** —
목업 대조는 `react-dom/server` 로 그려서 `useEffect` 가 아예 실행되지 않는다.
즉 여기서 생긴 회귀는 **아무 검사도 안 잡고 사람이 화면을 열어야 발견된다.**

그래서 규칙은 하나다.

| | |
|---|---|
| **더하기** | 값이 **원시값**이거나 `useRef` 객체이거나 `useCallback/useMemo(…, [])` 로 안정한 것임을 확인했을 때만. 그리고 그 값을 의존성으로 쓰는 훅이 연쇄로 다시 도는지 직접 확인해라 |
| **빼기** | **하지 마라.** 대신 `biome-ignore` 에 왜 지금이 맞는지 적어라 |

`biome` 의 `useExhaustiveDependencies` 는 **"몸통에서 읽는지" 만 본다.** 이 저장소에서
걸린 "불필요한 의존성" 은 거의 다 **일부러 넣은 재실행 방아쇠**였다 — 지웠다면
입력칸이 안 늘어나고(`dialog-input` 의 `textareaValue`), 탭을 바꿔도 안 따라오고
(`book-tabs` 의 `activeId`), 카드를 넘겨도 앞 소리가 계속 났다. 실제 목록은
`BLOCKERS.md` §3-b 에 있다.

**2026-08-24 에 한 번 어겼다.** 세 화면(`fill-blank` · `read-answer` ·
`listen-answer`)의 답 복원 효과에서 `[currentIndex, question?.id, savedAnswers]` 를
`[question, savedAnswers]` 로 좁혔다. "`question` 이 `useMemo` 배열의 원소라
`currentIndex` 가 바뀌면 `question` 도 바뀐다" 는 논리였는데, 그것이 참이려면 배열에
같은 객체가 두 번 없어야 하고 그 보장을 코드로 확인할 방법이 없었다. **되돌리고
재웠다** — 같은 린트 결과를 동작 변경 없이 얻었다. 얻는 것이 없으면 바꾸지 않는다.

## 남이 한 작업은 보고가 아니라 diff 로 확인해라

작업을 나눠서 하면(다른 세션·서브에이전트) 보고서가 온다. **보고서는 근거가 아니다.**
위의 의존성 축소도 "의존성 배열만 고쳤고 동작은 안 건드렸다" 는 보고와 함께 왔다.
그 말은 사실이었지만 **의존성 배열을 고치는 것이 곧 동작 변경**이라는 점이 빠져 있었다.

받은 뒤에 이렇게 봐라.

```bash
git show <커밋> --unified=0 -- 'app/src/**/*.tsx' | grep -E '^[-+].*\}, \['
```

줄바꿈 재포맷 때문에 짝이 없어 보이는 `-` 줄이 나올 수 있다 — 그때는 그 파일만
전체 diff 로 다시 봐라(실제로 그런 경우가 있었고, 알고 보니 값이 **추가**된 것이었다).

## 손대면 깨지는 것

- **`npm install` 을 하지 마라.** 이 프로젝트는 `pnpm` 이다. `package-lock.json` 이
  다시 생기면 프로덕션 빌드가 깨진다 — `BLOCKERS.md` §1
- `@tanstack/react-router` 셋은 캐럿 없이 **정확히 `1.136.8`** 이다. 올리면 React 19 의
  `use` 를 써서 죽는다 (이 앱은 React 18.3.1)
- `app/src/shared/data/n*.json` — 산출물이다. 위 표를 봐라
- 자모는 **검수 전이지만 배관은 끝났다 (2026-08-24).** `n8_jamo` 529행이 전부
  `draft` 인데 화면은 이미 그 JSON 을 읽는다 — 내용이 `problem.ts` 와 같으니
  검수가 배관을 막지 않는다. 라우트도 하나로 합쳤다(`/learn/jamo?level&lesson&group&sub`).
  검수로 남은 것은 받침·겹받침 낱자 목록뿐이다 — `BLOCKERS.md` §2 · §2-c

## 공개 금지

교재에서 뽑은 문장·어휘·듣기 지문이 저장소에 추적된 채로 있다(약 21MB).
**이 저장소를 공개로 돌리면 안 된다.** 부록 PDF · 원장 xlsx · 삽화는 `.gitignore` 가 막는다.
