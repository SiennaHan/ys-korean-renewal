# 이 폴더의 JSON 은 산출물이다

`n1_*` · `n2_*` · `n3_*` · `n4_*` · `n5_*` · `n6_*` · `grammar_list.json` 은
교재 콘텐츠 원장(`글로벌_교재기반_콘텐츠_v*.xlsx`)에서 만들어진다.
**손으로 고치지 마라 — 다음 생성에서 지워진다.** 고칠 것은 원장이다.

    python3 scripts/build-content.py            # 최신 원장으로 다시 만든다
    python3 scripts/build-content.py --check    # 쓰지 않고 무엇이 달라지는지만

원장은 교재 파생이라 저장소에 없다(`.gitignore`의 `*.xlsx`). 돌리려면 저장소
루트에 원장 파일이 있어야 한다.

`.ts` 로 된 것들(`chapter.ts` · `module.ts` · `unit.ts` · `clip.ts` 등)은
원장에서 나오지 않는다. 구 앱에서 그대로 가져온 것이라 생성 대상이 아니다.

## 과 구조는 `chapter.ts` 가 정본이다 — **`n*.json` 에서 세지 마라**

과가 몇 개인지, 어느 급에 몇 과인지는 **`chapter.ts` 만 답한다** (120과 — 한글 3 ·
일반 117, 여덟 권 모두 15과). 교재학습 탭도 거기서 만들어진다
(`components/main/textbook/labels.ts` 의 `buildBookTabs`).

**여기 있는 문항 JSON 으로 과를 세면 틀린다.** 1급 1~3과는 한글 파트라 어휘·듣기·
역할극 데이터가 없어서, 그 파일들에서는 **1급이 12과로 보인다.**
1급을 담은 파일 12개 중 **11개가 참값(15)과 다르게 보이고**, 그중 **10개가 12과**,
`n8_jamo` 하나가 3과다(`n3_listen_script` 만 1~15 를 다 갖고 있다).
하나만 열어 보고 세면 거의 확실히 틀린다.

    python3 scripts/chapter-source-check.py

이 검사가 매번 그 표를 찍고(어느 파일이 몇 과로 보이게 하는지), 콘텐츠가 정본
밖의 과를 가리키거나 활동이 하나도 없는 과가 생기면 실패한다.
`pnpm parity:activity` 안에 묶여 있다.

> 2026-08-29 에 정본 기획서 §0 이 이 함정을 밟아 **"1급만 12과"** 라고 적었다.
> `n1_word_list.json` 하나를 세었기 때문이다. 검사기는 그때 생겼다.

## 엑셀이 여는 따옴표를 먹는다

셀 맨 앞의 `'` 는 엑셀에서 "이건 텍스트다" 라는 서식 지시로 해석돼 **값에 남지 않는다.**
그래서 `'연필'은 받침이…` 라고 써도 `연필'은 받침이…` 로 저장된다.

`n4_blank_question` 의 해설 61개가 그 상태였고 2026-08-21 에 되돌렸다.
**생성기가 매번 검사한다.** 따옴표가 홀수인 **한글** 칸을 찾아 시트·행·칸 이름과
함께 경고한다 — 영어 아포스트로피(`I'm` · `o'clock`)는 정상이라 한글이 절반을
넘는 칸만 센다.

```bash
python3 scripts/build-content.py --check
```

경고가 뜨면 원장에서 그 칸의 맨 앞 `'` 를 되돌려라.

2026-08-21 에 `n4_blank_question` 의 해설 61개가 그 상태였고 되돌렸다.
같은 날 **해설 다섯 건이 다른 문항의 것**인 것도 찾아 고쳤다 — 1급 6·8·10·13과와
4급 9과. 앞의 넷은 받침 유무로 조사를 고르는 문항인데 해설이 **정답과 반대되는
쪽**을 설명하고 있었다. 무엇을 어떻게 고쳤는지는 원장의 `change_note` 와
`git log -- app/src/shared/data/n4_blank_question.json` 에 있다.

## 해설 전수 검수는 지금 하지 않는다 (2026-08-21 판단)

한때 "836행을 사람이 다 읽어야 한다" 고 적었는데, 재 보니 그럴 값이 아니다.

* 해설은 `n4_blank_question` **836행뿐**이고 전체 문항 7,305개의 **11.4%** 다.
  문법 빈칸 활동 하나에만 있다.
  <br>**7,305 가 무엇을 센 수인가** — 이 정의가 `_superseded/` 문서에만 있어서
  적어 둔다. 어휘 2,890 · 어휘퀴즈 1,146 · 역할극 993 · 듣고답 679 · 빈칸 836 ·
  읽고답 383 · 듣기지문 378 의 합이다. 자모(529) · 플래시카드 카드(2,329) ·
  미션대화(117) · 읽기 본문(117) · 듣기 스크립트 줄(1,815)은 안 센다.
  2026-08-27 에 다시 세도 일곱 값이 그대로여서 11.4% 도 그대로다.
* **지금 화면에 안 나온다.** 해설을 그리는 자리가 없다 — 서버 작업(문항 조회에
  해설을 얹는 것)이 끝나야 학습자 눈에 닿는다. 즉 노출이 0이라 급할 것이 없다.
* **위험한 종류는 기계가 덮는다.** 정답과 반대되는 이형태 0건 · 받침 판정이 실제와
  다른 것 0건(29개 검사) · 따옴표 홀수 0건(생성기가 계속 본다). 고친 다섯 건이
  전부 이 세 종류였다.

**언제 보나** — 해설을 화면에 붙일 때, 문법 빈칸 활동을 한 급씩 몇 과 돌려 보며
같이 본다. 문맥 없이 836줄을 읽는 것보다 그때가 판단이 빠르다.

곁들여 객관식 네 시트(2,044행)의 기본 정합성도 확인했다 — 정답 번호 범위 밖 0 ·
선택지 중복 0 · 선택지 부족 0.

## biome 이 이 폴더를 보지 않는다 (2026-08-24)

`biome.json` 의 `files.ignore` 에 이 폴더가 들어 있다. 이유가 둘이다.

* **네 파일은 biome 이 처리하지 못한다.** `clip.ts` 11MB · `dialog.ts` 1.9MB ·
  `n7_mission_chat.json` 1.5MB · `n1_word_list.json` 1.1MB 가 기본 상한 1MB 를 넘어
  "크기 초과" 진단만 낸다. 빼지 않으면 `biome check` 가 절대 통과할 수 없었다.
* **나머지도 코드가 아니라 데이터표다.** 서식을 맞춰도 읽는 사람이 없고,
  `problem.ts` 하나가 1.8만 줄짜리 diff 를 만든다.

**주의 — 여기 있는 작은 로더 모듈도 같이 빠졌다.** `word-list.ts` ·
`learn-data-check.ts` · `listen-answer.ts` · `mission-chat.ts` · `roleplay.ts` ·
`word-quiz.ts` 처럼 JSON 을 읽어 타입을 붙이는 파일들이다. 제외 직전에 `book.ts` · `dialog_word.ts` ·
`flashcard.ts` 에 `format` 진단이 각 1건 있었고 그것은 이제 적용되지 않는다(서식뿐이다).
그보다 **앞으로 이 파일들이 틀리면 린터가 안 잡는다**는 것이 중요하다. 손댈 때 눈으로 봐야 한다.

생성 JSON 자체는 서식이 이미 맞다 — `build-content.py` 가 `json.dumps(indent="\t")` 로
쓰고 그것이 biome 의 서식과 같다. 즉 빼는 이유는 서식 충돌이 아니라 위의 둘이다.

## 문항은 지문 없이 풀리면 안 된다

오답이 「나쁜 말」이라서 틀리면 안 되고, **지문이 다른 말을 해서** 틀려야 한다.
오답도 긍정문으로 두고 지문의 구체 사실(교통편·순서·값·시각·인물 역할)과
어긋나게 만든다.

    python3 scripts/question-leak-check.py

세 축을 본다 — 오답만 부정문 · 배타 표지(~만)가 오답에만 · 완화어 O/X 가 전부 O.
`pnpm parity:activity` 안에 묶여 있다.

> **통과해도 안심하지 마라.** 가장 크게 새는 「의미적으로 그럴듯한 것 고르기」는
> 규칙으로 안 잡힌다. **새 문항 열 개를 지문 가리고 풀어 봐라** — 4지선다에서
> 네 개 넘게 맞으면 오답을 다시 써야 한다. 2026-08-30 에 여덟 개 중 일곱을
> 맞혔고, 그때 27개를 다시 썼다. 자세한 것은 `docs/G1_content_gate_v1.html`.

> 오답을 지문과 **하나씩 대조**해라. 새로 쓴 오답 하나가 지문 그대로여서
> 정답이 둘이 될 뻔했다(`n5_read_answer_text` id=212).
