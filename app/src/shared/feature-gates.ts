/**
 * 아직 붙지 않은 것을 한곳에서 껐다 켠다.
 *
 * 깃발을 화면마다 두면 갈라진다 — 하나가 켜져도 다른 하나가 남아, 반쪽만
 * 살아난 채로 배포된다. 이 저장소가 가장 자주 어긋난 자리가 그것이다
 * (`free_scope.py` 머리말·`paywall-panel` 의 무료 목록).
 */

/**
 * 비밀번호 재설정 메일을 보낼 수단이 붙었나.
 *
 * **지금은 없다** — `api/` 에 SMTP·SES·SendGrid 어느 것도 없다(BLOCKERS §7).
 * 그래서 메일 링크로만 닿는 두 화면이 갈 곳을 잃었다:
 *
 *   `/new-password`  낡은 링크를 가진 사람은 닿는다 → 폼 대신 사실을 말한다
 *   `/check-email`   "보내드렸습니다" 는 화면인데 **보낸 적이 없다** →
 *                    사실을 말하는 `/reset-password` 로 보낸다
 *
 * 메일이 붙으면 **여기 한 줄만** true 로 바꾸면 둘이 같이 살아난다.
 * `NewPasswordForm` 의 TODO 도 그때 채운다.
 */
export const MAIL_RESET_READY = false;

/**
 * 이용약관·개인정보 처리방침의 **문안이 확정됐나.**
 *
 * **지금은 아니다.** 초안은 `docs/legal_draft_v1.html` 에 조문까지 있는데
 * 거기 **채우지 못한 빈칸이 34개**다. 코드로 메울 수 있는 것이 아니라
 * 기획자와 변호사가 정할 값들이다 — 그중 둘은 법정 필수 기재다:
 *
 *   사업자        상호 · 대표자 · 주소 · 사업자등록번호 · 통신판매업 신고번호
 *   보호책임자    개인정보 보호책임자 이름 · 직책 · 연락처
 *   보관기간      항목별 기간. 법정 보존의무와 맞춰야 한다
 *   14세 미만     가입을 받을지. 받으면 법정대리인 동의 절차가 통째로 더 필요하다
 *   관할          준거법과 관할 법원. **이용자 상당수가 국외**라 그냥 정하면 안 된다
 *   환불·가격·PG  결제가 붙을 때 같이 정해진다
 *
 * 그래서 `/terms` · `/privacy` 는 **문안을 지어 보여 주지 않는다.** 준비 중이라고
 * 말하고 문의로 보낸다 — 없는 약관을 있는 것처럼 보이면 그냥 틀린 것이 아니라
 * 위험하다. `docs/draft_auth.html` 규칙 01 이기도 하다.
 *
 * **문안이 오면** 여기를 true 로 바꾸고 두 화면에 조문을 넣는다. 가입 화면의
 * 링크도 같은 깃발을 보므로 그때 저절로 살아난다.
 */
export const LEGAL_DOCS_READY = false;
