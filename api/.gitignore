# 상류(koreanapi-master)가 쥐고 있던 규칙 그대로.
# 자격증명은 전부 환경변수로 읽는다 — .env 는 저장소에 들어오지 않는다.
.env*
# 다만 값이 없는 본보기는 추적한다 — 이름과 필수 여부가 그 파일의 값어치다.
# 이게 없어서 다음 사람이 무엇을 받아야 하는지조차 몰랐다(BLOCKERS §6-b).
!.env.example
*.log
*.pyc
__pycache__
ssh
logs
key
newssh

# 파이썬 가상환경
.venv/
venv/
