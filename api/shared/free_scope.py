"""무료 경계 — 계정 없이, 로그인 없이 볼 수 있는 범위.

**경계는 이 파일 한 곳에만 있다.** 판정 코드는 이 값을 읽을 뿐 계산하지 않는다.
마케팅이 "이번 달은 5급 3과도 열자" 고 하면 아래 표만 고친다.

정본은 `docs/access_and_pricing_v1.html` §02 다(2026-08-26 기획 확정).
바꿀 때는 그 문서를 같이 고쳐라 — 값이 두 곳에 있으면 반드시 갈라진다.

**왜 `ko_chapter.is_free` 가 아닌가.** §05 는 그 칸을 쓰라고 적었지만
`ko_chapter` 라는 표가 이 저장소에 없다(확인함 — `model.py` 에 없고, 과 목록
120개는 프런트 번들의 `app/src/shared/data/chapter.ts` 에 있다). 서버는 과를
값으로만 들고 있다(`chapter_seq`). §05 의 뜻은 "예외를 코드가 아니라 데이터로
두라" 는 것이므로, 없는 표를 만드는 대신 **데이터 파일 하나**로 지킨다.
나중에 과 표를 서버로 옮기면 그때 이 파일이 그 표로 이사한다.
"""

# ── 한글(자모) ──
# 1급 1·2·3과가 한글 파트다. 그중 1과만 무료.
FREE_JAMO_CHAPTERS = [1]

# ── 교재 ──
# 급 → 무료로 여는 과. 1급만 4과인 이유는 1~3과가 자모라 일반 활동이
# 하나도 없기 때문이다(어휘 0 · 어휘퀴즈 0 · 역할극 0 …). 그래서 무료 과를
# "seq == 1" 같은 식으로 정하면 1급 맛보기가 빈 화면이 된다.
FREE_CHAPTERS = {
    1: [4],
    2: [1],
    3: [1],
}

# ── 게임 ──
# 교재에 묶이지 않아 아무 급이나 즐길 수 있는 둘만 무료.
# 나머지(seoul-puzzle · card-sort · particle-sniper)는 유료다.
FREE_GAMES = ["vocashot", "spring-picnic"]

# ── 표현클립 ──
# 329편 전부. 유튜브 임베드라 앱이 쥔 자산이 아니고 유입 장치로서 값이 더 크다.
FREE_CLIPS = True

# 급 전체가 열리는 일은 무료에 없다. 구독하면 books 로 전 급이 들어온다.
FREE_BOOKS: list[int] = []
